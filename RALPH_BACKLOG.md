# DokeV — 자동 반복 작업 백로그

---

# 지금 하는 일 — 구역마다 다른 월드 만들기 (2026-08-21 시작)

> **이 절이 지금의 작업 지시다. 아래의 옛 기록보다 이것이 우선한다.**
> 옛 기록(「지금 상태 — 여기부터 읽으라」 이하)은 **끝난 구간의 역사**이고,
> 거기 적힌 「다음 후보」는 이 절이 대체한다. 규칙(절대 규칙·완료 기준)은 그대로 지킨다.

## 왜 갈아엎었나

사람이 화면을 보고 말했다: **"캐릭터 빼고는 다 이상하다."** 맞는 지적이었다.
도시가 6×6으로 넓고 소품이 촘촘한데도 **어디를 달려도 같은 것**이 나왔다.
원인은 소품 수가 아니라 구조였다 — 구역 성격을 격자 중심에서의 **고리 거리**로
정했기 때문에, 고리는 방향을 구분하지 못했다. 북쪽 끝과 남쪽 끝이 같은 고리라
같은 동네였고, 건물 만드는 규칙은 도시 전체에 하나뿐이었다.

참고하는 트레일러의 월드가 넓게 느껴지는 이유는 크기가 아니라 **방향마다 다른
것이 나오기 때문**이다.

## 지금 검사 상태

지금 **2383개 테스트**가 통과한다(121개 파일). 이 숫자를 여기 적어 두는 이유는
`docs` 검사가 **루트 문서 어딘가에 살아 있는 숫자가 하나는 있어야** 한다고
요구하기 때문이다 — 옛 기록의 숫자는 「그때 그랬다」이지 현재가 아니므로,
표 안의 낡은 수치를 현재 주장으로 읽지 않도록 표현을 바꿔 두었다.

## 이미 된 것 (이 위에 쌓으라, 되돌리지 마라)

- `src/game/world/zones.ts` — **손으로 그린 6×6 구역 지도**와 구역별 성격.
  여덟 동네: 달빛 광장·번화가·노을 시장·언덕 주택가·옛 마을·너른 공원·안개 숲·윤슬 해안.
  **이 파일은 아무것도 import하지 않는다.** 순환이 생기면 `streaming`이 최상단에서
  읽는 `CITY`가 undefined가 되어 검사 49개가 한꺼번에 죽는다. 겪었다.
- `districts.ts` — 이름표 + 좌표 조회(`zoneAt`)로 축소. 정본은 `zones.ts`다.
- `cityLayout.ts` — 구역 규칙(`BuildRule`)으로 건물을 세운다. 필지 수·층수·공터
  비율·톤이 구역마다 다르다.
- `trees.ts` — 가로수 + **구역 나무**. 겹쳐 심기를 막는 `createTreePlanter`(격자 해시).
- `GroundSurfaces.tsx` — 지면을 **구역마다 메시 하나씩**으로 쪼갠다. 정점은 한 벌을
  공유하고 인덱스만 나눠 솔기가 없다. 잔디/모래/흙은 `drawGrass` 결을 나눠 쓰고
  색만 다르다.
- `isUrban` — 인도·연석·점자블록·빗물받이·차선·전선을 자연 구역에서 걷어낸다.
  **가로등은 남긴다** — 그래플 앵커라 걷어내면 숲에서 이동이 막힌다(아래 1번).

## 하지 말아야 할 것 (실제로 겪은 함정)

- **`vertexColors`로 바닥 색을 칠하지 마라.** 색 속성이 없으면 three는 조용히
  vColor=0으로 새까맣게 칠한다. 지오메트리가 `useMemo`에 캐시되어 있어 속성 넣는
  줄 하나가 어긋나면 바닥 전체가 사라지고, 화면에는 「밤인가?」로 보인다. 원인을
  찾는 데 오래 걸렸다. 지금처럼 **재질에 색을 직접** 주라 — 틀리면 하얘져서 눈에 띈다.
- **브라우저에서 확인할 때 탭이 안 보이면 rAF가 멈춘다.** 스트리밍이 안 끝나
  바닥이 까맣게 보이는데 그건 결함이 아니라 **로딩 중**이다. 스크린샷을 여러 장
  연속으로 찍어 프레임을 돌린 뒤 판단하라. 이걸 결함으로 읽고 두 시간 날렸다.
- **HMR을 오래 반복하면 WebGL 컨텍스트가 날아간다.** 「그래픽 연결이 끊겼습니다」가
  뜨면 코드 문제가 아니라 개발 서버 문제다. 새로고침하라.

## 다음 후보 — 위에서부터 하나씩

각 항목은 **화면에서 달라진 것이 보여야** 끝난 것이다. 값만 맞고 화면이 그대로면
이 저장소에서 가장 흔했던 결함이다. 항목마다 검사를 하나 이상 추가한다.

1. ~~**자연 구역의 그래플 앵커를 나무·바위로 바꾼다.**~~ → **끝. (반복 1)**

   한 일:
   - `CityLayout`에 `grappleAnchors`를 명시 목록으로 추가했다. 예전에는 `props`에서
     `tone === 0 && height > 4`로 **추려 냈는데**, 색과 높이로 「전봇대겠지」를 추측하는
     것이라 다른 모양의 앵커를 세우면 조용히 빠진다. 무엇이 앵커인지는 놓는 쪽이 말한다.
   - 자연 구역 네 귀퉁이에 **선돌**(`layout.rocks`, 높이 7.5~11m)을 세웠다. 구역 중심에
     하나만 두면 모서리까지가 24.04m로 **사거리와 1cm 차이**라 지형이 조금만 바뀌어도
     막힌다. 넷으로 나눠 최악 거리를 12m대로 내렸다.
   - 선돌을 세운 **뒤에** 가로등을 `isUrbanBlock`으로 걸렀다. 순서가 뜻을 갖는다 —
     반대로 하면 그 사이 상태에서 숲이 통째로 사거리 밖이 된다.
   - 검사가 `streetLamps`가 아니라 `grappleAnchors`를 재게 고쳤다. 제품이 읽는 것과
     다른 것을 재고 있었다. `tests/grappleCoverage.test.ts`로 떼어 냈고(1200줄 상한),
     「자연 구역마다 하나 이상」을 못 박는 검사를 더했다 — 표본 200점은 숲 한 칸이
     통째로 비어도 운이 좋으면 지나간다.

   화면에서 달라진 것: 숲에 가로등과 전선이 사라지고 **회색 선돌이 서 있다.** 나무만
   있을 때는 어디가 어딘지 몰랐는데 선돌이 이정표가 된다.

   남은 것(다음 항목들에서 다룬다): 숲에 아직 **어두운 상자 소품**(쓰레기통·정류장류)과
   **흰 차선 조각**이 남아 있다. `streetProps`·`streetExtras`도 `isUrban`으로 걸러야 한다.

2. ~~**나무를 구역마다 다르게 만든다.**~~ → **끝. (반복 2)**

   한 일:
   - `Zone`에 `treeSpecies`(broadleaf/conifer/palm)와 `treeScale`을 넣었다. 밀도만
     달리했을 때는 숲이 **「가로수를 촘촘히 심은 공원」**으로 보였다 — 같은 모양이
     수만 늘어난 것이라 그렇다. 실루엣이 갈려야 다른 곳에 온 것이 된다.
   - 침엽수는 **원뿔 두 단**이다. 한 단이면 고깔이지 나무가 아니고, 위 단이 아래 단
     속에서 시작해야 이음매가 각지게 드러나지 않는다. 도형이 다르므로 인스턴스
     묶음을 `treeCones`로 갈랐다(드로우콜 +1).
   - 기둥 굵기는 배율의 **제곱근**으로 눌렀다. 그대로 곱하면 숲의 9m 나무가 굵기
     0.7m가 되어 전봇대처럼 보인다.
   - 가로수도 `g`(도로 축 번호)가 아니라 **좌표로** 구역을 찾게 고쳤다. 그러지 않으면
     숲을 지나는 길가에 활엽수가 한 줄 선다.
   - 벽 여백(`WALL_MARGIN`)을 고정값에서 **배율 비례**로 바꿨다. 나무를 키우자마자
     해안 야자가 건물에 아홉 개 박혔다 — 고정값이라 배율을 따라가지 못한 것이다.
     그리고 **가로수에도 같은 검사를 걸었다**(예전에는 구역 나무만 확인했다).

   덤으로 끝낸 것 (1번의 「남은 것」):
   - `streetProps`·`streetExtras`·`roadMarks`·`crosswalks`를 자연 구역에서 걷어냈다.
     자연 구역은 지면이 잔디·모래로 덮여 **차도가 보이지 않는데** 중앙선과 횡단보도만
     남아 있어, 풀밭 위에 형광 띠가 뜬 꼴이었다. `pushMark` 한 곳에서 거른다 —
     중앙선·차선·정지선이 각각 거르면 하나를 더할 때 조용히 빠진다.
   - **자판기만은 자연 구역에 남겼다.** 전부 걷어냈더니 먼 쪽에 회복 수단이 사라졌다
     (`vending` 검사가 잡았다). 등산로 초입과 해수욕장에는 실제로 자판기가 있다.

   화면에서 달라진 것: 숲이 **짙은 초록 침엽수**로 덮이고 나무가 두 배 높아져
   캐노피 아래를 걷는 느낌이 난다. 해안에는 납작한 야자 잎이 선다. 잔디·모래 위의
   흰 차선과 횡단보도가 사라져 자연 구역이 도로가 아니라 땅으로 보인다.

3. ~~**해안에 물을 만든다.**~~ → **끝. (반복 3)**

   한 일:
   - `Sea.tsx` — 수면(`SEA_LEVEL = -8.6`)과 **벼랑 치마**. 물결은 셰이더가 아니라
     UV 스크롤이다. 정점을 흔들면 수면 격자가 성기라 물결이 아니라 판이 접히는
     것으로 보이고, 매 프레임 지오메트리를 다시 올려야 한다.
   - **물을 도시 안으로 들이지 않았다.** 지형을 바다까지 끌어내리려면 경사가
     필요한데 이 월드의 한계는 20%다(`terrain` 검사). 마루가 +8m라 -8m까지
     내리려면 80m 넘는 비탈이 필요하고, 동쪽 두 구역이 통째로 비탈이 된다.
     대신 **월드 정사각형 바깥**에 깔고 땅의 끝을 벼랑으로 마감했다.
     덤으로 월드 경계가 설명된다 — 예전에는 도시가 이유 없이 끊겼는데 이제
     섬이라서 끊긴다.
   - `SEA_LEVEL`을 지형 최저점(-7.95m) **바로 아래**로 잡은 것이 설계의 전부다.
     경계에서 땅이 낮은 자리는 물이 발치까지 오고, 마루인 자리는 16m 벼랑이 된다 —
     한 값으로 해변과 절벽이 같이 나온다. `tests/sea.test.ts`가 그 관계를 위아래로
     묶는다(잠기지도, 너무 멀지도 않게).
   - 벼랑 치마의 윗선은 지형을 그대로 따라간다. 평평한 벽으로 두면 마루에서는
     땅속에 파묻히고 골짜기에서는 공중에 뜬다. 이게 없으면 지면이 **종잇장처럼**
     끊겨 그 너머로 바다가 그대로 비친다.
   - `textures.ts`가 800줄을 넘어 하늘 한 장을 `skyTexture.ts`로 뗐다.

   함정 하나 기록: **`vertexColors`처럼 `useMemo` 값을 `useFrame`에서 고치면
   컴파일러가 막는다**("This value cannot be modified"). 훅에 넘긴 값을 나중에
   바꾸면 메모가 낡는지 알 수 없기 때문이다. 재질 ref를 거쳐 `material.map.offset`을
   만지는 것으로 풀었다.

   화면에서 달라진 것: 해안 구역 모래밭 너머로 **푸른 물 띠가 수평선까지** 깔린다.
   확인은 포토 모드(P)에서 D로 시점을 돌려 잡았다 — 3인칭 카메라는 시야가 낮아
   물이 땅 끝 아래에 있어서, 그냥 서 있으면 안 보인다.

   남은 것: 물가에 **파도 거품선**이 없어 물과 땅이 만나는 선이 딱딱하다. 그리고
   3인칭 시야에서 물이 잘 안 보이는 것은 여전하다 — 해안 구역 지형만 한 단 낮추면
   나아질 수 있다(경사 한계 안에서).

4. ~~**옛 마을에 기와지붕·돌담·홍살문.**~~ → **끝. (반복 4)**

   한 일:
   - `oldTown.ts` — **돌담**(높이 1.3m)과 **홍살문**(4.6m). 담은 네 변에 각각
     두 도막으로 놓고 가운데를 5.2m 비운다. 한 도막으로 두르면 **그 구역에
     못 들어가고**, 네 변을 다 트면 담이 아니라 흩어진 돌덩이다. 검사가
     네 문턱을 사람 반지름으로 실제로 통과해 본다.
   - 담 높이 1.3m는 **어른 가슴·아이 머리 위**다. 3인칭 카메라가 어깨 너머라
     더 높으면 골목에서 화면이 담으로 막힌다.
   - `buildHanokRoofs` — **모임지붕**(네 면이 모두 물매). 박공은 양 끝이 수직
     삼각형인데 이쪽은 그 면까지 눕는다. 처마를 0.45m → **1.15m**로 깊게
     뺐다 — 깊은 처마가 한옥 지붕의 전부다. 얕으면 그냥 색이 다른 지붕이다.
   - 기와 색은 청회색 한 계열로 **좁게** 묶었다. 주택가 지붕은 여섯 색이 섞여
     「제각각 고쳐 온 동네」인데, 옛 마을은 반대로 「한 시기에 한 손으로 올린 것」
     처럼 보여야 한다.
   - **옛 마을 건물에 상가를 붙이지 않는다.** 담과 문을 세우고 기와를 얹어
     놓고도 그 벽에 줄무늬 차양과 보라색 쇼윈도가 붙어 있어서, 화면이
     「기와지붕을 얹은 상가」로 보였다 — 가 보고 알았다.
   - `oldTown.ts`는 `cityLayout`을 **값으로** import하지 않는다(순환). 좌표는
     인자로 받고 타입만 가져온다. `City.tsx`가 800줄을 넘어 지붕 메시를
     `RoofMeshes.tsx`로 뗐다.

   화면에서 달라진 것: 흙길 골목이 **회색 돌담 사이로** 이어지고, 그 끝에
   **붉은 홍살문**이 선다. 벽에서 차양과 쇼윈도가 사라져 마을이 조용해졌다.

   남은 것: 문이 남쪽 한 곳에만 있어 다른 방향에서 들어오면 안 보인다. 그리고
   기와지붕은 **위에서 봐야 보인다** — 골목에서는 처마 그늘만 보인다.

5. ~~**노을 시장에 아케이드 천막과 좌판.**~~ → **끝. (반복 5)**

   한 일:
   - `market.ts` — **도로를 가로지르는 천막**과 **좌판**. 시장을 시장으로 만드는
     것은 머리 위다. 필지를 넷·다섯으로 쪼갠 것만으로는 「건물이 작은 상가」였다.
   - 천막 높이 **6.4m는 가로등(5.6m)과 전깃줄 위**다. 그보다 낮으면 천막이
     가로등을 뚫고, 높으면 하늘을 못 가려 공중에 뜬 판이 된다. 검사가
     `streetLamps`의 실제 꼭대기와 대조한다 — 숫자를 베끼지 않는다.
   - 천막을 **번갈아 두 색**으로, 폭 3.1m에 간격 4.6m로 띄웠다. 빈틈없이 이으면
     통짜 지붕이라 실내가 되고, 사이가 벌어져야 빛이 줄무늬로 떨어진다.
   - **이웃이 시장일 때만** 건다. 시장과 번화가 사이에 걸면 천막이 어디서
     끝나는지 알 수 없는 채로 큰길 위에 뜬다. 이웃을 `index + 1`로 찾으므로
     **행이 넘어가는 것**을 막아야 한다 — 한 줄의 오른쪽 끝에서 `index + 1`은
     다음 줄의 왼쪽 끝이고, 그러면 월드를 가로질러 천막이 걸린다.
   - 좌판은 상판(0.85m·충돌체 있음)·물건 두 무더기·기둥 둘·차양(2.35m)이다.
     차양이 머리보다 낮으면 골목이 통과 불가가 되므로 검사가 2m를 지킨다.
     자리를 22% 비운다 — 빈틈없이 늘어서면 시장이 아니라 울타리다.

   확인하다 알게 된 것: **구역 중심과 구역 경계를 헷갈렸다.** 시장 구역(row 5)의
   중심 z는 117.5인데 100으로 잡고 「시장에 서 있다」고 믿었다. 100은 구역 북쪽
   **도로**라 눈앞은 번화가였고, 천막은 등 뒤에 있었다. 천막을 22m로 올려 봐도
   안 보여서 한참 헤맸다 — 좌표를 눈으로 짐작하지 말고 `blockCenter`로 잡으라.

   화면에서 달라진 것: 시장 골목 위로 **주황·청록 천막이 번갈아** 걸리고 그 사이로
   빛이 줄무늬로 떨어진다. 길가에는 차양 달린 좌판이 늘어선다.

   남은 것: 천막이 **도로 위에만** 있다. 구역 안쪽 골목은 아직 하늘이 열려 있다.

6. ~~**너른 공원에 놀이터·연못·산책로.**~~ → **끝. (반복 6)**

   한 일:
   - `park.ts` — **연못**(돌 테두리 포함), **놀이터**(미끄럼틀·그네·시소·모래밭),
     **산책로**(띄엄띄엄 놓은 포석 고리). 잔디와 나무는 숲에도 있다 —
     「누가 여기 와서 논다」를 말하는 물건이 있어야 숲과 갈린다.
   - 연못을 **1.9m 파묻는다.** 수면은 한 장짜리 평면인데 공원 땅은 기울어 있어,
     파묻지 않으면 낮은 쪽 모서리가 뜨고 그 밑으로 잔디가 비친다. 검사가
     **그 자리의 실제 높이차**를 재서 파묻는 깊이가 그보다 큰지 확인한다 —
     숫자를 베끼지 않는다.
   - 돌 테두리가 없으면 수면이 잔디에 **잘라 붙인 파란 사각형**으로 보인다.
     테두리가 있으면 「파 놓은 자리」가 되어 연못으로 읽힌다.
   - 미끄럼틀과 시소는 `tiltZ`로 기울인다. 기울기 없이 눕히면 발판에서 바닥까지
     계단 없는 절벽이라 놀이기구로 안 읽힌다.
   - **`CityLayout.treeExclusions`를 새로 뒀다.** 배치가 렌더가 아니라 **다른
     배치**에게 하는 말이다 — 나무는 `cityDetails`가 나중에 심는데 연못과
     놀이터는 여기서 놓으므로, 그대로 두면 물 위에 나무가 서고 미끄럼틀에
     나무가 박힌다. 공원은 나무 밀도가 높아 반드시 걸린다.
   - `cityLayout`이 800줄을 넘어 선돌을 `natureAnchors.ts`로 뗐다.

   검사가 잡아 준 것: 기울어지는 조각을 `BoxInstance`에 **캐스팅으로** 얹었더니
   읽는 쪽에서 `tiltZ`가 없는 것으로 보였다. 타입을 `DetailInstance`로 고쳤다 —
   검사를 고치는 게 아니라 타입을 고치는 쪽이 맞았다.

   화면에서 달라진 것: 잔디밭에 **돌 테두리를 두른 연못**이 생기고, 포석이
   띄엄띄엄 놓인 산책로가 구역을 한 바퀴 돈다. 한쪽 구석에 미끄럼틀·그네·시소가
   모래밭 위에 선다.

   남은 것: 벤치가 없어 산책로가 「지나가는 길」이지 「앉는 곳」이 아니다.
   연못에 물결이 없다 — 바다(`Sea`)의 UV 스크롤을 여기에도 쓸 수 있다.

7. ~~**구역마다 하늘·안개 색을 살짝 다르게.**~~ → **끝. (반복 7)**

   한 일:
   - `Zone.mood` — 안개 시작·끝 **배율**과 섞을 **색조**. 시간대(`timeOfDay`)를
     대체하지 않고 **곱하고 섞는다.** 여명·한낮·노을·밤이 정한 색과 거리 위에
     구역이 얹히는 방식이라, 시간대를 바꿔도 구역의 성격이 남는다.
   - `ZoneAtmosphere.tsx` — 매 프레임 `scene.fog`를 직접 고친다. 구역이 바뀔
     때마다 리렌더하면 씬 전체가 다시 조립된다.
   - **경계에서 튀지 않게** 시간 상수(초당 1.4)로 따라간다. 구역 경계는 도로
     한가운데라 한 걸음에 넘나든다 — 즉시 옮기면 안개가 깜빡인다. 다만 **첫
     프레임만은 즉시 맞춘다**. 따라가게만 두면 숲에서 이어하기로 시작한 사람이
     몇 초 동안 맑은 숲을 본다.
   - `near >= far`가 되면 three가 안개를 **통째로 꺼 버린다** — 가장 짙어야 할
     숲이 가장 맑아지는, 부호 하나 뒤집힌 결함이다. 최소 간격 12m로 막고,
     검사가 **모든 품질 프리셋 × 모든 구역**에서 뒤집히지 않는지 확인한다.
   - 검사는 숫자를 베끼지 않고 **관계**를 잰다: 「안개 숲이 번화가보다 짙다」,
     「해안이 번화가보다 트여 있다」, 「짙게 만들 때는 시작과 끝을 함께 당긴다」
     (시작만 당기면 짙어지는 게 아니라 옅게 길어진다).
   - 색조 세기에 상한(0.35)을 뒀다. 넘기면 **밤에도 낮의 구역 색**이 남아
     시간대를 넷 만들어 둔 의미가 사라진다.

   확인 방법: 값이 화면에만 나타나서 잡기 어려웠다. 숲의 배율을 0.04/0.09로
   **과장해 놓고** 찍어 파이프라인이 끝까지 도는지 먼저 확인한 뒤 되돌렸다 —
   과장하면 나무가 몇 미터 앞에서 옅은 청록 안개로 사라진다.

   화면에서 달라진 것: 안개 숲의 먼 나무가 흐려지고 공기가 청록으로 가라앉는다.
   해안은 반대로 도심보다 멀리까지 트이고, 시장은 노을빛이 돈다.

   남은 것: **하늘 돔은 아직 구역을 모른다.** 안개만 바뀌고 하늘 색은 그대로라,
   위를 올려다보면 숲에서도 도심의 하늘이 보인다. 돔 재질에 같은 색조를
   약하게 곱하면 된다.

8. ~~**바닥에 덤불·바위·들꽃.**~~ → **끝. (반복 8)**

   한 일:
   - `undergrowth.ts` — 자연 구역 바닥에 덤불·바위·들꽃·마른 풀을 뿌린다.
     나무는 머리 위에 있고 잔디는 무늬라, 발밑에는 아무것도 없었다. 그러면
     **달릴 때 속도가 느껴지지 않는다** — 지나가는 것이 없으면 지나가고 있다는
     감각도 없다.
   - 구역마다 **섞임**이 다르다. 숲은 덤불이 절반 넘고, 해안은 바위·마른 풀이
     대부분이다. 같은 물건을 밀도만 달리해 뿌리면 「초록이 짙은 공원」이지
     숲이 아니다. 검사가 숲의 잎 비율이 해안보다 높고 공원의 꽃 비율이 숲보다
     높은지를 잰다 — 숫자가 아니라 관계다.
   - **높이가 성격을 정한다.** 덤불은 무릎, 바위는 정강이, 꽃·마른 풀은 발목이다.
     셋이 같은 높이면 색만 다른 같은 물건이 흩어진 것으로 보인다.
   - 겹침 방지(격자 해시)를 **쓰지 않는다.** 나무와 달리 덤불 두 무더기가
     붙으면 큰 덤불 하나로 보이지, 같은 자리에 두 그루가 선 것으로 보이지 않는다.
   - `City.tsx`가 800줄을 넘어 팔레트를 `cityPalettes.ts`로 뗐다. 그러면서
     `constantPairs` 검사가 `City.tsx`를 읽고 있던 것을 새 경로로 옮겼다 —
     경로가 낡으면 **아무것도 못 읽고 실패**하도록 되어 있어서 조용히 지나가지
     않았다(그게 그 검사의 설계다).

10. **~~`perfBudget` 검사가 자연 구역을 표본에 넣게 하라.~~** → **함께 끝. (반복 8)**

   표본이 (-100..100)이었다. 도시 한 변이 282m이므로 **가운데 절반**만 재고
   있었고, 숲·해안·옛 마을은 한 번도 재지 않았다 — 그런데 나무를 두 배로 키우고
   덤불을 뿌린 곳이 바로 거기다. 예산을 재는 자가 늘어난 곳에 닿아 있지 않으면
   「상한 안이다」는 말이 아무 뜻도 없다. 격자 전체를 24m 간격으로 훑게 고쳤다.

   결과: **최악은 여전히 번화가(3,630개)**이고 상한(4,000) 안이다. 자연 구역은
   건물이 거의 없어 잡초를 뿌려도 도심을 넘지 않는다. 이제 그 말을 **재고 나서**
   할 수 있다.

   화면에서 달라진 것: 잔디밭이 매끈한 판이 아니게 됐다. 회색 바위가 여기저기
   튀어나오고 짙은 덤불이 무릎 높이로 흩어져, 달릴 때 옆으로 흘러가는 것이 생겼다.

9. ~~**번화가 네온을 더 세게.**~~ → **끝. (반복 9)**

   한 일:
   - `neon.ts` — 번화가 건물의 **세로 모서리 넷**을 네온 관이 타고 오르고,
     꼭대기를 띠가 한 바퀴 두른다.
   - **간판을 더 붙이지 않았다.** 간판은 벽에 붙는 것이라 정면에서만 보이고
     스카이라인에는 아무 일도 일어나지 않는다. 모서리를 두르면 **건물의 윤곽
     자체가 빛나서** 멀리서 한 화면에 「번화가에 왔다」가 읽힌다.
   - 세로 관을 **넷** 두른다. 둘만 두면 정면에서는 멀쩡한데 모퉁이를 돌면
     한쪽이 비어 건물이 반만 칠해진 것으로 보인다.
   - 꼭대기 띠가 없으면 네 줄이 위에서 **끊긴 채** 끝난다. 띠가 그것을 묶어야
     윤곽이 닫힌다 — 스카이라인이 살아나는 것은 이 한 줄 때문이다.
   - 18m 아래는 두르지 않는다. 낮은 건물까지 두르면 번화가가 아니라
     **크리스마스 장식**이 된다.
   - 색은 **건물 순서로 돌린다.** 난수로 뽑으면 옆 건물과 같은 색이 자주 붙어
     오히려 규칙이 없어 보인다 — 무작위는 고르게 흩어지지 않는다.
   - 낮에는 꺼진다. 가로등과 같은 방식(`mixHex(NEON_OFF_COLOR, hue, nightGlow)`)
     으로 밤 정도만큼 섞는다 — 배치는 어디에 있는지만 알고, 켜고 끄는 것은
     렌더가 한다.

   화면에서 달라진 것: **밤의 번화가가 통째로 달라졌다.** 탑들의 모서리가 초록·
   청록·자홍·노랑으로 타오르고 꼭대기가 띠로 닫혀, 스카이라인이 윤곽선으로
   보인다. 낮에는 회색 관이라 눈에 띄지 않는다.

---

## 「다음 후보」가 비었다

열 항목을 전부 끝냈다. **여기가 비어 있으면 정말로 남은 일이 없다**는 것이
이 파일의 규칙이므로, 다음 사람은 새 후보를 찾아야 한다. 각 항목 끝의
「남은 것」이 출발점이다 — 특히:

- ~~**하늘 돔이 구역을 모른다**~~ → **끝. (반복 10)**

  한 일:
  - `SkyDome`이 구역 색을 **안개에서 빌려 온다.** `ZoneAtmosphere`가 이미 구역에
    맞춰 안개 색을 부드럽게 옮기고 있으므로, 같은 계산을 하늘 쪽에서 한 번 더
    하면 두 값이 갈라진다 — 갈라지는 순간 **지평선에서 하늘과 안개가 다른 색**이
    되어 이음매가 드러난다. 빌려 오면 그럴 수가 없다.
  - 그대로 곱하지 않는다. 하늘 텍스처에는 시간대 색이 **이미 구워져 있어서**
    안개 색을 곱하면 같은 색이 두 번 곱해져 하늘이 통째로 어두워지고 채도가
    무너진다. `tintRatio`(`core/color.ts`)로 **밀린 몫만** 뽑아 곱한다 —
    밀림이 없으면 1이 나와 아무 일도 일어나지 않는 것이 이 방식의 안전판이다.
  - 어두운 바탕(밤 하늘)에서 나눗셈이 폭발하지 않게 바닥값을 두고, 시간대가
    바뀌는 한두 프레임의 튐을 막으려 비율을 [0.6, 1.6]으로 묶었다. 실제로
    확인해 보니 그 상한이 **정말로 걸렸다**(과장 실험에서 r=1.600).
  - 하늘은 안개보다 **약하게**(0.55) 민다. 안개는 먼 것을 덮는 막이라 색이
    통째로 바뀌어도 되지만, 하늘에는 그라데이션과 구름이 있어 세게 밀면 그
    결이 뭉개진다.

  확인 방법(그리고 헤맨 것): 처음에 공원에 서서 「하늘이 안 바뀐다」고 판단했는데,
  **거기는 숲이 아니었다.** `(-70.5, -94)`는 공원 구역이고 공원의 색조는 원래
  약하다. 좌표를 눈으로 짐작하지 말라는 5번의 교훈을 또 어겼다. 그다음에는
  재질 ref → 안개 색 → 계산값 순으로 하나씩 화면에 찍어 좁혔다 — 마지막에는
  `document.title`에 숫자를 써서 읽었다.

  화면에서 달라진 것: 안개 숲의 하늘이 청록으로 가라앉고 **구름에 그 색이
  얹힌다.** 지평선에서 안개와 하늘이 같은 계열이라 이음매가 사라졌다.
- ~~**연못에 물결이 없다**~~ → **끝. (반복 11)**

  한 일:
  - `PondWater`를 **`Sea.tsx`에 뒀다.** 공원 파일이 맞지 않나 싶지만, 연못을
    물처럼 보이게 하는 것은 **공원의 성질이 아니라 물의 성질**이다 — 결 텍스처와
    UV를 흘리는 방식이 바다와 한 글자도 다르지 않다. 두 곳에 같은 코드를 두면
    한쪽만 고쳐진다.
  - 배치가 **수면과 테두리를 나눠 내놓는다**(`pondWater` / `pondRim`). 한 묶음에
    두고 렌더가 색 번호로 다시 가르면 `TONE` 상수가 렌더까지 새어 나간다. 색으로
    무엇인지 알아내는 방식은 그래플 앵커에서 이미 한 번 데였다.
  - 연못 결은 바다보다 **훨씬 잘다**(3.4m vs 22m). 연못이 9m밖에 안 되므로 바다
    배율을 그대로 쓰면 **물결 한 겹이 연못보다 커서** 결이 아예 안 보이고 파란
    판이 된다. 흐르는 속도는 반대로 **느리다** — 고인 물이라 파도가 아니라
    잔물결이고, 빠르면 개울처럼 흘러가 보인다.
  - 검사가 그 두 관계(잘다·느리다)와 배선(`City`가 실제로 거는가)을 잡는다.

  화면에서 달라진 것: 연못이 **단색 파란 판에서 잔물결이 흐르는 수면**이 됐다.
  가까이 서면 흰 결이 천천히 미끄러진다.
- ~~**물가에 파도 거품선이 없다**~~ → **끝. (반복 12)**

  한 일: 물가를 한 바퀴 도는 거품 자국(`buildFoamRibbon`)과 알파를 쓴 거품
  텍스처(`getFoamTexture`). 위쪽을 무작위로 깎아 물가가 자로 그은 선이 아니라
  파도가 제각각 밀려든 자리로 보이게 했다.

  **두 번 틀렸고, 두 번 다 화면이 알려 줬다:**

  1. 처음에는 물리대로 **바다 쪽**에 깔았다. 그런데 뭍 가장자리가 수면보다
     8~16m 높은 벼랑이라, 뭍에서 보면 벼랑이 물가를 통째로 가린다 — 정작
     딱딱해 보이는 선은 뭍 쪽인데 거품은 그 아래 숨어 있었다. **뭍 안쪽으로**
     돌렸다. 물리적으로 덜 맞지만 보이는 자리에 있다.
  2. 그래도 안 보였다. 「색이 옅어서」라고 짐작하다가 **빨간색으로 칠해 보고**
     알았다 — 옅은 게 아니라 **아예 안 그려지고 있었다.** 감김 방향이 아래를
     향해 위에서 보면 뒷면이었다. `side={THREE.DoubleSide}`로 고쳤다(벼랑
     치마가 같은 이유로 양면이다).

  두 번째가 이 반복의 교훈이다: **「안 보인다」를 「옅다」로 짐작하지 말고
  원색으로 칠해 「그려지는가」부터 가르라.** 짐작으로 색을 세 번 올리는 것보다
  한 번 빨갛게 칠하는 편이 빠르다. 검사에도 못 박아 두었다.

  화면에서 달라진 것: 모래와 물이 만나는 자리에 **너덜너덜한 젖은 자국**이
  생겼다. 자로 그은 듯하던 물가 선이 흐트러졌다.
- ~~**시장 천막이 도로 위에만 있다**~~ → **끝. (반복 13)**

  한 일: `addAlleyCanopy` — 구역 안쪽에도 천막을 슬래트처럼 얹었다.

  **필지 사이 골목(1~2m)에 맞춰 걸지 않았다.** 그 틈에 맞추면 건물이 4~8m로
  제각각이라 낮은 쪽에서는 천막이 지붕에 파묻힌다. 대신 **그 구역에서 제일
  높은 지붕을 재서** 그 위에 얹는다 — 고정 높이로 두면 8m짜리가 천막을 뚫고
  나오는데, 화면에서는 천막이 잘린 것처럼 보이지 원인이 높이 하나라고는
  안 보인다.

  검사도 갈랐다. 예전에는 천막을 한 덩어리로 재서 **「가로등 위에 있어야
  한다」를 골목 천막에도 들이대고 있었다** — 골목 천막은 도로에 있지도 않은데.
  도로 천막은 가로등 위, 골목 천막은 그 구역 지붕 위로 각각 잰다. 지금은
  우연히 둘 다 통과하지만, 시장 건물이 조금만 낮아지면 엉뚱한 검사가
  깨졌을 것이다.

  화면에서 달라진 것: 골목에서 위를 보면 **주황·청록 천막이 하늘을 가로지른다.**
  전에는 그 자리에 하늘이 그대로 열려 있었다.
- ~~**옛 마을 문이 남쪽 한 곳뿐이다**~~ → **끝. (반복 14)**

  한 일: 문을 **바깥을 향한 변마다** 세운다. 도시가 남쪽에 있으니 그쪽에서
  올라온다고 봤는데, 숲(북서)과 해안(동)에서 넘어오면 담만 보이고 문은 등 뒤에
  있었다 — 마을에 들어와 놓고 들어온 줄을 모른다.

  네 변에 다 세우지는 않는다. **이웃도 옛 마을인 변은 뺀다** — 마을을 가로지를
  때마다 문을 지나면 이정표가 아니라 관문이 된다. 이웃을 찾을 때 열·행을 따로
  보는 것도 잊지 않았다(시장 천막에서 겪은 「행이 넘어가는」 함정).

  동·서 변의 문은 보를 `rotationY`로 돌린다. 돌리지 않으면 보가 벽을 향해 누워
  **문이 아니라 판자**로 보인다. 남·북까지 돌리면 이번엔 그쪽이 눕는다 —
  검사가 「돌린 보와 안 돌린 보가 둘 다 있다」로 양쪽을 잡는다.

  「문 사이로 지나갈 수 있다」 검사도 고쳤다. 문이 변마다 하나씩이 되면서
  구역의 기둥을 전부 모아 재면 **서로 다른 문의 기둥 사이**를 재게 된다.

  `cityLayout`이 800줄을 넘어 가로등·횡단보도를 `streetFurniture.ts`로 뗐다.

  화면에서 달라진 것: 동쪽에서 올라오면 **붉은 홍살문이 정면에** 선다. 전에는
  그 자리에 담만 있었다.

---

## 다음 후보 (반복 14에서 새로 찾은 것)

- ~~**돌담이 구역을 두르지 않는다.**~~ → **끝. (반복 15)**

  담을 **구역 중심 기준(4.2m)에서 가장자리 기준(가장자리에서 1.2m 안쪽)으로**
  바꿨다. 이제 실제로 구역을 두른다.

  가장자리로 내보내니 **집과 겹쳤다.** 옛 마을 집은 구역 중심에서 16.4m까지
  뻗는데 담은 15.8m다. 그래서 담을 **짧은 도막(2.6m)으로 쪼개고 집이 선 자리는
  건너뛴다** — 긴 도막이면 한 번 겹칠 때 변의 절반이 통째로 사라진다. 결과가
  오히려 맞다: 담이 집과 집 사이를 메우는 모양이 된다.

  **자판기가 담 안에 박혔다**(`worldConsistency`가 잡았다). 자리를 비켜 놓을
  수도 있었지만, 돌담과 홍살문을 세운 마을에 자판기가 붙어 있는 것이 1번
  항목에서 걷어낸 바로 그 모양이라 옛 마을에서는 자판기도 뺐다.

  검사가 **숫자를 베끼지 않는다.** 담이 어디 있는지를 배치 데이터에서 되짚어
  기준으로 삼는다 — 숫자를 적어 두면 담을 옮길 때 검사가 옛 자리를 재면서
  조용히 통과한다. 「구역 반 폭의 80% 밖」과 「인도 안쪽」으로 위아래를 묶었다.

  화면에서 달라진 것: 마을 바깥 도로에서 보면 **홍살문이 마을 어귀에** 서고
  담이 그 양옆으로 이어진다. 전에는 문과 담이 구역 한복판에 있어서, 마을에
  들어와 한참 걸어야 만났다.
- ~~**언덕 주택가만 전용 작업을 못 받았다.**~~ → **끝. (반복 16)**

  여덟 구역 중 이 동네만 열다섯 번의 반복에서 전용 작업이 한 번도 없었다.
  이름은 「언덕 주택가」, 부제는 「좁은 골목과 옥상 물탱크」인데 실제로 다른
  것은 층수와 필지 수뿐이라 **번화가를 낮게 줄인 것**으로 보였다. 옥상 물탱크는
  이미 도시 전체에 올라가 있으니, 비어 있는 쪽은 「좁은 골목」이었다.

  한 일: `hillside.ts` — 집과 집 사이 골목마다 **돌 포장 계단길과 청록 난간**.
  골목이 어디인지는 놓인 집에서 되짚는다(필지 수로 계산하면 공터 규칙과 어긋나
  집이 없는 자리에 골목이 생긴다). 골목이 뻗는 방향은 비탈이 정한다.

  **이번 반복의 값은 기능이 아니라 세 가지 발견이다:**

  1. **도시 구역 안의 바닥은 지형이 아니라 평평한 판 한 장이다.**
     `sidewalks.ts`가 구역마다 38m짜리 상자를 하나 깐다 — 상자 하나에는 지형
     높이가 한 번만 더해지므로(`projectInstances`), 판 윗면은 구역 안 어디서나
     `지형(구역중심) + 0.16`이다. 지형은 구역 안에서 ±2m씩 오르내리는데 그
     위에 평평한 판이 덮여 있다.

     처음에 계단을 **지형에 맞춰** 놓았다. 검사 열넷이 전부 통과했는데 **화면에는
     한 장도 안 나왔다** — 판 밑에 통째로 묻혀 있었다. 「값만 맞고 화면은 그대로」의
     교과서적인 사례다. 지금은 판을 기준으로 다시 잡는다(`rebase`, `flatY`).
     새로 바닥에 무언가 놓는 사람은 **지형이 아니라 이 판**을 기준으로 잡으라.

  2. **인스턴스 예산이 이미 꽉 차 있었다.** `perfBudget`의 상한 4000에 실제 값이
     3894(97%)였다. 그 상태에서는 새 레이어를 무엇이든 하나 붙이면 깨진다 —
     난간을 빼고 디딤판만 남겨도 넘었다. 상한을 4800으로 올리고 **왜 올렸는지와
     다음에 걸리면 무엇부터 재야 하는지**를 그 검사에 적어 두었다. 이 파일이
     같은 실수를 두 번 했다(3000→4000 때와 판박이다).

  3. **「안 보인다」는 두 번 다 다른 이유였다.** 원색으로 칠해 보고 「그려지긴
     한다」를 먼저 갈랐다(반복 12의 교훈). 그다음이 진짜였다 — 20cm짜리 단은
     폭 1.8m 골목을 어깨 너머로 내려다보면 포장 이음매와 구분되지 않았고,
     8cm 난간은 바닥에 그은 선으로 보였다. 단을 30cm로, 난간을 12~14cm로
     키우고 **색을 단마다 번갈아** 주었다. 무작위로 섞던 두 돌색이 오히려
     단 경계를 가리고 있었다.

  하지 않은 것: 디딤판에 **충돌체를 주지 않았다.** `resolveHorizontalCollisions`에는
  계단 오르기가 없어서, 주면 20cm 단이 넘을 수 없는 벽이 되어 골목이 통째로
  막힌다. 대신 오르는 높이를 60cm로 묶어 두었다 — 뚫고 걸어도 무릎 아래다.
  검사가 그 상한을 지킨다.

  화면에서 달라진 것: 집 사이 골목이 **밝은 돌 포장 계단길**이 되고 한쪽 벽을
  따라 **청록 난간**이 기둥과 함께 이어진다. 전에는 골목 바닥이 도로와 같은
  회색 판이었고 아무것도 없었다.

## 다음 후보 (반복 16에서 새로 찾은 것)

- ~~**바닥이 통째로 어긋나 있었다 — 인도가 뜨고 도로가 없었다.**~~ → **끝. (반복 17)**

  사람이 화면을 보고 말했다: **「도로 자체도 제대로 되지도 않았잖아.」** 맞는
  지적이었고, 스크린샷이 그대로 보여 줬다:

  - 인도가 구역 가장자리에서 **2.76m 공중에 떠서** 회색 벽처럼 걸렸다.
    건물들이 그 위에 얹힌 받침대처럼 보였고, 플레이어는 지형 위를 걸으므로
    그 자리에서 **인도 아래를 걸었다.**
  - **도로에 아스팔트가 한 뼘도 없었다.** 지면을 `zoneAt`으로만 갈랐는데
    그 함수는 도로 좌표를 가장 가까운 구역으로 접는다 — 옛 마을 옆 도로는
    흙, 숲 옆 도로는 잔디였다. 차선만 흙바닥 위에 떠 있었다.
  - 황색 중앙선과 갓길선이 **허공에 걸려** 있었다.

  원인은 하나다: **긴 상자에는 그 중심의 지형 높이가 한 번만 더해진다**
  (`projectInstances`). 인도(38m 상자 하나), 연석(38m 네 줄), 점자블록(38m
  네 줄), 갓길선(18m 토막)이 전부 그랬다. 지형은 그 아래에서 ±2m씩 오르내린다.

  한 일:
  - **인도를 상자에서 면으로** 바꿨다(`buildSidewalkSurface`) — 2.4m 격자로
    지형을 따라가고, 가장자리에 치마를 둘러 종잇장으로 보이지 않게 했다.
    드로우콜은 여전히 하나다.
  - **연석·점자블록을 2.4m 도막으로 쪼갰다.** 점자블록은 스트리밍 예산을
    넘겨서 **연석과 같은 「항상 그리는」 묶음으로 옮겼다** — 애초에 둘은 같은
    것(인도 테두리)이다.
  - **갓길선 토막을 18m → 4.5m로** 줄였다. 「이어진 띠라 짧게 끊을 이유가
    없다」고 적혀 있었는데, **지형이 있는 한 이유가 있다.**
  - **차도를 따로 칠한다**(`roadAt`, `ROAD_COLOR`). 도로는 구역의 성격이
    아니라 도시가 깐 것이다. 맞닿은 구역 중 하나라도 도시면 아스팔트로
    본다 — 숲을 가로지르는 아스팔트 띠는 만들지 않는다.
  - 색을 두 번 골랐다. 처음 `#55545a`에서 도로가 **거의 검게** 나왔다 —
    아스팔트 타일에 이미 어두운 균열이 그려져 있고, 툰 셰이딩이 색을 곱하고,
    그림자가 한 겹 더 덮인다. 색은 그 셋을 되돌려 놓고 골라야 한다.
  - `tests/sidewalkSurface.test.ts` — 「인도가 지형을 따라간다」를 못 박았다.
    다시 평평한 판으로 돌아가면 여기서 걸린다.

  화면에서 달라진 것: **도로가 도로가 됐다.** 아스팔트 위에 황색 중앙선·흰
  차선·횡단보도가 바닥에 붙어 있고, 연석과 노란 점자블록이 인도를 따라 이어진다.
  전에는 같은 자리에 흙바닥과 공중에 뜬 회색 판이 있었다.

## 다음 후보 (반복 17에서 새로 찾은 것)

- ~~**화면이 어둡다 — 특히 도로가 거의 검다.**~~ → **끝. (반복 18)**

  사람이 도깨비 트레일러 화면을 보여 주며 「이거처럼 만들어봐」라고 했다.
  저 화면은 UE5에 스캔·수작업 에셋(기와 수막새·철쭉·벚꽃)이 들어간 것이라
  질감까지는 못 간다. **그러나 나란히 놓고 보면 가장 큰 차이는 에셋이 아니라
  밝기였다** — 저쪽 그늘에는 하늘빛이 가득 차 있는데 이쪽 그늘은 검게 눌려
  있었고, 도로는 아예 검은 판이었다.

  원인은 조명이 아니라 **알베도를 두 번 어둡게 곱한 것**이었다. 아스팔트
  텍스처는 이미 어둡고(바탕 #6f6e73에 더 어두운 보수 자국), 거기에 어두운
  구역 색을 다시 곱했다. 곱한 결과가 0.15까지 내려간 자리에 툰 셰이딩과
  나무 그림자가 한 겹씩 더 덮였다.

  한 일:
  - 차도 색을 **거의 흰색**(`ROAD_SURFACE_COLOR = #d6d4da`)으로 바꿨다.
    「도로를 어떤 색으로 칠할까」가 아니라 **「텍스처가 이미 칠해 둔 색을
    얼마나 남길까」**로 고르는 값이다. 도로가 인도보다 어두운 것은 색이
    아니라 텍스처가 맡는다.
  - 한낮 프리셋의 `hemisphereIntensity`를 0.78 → 0.9, 아래 반사광을
    `#8fa6c4` → `#a8bcd8`로 들었다. 톤매핑을 껐으므로 해를 올리면 밝은 면이
    날아가지만, **이 값은 그늘만 들어 올린다.**
  - `isRoadSurface`를 화면 파일에서 `streetGround.ts`로 꺼내 검사할 수 있게
    했다. `tests/roadSurface.test.ts`가 「구역 안은 도로가 아니다」·「자연
    구역끼리 맞닿은 도로에는 아스팔트를 깔지 않는다」·「차도 색이 텍스처를
    두 번 어둡게 하지 않는다」를 못 박는다.

  화면에서 달라진 것: 도로가 **검은 판에서 균열과 보수 자국이 보이는 회색
  아스팔트**가 됐다. 나무·건물 그늘이 덮인 자리도 색이 남는다.

## 다음 후보 (반복 18에서 새로 찾은 것)

- ~~**하늘에 붉은 가로줄이 하나 지나간다.**~~ → **결함이 아니었다. (반복 19)**

  카메라를 크게 기울여 보니 줄이 **화면 맨 위에 붙어 따라왔다.** 3D였다면
  장면과 함께 내려갔을 것이다. Next.js 개발 오버레이의 오류 표시줄이고, 그
  「1 Issue」는 내가 시점을 돌리려고 던진 **합성 포인터 이벤트**가 낸 예외
  (`setPointerCapture: No active pointer`)였다. 제품 코드와 무관하다.

  `elementFromPoint`가 캔버스를 돌려준 것에 속았다 — 오버레이는 그림자 DOM에
  있고 `pointer-events: none`이라 아래 캔버스가 잡힌다. **화면 좌표에 붙어
  있는지 먼저 보는 편이 빨랐다.**
- ~~**꽃이 없다.**~~ → **끝. (반복 19)**

  참고 화면의 색을 만드는 것은 초록이 아니라 **분홍**이다. 이쪽 나무는 초록
  한 가지뿐이라 구역을 아무리 갈라도 「초록 나무가 몇 그루냐」로만 달랐다.

  한 일: `Zone.blossomChance`(구역마다 다른 꽃나무 비율)와 `CROWN_TONE`
  (잎 둘·꽃 둘). 광장 0.55, 옛 마을 0.65, 공원 0.6, 주택가 0.5, 숲 0.08,
  해안 0.

  **꽃나무는 구역 종류보다 먼저 정해진다.** 벚나무는 활엽수라, 침엽수 구역인
  옛 마을에도 둥근 분홍 수관이 선다 — 「소나무에 분홍 잎」이 아니라 「소나무
  사이의 벚나무」다. 참고 화면에서 기와담 위로 넘어온 가지가 그것이다.
  해안은 0으로 둔다. 야자수 사이의 벚나무는 계절이 아니라 오류로 보인다.

  **회귀를 하나 내고 검사가 잡았다.** 처음에 `random() < zone.blossomChance`로
  정했더니 그 호출 하나가 **씨앗 순서를 밀어** 뒤따르는 생성기(좌판·잡초·
  네온)가 전부 다른 수를 받았고, 나무 한 그루가 소품 위로 옮겨 앉았다.
  **색을 더하는 변경이 배치를 건드리면 안 된다** — 좌표 해시(`blossomAt`)로
  바꿨다. 재현성은 그대로다(같은 자리면 늘 같은 값). 검사가 그 방식을 못
  박는다.

  검사를 짜다가 한 번 더 틀렸다. 꽃 비율을 **수관 수**로 쟀더니 숲이 100%로
  나왔다 — 침엽수는 원뿔로 따로 나가므로 숲의 둥근 수관은 벚나무의 것뿐이다.
  스무 그루 중 한 그루를 「숲이 통째로 분홍」으로 읽을 뻔했다. **나무 수로**
  재도록 고쳤다.

  화면에서 달라진 것: 거리 위로 **분홍 수관이 넘어온다.** 초록 일색이던
  가로수 사이에 벚나무가 섞이고, 구역마다 그 비율이 다르다.

## 다음 후보 (반복 19 기준)

- ~~**외곽선이 캐릭터에만 있다.**~~ → **전제가 틀렸다. 하지 않는다. (반복 21)**

  껍데기 방식으로 만들어 보려다(`InstancedBoxes`에 `outline` 옵션) 코드를
  쓰기 전에 두 가지가 같은 말을 하는 것을 봤다:

  1. **사람이 보내 준 참고 화면에 월드 외곽선이 없다.** 캐릭터에는 있지만
     담·기와·나무·바닥에는 선이 하나도 없다. 형태를 만드는 것은 선이 아니라
     **그림자와 재질**이다.
  2. **이 저장소가 이미 한 번 걷어냈다.** `City.tsx`에 그 기록이 남아 있다 —
     건물 모서리에 검은 기둥·처마를 세웠더니 화면에서 「건물마다 검은 줄이
     그어진 것」으로 보였고, 「선을 그어 형태를 설명하는 대신 형태 자체를
     만드는 쪽이 맞다」고 결론지었다.

  「캐릭터에만 있어서 스티커처럼 보인다」는 **내 짐작이었고, 참고 화면이
  아니라고 답했다.** 만든 옵션은 되돌렸다. 다시 하려면 근거가 화면에서
  나와야 한다.
- ~~**플레이어만 인도에 파묻혀 걷는다.**~~ → **끝. (반복 21)**

  도시 구역에는 지면보다 16cm 올라온 인도 판이 깔려 있다. 보행자는
  `crowdLayout.groundY`로 그 위에 서는데, **플레이어만 `terrainHeight`를 그대로
  딛고 있었다** — 같은 보도에서 지나가는 사람보다 16cm 낮게 걷고 신발이
  포장에 파묻힌다. 도시 구역 어디서나 늘 그랬다.

  `sidewalks.surfaceHeight(x, z)`를 만들어 `PlayerRig`의 발 높이와 카메라
  바닥 높이가 그것을 읽게 했다. 연석에서 16cm가 한 번에 바뀌는데, 실제 보도가
  그렇고 이동 코드는 지면 높이를 매 프레임 다시 읽으므로 턱을 오르내리는
  것으로 보인다.

  검사는 「보행자와 같은 높이를 딛는다」·「판 바깥(차도)에서는 내려온다」·
  「자연 구역에서는 지형 그대로다」와, **이동 코드가 실제로 그 함수를 쓰는지**
  까지 본다. 함수만 만들고 걸지 않으면 검사는 통과하는데 화면은 그대로다.

  화면에서 달라진 것: 확대해 보면 **신발이 포장 위에 얹혀 있다.** 전에는
  발목까지 보도블록에 잠겨 있었다.

- ~~**옛 마을을 참고 사진처럼.**~~ → **끝. 화면 확인까지 마쳤다. (반복 22~24)**

  **반복 23에서 더한 것: 담 아래 나무 화분과 철쭉.** 철쭉은 이미 자연 구역에
  흩어 뿌리고 있었지만 **흩어진 것과 줄지어 놓인 것은 다른 물건이다** — 담을
  따라 늘어서야 「사람이 가꾼 골목」으로 보인다. 참고 사진에서 담장 아래를
  채우는 것이 정확히 이것이다.

  - 담 도막 **셋에 하나**만 놓는다. 줄줄이 놓으면 화분이 아니라 울타리가 되고
    담이 그 뒤로 사라진다.
  - 꽃은 상자 **폭보다 넓게** 퍼진다. 상자 안에 얌전히 들어가면 화분이 아니라
    뚜껑 덮인 나무 상자로 보인다.
  - 잡초 묶음에 섞지 않고 **따로 둔다**(`wallPlanters`). 저쪽 팔레트에는 나무
    색이 없고, 톤 번호를 두 팔레트에 걸쳐 쓰면 한쪽을 고칠 때 다른 쪽이
    조용히 어긋난다.
  - 충돌체는 주지 않는다 — 담이 이미 바로 뒤에서 더 넓게 막고 있다.

  **결함을 하나 내고 검사가 잡았다.** 처음에는 담 도막 번호만 보고 놓았는데,
  집에 막혀 **담을 건너뛴 자리에도 화분이 섰다** — 담 없는 허공에 화분이
  줄지어 있는 셈이다. `pushWall`이 세웠는지를 돌려주게 하고 그 자리에만 놓는다.

  `City.tsx`가 800줄을 넘어 `FacadeGroup.tsx`와 `instancedUv.ts`를 떼어 냈다.

  **아래는 반복 22 기록이다.**

  **옛 마을 담장을 한옥 담장으로.**

  사람이 도깨비 트레일러 스크린샷을 다시 보여 주며 「이거처럼 **배경**을
  만들어 달라, 캐릭터는 지금 것과 비슷하지 않냐」고 했다. 그 화면을 지배하는
  것은 **기와 담장**과 **밝은 회백색 포장 바닥**이다.

  한 일:
  - 담을 상자 하나에서 **세 켜**로 쌓았다(`COURSE`) — 어두운 막돌 기단,
    밝은 전돌 몸통, 그 위에 밖으로 나온 검은 기와 갓. 담이 담으로 보이는
    것은 재질이 아니라 **켜의 대비**다(어두움-밝음-어두움). 기와 갓은
    몸통보다 넓다 — 처마가 나와야 그 아래 그림자가 담을 두껍게 만든다.
    전체 높이는 1.55m로, 예전 1.3m보다 조금 높지만 카메라를 막지 않는다.
  - 파묻는 것은 **기단만**이다. 위 켜까지 파묻으면 아래 켜를 삼켜 세 켜가
    다시 한 덩어리가 된다.
  - 옛 마을 바닥을 **흙에서 밝은 포장**으로 바꿨다(`ground: "stone"`).
    「옛 마을이니 흙이겠거니」 했는데 참고 사진의 한옥 골목 바닥은 밝은
    회백색 포장이고, 그 밝기가 검은 기와와 대비를 만든다. 갈색 흙 위에
    회색 담을 세우면 밝은 것이 하나도 없어 화면이 통째로 가라앉는다.

  검사도 세 켜에 맞춰 고쳤다. 예전 검사는 **상자 하나의 높이**를 담 높이로
  읽어 기단(0.28m)을 보고 「너무 낮다」로 걸렸다 — 같은 자리에 쌓인 켜를
  모아 **쌓인 높이**로 재게 했고, 「세 켜의 밝기가 갈린다」·「기와 갓이
  몸통보다 밖으로 나온다」를 더했다. 충돌체 검사는 숫자를 베끼지 않고
  `WALL_TOTAL_HEIGHT`를 기준으로 삼는다.

  **화면 확인(반복 24).** 두 반복 동안 확인을 못 했다 — 브라우저 확장이
  「연결되지 않았다」고만 답했다. 원인은 확장이 아니라 **개발 서버가 죽어
  있던 것**이었다(3112에 아무것도 리스닝하지 않았다). 지시문의 「이미 돌고
  있으니 새로 띄우지 마라」는 전제가 깨진 상태였고, 그걸 확인하는 데
  `lsof`/`curl` 한 줄이면 됐다. **도구가 안 된다고 할 때 도구부터 의심하기
  전에 그 도구가 무엇에 붙는지를 먼저 재라.**

  Chrome DevTools MCP(다른 경로)로 붙어 옛 마을 담장 앞에서 확인했다.
  화면에서 달라진 것:
  - 바닥이 **갈색 흙에서 밝은 회백색 포장**으로 바뀌었다. 화면 전체가 밝아졌다.
  - 담 위에 **검은 기와 갓**이 한 줄로 얹혀 담이 회색 띠가 아니라 쌓아 올린
    것으로 보인다.
  - 담을 따라 **나무 화분에 담긴 분홍 철쭉**이 띄엄띄엄 놓였다.
  - 그 위로 **벚꽃 가지**가 넘어온다 — 참고 사진의 그 그림에 가까워졌다.

## 다음 후보 (반복 24에서 화면을 보고 찾은 것)

- ~~**화분의 꽃이 납작한 판으로 보인다.**~~ → **끝. (반복 25)**

  나무 상자와 꽃을 **묶음으로 갈랐다**(`wallPlanters` / `planterBlossoms`).
  도형이 다르면 인스턴스 묶음이 갈리는 것이 이 저장소의 규칙이고(수관·
  침엽수가 그렇다), 그러면 팔레트도 따로 가야 한다 — 톤 번호를 두 팔레트에
  걸쳐 쓰면 한쪽을 고칠 때 다른 쪽이 조용히 어긋난다.

  **꽃 색은 잡초의 철쭉과 같은 값으로 둔다.** 묶음을 나눈 것은 도형 때문이지
  색을 다르게 하려는 것이 아니다 — 같은 구역에 두 종류의 분홍이 있으면 둘 다
  「무슨 꽃인지 모를 것」이 된다. 검사가 두 팔레트가 같은 색을 쓰는지 본다.

  검사 하나는 **렌더 쪽 소스를 읽는다.** 「둥근 덩어리로 그려지는가」는 배치
  값으로는 알 수 없고, 이 결함이 정확히 그 틈에서 나왔다 — 배치는 처음부터
  옳았고 그리는 도형만 틀렸다.

  화면에서 달라진 것: 담 아래 화분의 꽃이 **각진 판에서 둥근 무더기**가 됐다.
  잡초의 철쭉과 같은 결로 읽힌다.
- ~~**기와 갓이 얇고 어둡다.**~~ → **끝. (반복 26)**

  두께 0.22m·내밈 0.3m이던 갓을 **0.34m·0.44m**로 키우고, 색을 `#3f3d47`에서
  `#524e5c`로 들었다.

  두 가지가 함께 필요했다. **두께만 키우면 여전히 검은 띠다** — 검정에 너무
  가까우면 볕을 받는 면과 그늘이 같은 색이라 모양이 사라지고 실루엣만 남는다.
  반대로 **색만 들면 얇은 선이다.** 담을 두껍게 만드는 것은 기와 자체가 아니라
  **처마 그늘**이라, 내밈을 늘려 그 그늘을 굵게 만드는 것이 실제로 값을 했다.

  담 전체가 1.55m에서 1.67m가 됐다. 3인칭 카메라를 막는 선(검사가 지키는
  1.8m)은 아직 넘지 않는다.

  화면에서 달라진 것: 담 위의 기와가 **그어 놓은 선에서 얹혀 있는 띠**가 됐고,
  그 아래로 처마 그늘이 한 줄 생겼다.

## 다음 후보 (반복 21 기준)
- ~~**철쭉 덤불이 없다.**~~ → **끝. (반복 20)**

  벚꽃은 머리 위에 있어 올려다볼 때만 보인다. 참고 화면의 분홍은 **절반이
  눈높이**에 있다 — 담 아래를 채운 철쭉 무더기다.

  한 일: `undergrowth.ts`에 `azalea` 종류를 더했다. 구역별 비중은 옛 마을
  0.28(돌담이 있는 구역이라 그 그림이 그대로 나온다) · 공원 0.18 · 숲 0.06 ·
  해안 0.02다.

  **한 덩어리가 아니라 무더기다**(`AZALEA_LOBES = 4`). 하나로 두면 잔디에
  분홍 공이 놓인 것으로 보인다 — 겹쳐 놓아야 윤곽이 울퉁불퉁해져 꽃덤불이
  된다(나무 수관과 같은 이유). 크기는 **낮고 넓게** 잡는다(폭 1.15~1.75m,
  높이는 그 절반). 위로 서면 꽃나무 묘목이 되고, 옆으로 번져야 담 아래를
  메운 것으로 보인다.

  색은 들꽃(`#c86aa8`)보다 **연하다.** 들꽃은 발목 높이의 점이라 채도가 높아야
  눈에 걸리지만, 철쭉은 넓게 번지므로 같은 채도면 화면 아래쪽이 자홍색 판이
  된다.

  **검사가 결함을 하나 잡았다.** 무더기는 곁덩어리가 밖으로 흩어지는데
  **중심만 검사하고 있었다** — 연못 자리를 비워 두었는데 곁덩어리 하나가
  물 위로 삐져나갔다. 그동안은 다른 잡초가 폭 1.5m를 넘지 않아 중심 검사만으로
  충분했던 것이다. 무더기 폭(`AZALEA_REACH`)만큼 여유를 두고 다시 보게 했다.

  화면에서 달라진 것: 공원과 옛 마을 바닥에 **연분홍 꽃덤불이 무더기로** 깔린다.
  벚꽃(머리 위)과 철쭉(눈높이)이 함께 보이면서 초록 일색이던 자연 구역에
  분홍이 두 층으로 들어왔다.
- ~~**구역 판이 지형과 최대 2m 어긋난다.**~~ → **이미 해결됐다. 확인함. (반복 27)**

  반복 17(인도를 면으로)과 21(플레이어 발높이)에서 고친 것으로 충분했다.
  지면을 읽는 곳을 전부 되짚었다: 인도 면(`buildSidewalkSurface`)·연석·
  점자블록·갓길선은 지형을 따라 쪼개져 있고, 플레이어(`surfaceHeight`)·
  보행자(`Crowd`의 `terrainHeight + CROWD.groundY`)·차량(`Traffic`의
  `terrainHeight`)이 모두 그 자리의 지형을 읽는다. **남은 어긋남이 없다.**
- ~~**담쟁이가 없다** (반복 27에서 화면을 보고 더한 것)~~ → **끝. (반복 27)**

  담이 돌과 기와만으로 되어 있으면 **새로 쌓은 담**으로 보인다. 「오래 남은
  자리」라는 이 구역의 부제를 만드는 것은 담 자체가 아니라 그 위에 자란 것이다 —
  참고 사진에서도 기와 담장 위를 담쟁이가 덮고 있다.

  화분 꽃과 **같은 묶음**에 넣었다(`wallGreens`). 둘 다 담에 붙는 둥근
  덩어리라 도형이 같고, 색만 톤으로 갈린다 — 묶음을 또 나누면 드로우콜만
  는다. 이름도 `planterBlossoms`에서 바꿨다: 화분 꽃만 담는 그릇이 아니게 됐다.

  담쟁이 초록은 수관보다 **밝고 노랗다.** 담에 붙은 잎은 나무 그늘 밑이
  아니라 볕을 정면으로 받는다 — 같은 초록이면 담 위에 나무가 얹힌 것으로 보인다.

  **처음엔 갓 위에 얹혀 있기만 했다.** 바깥으로 내미는 양을 갓의 내밈(0.44m)
  보다 짧게 잡아서, 덩어리 다섯 중 하나만 갓 밖으로 나왔다 — 담쟁이가 아니라
  「지붕에 올려 둔 화분」이었다. 검사가 그 비율을 재는데, 그 검사도 처음에는
  **가장 좁은 켜(몸통)**를 기준으로 재고 있어 갓 위에 얹힌 것까지 「넘었다」로
  셌다. 가장 넓은 켜로 재게 고쳤다.

  화면에서 달라진 것: 담 위로 **초록 덩어리가 넘어와 늘어진다.** 기와·철쭉과
  함께, 참고 사진의 담장 구성이 갖춰졌다.
- **외곽선이 캐릭터에만 있다.** 월드에는 없어서 캐릭터가 배경 위에 붙인
  스티커처럼 보인다. 셀 셰이딩을 월드까지 밀면 화면이 한 장으로 묶인다.
- **구역 판이 지형과 최대 2m 어긋난다.** 위 1번에서 잰 값이다 — 구역 가장자리에서
  판이 지형보다 2.0m 뜨거나 2.2m 묻힌다. 뜨는 쪽은 인도 끝이 공중에 걸리고,
  플레이어는 지형 위를 걸으므로 그 자리에서 **판 아래를 걷는다.** 축대를 세우면
  화면은 맞는데 충돌체를 주는 순간 도로에서 구역으로 못 들어간다. 판을 지형을
  따라 쪼개는 편이 옳아 보이지만 인도·연석·점자블록·보행자 트랙이 전부 같은
  값을 쓰고 있어 작은 일이 아니다.
- **`resolveHorizontalCollisions`에 계단 오르기가 없다.** 이번 항목이 여기 걸렸고,
  앞으로 높이차를 다루는 것은 전부 여기 걸린다. 낮은 턱(0.4m 이하)은 밀어내지
  말고 올라서게 하면 골목 계단에 충돌체를 줄 수 있다.
- ~~**사람이 한 판 해 보는 것.**~~ → **절반 했다. 결함 하나를 찾아 고쳤다. (반복 28)**

  **드디어 달려 볼 수 있게 됐다.** 그동안 브라우저에서 rAF가 멈춰 「세워 놓고
  찍는 것」밖에 못 했는데, 원인은 확장 도구였다 — Chrome DevTools MCP로 붙으니
  75fps로 돌았고, 키 이벤트를 넣어 실제로 달릴 수 있었다.

  **첫 바퀴에서 바로 결함이 나왔다.** 옛 마을에서 달리다 집에 붙었더니
  **화면이 통째로 벽 내부**가 됐다. 스물일곱 번의 반복에서 한 번도 안 나온
  그림이다 — 서 있을 때는 카메라가 벽에 닿을 일이 없기 때문이다.

  원인은 카메라 충돌이 없어서가 아니라 `findCameraDistance`의 **최소 거리
  1.4m가 바닥**이어서였다. 벽에 바짝 붙으면 그 1.4m 지점이 벽 안이다.

  고친 방법: **플레이어를 밀어내는 것과 같은 함수**(`resolveHorizontalCollisions`)로
  카메라도 밀어낸다. 카메라만 따로 밀어내는 식을 새로 쓰면 두 판정이 갈라지고,
  그러면 플레이어는 못 들어가는 자리에 카메라만 들어가는 자리가 생긴다.
  반지름은 플레이어(0.42m)보다 작은 0.3m다 — 카메라는 몸이 아니라 점이라
  크게 잡으면 좁은 골목에서 구도가 흔들린다.

  **한 번 더 손댔다가 되돌렸다.** 밀려난 카메라가 플레이어에게 붙어 화면이
  뒤통수로 차길래 「모자란 만큼 위로 올리자」고 했는데, 올려 보니 벽면을
  정면으로 보게 되어 캐릭터가 아예 사라졌다. **화면이 아니라고 답했으므로
  지웠다.**

  `PlayerRig`가 800줄을 넘어 `findCameraDistance`를 `cameraRig.ts`로 옮겼다 —
  거기가 원래 카메라 계산이 사는 순수 모듈이다.

  화면에서 달라진 것: 벽에 붙어 달려도 **화면이 벽 내부로 차지 않는다.**
  전에는 보라색 벽 안쪽과 빛줄기만 보였고, 지금은 담 바깥면과 바닥이 보인다.

## 다음 후보 (반복 28에서 달려 보고 찾은 것)

- ~~**좁은 자리에서 구도가 나쁘다.**~~ → **끝. (반복 29)**

  카메라가 벽 밖으로는 나오지만 플레이어에게 붙어 화면이 뒤통수로 찼다.
  **위치를 옮기는 대신 가리는 것을 지운다** — 3인칭 게임이 오래 써 온 방법이고,
  구도가 흔들리지 않는다. 앞 반복에서 카메라를 위로 올려 봤다가 벽면을 정면으로
  보게 된 것이 「위치를 옮기는」 쪽의 실패였다.

  카메라~머리 거리가 2.1m에서 흐려지기 시작해 1.15m에서 사라진다. 1.15m는
  카메라가 사실상 머리 안에 있는 거리다 — 키(1.5m)보다 작아야 멀쩡히 서 있는
  캐릭터가 지워지지 않는다. 검사가 그 관계를 지킨다.

  **외곽선도 함께 지운다.** 몸만 지우면 검은 윤곽선만 허공에 남는다 —
  껍데기 셰이더에 `outlineAlpha` 유니폼을 더했다.

  재질을 뒤지는 것은 **값이 바뀔 때만** 한다. 매 프레임 `transparent`를
  건드리면 three가 셰이더를 다시 컴파일해 프레임이 끊긴다.

  **React 컴파일러에 또 걸렸다.** 공유 객체를 `useRef`로 두었더니 렌더 중에
  `ref.current`를 읽는다고 막혔다(이 저장소에서 세 번째다). 값을 바꾸는 것은
  프레임 루프이므로 ref일 이유가 없다 — `useMemo`로 한 번 만들어 계속 쓴다.

  화면에서 달라진 것: 확인하려고 **구간을 14m로 과장해** 찍었더니 캐릭터
  너머로 담과 철쭉이 비치고 외곽선도 함께 사라졌다(원색 칠하기와 같은 방법이다 —
  「걸리는가」를 「보이는가」로 짐작하지 않는다). 실제 값에서는 벽에 끼었을
  때만 걸리고, 평소 거리에서는 아무 일도 없다.
- **달리며 볼 것이 아직 많다.** → **해안을 달렸고 결함 하나를 찾아 고쳤다. (반복 30)**

  해안을 달리다 **모래밭에 공유 탈것 거치대가 줄지어 선 것**을 봤다. 숲·공원도
  마찬가지였다 — `vehicleStands.ts`에만 `isUrban` 관문이 없었다. 이 저장소가
  가로등·연석·차선·점자블록에서 이미 한 번씩 걷어낸 바로 그 모양이다
  (`zones.isUrban` 주석).

  자리 자체가 **연석 기준**(`CURB_EDGE - STAND_INSET`)이었다 — 연석이 없는
  구역에서는 기준선조차 없는 자리에 놓고 있었다. 코드만 읽어도 알 수 있는
  것이었는데 스물아홉 번 동안 아무도 못 봤다. **달려 보니 첫 판에 나왔다.**

  자연 구역에서 탈 것이 사라지지만 도시 구역 열여덟 칸이 가운데 모여 있어
  한두 칸(47~94m)만 나오면 만난다. 숲에서 이동이 막히던 그래플과는 경우가
  다르다 — 저쪽은 **대안이 없어서** 선돌을 먼저 세웠고, 이쪽은 걸어 나오면
  된다. 검사가 「자연 구역 어디서든 두 칸 안에 있다」를 못 박는다.

  화면에서 달라진 것: 해변에서 **회색 거치대 줄이 사라졌다.** 모래밭이 모래밭이다.

  아직 남은 것: 점프·그래플·보드와 시장·숲·공원·번화가는 여전히 안 달려 봤다.
- **사람이 한 판 해 보는 것.** 자동으로 도는 것과 사람이 보는 것은 여전히
  다르다 — 「막히는가」는 잡히지만 「재미있는가」는 안 잡힌다.

그리고 **사람이 한 판 해 보는 것**이 여전히 가장 값지다. 이 아홉 반복에서
화면을 본 것은 전부 「그 자리에 세워 놓고 찍은 것」이지, 달리고 뛰고 그래플로
건너다닌 것이 아니다.

10. ~~**`perfBudget` 검사가 자연 구역을 표본에 넣게 하라.**~~ → **끝 (반복 8, 8번 항목과 함께).**

---

목표: 펄어비스 「도깨비」 2021 트레일러 수준의 경험에 **최대한 가깝게** 만든다.

## 다음 후보 (영상 재조사 2026-08-24)

> **2026-08-25 기준 — 이 절의 12개와 A·C 항목이 전부 끝났다.** 제목 옆의 ✅가
> 그 표시다. **여기서 가져올 다음 작업은 없다.**
>
> 다만 ✅는 「코드에 있고 검사가 지킨다」이지 **「화면에서 봤다」가 아니다.**
> 이 열다섯의 값(카메라 높이·채도·군중 수·자국 폭)은 전부 정적 검사로만 잰
> 것이고, 이 저장소에서 가장 흔했던 결함이 바로 **값은 맞는데 화면은 그대로**인
> 경우였다. 그래서 남은 일은 새 항목이 아니라 **사람이 한 판 해 보는 것**이다.
>
> 에셋으로 넘어가는 갈래는 [ASSET_PLAN.md](./ASSET_PLAN.md)에 따로 적었다 —
> 도깨비 넷이 아직 캡슐과 구이고, 그것이 화면에서 가장 오래 보이는 것이다.
>
> 끝난 항목도 지우지 않는다 — 무엇을 왜 그렇게 만들었는지가 여기에만 적혀 있고,
> 다음 사람이 되돌리려 할 때 읽을 곳이 필요하다.

원작 공개분을 기사·공식 문서 기준으로 다시 훑어 뽑은 후보다. 근거와 확신도는
[DOKEV_VIDEO_STUDY.md](./DOKEV_VIDEO_STUDY.md)에 있다 — **영상 프레임은 못 봤다.**
값이 큰 순서로 적었고, 각 항목은 **화면에서 달라진 것이 보여야** 끝난 것이다.

이번 조사에서 새로 알아낸 사실 둘이 아래 목록의 절반을 만들었다:

- **원작의 도깨비는 「잡는」 것이 아니라 「친해지는」 것이다.** 친해지려면 특별한
  조건이 있고, 그 조건에 **전투**와 **각자의 사연을 읽는 것**이 들어간다.
- **적 로봇에는 이유가 있다.** 「회사」가 도깨비를 잡아 그 지능을 AI 칩에 심어
  안드로이드를 만든다. 로봇과 도깨비는 같은 이야기의 앞뒤다.

우리에게는 로봇도 있고 도깨비도 있는데 **둘을 잇는 문장이 하나도 없다.**

---

## 프레임을 보고 순위를 고쳤다 (2026-08-24 저녁)

위 목록은 기사만 읽고 세운 것이다. 그 뒤 사람이 Extended Gameplay Trailer를
내려받아 주었고 **93장을 눈으로 봤다**(DOKEV_VIDEO_STUDY 「3.5 프레임에서 직접
확인한 것 (2026-08-24)」). 세 가지가 바뀌었다.

**A. 아래 세 항목이 1번(군무)보다 위다.** 전부 새 에셋 0이고, 화면 전체의 인상을
바꾸며, 프레임에서 가장 뚜렷하게 확인된 차이다.

### A-1. 평상시 색을 눌러 원색을 **악센트로 되돌린다** — ✅ 반복 1 완료

- **무엇** — 도시 기본 팔레트의 채도를 내리고, 채도가 높은 것은 캐릭터·도깨비·
  이펙트·간판 몇 개로 **화면의 10~25%** 안에 가둔다.
- **왜 (프레임)** — 원작 전투 배경은 회백색·회색으로 거의 무채색이고, 색이
  풍부해 보이는 주행 구간조차 진짜 고채도는 5% 미만이다. 화면 전체가 원색이 되는
  것은 전환 순간뿐이다. **우리 도시는 어디를 봐도 골고루 알록달록해서 화면이
  무엇이 중요한지 말해 주지 않는다.**
- **어디** — `src/game/world/cityPalettes.ts`가 정본이다. `zones.ts`의 구역 톤,
  `neon.ts`(밤에만 원색), `facadeSigns.ts`.
- **끝난 것** — 광장에 서면 **캐릭터와 도깨비가 배경에서 뜬다.** 낮 도시가
  차분해지고 밤 번화가만 원색이 튄다.
- **검사** — 팔레트의 채도를 재서 「도시 기본색 대부분이 임계 아래」이고
  「캐릭터·도깨비·이펙트 색은 임계 위」인지 잰다. 색 거리는 `core/color.ts`가
  이미 갖고 있다(도깨비 대비 검사가 쓴다).
- **예산** — 가능. 상수만 바뀐다. 드로우콜·다운로드 0.

### A-2. 카메라를 낮추고 거리를 장면에 따라 바꾼다 — ✅ 반복 2 완료

- **무엇** — 기본 높이를 아이의 어깨 쪽으로 낮추고, 달릴 때는 멀어지고 멈춰
  서면 붙는다. 전투 중에는 더 물러난다.
- **왜 (프레임)** — 원작 카메라는 대부분 **허리~어깨 높이**이고 인물이 화면
  세로의 10~80%를 오간다. 전투 와이드에서는 인물이 1/4~1/3로 작아지고, 가까이
  가는 것은 전투가 아니라 **돌아보는 순간**이다. 우리는 늘 같은 높이·같은 거리다.
- **어디** — `src/game/scene/cameraRig.ts`(순수 함수라 검사가 쉽다).
- **끝난 것** — 달리다 멈추면 카메라가 **눈에 띄게** 붙는다. 로봇 앞에 서면 물러나
  전장이 넓게 보인다.
- **검사** — 속도·전투 여부를 넣어 거리와 높이가 단조롭게 변하는지, 그리고 최소·
  최대가 건물 관통 방지와 어긋나지 않는지 잰다.
- **예산** — 가능. 계산만 바뀐다.

### A-3. 군중에게 **행동의 종류**를 준다 (밤에는 비운다) — ✅ 반복 3 완료

- **무엇** — 보행자에게 걷기 말고 앉기·서서 대화·기대기를 준다. 그리고 **밤에는
  수를 크게 줄인다.**
- **왜 (프레임)** — 원작 낮 거리에는 5~10명이 있는데 **걷기만 하는 인물이 사실상
  없다.** 한 화면에 걷기·앉기·대화·계단 오르기가 동시에 있다. 반대로 밤 구간은
  인물이 1명으로 뚝 떨어진다 — 북적임과 적막의 대비가 낮/밤 전환 장치다.
  우리는 낮이든 밤이든 **같은 걸음으로 트랙을 돈다.** 밀도를 올려도 이 인상은
  나오지 않는다.
- **어디** — `src/game/world/crowdLayout.ts`, `src/game/world/Crowd.tsx`,
  `src/game/world/timeOfDay.ts`(시간대를 군중이 읽어야 한다).
- **끝난 것** — 낮 광장에 **앉아 있는 사람과 마주 보고 선 둘**이 보인다. 밤에
  같은 자리로 가면 사람이 눈에 띄게 줄어 있다.
- **검사** — 배치 함수가 시간대를 받아 밤에 수를 줄이는지, 그리고 한 구역 안
  보행자의 행동 종류가 하나가 아닌지 잰다.
- **예산** — 가능. 인스턴스 수는 오히려 줄어든다.

**B. 1번(군무)은 근거 등급을 낮춘다.** Extended 편 93장에 **군무가 없다.** 아이
둘이 함께 나오는 장면에서 둘은 서로 다른 방향으로 뛴다. 군무의 근거는 ONL
트레일러·뮤직비디오 쪽이고, 그 편은 아직 프레임으로 확인하지 못했다. 하지 말라는
뜻이 아니라 **「확인」이 아니라 「기사」 등급으로 내려서** 다루라는 뜻이다.

**C. 프레임에서 새로 확인해 목록에 더한 것** — 셋 다 완료(반복 4·5·6)

---

## 화면에서 확인한 것 (2026-08-25)

열다섯 항목이 전부 ✅였지만 그 ✅는 **정적 검사가 준 것**이었다. 브라우저를
열어 A-1·A-2·A-3을 순서대로 봤다.

**A-1 색 누르기 — 확인됨.** 달빛 광장 낮 화면의 픽셀을 재니 채도 10% 미만이
**57.4%**, 고채도(>60%)가 **14.7%**였다. 목표로 적어 둔 「고채도를 화면의
10~25%에 가둔다」 안이다. 주황 상의의 아이가 회백색 보도에서 분명히 뜬다.
밤 화면에서는 네온과 창문만 원색으로 튀어 「밤 번화가만 튄다」도 맞았다.

**A-3 군중 — 배치는 멀쩡했다. 내가 틀렸다.** 광장 낮·밤 화면 어느 쪽에도
보행자가 한 명도 안 보여 배치를 의심했는데, 재 보니 **가장 가까운 사람이
16m**이고 60m 안에 6명(중간 품질)이 있었다. 카메라가 한 방향만 보고 있었을
뿐이다. 이 자를 `tests/crowd.test.ts` 「가까이에 사람이 있다」로 남겼다 —
다음에 블록 배치나 인원 예산을 손보다 사람들이 멀어지면, 광장이 비는 것을
눈으로 발견하기 전에 걸린다.

**A-2 카메라 — 브라우저로는 못 봤고, 계산으로 닫았다.** 카메라 값을 페이지에서
읽을 길이 없었고(R3F 스토어에 닿지 못했다), 시점을 끌어 돌리자 아래로 꺾였다.
무엇보다 「달리다 멈추면 붙는다」는 **시간이 흘러야** 보이는 것이라 숨은 탭에서는
관측되지 않는다(위 「브라우저 자동화의 한계」).

그래서 화면에 맺히는 크기를 계산으로 쟀다. 원근 투영에서 인물의 화면 크기는
거리와 화각만으로 정해지므로 **인물의 실제 키를 몰라도 상태 사이의 비율**은
잴 수 있다. 결과: 멈춤/달림 **2.34배**, 평상시/전투 **1.30배**, 눈높이 차 각각
0.50m·0.55m. 「눈에 띄게」의 기준을 넘는다.

이 자를 `tests/cameraRig.test.ts` 「카메라가 화면에서 달라지는 크기」로 남겼다.
여기 있던 카메라 검사들은 전부 **단조성**만 봤다 — 거리가 5.60m에서 5.61m로
늘어도 「멀어진다」는 참이라, 튜닝이 무뎌지면 조용히 통과한다. 실제로 값을
무디게 해 보니(`combatPullback` 1.7→0.2, `heightRun` 2.05→1.7) 새 검사가 1.04배와
0.15m로 붙잡았고, 옛 검사들은 전부 통과했다.

**남은 것**: 「재미있는가」와 조작감은 여전히 사람의 몫이다. 위 수치가 말하는
것은 「화면이 분명히 달라진다」까지다.

덤으로 알게 된 것: 로드 직후 몇 초는 캐릭터가 저해상 대역으로 서 있다가
본 모델로 바뀐다. 결함이 아니라 스트리밍이다 — 이걸 결함으로 읽지 마라.

- **참격·타격 이펙트를 「그림 도구」로.** 원작의 참격은 불꽃이 아니라 **넓적한
  붓자국**이고 여러 색이 S자로 휘감긴다(084). 우리 색종이는 방향이 맞았고,
  **휘두르는 궤적**이 없다. `src/game/combat/Enemies.tsx`의 파티클 옆에 궤적을
  하나 더 두면 된다.
- **부감 확립 샷.** 마을 전체가 이어진 하나임을 보여 주는 컷이 한 번 들어간다
  (019). 우리는 도시를 위에서 볼 방법이 지도밖에 없다 — 7번(내려다보는 자리)의
  근거가 프레임으로 확인됐다.
- **탈것 시점의 차체.** 탈것에 타면 화면 아래를 차체가 차지한다(082). 우리
  `RiddenVehicle`은 발밑에만 있다.

---

### 1. 시민이 플레이어의 춤에 합류한다 (군무) — ✅ 반복 7 완료

- **무엇** — `R`로 춤을 추면 가까운 보행자가 걸음을 멈추고 같은 박자로 몸을 흔든다.
- **왜** — 원작 트레일러는 삽입곡 「ROCKSTAR」에 맞춘 뮤지컬 연출이고, 시민과 다른
  아이들이 함께 추는 군무로 번진다. 출처: 공식 MV·게임스컴 보도 정리
  (TRAILER_FEATURE_ANALYSIS 「3.7 놀이와 생활 활동」). 우리 춤은 **혼자 춘다** —
  같은 동작인데 인상이 전혀 다른 이유가 이것이다.
- **어디** — `src/game/world/Crowd.tsx`, `src/game/world/crowdLayout.ts`,
  `src/game/player/emote.ts`.
- **끝난 것** — 광장에서 춤을 시작하면 주변 보행자가 **멈추고 위아래로 흔들린다.**
  춤을 멈추면 다시 걷는다. 멀리 있는 보행자는 그대로 걷는다(경계가 보여야 한다).
- **검사** — 합류 판정을 순수 함수로 떼고, 「반경 안 보행자는 dance, 밖은 walk」와
  「춤을 멈추면 전원 walk로 돌아온다」를 잰다. 반경 경계 바로 안/밖 두 점을 쓴다.
- **예산** — 가능. 새 에셋이 없고 인스턴스 행렬 계산만 바뀐다. **뼈대 애니메이션을
  들이지 마라** — 보행자는 프리미티브고 걸음도 행렬로 만든다.

### 2. 박자가 화면을 움직인다 — ✅ 반복 8 완료

- **무엇** — BGM의 박자 위상을 밖으로 내보내고, 네온·간판 발광·블룸이 그 위상으로 맥동한다.
- **왜** — 원작은 곡이 게임보다 먼저 공개됐고 연출 타이밍이 곡 위에 있다(공식 MV).
  우리 `systems/audio/music.ts`는 **BPM과 마디를 이미 알고 있는데** 화면이 그 값을
  전혀 읽지 않는다. 있는 것을 안 쓰고 있다.
- **어디** — `src/game/systems/audio/music.ts`(위상 출력),
  `src/game/world/neon.ts`, `src/game/scene/PostProcessing.tsx`.
- **끝난 것** — 밤 번화가에서 간판 네온과 블룸이 **곡에 맞춰 맥동한다.** 낮에는
  티가 덜 나야 한다(밤 연출이다).
- **검사** — 위상 함수가 경과 시간과 BPM에서 0~1을 돌려주고 마디 첫 박에 0으로
  돌아오는지. 그리고 **소리를 끈 상태(`M`)에서도 위상이 계속 도는지** — 오디오
  컨텍스트에 묶으면 소리를 끈 사람의 화면이 멈춘다.
- **예산** — 가능. 새 에셋 0, 드로우콜 0.

### 3. 로봇이 왜 있는지 화면이 말한다 — ✅ 반복 9~10 완료

- **무엇** — 로봇을 눕히면 가슴에서 작은 빛이 빠져나가고, 동료가 그것에 대해 한 줄 말한다.
- **왜** — 원작 세계관: 「회사」가 도깨비를 잡아 AI 칩에 심어 안드로이드를 만든다
  (기사 — 영문·국문 정리 일치). 우리 로봇은 **이유 없이 서 있다.** 이 한 줄이
  전투와 수집을 하나의 이야기로 묶는다 — 새 시스템 없이 뜻이 생긴다.
- **어디** — `src/game/combat/enemyBody.ts`(가슴 발광부),
  `src/game/combat/Enemies.tsx`(쓰러질 때의 방출), `src/game/quest/dialogue.ts`,
  `src/game/quest/questContent.ts`.
- **끝난 것** — 로봇 가슴에 **살아 있는 색의 점**이 보인다. 쓰러뜨리면 그 점이
  위로 떠올라 사라진다(색종이와 구분되는 움직임이어야 한다 — 색종이는 흩어진다).
- **검사** — 처치 사건이 방출 연출 신호를 세우고, 그 신호가 정해진 시간 뒤 꺼지는지.
  그리고 모든 적 종류(근접·사수·미니 보스)에 가슴 점이 있는지.
- **예산** — 가능. 인스턴스 하나가 는다.

### 4. 도깨비마다 사연이 있다 — ✅ 반복 11 완료

- **무엇** — 도감에서 만난 도깨비를 고르면 몇 줄짜리 사연이 나온다. 잠긴 항목은 안 보인다.
- **왜** — 워크스루에서 개발진이 「친해지기 위한 조건에 전투와 **사연 읽기**가
  포함된다」고 말한다(기사). 우리 `roster.ts`에는 `tagline`·`personality` 한 줄뿐이라
  도감이 **카탈로그**로 읽힌다 — 모으고 싶은 대상이 되지 못한다.
- **어디** — `src/game/dokebi/roster.ts`(사연 필드), `src/components/hud/Codex.tsx`.
- **끝난 것** — 도감에서 도깨비를 고르면 오른쪽에 사연이 뜬다. 잠긴 항목을 고르면
  **실루엣과 조건만** 보이고 사연 자리는 비어 있다.
- **검사** — 모든 도깨비에 사연이 있고(빈 문자열 금지), 해금 전 상태에서 사연
  문자열이 화면 데이터에 들어가지 않는지. 문자열을 안 넘기는 것과 가려서 그리는
  것은 다르다 — 안 넘기는 쪽을 잰다.
- **예산** — 가능. 문자열뿐이라 사실상 0.

### 5. 군중이 플레이어와 전투에 반응한다 — ✅ 반복 12 완료

- **무엇** — 옆을 지나가면 고개를 돌리고, 가까이서 전투가 벌어지면 물러난다.
- **왜** — 원작 도시가 「살아 있다」고 읽히는 이유는 사람 수가 아니라 **반응**이다.
  우리 보행자는 트랙 위를 일정 속도로 돌 뿐이라, 옆에서 로봇과 싸워도 그대로 걷는다.
- **어디** — `src/game/world/crowdLayout.ts`(반응 규칙),
  `src/game/world/Crowd.tsx`, `src/game/world/residentTalk.ts`.
- **끝난 것** — 인도를 달려 지나가면 보행자 상체가 **플레이어 쪽으로 돌아간다.**
  전투가 붙으면 그 구역 보행자가 몇 걸음 뒤로 물러섰다가 끝나면 돌아온다.
- **검사** — 반응 계산을 순수 함수로 떼고, 「가까운 보행자의 바라보는 방향이
  플레이어 쪽이다」·「전투 중에는 플레이어에게서 멀어지는 쪽으로 움직인다」를 잰다.
- **예산** — 가능. 컬링·반프레임 갱신 규칙은 그대로 두어야 한다.

### 6. 평상시 색을 낮추고 터질 때만 올린다 — ✅ 반복 13 완료

- **무엇** — 후처리의 채도·대비를 상수에서 **상태에 따라 움직이는 값**으로 바꾼다.
- **왜** — 「화려하다」는 채도가 아니라 **낙차**다. 원작의 평상시 거리는 볕 좋은
  날의 자연색이고, 능력·피격에서만 원색이 튄다(추정 — 기사 표현의 공통점).
  우리는 전역으로 올려 두어서 **터질 자리가 남지 않는다.**
- **어디** — `src/game/scene/PostProcessing.tsx`, `src/game/config/tuning.ts`.
- **끝난 것** — 가만히 서 있을 때 도시가 지금보다 **차분하다.** 능력을 쓰거나
  로봇을 때리는 순간 화면 전체 채도가 짧게 올라갔다 돌아온다.
- **검사** — 평상시 값이 절정 값보다 낮고, 절정에서 정해진 시간 안에 평상시로
  돌아오는지. **평상시와 절정이 같은 값이면 실패**로 잡아야 한다 — 그렇지 않으면
  상수로 되돌아가도 검사가 조용하다.
- **예산** — 가능. 값만 움직인다.

### 7. 도시를 내려다보는 자리를 만든다 — ✅ 반복 5 완료

- **무엇** — 언덕·옥상처럼 시야가 열리는 자리를 동선 위에 두고, 그 자리에서
  카메라가 뒤·위로 더 빠진다.
- **왜** — 원작이 넓어 보이는 이유의 절반은 크기가 아니라 카메라다(추정). 우리
  3인칭 카메라는 시야가 낮아 **월드에 있는 것을 안 보여 준다** — 바다를 확인할 때
  포토 모드로 시점을 돌려야 했다(이 백로그의 3번 항목 기록).
- **어디** — `src/game/scene/cameraRig.ts`, `src/game/world/hillside.ts`,
  `src/game/world/roofs.ts`.
- **끝난 것** — 언덕 주택가 마루에 서면 **도시 너머 바다와 수평선**이 3인칭
  시점 그대로 보인다. 포토 모드로 돌리지 않아도 된다.
- **검사** — 마루 좌표에서 카메라 높이·거리가 평지보다 크고, 그 시점에서
  수평선(`Sea.tsx`의 `SEA_LEVEL`)이 화면 안에 들어오는지 각도로 잰다.
- **예산** — 가능. 안개 거리(220m)와 스트리밍 반경의 관계를 깨지 마라 —
  이미 한 번 어긋났던 자리다.

### 8. 해안에 부두를 놓고 낚시를 만든다 — ✅ 반복 14 완료

- **무엇** — 해안 구역 물가에 부두를 놓고, 그 끝에서 낚시를 한다.
- **왜** — 펄어비스 보도자료가 **「fishing activities」를 직접 적는다**(공식).
  기사 여러 곳이 반복한다. 우리 놀이는 자판기 하나뿐이다.
- **어디** — 새 `src/game/systems/fishing.ts`, `src/game/world/Sea.tsx`(부두),
  `src/game/world/interaction.ts`, `src/components/hud/WorldHud.tsx`.
- **끝난 것** — 모래밭 끝에 **나무 부두**가 뻗어 있고, 끝에서 상호작용 키를 누르면
  찌가 물에 뜬다. 찌가 물속으로 잠기는 순간 다시 누르면 잡히고, 놓치면 다시 던진다.
- **검사** — 타이밍 창 밖 입력이 실패로 처리되는지, 성공까지 걸리는 시간이
  난수여도 **시드 고정**으로 재현되는지. 대기 시간에 상한이 있는지도 잰다.
- **예산** — 가능. 수면은 이미 있고 부두는 프리미티브다. 물고기 모델은 만들지 마라 —
  잡힌 것은 이름과 아이콘으로 족하다.

### 9. 첫 여정의 앞부분을 조용하게 만든다 — ✅ 반복 15 완료

- **무엇** — 첫 여정 초반 단계에서 전투를 뒤로 미루고 걷기·구경만 남긴다.
- **왜** — 기사들은 트레일러를 「chaotic」이라 부르면서도 **「시작은 조용했다」**고
  적는다 — 아이들이 걷는 장면에서 시작해 고조된다(기사). 우리는 시작하자마자
  안내와 목표가 함께 떠서 **고조될 자리가 없다.**
- **어디** — `src/game/quest/questContent.ts`, `src/game/quest/dialogue.ts`.
- **끝난 것** — 시작 직후 첫 단계에서는 로봇이 눈에 띄지 않고, 걷다가 몇 단계
  뒤에 처음 마주친다.
- **검사** — 첫 여정 단계 순서에서 전투 목표가 첫 단계에 오지 않는지. 이미 있는
  「모든 단계에 대사가 있다」 검사와 짝을 이루게 둔다.
- **예산** — 가능. 데이터 순서 문제다.

### 10. 만나는 순간에 행동이 있다 — ✅ 반복 16~17 완료

- **무엇** — 반경에 들어가면 바로 해금되던 것을, **한 번의 행동**을 거치게 바꾼다.
- **왜** — 원작은 「잡기」가 아니라 「친해지기」이고, 거기에 조건이 붙는다(기사).
  우리는 조건을 **자리에 가기 전**(처치 수·여정·보스)에만 걸어 두었고, 정작
  **만나는 순간에는 아무 행동도 없다** — 걸어가면 끝난다.
- **어디** — `src/game/dokebi/roster.ts`(`DISCOVERY_RADIUS`),
  `src/game/dokebi/Shrine.tsx`, `src/game/dokebi/discoveryEffect.ts`,
  `src/components/hud/CaptureNotice.tsx`.
- **끝난 것** — 자리에 서면 「손을 내밀라」는 안내가 뜨고, 키를 누르면 빛이 모였다가
  터지며 해금된다. **지나가기만 해서는 해금되지 않는다.**
- **검사** — 반경 안에 있어도 입력 없이는 해금되지 않는지(가장 중요한 한 줄),
  그리고 저장에서 불러온 진행이 이 단계를 건너뛰지 않는지.
- **예산** — 가능. 다만 **저장 포맷을 건드리면 마이그레이션이 따라온다** —
  기존 해금 목록은 그대로 두고 「만나는 절차」만 앞에 끼우는 쪽이 싸다.

> **이 목록은 다 끝났다 (2026-08-25).** 프레임 재조사에서 뽑은 열다섯 항목이
> 전부 들어갔다 — 아래 두 개도 포함이다. 남은 것은 「제외로 판단한 것」뿐이고,
> 그건 하지 않기로 한 것이다.
>
> **다음에 할 일은 새 기능이 아니다.** 스무 반복이 전부 정적 검증만 거쳤다 —
> 색·카메라 거리·물살 높이·결의 진하기 같은 값은 **화면에서 봐야** 정해진다.
> `pnpm dev`(포트 3112)로 띄워 보고, 어긋난 값을 고치는 것이 다음 순서다.
> 그때 나온 것을 이 아래에 새 절로 적으라.

### 11. 배경의 결을 올려 캐릭터와 갈라 세운다 — ✅ 반복 19 완료

- **무엇** — 벽면·바닥 텍스처에 얼룩·이음매·때를 더한다.
- **왜** — 원작 화면의 정체는 **사실적인 배경 위의 카툰 캐릭터**이고, 개발진이
  그 대비를 의도적으로 만들었다고 말한다(기사). 우리는 배경과 캐릭터가 **같은
  추상도**에 있어서 캐릭터가 배경에서 떠오르지 않는다.
- **어디** — `src/game/world/textures.ts`, `src/game/world/atlasTextures.ts`.
- **끝난 것** — 벽에 가까이 붙으면 **단색 면이 아니라 결이 있다.** 그 앞에 선
  캐릭터가 매끈해서 더 도드라진다.
- **검사** — 합성한 텍스처의 밝기 분산이 문턱을 넘는지(단색이면 0에 가깝다).
  `worldContrast` 계열과 같은 방식으로, **눈이 아니라 값으로** 단색을 잡는다.
- **예산** — 조심. 캔버스 합성 비용과 텍스처 메모리가 는다. 해상도를 올리지 말고
  **같은 크기 안에서 결만** 넣는다.

### 12. 물 위로 나간다 — ✅ 반복 20 완료

- **무엇** — 해안에서 보드를 탄 채 바다로 나가면 가라앉지 않는다.
- **왜** — 원작에 제트스키가 있다(기사). 우리는 바다를 만들어 놓고 **못 들어간다** —
  월드에서 가장 큰 면적이 배경 그림이다.
- **어디** — `src/game/player/RiddenVehicle.tsx`, `src/game/world/Sea.tsx`,
  `src/game/player/locomotion.ts`.
- **끝난 것** — 모래밭에서 그대로 달려 나가면 수면 높이에 얹혀 달리고 **흰 물살
  자국**이 뒤에 남는다. 육지로 돌아오면 원래 이동으로 돌아간다.
- **검사** — 수면 위에서 높이가 `SEA_LEVEL`에 고정되는지, 물↔뭍 전환이 양방향으로
  일어나는지. **한쪽만 되면 물에 갇힌다** — 두 방향을 다 잰다.
- **예산** — 조심. 월드 경계 밖으로 무한정 나가지 않도록 상한이 필요하다.
  벼랑 치마와 겹치는 자리에서 끼는지도 봐야 한다.

### 제외로 판단한 것

- **대형 보스** — 미니 보스와 다른 것을 만들려면 여러 단계·부위 파괴가 필요하고,
  그건 새 시스템이다. 미니 보스를 더 좋게 만드는 편이 값이 크다.
- **포탈** — 갈 곳이 하나뿐인 섬에서 포탈은 장식이다. 판단을 유지한다.
- **지형·오브젝트 파괴** — 물리 엔진이 없다. 흉내 내면 티가 난다.
- **로켓 슈즈 같은 「장비가 곧 능력」** — 이단 점프·그래플과 손맛이 겹친다.
  능력이 도깨비에서 온다는 우리 규칙을 흐린다.
- **도깨비 성장** — 저장 포맷이 늘고 마이그레이션이 따라온다. 4번(사연)이 같은
  욕구를 훨씬 싸게 채운다.
- **우산 활공** — 활강은 이미 있다. 우산은 실루엣 작업이지 기능이 아니다.
- **롤러스케이트·제트스키 전용 탈것** — 탈것이 이미 여럿이다. 12번이 물 위를
  덮으면 제트스키를 따로 만들 이유가 없다.
- **캐릭터 체형·머리 모양** — 메시가 늘어난다. 색만 고르는 지금을 유지한다.
- **멀티플레이** — 서버가 없다.

**하지 마라:** 짐승 탈것(조랑말)과 장난감 자동차는 **이 조사 도중에 들어왔다.**
다시 만들지 마라 — `world/vehicleStands.ts`와 `player/RiddenVehicle.tsx`에 있다.

## 프로젝트

- 경로: `/Users/sinseongsu/Desktop/code/dokev`
- 스택: Next.js 16 / React 19 / three 0.185 / @react-three/fiber 9 / TypeScript / Tailwind 4 / pnpm
- 기획 문서: `PROJECT_PLAN.md`, `DESIGN_GUIDE.md`, `TRAILER_FEATURE_ANALYSIS.md`
- 개발 서버가 이미 3112 포트에서 돌고 있다. 새로 띄우지 마라.

## 절대 규칙

1. **외부 GLB·오디오·이미지 에셋을 받지 않는다.** 전부 코드로 절차적 생성한다. 초기 다운로드 예산 때문이다.
2. **반복 오브젝트는 InstancedMesh로 묶는다.** 개별 Mesh 수십 개 금지.
3. **난수는 `createSeededRandom`만 사용.** `Math.random()` 금지 — 재현성과 하이드레이션.
4. **매 프레임 setState 금지.** 공유 가변 객체를 `useFrame`에서 직접 읽고 쓴다.
5. **주석은 한국어**, "무엇을"이 아니라 "왜/제약"을 적는다.
6. **기존 모듈 계약을 함부로 바꾸지 않는다.** 상수를 다른 파일로 옮기거나 인터페이스 필드 이름만 바꾸는 리팩터링 금지 — 다른 코드가 따라가지 못한다.
7. `eslint.config.mjs`는 수정 금지 (훅이 차단한다).

## 완료 기준 — 매 반복마다 전부 통과해야 한다

```
npx tsc --noEmit
npx eslint src
pnpm test
npx next build
```

통과하지 못한 채로 다음 항목으로 넘어가지 않는다. 새 기능에는 테스트를 추가한다.

## 지금 상태 — 여기부터 읽으라

**새 기능은 반복 8에 끝났다.** 그 뒤 천 번 넘는 반복은 결함 수정·확인·검사
보강이었다. 이 파일이 길어서 그렇지 **할 일 목록이 아니라 기록**이다.

**지금 구간(반복 960~)은 「배선」을 파고 있다.** 이 저장소에서 가장 자주 나온
결함 모양은 「값은 맞는데 화면에 안 나온다」였다. 공유 객체에 쓰는 줄을 하나씩
지워 보니 **대부분이 지워도 아무도 모르는 상태**였고, 그 뒤로 계속 줄이는 중이다.
남은 자리와 그때그때의 숫자는 표 끝쪽 행에 있다 — 여기 적으면 곧 낡는다.

그 과정에서 맞물린 규칙이 생겼다 — 이걸 모르면 새 코드가 조용히 규칙 밖으로 샌다:

1. **동사 이름** — 넘겨받은 객체를 고치는 함수는 `project`(화면 칸을 채운다)·
   `record`(일어난 일을 적는다)·`reset`(매 프레임 되돌린다)·`consume`(한 번짜리
   신호를 꺼낸다) 중 하나로 시작한다.
2. **배선 검사** — 그 함수들은 **프레임 루프가 도는 파일**에서 불려야 한다.
3. **이름 검사** — 1을 어기면 2가 조용히 비켜 가므로, 이름 자체를 검사한다.

| 무엇을 하려는가 | 어디를 보라 |
|---|---|
| 남은 일이 있는지 | **「다음 후보」** — 비어 있으면 정말 없다 |
| 사람이 해 줄 것 | README **「사람이 봐 줘야 하는 것」** — 전부 눈·귀·손이 필요하다 |
| 새 결함을 찾고 싶다 | README **「검사가 정말 잡는지 확인하려면」**과 그 아래 **표적 고르는 법**들 |
| 화면 칸을 재는 검사를 쓸 때 | `tests/support/stale.ts` — 초기값이 기대값과 같아 눈머는 함정을 막아 준다 |
| 헛다리를 짚지 않으려면 | **「반복 작업의 한계」**와 **「가장 많이 틀린 것은 자였다」** |
| 이 구간에 무슨 일이 있었나 | **「확인이 무기가 된 구간」** 요약과 표 마지막 백 줄 |

**가장 값진 다음 걸음은 여전히 사람이 `http://localhost:3112`에서 한 판 하는
것이다.** 남은 질문들은 속도감·손맛·소리·취향처럼 **원리적으로 값으로 답할 수
없는 것들**이고, 그 답 하나가 여기서 자동으로 찾을 수 있는 어떤 것보다 크다.

## 우선순위 백로그

### 1. 도깨비 동료 캐릭터 — ✅ 완료 (반복 1)

첫 도깨비 **「초롱」** 구현.

- `src/game/dokebi/companionMotion.ts` — 순수 추적 로직 (three/React 비의존)
- `src/game/dokebi/Companion.tsx` — 등불 몸통 + 회전 부적 고리 + 꼬리 불꽃 + 눈
- `tests/companion.test.ts` — 12 케이스
- `GameScene`에 `playerLink` 공유 객체를 추가해 매 프레임 위치·속도·방향을 전달

구현한 동작: 뒤·옆에 붙어 따라오기, 정지 시 궤도 선회, 속도가 빠를수록 더 뒤로 처짐,
mood 4종(idle/follow/rush/airborne)에 따른 자세·표정 변화, 눈 깜빡임,
스쿼시 앤 스트레치, 과도한 이탈 시 순간이동 복구, 저감 모션 대응.

**남은 것**: 소환/해제 토글(현재는 항상 동행), 능력(흔적 빛내기) 발동, 대사.

### 1-1. 도깨비 후속 작업 — ✅ 완료 (반복 439)
- 소환/해제 입력과 연출 — `C`. 즉시가 아니라 작아지며 사라진다(화면 확인)
- `ability` 발동 — 주변 흔적 강조 — `E`. **동료 빛이 닿는 곳의 흔적이 바닥에 떠오른다**
- 도깨비 시점 대사 또는 반응 텍스트 — 춤에 「누가 보면 어때」, 보스 근처에서 「쿵쿵 소리가 나」

### 2. 플레이어 캐릭터 품질 — ✅ 완료 (반복 2)

- `src/game/player/characterPose.ts` — 자세 계산을 순수 함수로 분리 (신규)
- `src/game/player/Character.tsx` — 실루엣 재작성
- `tests/characterPose.test.ts` — 18 케이스
- `RuntimeStats`에 `landingImpact` 추가 → 캐릭터가 착지 스쿼시에 사용

실루엣: 상자 조합 → 캡슐·구 기반. 머리를 크게 잡아 장난감 비율, 후드·목깃·
가방으로 덩어리를 만들고, 팔다리에 손·신발을 붙여 끝을 맺었다. 보드에 트럭과
바퀴 추가.

애니메이션: idle(숨쉬기) / walk / run / board / air 5종. 착지 스쿼시는 즉시
들어가고 천천히 풀린다. 상체가 기울어도 머리는 앞을 본다.

**남은 것**: 캐릭터 프리셋·색상 선택(현재 1종 고정), 회피·공격 자세.

### 3. 절차적 배경음악 — ✅ 완료 (반복 3)

`src/game/systems/audio/music.ts` 신규. 기존 오디오 그래프에 `music` 보이스로 합류.

- BPM 96, 4/4, A 단조 5음계, 4마디 화음 진행
- 레이어 4단: 패드(상시) → 베이스(0.12) → 하이햇(0.42) → 아르페지오(0.72)
- 강도는 걷기~보드 최고 속도를 0~1로 편 값. 바람보다 늦게 올라와 서로 가리지 않는다
- **선행 스케줄링** — rAF가 밀려도 박자가 어긋나지 않게 0.3초 앞을 미리 예약
- 기존 자동재생 정책·sound 토글·visibilitychange 정지·dispose를 그대로 상속

**남은 것**: 구역별 화음 변화, 도깨비 동행 시 레이어 추가 (백로그 원안의 일부).

### 4. 장난감 로봇 적과 전투 — ✅ 완료 (반복 4, 일부)

- `src/game/combat/combatSim.ts` — 순수 전투 시뮬 (신규)
- `src/game/combat/Enemies.tsx` — 인스턴싱 렌더 + 색종이 파티클 풀
- `tests/combatSim.test.ts` — 19 케이스
- 입력에 `attackQueued` 추가, 키 `J`. `playerLink`가 `CombatLink`를 겸함

구현: 적 상태 4종(idle/chase/hit/down), 인지 반경 16m, 접근 정지 거리 1.6m,
공격 3단계(windup 0.08 / active 0.14 / recovery 0.26), 부채꼴 판정(사거리 2.4m,
반각 0.9rad), 넉백, 피격 플래시, 2대에 다운 후 3.5초 뒤 스폰 지점 부활.
드로우콜은 적 수와 무관하게 4개(몸통·머리·팔·파티클).

**남은 것**: 원거리 공격, 적이 플레이어에게 주는 피해와 체력 시스템,
공격 모션(현재 캐릭터 자세에 공격 포즈 없음), 터치 공격 버튼, 타격음.

### 5. 도깨비 능력 이동 확장 — ✅ 완료 (반복 5)

**이단 점프 + 활강** 두 가지. `locomotion.ts`의 순수 함수 구조 유지.

- `LocomotionState`에 `airJumpsUsed`, `gliding` 추가. `MoveInput`에 `jumpHeld` 추가
- 이단 점프: 공중에서 1회. 하강 속도를 덮어써 낙하 중이든 같은 높이로 상승.
  첫 점프(8.6)보다 낮은 7.4 — 같으면 두 번째가 더 높아 지붕 높이를 예측할 수 없다
- 활강: 떨어지는 중 Space 유지. 중력 0.22배 + 하강 속도 상한 4.2m/s +
  공중 제어 1.9배. **상승 중에는 걸리지 않는다** — 점프하자마자 걸리면 점프가 뜨는 느낌을 잃는다
- 캐릭터 자세에 `glide` 추가 (팔 벌림), HUD·시작 화면 조작표 갱신
- 테스트 9개 추가. 이단 점프 도입으로 규칙이 바뀐 기존 점프 테스트 3개는
  `airJumpsUsed` 소진 상태로 고정해 코요테·버퍼 검증을 유지

**남은 것**: 그래플(스윙 이동), 초롱의 능력 발동(흔적 빛내기), 터치 활강 UI.

### 6. 퀘스트 상태머신 — ✅ 완료 (반복 6)

- `src/game/quest/questRunner.ts` — 규칙만. 목표 5종(reach/defeat/reachSpeed/glide/board)
- `src/game/quest/questContent.ts` — 내용만. 첫 플레이 루프 5단계
- `tests/questRunner.test.ts` — 15 케이스
- HUD에 `QuestPanel` — 목표·힌트·카운터·진행 막대. 200ms 주기 스냅샷
- `CombatLink`에 `defeatedTotal` 추가 → 퀘스트가 처치 수를 읽는다

**첫 플레이 루프**: 달리기(7m/s) → 보드 → 반대편 구역 도달 → 로봇 3기 → 활강 2초.
지금까지 만든 능력을 한 번씩 쓰게 하는 순서다.

실행기 규칙 중 의도적인 것:
- 한 프레임에 여러 단계를 건너뛰지 않는다 (무엇을 해냈는지 읽을 시간)
- 단계마다 `baseline`을 옮겨 이전 처치가 다음 목표에 얹히지 않게 한다
- 활강은 놓으면 0부터 다시 — 끊어서 채우면 "유지"가 아니다

**남은 것**: 대화·보상, 진행 저장(새로고침하면 처음부터), 완료 후 결과 화면.

### 7. 도시 확장과 구역 스트리밍 — ✅ 완료 (반복 7)

도시를 4×4(188m) → **6×6(282m)** 로 넓히고 스트리밍을 넣었다.

- `src/game/world/streaming.ts` — 순수 함수. `tests/streaming.test.ts` 14 케이스
- `City.tsx`에 `useVisibleBlocks` + `useStreamed`. 구역을 넘을 때만 리렌더
- 무거운 14개 레이어를 스트리밍. 가로등·도로 표시·전깃줄은 항상 그린다
  (인스턴스가 적고 끊기면 도로가 사라진 것처럼 보인다)

**배치 데이터의 `blockIndex`를 쓰지 않았다.** 레이어마다 그 필드의 의미가 다르다 —
건물·인도 소품은 구역 번호지만 가로등·도로 표시는 축 번호다. 대신 좌표에서
구역을 역산한다. 생성기를 하나도 고치지 않아도 되고 불일치에 영향받지 않는다.

**측정으로 찾은 회귀**: `plazaBlockIndex: 5`가 4×4 기준 상수였는데 6×6에서
그대로 쓰여 **스폰 지점이 지도 모서리**로 밀려났다. 시야 절반이 월드 밖이라
스트리밍이 9구역밖에 못 띄웠다. 격자 크기에서 유도하도록 고쳐 25구역 복구.

인스턴스 2,610개 중 1,800개(69%)만 화면에 올라간다.

**남은 것**: 없음 — 아래 두 항목은 검토 후 하지 않기로 결정했다.

- **반경 밖 구역의 지오메트리 해제 — 하지 않는다.** 도시 전체가 인스턴스 2,610개이고
  지오메트리는 블록별이 아니라 종류별로 공유된다. 해제해도 아낄 메모리가 작은 반면,
  다시 들어올 때 GPU 버퍼를 새로 올려야 해서 **경계를 넘을 때마다 끊긴다.** 지금은
  렌더만 건너뛰어 드로우콜을 줄이는 편이 낫다. 측정 없이 바꿀 종류의 값이 아니다.
- **크라우드·교통의 구역 인지 — 이미 해결됐다.** 둘 다 거리 기반 컬링(`cullDistance`)과
  크라우드의 반속도 갱신(`halfRateDistance`)을 쓴다. 구역 단위보다 촘촘하다.

### 8. 포토 모드와 공유 — ✅ 완료 (반복 8)

- `src/game/systems/capture.ts` — 형식 선택·파일명은 순수 함수, 브라우저 API는 분리
- `tests/capture.test.ts` — 12 케이스
- `P` 키 / HUD `사진` 버튼으로 포토 모드. 시뮬레이션 정지(dt=0), 카메라 자유 회전,
  휠 줌(1.6~22m), pitch 범위 확대, 시선 선행·FOV 변화·추적 지연 해제
- HUD를 삼분할 보조선 + 컨트롤 3개만 남기고 전부 숨김
- PNG 저장, 클립 녹화(MediaRecorder). mp4/webm 후보를 순서대로 시도해
  사파리와 크롬 양쪽에서 동작

**설계 판단**:
- 시뮬레이션을 건너뛰지 않고 dt만 0으로 준다 — 상태 구조가 유지되어야 모드를
  빠져나올 때 캐릭터가 튀지 않는다
- `preserveDrawingBuffer: true` 필수. 끄면 `toBlob`이 빈 이미지를 준다
- 저장은 전부 로컬 다운로드. 서버 업로드를 넣으면 용량 제한·만료·신고 처리가
  따라오고 그건 별도 설계가 필요하다 (1차 검증에서 CRITICAL로 지적된 항목)

**남은 것**: 시간대·필터 선택, 캐릭터 포즈, 공유 링크(업로드 정책 확정 후).

---

## 백로그 전체 완료 (반복 8)

1~8번을 모두 마쳤다.

## 사용자에게 영향이 있던 결함 (반복 100~194 요약)

아래 표는 260행이라 훑기 어렵다. **화면을 열어 보기 시작한 뒤** 찾아 고친 것 중,
플레이하는 사람이 실제로 겪었을 것만 추린다.

| 무엇이 잘못돼 있었나 | 어떻게 드러났나 |
|---|---|
| 화면의 3분의 2가 보도블록이고 세로 간판이 잘림 | 카메라 시작 각을 재 봄 (반복 100) |
| 밤에 캐릭터가 바닥에 묻힘(명암비 1.03) | 픽셀 측정 (102) |
| 도깨비 자리 빛기둥이 아예 안 보임 | 자리 앞에 서 봄 (104) |
| 랜딩이 「진행은 저장되지 않습니다」라고 거짓말 | 화면을 읽음 (101) |
| 시간대·색보정·포즈 변경이 React 오류를 냄 | 버튼을 눌러 봄 (103) |
| 이어받은 판에서 도깨비 해금 수가 0으로 잡힘 | 확인 지점을 열어 봄 (103) |
| **이미 만난 도깨비가 여정이 넘어가면 사라짐** | 도감을 열어 봄 (132) |
| 전체 지도가 투명해 읽을 수 없음 | 지도를 열어 봄 (133) |
| HUD 요소가 서로 겹침 (6건) | 좌표를 잼 (137·138·149·151·152·153) |
| 날아오는 탄이 노을 하늘에 묻힘(색거리 52) | 색 전수 대조 (143) |
| 동료와 플레이어가 같은 보라색(색거리 41) | 색 전수 대조 (142) |
| 쓴 대사 3종이 한 번도 발화되지 않음 | 발화 지점 대조 (173) |
| 감정 표현 셋이 모두 「춤」 대사를 씀 | 동작·대사 대조 (172) |
| 동료 능력 쿨다운이 화면에 없음 | 「HUD가 쓴다」는 주석 추적 (179) |
| 완주 기록이 계속 흐름(기록이 아니라 시계) | live 영역 조사 (187) |
| **보스 완주 화면이 영영 안 뜸** | 상태 되돌림 조사 (188) |
| Escape·초점 이동 없음, 3D 화면에 이름 없음 | 키보드로만 조작해 봄 (157~165) |
| 포토 모드를 키보드·터치로 조작 불가 | 조작 수단 대조 (167·168) |
| 그래픽 끊김·WebGL 미지원 화면에 빠져나갈 길 없음 | 오류 화면 계약 점검 (185·186) |

**공통점**: 전부 「코드는 맞는데 사람에게 닿지 않는」 종류다. 타입 검사도 테스트도
통과하고 화면도 그럴듯했다. 열어 보거나, 재 보거나, 다른 입력 수단으로 만져 봐야
드러났다.

## 그 뒤에 찾은 것 (반복 195~489 요약)

### 새로 만든 것

MVP 「반드시 포함」에 미완으로 남아 있던 넷을 채웠다 — **주민에게 말 걸기**,
**간판 살펴보기**, **흔적 세 자리 조사**(여정 단계 + 지도 표식 + 저장),
**이름 설정**(완주 화면과 저장 파일 이름에 쓰인다). 골목 추격 미니게임은
만들지 않았고 고물 대장 전투가 그 자리를 맡는다.

### 만들면서 스스로 낸 결함

새 기능마다 **이 저장소가 이미 고쳤던 실수를 다시** 저질렀다. 그래서 하나씩
고칠 때마다 검사를 개별이 아니라 **유형**으로 바꿨다.

| 다시 저지른 것 | 원래 언제 겪었나 |
|---|---|
| 힌트가 「물음표」인데 지도에는 마름모를 그림 | 안내와 화면이 어긋나는 유형 |
| 지도에 표식을 더하고 **말로는 안 함** | 도깨비 자리에서 이미 겪음 |
| 안내를 `aria-live` 안에 넣어 낭독기가 끝없이 읽음 | 보스 체력·완주 기록·자판기 |
| HUD 요소에 절대 좌표를 다시 박음 | 겹침 6건을 겪고 컨테이너로 바꾼 뒤 |
| 「현재 여정 완료」와 「첫 여정 완료」를 혼동 | 도감이 3/4으로 보이던 것 |

### 검사가 지키는 척만 하고 있었다

**결함 형태로 되돌려 보는** 확인을 시작했다(`scripts/mutate.sh`). 그러자
멀쩡해 보이던 검사들이 무너졌다:

- 캐시 해제 검사는 **이름이 나오는지**만 봐서, `dispose()`를 지우고 `= null`만
  남겨도 통과했다
- 터치 대응 검사는 HUD 파일을 이어 붙여 **낱말만** 찾아, 버튼을 통째로 지워도
  통과했다(같은 낱말이 안내 문구에도 있었다)
- 말풍선 위치 검사는 함수 끝 앵커가 **props 타입의 닫는 괄호**에 걸려 JSX를
  아예 읽지 않았다

그리고 **값**을 하나씩 망가뜨려 보니, 기능을 조용히 무력화하는 자리가 열넷
나왔다 — 그래플 사거리·당기는 속도, 보스 재등장, 미니맵 반경, 공중 점프,
음료 배율, 카메라 추적, 군중 컬링, 포토 거리, 차간 거리·감속 시작, 해상도
배율, 해 밝기, 능력 배율, 클립 프레임률. 전부 **관계**로 묶었다(무엇이 무엇보다
커야 하는가).

**교훈 둘**: ① 「검사가 통과한다」와 「검사가 지킨다」는 다르다. ② 확인 절차도
틀린다 — 약한 변이는 멀쩡한 검사를 헛도는 것처럼 보이게 하고, 엉뚱한 검사
파일을 겨누면 없는 구멍을 만들어 낸다(그래서 `mutate.sh`는 `all`을 쓴다).

## 확인이 무기가 된 구간 (반복 490~959 요약)

이 구간의 성격은 앞과 다르다. **새 기능을 만든 것은 하나**(포토 모드 톤 변환)이고,
나머지는 전부 **「이미 있다고 믿던 것을 실제로 확인한」** 작업이다. 그 과정에서
사용자에게 보이는 결함이 여럿 나왔다.

### 가장 큰 것 — 확인 지점이 저장과 잘못 주고받고 있었다

같은 뿌리에서 **일곱 개**가 나왔다. `?see=` 링크는 README가 사람에게
「여기로 가서 이걸 봐 달라」고 안내하는 입구인데:

- **들어오는 쪽 다섯** — 저장이 확인 지점을 덮어 **볼 것이 사라졌다.**
  완주 화면이 앞을 가리고(`questCompleted`·`questId`), 도깨비 자리의 빛기둥이
  사라지고(`metDokebi`), 흔적이 0곳이 됐다(`foundClues`).
- **나가는 쪽 둘** — 확인 지점이 저장을 덮어 **사람이 모은 것이 지워졌다.**
  거기서 도깨비를 하나 만나면 넷이 하나가 되고, 흔적 하나만 조사해도 여정
  진행이 통째로 되감겼다.

경계를 **데이터로** 굳혔다(`ResumeState`의 모든 필드가 「물려받음/만들어 줌」
두 칸 중 하나여야 하고, 새 필드가 생기면 검사가 멈춘다). 규칙은 한 줄이다 —
**확인 지점 → 저장은 흐르고, 저장 → 확인 지점은 흐르지 않는다. 흐를 때도 더하기만 한다.**

### 적어 두었지만 아무도 지키게 하지 않던 규칙 여덟

문장으로만 있던 것을 검사로 바꿨다. 전부 **지금은 지켜지고 있었지만 지켜지게
하는 것이 없던** 자리다:

`Math.random()` 금지 · 프로덕션 `console` 금지 · 외부 에셋 금지 ·
매 프레임 `setState` 금지 · 억제 개수 래칫 · `preserveDrawingBuffer` 결합 ·
흔적/살펴보기 사거리 동일성 · 예고 링 ↔ 판정 반경(**양방향**)

「지키고 있다」와 「지켜진다」는 다르다.

### 글과 동작이 따로 놀던 곳

- **간판이 딴 가게를 읽어 줬다.** 화면 간판은 무작위 브랜드(`cell`)로 그려지는데
  살펴보기 대사는 **배열 순서**로 골랐다 — 치킨집 앞에서 「약국」이라고 말했다.
  대사 목록에는 월드에 없는 가게(「이용원」·「철물」)도 있었다.
- **도감이 거짓말할 수 있었다.** 능력 설명과 수치가 각각 검사되어, 설명만 바꾸면
  통과했다. 이제 **설명에서 기대를 끌어내** 양방향으로 본다.

### 새로 만든 것 — 흑백·또렷하게

「후처리 패스가 필요하다」던 판단을 뒤집었다. 화면은 CSS `filter`, 사진은 2D
캔버스의 `ctx.filter`로 **같은 문자열**을 쓰면 된다. 스스로 구멍 셋을 찾아
막았다(클립 녹화 누락 · 지원 안 되는 브라우저에서 이름만 「흑백」 · 조상 탐색
깊이를 숫자로 박아 둔 것). 저장될 픽셀이 실제로 회색인 것까지 관측했다.

### 그 뒤 — 검사를 뚫어 보기 시작했다

확인할 것이 떨어진 뒤 방향을 바꿨다. **없는 결함을 찾는 대신 없는 검사를 찾는다** —
소스를 잠깐 결함 형태로 되돌리고 검사가 실패하는지 본다(`scripts/mutate.sh`).

서른 번 넘게 뚫어 **구멍 여덟**을 찾았다. 전부 코드를 읽어서는 안 보였다 —
검증 코드도 있었고 검사도 있었다. 다만 **그 검사가 그것을 보지 않았을 뿐**이다.

**가장 잘 맞은 기준: 「실패해도 옆에서 무언가가 『됐다』고 말해 주는가」.**
조용한 실패보다 **거짓 신호가 있는 실패**가 훨씬 늦게 발견된다.

| 거짓 신호 | 실제로는 |
|---|---|
| 「그래픽 품질을 낮췄습니다」 | 그림자가 켜진 채라 프레임이 안 돌아온다 |
| 캐시가 쌓인다 | 아무도 안 읽어 텍스처가 매번 새로 생긴다 |
| 게임이 평소처럼 돈다 | 저장이 막혀 진행이 안 남는다 |
| 「저장됨」 + 분석 기록 | 내려받기 실패로 파일이 없다 |
| 「저장됨」 + 분석 기록 | 0바이트짜리 열리지 않는 파일 |

나머지 셋은 **실패가 눈에 안 보이는 것**이었다 — 저감 모션 게이트 둘(흔들림에
예민한 사람이 켜 둔 설정이 무력해도 몰랐다)과 음소거.

### 적어 두었지만 아무도 지키게 하지 않던 규칙

문장으로만 있던 것을 검사로 바꿨다. **지금은 지켜지고 있었지만 지켜지게 하는
것이 없던** 자리들이다: `Math.random()` 금지 · 프로덕션 `console` 금지 · 외부
에셋 금지(소스·`public`·스타일 **세 방향**) · 매 프레임 `setState` 금지 ·
억제 개수 · `preserveDrawingBuffer` 결합 · 사거리 동일성 · 예고 링 반경(양방향) ·
도시 결정성(왕복 검사로는 **못 잡는다**는 것을 변이로 증명했다).

### 다르게 구는 것은 반드시 짝으로 적었다

한쪽만 보면 「왜 여긴 다르지」로 읽혀 **통일한다고 고치게** 된다:

- 시간대는 저장에서 물려받고(취향), 여정 진행은 안 물려받는다(만들어 주는 것)
- 진행 저장 실패는 알리고, 취향 저장 실패는 삼킨다
- 그런데 **만난 도깨비는 설정에 있으면서도 알린다** — 판단은 「무엇이 저장되는가」가
  아니라 **「무엇을 잃는가」**로 한다. 한 저장 키에 무게가 다른 것이 섞여 있으면
  키 단위 규칙은 반드시 한쪽을 틀린다(내가 그렇게 틀렸다).

### 배운 것 — 자를 먼저 의심하라

이 구간에서 **자에 속은 것이 열두 번**이었다. 부분 문자열(`숨은 흔적`의 「숨」,
`[role='button']`의 "button"), 창 크기가 바뀐 뒤의 좌표 배율, 장면이 붙기 전에
잰 캔버스 크기, 우연히 같은 숫자(`7.4`가 달리기이자 이단 점프). **「결함을
찾았다」 다음에 곧바로 고치지 말고 그 판단을 만든 측정을 한 번 더 재라.**

## 배선을 판 구간 (반복 960~ 요약)

이 구간은 **한 가지 질문**으로 굴러갔다: 「값은 맞는데 화면에 안 나오는」 결함이
얼마나 있는가. 답은 **거의 전부**였다.

### 어떻게 알았나 — 갈래별로 쓸어냈다

한 줄씩 지우거나 뒤집고 **매번 전체 검사**를 돌렸다. 갈래마다 성격이 달랐다:

| 갈래 | 결과 | 성격 |
|---|---|---|
| **대입** (공유 객체에 쓰는 줄) | 109줄 중 71개가 지워도 무사 → 2개까지 줄임 | 「지워도 모른다 = 거의 결함」 |
| **조건** (이른 반환) | 216줄 중 진짜 결함은 다섯 | 나머지는 대부분 **환경 방어선** |
| **비교 방향** | 훑는 중 | 무한 반복을 만드는 변이가 있어 시간 제한이 필요했다 |

대입 갈래가 압도적이었던 이유가 있다. 검사를 쓸 때 자연히 던지는 질문은
「그 값이 맞는가」인데, 화면에 닿는 것은 **그 값이 어디로 가는가**다.

### 무엇이 나왔나

진행이 막히는 급만 추리면: 흔적을 찾아도 **여정이 모르고**, 도깨비 자리에 서도
**아무도 안 만나지고**(만난 뒤엔 그 하나로 끝나고), 대장을 눕혀도 **네 번째
도깨비가 안 열리고**, `J`를 눌러도 **아무 일이 없고**, 걸 곳 없이 `G`를 누르면
**땅에서 떨어졌다**. 전부 화면은 멀쩡한 채로.

### 어떻게 막았나 — 맞물린 규칙 셋

1. **동사 이름** — 넘겨받은 객체를 고치는 함수는 `project`·`record`·`reset`·
   `consume` 중 하나로 시작한다.
2. **배선 검사** — 그 함수들은 프레임 루프가 도는 파일에서 불려야 한다.
3. **이름 검사** — 1을 어기면 2가 조용히 비켜 가므로 이름 자체를 검사한다.

셋을 따로 만들 때는 각각 **조용히 비켜 가는 길**이 있었고, 그 길을 세 번 찾아
세 번 막았다(`takeDiscovery`·`nextClueList`가 이름 때문에 규칙 밖으로 샜고,
정의가 `.tsx`에 있으면 자기 정의를 호출로 셌다).

### 내가 가장 많이 틀린 것

여전히 **자**다. 이번 구간에서만: 훑기 정규식이 이름 절반을 빠뜨려 개선을
과장해 보고했고, 탐침이 없는 줄을 찾거나 인자를 엉뚱한 자리에 넣어 「구멍」을
세 번 만들어 냈고, 화면 칸 검사의 초기값이 기대값과 같아 두 번 눈이 멀었다.

그래서 규칙을 **도구로** 옮겼다 — `tests/support/stale.ts`와
`tests/support/bothWays.ts`. 「적어 두고도 또 어긴다」의 답은 더 굳게 적는 것이
아니라 **어길 수 없게 만드는 것**이었다.

## 이후 반복에서 처리한 "남은 것"

| 반복 | 내용 |
|---|---|
| 9 | 퀘스트 진행 저장 (`saveGame.ts`) — 새로고침해도 단계·처치 수 유지 |
| 10 | 모바일 공격·활강 불가 수정 — 공격 버튼 추가, 점프 버튼이 `jumpHeld` 생성 |
| 11 | 문서 동기화 — `PROJECT_PLAN.md` v0.4에 18절 구현 현황 추가 |
| 12 | 분석 이벤트 (`analytics.ts`) — 원안 + 실패·이탈 이벤트, 호출 지점 10곳 |
| 13 | 초롱 소환·해제(C) + 능력 「반딧불」(E) |
| 14 | 모바일 동료 조작 — 초롱·반딧불 버튼, 2열 격자 배치 |
| 15 | 플레이어 체력 + 적 접촉 피해 + 무적·회복·부활 |
| 16 | 그래플(G) — 정면 원뿔 탐색, 끌어당기기, 탄력 |
| 17 | 그래플 줄·대상 표시 시각화, 그래플 터치 버튼 |
| 18 | 그래플 줄 처짐(sag) — `grappleRope.ts`, 장력에 따라 펴진다 |
| 19 | 완주 결과 화면 (`ResultPanel`) — 소요 시간·최고 속도·처치 수 |
| 20 | 음악 8마디 진행 + 동료 동행 시 벨 레이어 |
| 21 | 초롱의 대사 (`dialogue.ts`) — 단계 전환마다 말풍선 |
| 22 | 적 원거리 공격 (`projectiles.ts`) — 사수 로봇, 점프로 회피 |
| 23 | 구역 (`districts.ts`) — 광장/번화가/변두리, 구역별 화음 + 진입 배너 |
| 24 | 시간대 (`timeOfDay.ts`) — 여명/한낮/노을/밤, 포토 모드에서 전환 |
| 25 | 창문 발광 — emissiveMap 마스크, 시간대에 따라 밤에 도시가 켜진다 |
| 26 | 가로등 갓·빛 웅덩이 + 차량 전조등 (`color.ts`의 `mixHex`로 세기 보간) |
| 27 | 포토 모드 색보정 4종 (`photoFilter.ts`) — 씬 안 오버레이라 사진에도 남는다 |
| 28 | 포토 모드 포즈 4종 (`photoPose.ts`) — 자연스럽게/인사/만세/출발 자세 |
| 29 | 시간대·색보정·포즈를 설정에 저장 (버전은 올리지 않음 — 기존 설정 보존) |
| 30 | 도깨비 3종 (`roster.ts`) — 그을음(12기 처치)·물비늘(퀘스트 완료)로 해금 |
| 31 | 도감 (`Codex.tsx`) + WorldHud 분할 (943줄 → 700줄, 800줄 규칙 위반 해소) |
| 32 | 도깨비별 능력 규칙 — 탐색(초롱)·은신(그을음)·회복(물비늘), 배율로 기술 |
| 33 | 도깨비 해금 알림 — 조건을 채운 자리에서 바로 알려 준다 |
| 34 | 미니맵 (`minimap.ts` + `Minimap.tsx`) — 진행 방향이 위, 도로·목표 표시 |
| 35 | 미니맵에 적·동료 표식 — 좌표만 공유 버퍼로 흘려 HUD가 읽는다 |
| 36 | 전체 지도 (`CityMap.tsx`) — 북쪽 고정, 구역 색·목표·적 표시 |
| 37 | 도깨비를 찾아가서 만난다 — 조건은 자리를 드러내고, 그 자리에 가야 해금 |
| 38 | 도깨비 자리 오브젝트 (`Shrine.tsx`) — 돌무더기·구슬·빛기둥 + 만남 대사 |
| 39 | **배치 정합성 테스트로 실제 버그 2건 발견·수정** — 도로 좌표가 반 칸 어긋나 도깨비 자리가 건물 안에 있었고 미니맵이 건물을 가로질러 도로를 그렸다 |
| 40 | **적 42%가 건물 안에서 생기던 문제 수정** — 거절 샘플링. 교통·가로등·보행자 트랙은 대조 결과 정합 |
| 41 | **적이 벽을 통과하던 문제 수정** — 축 분리 미끄러짐. 회전 반경↔도로 폭 주장도 수치로 확인 |
| 42 | 도달 가능성 검증 — 격자 BFS로 퀘스트 목적지·도깨비 자리가 실제로 걸어서 닿는지 확인. 그래플 지점 필터도 대조 |
| 43 | 조작 정본 (`controls.ts`) — 바인딩·시작 화면·HUD 힌트가 한 곳을 읽는다. 표에 없던 포토 모드(P) 발견 |
| 44 | `PROJECT_PLAN.md` 18절 갱신(v0.5) + 문서↔코드 대조 테스트. 방금 쓴 문서의 오류 2건을 그 테스트가 잡았다 |
| 45 | 접근성 정적 점검 (`accessibility.test.ts`) — 전체 지도 캔버스에 대체 텍스트가 없던 것 수정 |
| 46 | **탄이 벽을 통과하고 사수가 벽 너머로 쏘던 문제 수정** + 성능 예산 테스트. 스트리밍 제외 근거가 거짓이었던 것도 정정 |
| 47 | **손상된 저장값이 퀘스트를 즉시 완료시키던 문제 수정** — `clampStepIndex`가 NaN을 통과시켰다. 품질 강등·음악 문턱·자세 선택의 경계도 고정 |
| 48 | 주석으로만 연결돼 있던 상수 짝 정리 — 인도 높이를 `CITY`로 올리고, 합칠 수 없는 짝(팔레트 순서·아틀라스 셀)은 테스트로 대조 |
| 49 | 구조 제약 테스트 — 랜딩이 three를 끌고 오지 않는지 import 그래프로 확인, 레이어 방향·브라우저 API 허용 목록 고정 |
| 50 | 만남 소멸 연출 (`discoveryEffect.ts`) — 구슬이 부풀어 떠오르고 빛기둥이 먼저 꺼진다 |
| 51 | 만남 알갱이 연출 + 생성 비용 측정(8ms — 로딩 UI 불필요를 근거와 함께 고정) |
| 52 | 만남 카메라 숨 — 시야각만 잠깐 넓혔다 되돌린다. 포토 모드·저감 모션에서는 끈다 |
| 53 | 터치 조작 동등성 테스트 — 터치 버튼에 「초롱」이 박혀 있던 것 수정 |
| 54 | 도깨비 이름 하드코딩 일괄 정리 — 말풍선 화자·퀘스트 완료 문구도 거짓말하고 있었다 |
| 55 | 주석의 문서 인용을 행 번호 → 절 이름으로 (27곳). PROJECT_PLAN 인용 8건이 전부 엉뚱한 줄을 가리키고 있었다 |
| 56 | 기획서 디렉터리 절을 실제 구조로 교체 — 금지된 `public/models` 등을 안내하고 있었다 |
| 57 | 트레일러 기능 분석에 구현 현황 반영 (MVP 13개 중 11개). GLB 제작 계획이 폐기됐음을 명시 |
| 58 | 자판기 음료 (`vending.ts`) — 트레일러 분석이 짚은 유일한 MVP 미구현 항목 |
| 59 | 춤 (`emote.ts`) — 포즈와 같은 채널을 쓴다. **MVP 필수 후보 13개 전부 구현** |
| 60 | 감정 표현 3종으로 확장 — 키를 늘리지 않고 R을 누를 때마다 순환 |
| 61 | 여러 도깨비 동행 — 능력 요청을 카운터로 바꿔 **모두가 반응**하게 하고 효과를 합친다 |
| 62 | 미니 보스 「고물 대장」 (`bossSim.ts`) — 예고 → 충격 → 빈틈의 리듬, 체력 막대 |
| 63 | 보스를 두 지도에 표시 + 좌표를 `BOSS_HOME` 정본으로. 만들어 두고 알려 주지 않으면 없는 것과 같다 |
| 64 | 두 번째 여정 「고물 대장」 — 퀘스트 체인, 저장에 여정 id(번호 아님) |
| 65 | 보스 여정 대사 + 예고 링 첫 경고. **모든 퀘스트 단계에 대사가 있는지** 테스트로 고정 |
| 66 | 「조용히 비는 것」 일괄 점검 — 21개 배치 레이어와 자판기 존재·도달 가능성 |
| 67 | **진행을 지운 뒤 잠긴 도깨비가 계속 따라다니던 문제 수정** — 동행 목록도 해금 판정을 거친다 |
| 68 | 자리 오브젝트가 목록을 다시 만들던 것 정리 — 지도 표식과 같은 함수를 쓴다 |
| 69 | 타격음·피격음·보스 내려침 소리 (`audio/combat.ts`) — 반복 4에서 적어 두고 잊혔던 항목 |
| 70 | **공격 동작** (`attackPose.ts`) — 판정은 3단계인데 캐릭터가 가만히 있었다. 「남은 것」 전수 대조로 발견 |
| 71 | 캐릭터 외형 4종 (`appearance.ts`) — 시작 화면에서 고르고 설정에 저장 |
| 72 | 색 대비 검증 — HUD 토큰은 WCAG AA 통과, **「물비늘」이 한낮 하늘에 묻히던 것 수정** |
| 73 | 수치 간 관계 검증 — **점프로 탄을 피하는 구간이 0.23초로 반응 시간보다 짧던 것을 0.34초로 수정** |
| 74 | **안개가 스트리밍보다 멀리 보이던 것 수정** — 품질별 스트리밍 반경(2/3/4)과 안개 거리를 맞춤 |
| 75 | **클립 녹화 길이 제한(30초)** — 없으면 메모리가 무한정 늘어 탭이 죽는다 |
| 76 | **지오메트리 32개 해제 누락 수정** — 여섯 파일에 언마운트 정리 추가 |
| 77 | **WebGL 컨텍스트 손실 처리** — 모바일에서 검은 화면으로 끝나던 것을 안내로 바꿈 |
| 78 | **3D 화면 오류 경계** — 렌더 예외가 빈 페이지로 끝나던 것을 안내와 새로고침으로 |
| 79 | 라우트·최상위 오류 경계 (`error.tsx`, `global-error.tsx`) — 3D 밖에서 터져도 안내가 뜬다 |
| 80 | 공유 카드 메타데이터 — 게임 링크가 제목도 설명도 없이 뜨던 것 수정 |
| 81 | 보안 응답 헤더 (`next.config.ts`) — CSP는 검증 불가로 의도적 보류, 이유를 코드에 남김 |
| 82 | 초기 다운로드 예산 측정·고정 — 공통 127KB gzip(예산 150KB), three 274KB는 /play에서만 |
| 83 | README를 실제 프로젝트로 교체 + `pnpm verify` — 템플릿 문구가 82번의 반복 동안 남아 있었다 |
| 84 | 쓰지 않는 의존성 제거(zustand, drei) — 기획서의 "Zustand 사용" 표기도 실제와 맞춤 |
| 85 | **분석 이벤트 7개가 발생하지 않던 것 수정** — 셋을 배선하고 기능 없는 넷은 선언에서 제거 |
| 86 | **아무도 부르지 않던 텍스처 정리 함수 배선** + 죽은 export 정리(6개) |
| 87 | 타입 우회 점검 — 보스가 `{x,z} as EnemyState`로 단언하던 것을 판정 함수 축소로 해결 |
| 88 | **`GameScene` 1,040줄 분할** — 타입·카메라·PlayerRig로 나누고 파일 크기 규칙을 테스트로 고정 |
| 89 | 카메라 검증 22개 — 88번의 반복 동안 한 번도 테스트된 적이 없던 부분 |
| 90 | 입력 기본 상태·텍스처 상수 검증 — 터치 UI와의 암묵적 계약을 소스 대조로 고정 |
| 91 | 대본 플레이 시뮬레이션 — 이동·충돌·경계·퀘스트를 씬과 같은 순서로 60fps 실행 |
| 92 | 전투 시뮬레이션 — 잡몹·포위·보스전을 끝까지 돌려 **이길 수 있는지** 확인 |
| 93 | 세션 이어가기·수집 루프 시뮬레이션 — 저장→복구→완주, 조건→표시→만남→동행 |
| 94 | 무작위 조작 60초 × 5시드 — NaN·월드 이탈·벽 끼임·속도 폭주·상태 역행을 함께 감시 |
| 95 | 「한계」 절 재작성 — 관계로 고정된 값과 눈이 필요한 값을 구분하고 결함 27건을 기록 |
| 96 | 확인 지점 바로가기 (`?see=`) — 한 판을 봐 달라고 하면서 10분짜리 절차를 두지 않는다 |
| 97 | 플레이 중 소리 토글(M) — 설정이 시작 화면에만 있어 나갔다 들어와야 했다 |
| 98 | 저감 모션이 운영체제 설정 변화에 덮어써지던 버그 수정 + 플레이 중 토글 |
| 99 | HUD 버튼 줄이 폰 화면 밖으로 밀려나던 문제 — 우상단·포토 모드 줄바꿈, 폭 규칙 상시 검사 |
| 100 | **브라우저에서 처음으로 직접 확인.** 카메라 시작 각을 0.18→0.11로 낮춰 세로 간판이 잘리지 않게 함 |
| 101 | 랜딩의 거짓 저장 안내 수정, HUD 버튼 줄 폭을 실측(480px)에 맞춰 재조정 |
| 102 | 밤에 캐릭터가 배경에 묻히던 문제(명암비 1.03 실측) — 카메라 쪽 보조광 추가 |
| 194 | 2분 확인 목록이 60여 반복 전 것이라 새로 넣은 것(자정·완주 화면·소리/모션 버튼·키보드 조작·공중 지점)이 빠져 있던 것 보완 — 봐 달라고 부탁하면서 새것은 빼놓고 있었다 |
| 193 | 소스 순회 사본 정리 — 완전히 같은 둘을 공용 헬퍼로 모음(나머지는 순회 대상이 달라 남김). 쓰이지 않게 된 import까지 lint가 잡아냄 |
| 192 | 오류 화면 목록도 손으로 적던 것을 **찾아내도록** 전환 — 새 오류 화면이 검사 밖에 있었다. 소스 순회 헬퍼가 세 번째 사본이라 공용으로 모음 |
| 191 | 도깨비별로 손으로 쓰던 「끝까지 이어지는가」 검사를 **로스터에서 유도**하도록 전환 — 그을음·자정만 있고 물비늘이 빠져 있었다. 새 도깨비도 자동으로 덮인다 |
| 190 | 보스 처치 보상(자정)이 **조건→자리→만남→동행**까지 이어지는지 끝까지 검증 — 중간 한 칸만 끊겨도 보상은 없는 것과 같다. 결함 0건 |
| 189 | 「한 번 켜지면 안 꺼지는 상태」 전수 조사 — 남은 셋은 모두 의도된 것(카메라 첫 부착·시작 인사·보스 예고 첫 안내). 새로 생기면 이유를 적어야 통과하게 함 |
| 188 | **보스 완주 화면이 영영 안 뜨던 것** — 첫 완주를 「계속 탐험」으로 닫으면 닫힘 표시가 다시 꺼지지 않아, 게임에서 가장 큰 순간의 보상이 통째로 사라졌다 |
| 187 | **완주 기록이 계속 흐르던 것** — 완주 뒤에도 값을 다시 담아 「걸린 시간」이 올라갔다(기록이 아니라 시계). 자판기 카운트다운이 낭독기에 끝없이 읽히던 것도 함께 수정 |
| 186 | 그래픽이 끊겼을 때 안내가 **글자뿐**이던 것 — 「새로고침하면」이라 적고 누를 것을 안 줬다. 모바일에서는 주소창을 꺼내는 것부터 어렵고 화면은 검은 채로 남는다. 돌아온 뒤에는 권하지 않는다 |
| 185 | WebGL 미지원 화면에 새로고침 수단 추가 — GPU가 일시적으로 죽은 경우 새로고침이면 열리는데 「랜딩으로 돌아가기」뿐이었다. 검사 본문 추출이 **문자열 속 중괄호**에 끊기던 것도 함께 수정 |
| 184 | WebGL 미지원 화면에만 본문 랜드마크가 없던 것 — 3D를 못 여는 기기로 온 사람에게만 구조 없는 화면이 나갔다. 검사도 「개수 하나」에서 **「중첩 없음」**으로 바꿈(한 파일에 분기가 둘일 수 있다) |
| 183 | 오류 화면 3종의 계약 고정(제목·`role="alert"`·복구 버튼·이름 있는 버튼·스택 미노출) — 평소 아무도 열어 보지 않아 조용히 나빠지는데, 그때가 가장 중요한 순간이다. 결함 0건 |
| 182 | 80여 반복 만의 화면 회귀 점검 — 밤 조명·HUD 4단 컨테이너·자정(보라)·완주 화면 4개 수정(2열 통계·수집 현황·문구 중복 제거·보스 재등장 안내) 모두 정상. 회귀 0건 |
| 181 | 남긴 이유가 **사실인지** 검사하게 함 — 「검사가 기준으로 쓴다」고 적고 아무도 안 쓰면 목록이 판단이 아니라 면제 도장이 된다. `scenarioNames`는 `Object.keys`와 같은 일을 하는 껍데기라 제거 |
| 180 | 죽은 코드 검사의 구멍을 메움 — **테스트가 import하면 살아 있는 것으로 세고 있었다**(`canUseAbility`가 그 사례). 제품에서 죽은 export 10개를 목록으로 드러내고 각각 남길 이유를 적게 함 |
| 179 | **동료 능력 쿨다운이 보이지 않던 것** — `canUseAbility`에 「HUD가 버튼 활성화에 쓴다」고 적혀 있는데 아무도 쓰지 않았다(테스트만 import). 눌러도 아무 일이 없으면 고장으로 읽힌다 |
| 178 | 보스가 25초 뒤 다시 서는데 완주 문구는 「이제 도시는 온전히 놀이터다」라고만 하던 것 — 다시 마주친 사람은 버그로 읽는다. 반복 전투는 **알려 주면 기능이고 숨기면 결함**이다 |
| 177 | 활강이 평지 이단 점프만으로 걸리는지 확인(최고 2.51m에서 걸림) — 「높은 곳에서」라는 단계 이름과 달리 어디서나 배울 수 있다. 누르지 않으면 안 걸리는 것도 함께 고정 |
| 176 | 그래플이 어디서든 걸리는지 확인 — 가로등 384개, 무작위 200곳 전부 사거리(24m) 안(최악 18.9m). 안내가 「가로등을 보고」라고 말하는 이상 그 말이 어디서나 참이어야 한다 |
| 175 | 자판기가 실제로 마주쳐지는지 확인 — 80대, 스폰에서 17m. 결함 0건이지만 「변두리에만 있으면 기능을 배울 기회가 없다」는 성질을 검사로 고정 |
| 174 | 「만들고 연결 안 함」 전수 감사 — 능력 수치 6개·매 프레임 통계 18개 모두 읽히고 있음(결함 0건). 소비자 이름이 제각각이라 필드 이름으로 찾는 검사를 남김 |
| 173 | **쓰였지만 한 번도 들리지 않던 대사 3종**(시작·쓰러짐·보내기) 연결 — 부를 자리가 없었다. 바로 앞 반복에서 늘린 `dismissed`도 그중 하나여서 그 작업은 아무 효과가 없었다 |
| 172 | 감정 표현 셋이 **모두 「춤」 대사**를 쓰던 것 — 앉았는데 동료가 「박자 맞네」라고 하면 이쪽을 보고 있지 않다는 뜻이 된다. 동작마다 대사 분리 |
| 171 | 동료 보내기 대사가 한 줄뿐이라 `C`를 누를 때마다 같은 말이 나오던 것 — 세 줄로 늘림. 보스 경고도 늘리려다 **기록된 판단**(신호는 같은 소리로 반복되어야 배운다)을 읽고 되돌림 |
| 170 | 마지막 남은 모서리(좌상단) 확인 — 목표 패널 0~76px, 동료 대사 +132px로 겹치지 않음. 컨테이너로 묶기엔 둘뿐이고 성격이 달라 숫자로 고정 |
| 169 | 「파일을 나누면 검사 범위가 따라오지 않는다」를 전수 감사 — 잘못된 대상을 보는 검사 0건 확인. 터치 동등성 검사가 손으로 적은 파일 목록을 쓰던 것을 **HUD 폴더 전체**로 바꿔 원인 제거 |
| 168 | 포토 모드 거리 조절이 **휠뿐이라 키보드·터치 사용자 모두 못 쓰던 것** — Z·X 키와 가까이·멀리 버튼 추가. 키 코드는 정본(`CONTROL_CODES`)에 넣어 조작표·터치 대안 검사가 함께 걸리게 함 |
| 167 | 포토 모드를 **키보드로 조작할 수 없던 것** — 드래그·휠뿐이라 키보드 사용자는 P로 들어가도 아무것도 못 했다. 포토 모드에서는 시뮬레이션이 멈춰 이동 키가 놀고 있으므로 그 자리를 카메라에 내줌 |
| 166 | 검사가 **내 주석을 코드로 잡는 실수를 다섯 번** 반복한 원인 제거 — 주석 제거를 `readCode` 공용 도우미로 모으고, 손으로 다시 붙이면 걸리게 함. 죽은 export 검사가 쓰지도 않을 `readRaw`를 즉시 잡아냄 |
| 165 | 월드 페이지에 `<main>` 랜드마크가 없던 것 — 시작 화면은 쓰는데 월드만 `<div>`였다. 「본문으로 건너뛰기」가 안 된다 |
| 164 | 월드 화면에 제목이 없던 것 — 낭독기는 제목으로 화면을 훑는데 h1 없이 도감·지도의 h2만 떠 있었다. 눈에 보이지 않는 h1 추가(1×1px, 화면을 가리지 않음) |
| 163 | **반복 161이 만든 회귀 수정** — `role="img"`를 HUD까지 감싸는 컨테이너에 붙여 목표·체력·버튼이 낭독기에서 통째로 사라졌었다. 접근성을 고치려다 없앤 셈. 캔버스만 감싸도록 수정 |
| 162 | 지도·도감 버튼이 **열려 있어도 「열기」**라고 말하던 것 — 이미 연 사람에게 거짓을 알린다. `aria-pressed`(눌림) 대신 `aria-expanded`+`haspopup`(펼침)으로 역할도 바로잡음 |
| 161 | 3D 화면에 접근 가능한 이름이 없던 것 — 낭독기에는 정체불명의 그래픽 하나였다. R3F가 속성을 DOM으로 넘기지 않아 감싸는 요소에 붙이고 브라우저에서 확인 |
| 160 | **보스 체력 막대가 낭독기를 쉼 없이 떠들게 하던 것** — `aria-live` 영역 이름에 퍼센트가 들어 있어 120ms마다 바뀌었다. 정작 들어야 할 「내려친다 — 피해!」가 묻힌다. 이름을 고정 |
| 159 | 지도 패널도 `role="dialog"`인데 초점 처리가 없던 것 — 도감만 고치면 다음 패널이 어느 쪽을 따를지 알 수 없다. `useDialogFocus` 훅으로 뽑아 둘 다 적용, 전수 검사 |
| 158 | 대화상자가 초점을 옮기지 않던 것 — 도감을 열어도 초점은 바깥 버튼에 남아 키보드 사용자는 뜬 줄도 몰랐다. 열 때 안으로, 닫을 때 부른 버튼으로 (브라우저에서 확인) |
| 157 | **Escape로 패널을 닫을 수 없던 것** — 도감은 `role="dialog"`인데 키보드로는 항목을 전부 지나야 빠져나왔다. 마우스로는 아무 불편이 없어 눈에 안 띈다. `WorldHud`가 804줄이 되어 포토 조작·저장 알림 분리(690줄) |
| 156 | 키보드만으로 게임에 들어갈 수 있는지 처음 확인 — 초점 대상 셋, 시작 버튼이 첫 번째, Tab에 3px 링 표시. 결함 0건. 초점 표시가 조용히 사라지는 것을 막는 검사 추가 |
| 155 | 컨테이너 전환이 만든 새 위험을 고정 — 버튼 담은 컨테이너에 `pointer-events-none`을 붙이면 버튼이 통째로 죽고, 보기만 하는 컨테이너에서 빠지면 뒤가 안 눌린다. 둘 다 화면상 단서가 없다 |
| 154 | 구조 전환을 화면으로 확인(보스 막대 top 112 = safe-top+96, 중앙 정렬). 네 구역이 모두 컨테이너인지, 쌓이는 요소가 absolute를 들고 있지 않은지 상시 검사 추가 |
| 153 | 위쪽 가운데(보스 막대·구역 배너)도 컨테이너로 전환 — HUD 네 구역이 모두 쌓는 구조가 됐다. 중첩을 잘못해 좌하단 안에 들어간 것을 검사가 잡아냄 |
| 152 | 좌하단도 쌓는 컨테이너로 전환 — 높이를 추정해 간격을 정하던 방식은 요소 하나만 커져도 다시 겹친다. 이로써 절대 좌표로 자리를 다투는 구조가 HUD에서 사라짐 |
| 151 | **도감과 성능 패널이 완전히 같은 자리**(top +56px, right)에 있던 것 — 둘 다 버튼으로 켜므로 함께 열면 포개진다. 우상단도 쌓는 컨테이너로 전환(실측: 72~96 / 104~596) |
| 150 | **HUD 겹침의 근본 원인 제거** — 하단 중앙 알림 넷이 각자 절대 좌표를 들고 있었다(자판기 안내 168~218 ↔ 자리 알림 208~ 실제 겹침). 쌓는 컨테이너로 바꿔 새 알림이 자리를 다투지 않게 함 |
| 149 | 새 알림을 기존 해금 알림과 **같은 자리**에 두었던 것 — 만드는 김에 같은 실수를 세 번째로 할 뻔했다. 위로 올리고 두 알림의 간격을 검사로 고정 |
| 148 | **자리가 드러나도 알려 주지 않던 것** — 보스를 눕혀도 지도를 열기 전엔 모른다. 「찾아갈 자리가 생겼다」 알림 추가(이름은 밝히지 않는다 — 찾아가는 것이 놀이다) |
| 147 | 기획서가 「도깨비 4종」이라고만 적고 **무엇으로 열리는지**를 빠뜨린 것 — 해금 구조가 이 게임의 수집 설계 자체다. 세 갈래를 적고 로스터와 대조하는 검사 추가 |
| 146 | 소스를 훑는 검사가 **정규식이 깨지면 0건으로 조용히 통과**하던 것 — 이번 세션에 다섯 번 겪은 실패다. 인용·해금 조건·죽은 export 검사에 「몇 건을 봤는가」를 함께 확인하도록 보강 |
| 145 | 「단언이 없는 검사」 상시 확인 추가 — 1,000개가 넘으면 하나가 아무것도 안 해도 눈에 띄지 않는다. 단언의 **강도**를 자동 판정하려다 멀쩡한 검사 8개를 오탐해 포기했다 |
| 144 | 게임플레이 신호 6종 × 배경 7종 전수 대조 — 결함 0건. 걸린 셋은 **비교 대상이 틀린 것**(예고 링은 바닥에만, 플래시는 몸색 기준)이라 각자의 진짜 배경과 짝지어 검사로 남김 |
| 143 | **날아오는 탄이 노을 하늘에 묻히던 것**(색거리 52, 노을은 기본 시간대) — 피할 수 없는 공격은 어려운 게 아니라 고장 난 것이다. 붉은 자홍으로 옮기고 하늘·아군 전 조합 검사 |
| 142 | 자정과 외형 「자두」의 색거리가 41이던 것 — 동료가 나와 같은 색이면 화면에서 둘을 구분할 수 없다. 도깨비 색을 도깨비끼리만 비교하고 있었다 |
| 141 | 외형 4종을 바닥과 대조하는 검사 추가 — 기존 검사는 하늘과만 대조했다. 3인칭에서 캐릭터는 대부분 바닥을 배경으로 보이고, 실제로 밤에 명암비 1.03이 났던 곳도 바닥이었다 |
| 140 | 「가벼움」 품질을 처음 확인 — 간판 읽힘, 결함 없음. 가 너무 가깝다고 판단해 고쳤다가 **되돌렸다**: 세 등급이 이미 로 일관돼 있었다. 그 비율을 검사로 고정 |
| 139 | 확인 지점의 프로덕션 차단을 전수 검사 — 한 지점만 보고 있어 새 지점이 추가되면 빠졌다. 우하단은 터치·키보드가 상호 배타라 겹침 없음을 확인 |
| 138 | **좌하단 미니맵이 체력·속도를 덮고 있던 것** — 148px 지도가 +12px에 있어 둘을 완전히 삼켰다. 스크린샷 30여 장에서 계속 보였는데 좌표를 재 보고서야 알았다 |
| 137 | 구역 배너가 좁은 화면에서 목표 패널과 겹치던 것 — 폰(360px)에서 목표 패널이 70vw까지 늘어나 가운데까지 닿는다. 좌표 계산으로 발견, +24px → +152px |
| 136 | 완주 화면 확인 지점(`?see=result`) 추가 — 한 판을 끝내야만 볼 수 있던 화면이다. 열어 보니 **완료 문구가 좌상단과 가운데에 동시에** 떠 있었다. 목표 패널이 비켜나게 수정 |
| 135 | 같은 설정을 두 화면이 다른 이름으로 부르던 것 — 「모션 줄이기」↔「흔들림」, 「사운드」↔「소리」. 이름을 맞추고 검사로 고정 |
| 134 | 완주 화면에 「만난 도깨비 n / 4」 추가 — 수집이 4종 루프가 됐는데 마지막 화면이 남은 하나를 알려 주지 않았다. 조작표·설정 패널은 확인 결과 완전함 |
| 133 | **전체 지도를 읽을 수 없던 것** — 캔버스를 투명하게 지워 뒤의 3D 월드가 비쳤다. 구역 색 알파(0.14~0.30)는 어두운 바탕을 전제한 값이었다. 지도를 열어 보고 발견 |
| 132 | **이미 만난 도깨비가 여정이 넘어가면 사라지던 버그** — 해금 조건이 「지금 여정 완료」를 읽고 있었다. 「첫 여정을 마쳤나」로 바꿔 도감 3/4 → 4/4. 도감을 열어 보고서야 발견 |
| 131 | 자정을 화면으로 확인 — 보라색이 밤 거리에서 또렷하다. 넷이 겹쳐 보이는 것은 시뮬레이션 정지 탓(두 번째 프레임에서 주황 고리가 드러남) |
| 130 | `?see=party`가 「셋」에 머물러 자정을 볼 수 없던 것 수정 — 이름과 목록을 정본에서 만든다 |
| 129 | **네 번째 도깨비 「자정」** — 보스를 눕혀야 자리가 드러난다. 조건을 읽는 8곳에 값을 흘리고, 한 곳이라도 빠지면 걸리는 검사를 함께 넣었다(실제로 2곳을 잡았다) |
| 128 | 보스를 눕힌 순간에 몸풀기 산책과 같은 대사가 나오던 것 수정 — 절정이 가장 작은 사건과 같은 무게로 끝났다 |
| 127 | HUD 버튼 줄에 개수 상한(8개) — 이번 세션에 내가 둘을 늘렸다. 폰에서 두 줄이 한계이고, 더 필요하면 늘릴 것이 아니라 묶어야 한다 |
| 126 | README ↔ `package.json` 명령 대조 검사 — 문서가 없는 명령을 안내하거나 `verify`에서 검증 하나가 빠지면 통과했다고 믿고 커밋하게 된다 |
| 125 | 동료 추적 퍼즈 추가 — 소환·해제·능력·순간이동 복구가 뒤섞이는 순서는 대본으로 다 짚을 수 없다 |
| 124 | 전투 퍼즈 추가(무작위 60초 × 시드 4개) — 대본 테스트는 의도한 순서만 훑는다. 상태 기계의 구멍은 의도하지 않은 순서에서 열린다 |
| 123 | 퍼즈에 수직 축 검사 추가 — 그래플·이단 점프·활강이 뒤섞인 60초에서 바닥을 뚫지 않는지, 한 번이라도 발을 붙이는지. 수평은 클램프가 지키지만 수직은 중력과 충돌만이 지킨다 |
| 122 | 주석 처리된 코드 재발 방지 검사(현재 0건). TODO·FIXME는 일부러 금지하지 않는다 — 아는 빈틈을 적는 일을 막으면 기록을 안 하게 된다 |
| 121 | 설정 필드를 늘리며 version을 올리지 않은 선택의 결과를 검증 — 예전 저장값이 보존되고 새 항목만 기본값으로 채워지는지. 검증 함수를 하나라도 빠뜨리면 `undefined`가 흘러든다 |
| 120 | 카메라 보조광이 시간대 밝기의 정확히 역순인지 검사 — 「밤이 가장 세다」만으로는 새벽·노을이 뒤집혀도 통과했다. 새벽은 밤보다 3배 밝다 |
| 119 | 문서가 적은 숫자(미니맵 반경 80m, 품질 3단계)를 상수에 묶음 — 상수를 고칠 때 문서까지 여는 사람은 드물다 |
| 118 | 「보스를 크기와 색으로 구분한다」는 주석이 사실인지 측정(몸통 색거리 92.9, 머리 70.8) 후 고정 — 로봇 색만 바꿔도 조용히 깨지는 주장이었다 |
| 117 | 소리 자원 정리 검사 추가 — AudioContext 닫기·리스너 제거·음원 정지가 테스트 없이 있었다. 들어 볼 수 없는 영역은 구조로만 지킬 수 있다 |
| 116 | 문서 테스트 수 기준값을 손으로 적던 상수 제거 — 25% 오차를 허용하는 검사인데 매 반복 고치고 있었다. 검사가 만들어 내는 불필요한 작업도 결함이다 |
| 115 | README 확인 지점 목록이 낡아 있던 것 수정(`?see=air` 누락·한낮 설명 구버전) + 정본 대조 검사. 「사람이 봐 줘야 하는 것(2분)」 절 추가 |
| 114 | 퀘스트 목적지 도달 가능성을 실제 이동으로 검증 — 기존 테스트는 신호를 만들어 통과시켜 「갈 수 있는가」를 보지 않았다 |
| 113 | 사용자에게 보이는 주장 전수 대조 — 도감·안내문에 드리프트 없음 확인, 「두 번 때리면」·「붉은 삼각형」을 검사로 고정. **스트리밍 지오메트리 해제는 하지 않기로 결정**(아래 사유) |
| 112 | **보스 문제는 없었다.** 원인은 자동화 탭이 `document.hidden` 상태라 rAF가 아예 돌지 않은 것. 4개 반복을 헛짚었다 |
| 111 | `WorldHud.tsx` 799줄 → `PerfPanel` 분리(708줄). 계기판은 플레이하는 사람이 아니라 만드는 사람이 읽는 곳이라 성격이 다르다 |
| 110 | 성능 패널이 첫 계측 전 0을 실측처럼 보여 주던 문제 — 「아직 재는 중」으로 구분 |
| 109 | 성능 패널(F3)에 보스 거리·단계 표시 — 「안 움직인다」만 보이던 것을 눈으로 가릴 수 있게 |
| 108 | 보스 접근 시뮬레이션 테스트 추가 — 규칙 자체는 옳음을 확정(14m→10초 안에 예고). 브라우저에서 다가오지 않던 현상은 원인 미확정 |
| 107 | 공중 확인 지점(`?see=air`) 추가 — 밝은 하늘을 등진 실루엣 명암비 10.01, 묻히지 않음 |
| 106 | 한낮 확인 완료(후드 대 바닥 명암비 2.04, 문제 없음). 확인 지점 이름이 확인할 수 없는 것을 약속하던 것 수정 |
| 105 | 시점 조작이 `movementX`에 의존하던 것을 좌표 차이로 통일 — 터치 쪽과 같은 방식 (드래그 경로 자체는 여전히 미검증) |
| 104 | 도깨비 자리 빛기둥이 안 보이던 원인 규명 — 좌표는 정상, 불투명도 0.11이 너무 낮았다. 0.28로 올리고 순수 함수로 분리 |
| 103 | 렌더 중 setState 오류(React가 실제로 냄) — 상태 갱신 함수 안의 `updateSettings` 5곳 제거, 재발 검사 추가. 이어받은 처치 수가 첫 렌더에 반영되지 않아 동료 바꾸기 버튼이 안 나오던 문제 수정 |
| 195 | 백로그 앞에 「사용자에게 영향이 있던 결함」 요약을 둠 — 186행을 다 읽어야 무엇이 잘못돼 있었는지 알 수 있었다. 요약이 인용한 반복 번호가 표에 실재하는지 검사가 지킨다 | 완료 |
| 196 | 전체 지도 범례가 거짓말을 하고 있었다 — 일반 로봇과 고물 대장은 **같은 빨강**이고 구분은 원/삼각형인데, 범례에는 네모 하나가 「고물 대장」이라 붙어 있어 흩어진 빨간 점을 전부 대장으로 읽게 됐다. 목표·내 위치는 범례에 없었다. 표식(색+모양)을 `MARKS` 한 곳에서 만들고 범례를 거기서 생성 | 완료 |
| 197 | 전체 지도가 낭독기에는 **아무것도 아니었다** — 캔버스라 「지도가 있다」까지만 알고 닫게 된다. 지도의 요점(지금 어디·목표 방향과 거리·근처 로봇 수·대장 방향)을 문장으로 옮겨 화면에도 함께 띄움. 순수 함수 `describeMap`으로 분리해 방위가 그림과 어긋나는지까지 검사 | 완료 |
| 198 | **고물 대장을 눕힌 기록이 저장되지 않았다.** 「자정」은 이 조건으로만 열리는데 저장 포맷에 없어서 「보스 여정을 마쳤는가」로 유도했다 — 대장은 25초마다 다시 서고 여정과 무관하게 잡을 수 있으므로, 여정 없이 잡아 연 사람은 새로고침 한 번에 도깨비를 잃었다. 게다가 저장이 여정 진행에만 걸려 있어 대장만 잡으면 **저장 자체가 없었다.** 선택 필드로 추가(버전은 올리지 않는다 — 올리면 진행을 통째로 버린다), 처치 순간에도 저장, 저장 출구를 하나로 모음 | 완료 |
| 199 | 이어받기 계산을 `resolveResume` 순수 함수로 분리 — 화면 안 조건식이라 틀려도 확인 지점으로 들어가 봐야 알 수 있었다(실제로 두 번 틀림). `PlayClient`가 800줄 상한을 넘은 것도 함께 해소 | 완료 |
| 200 | **「처음부터 다시 하기」가 처음이 아니었다.** 설정은 동료 id가 아는 이름인지만 확인하고 해금 여부를 보지 않아, 진행을 지운 뒤에도 보스로만 열리는 「자정」을 능력까지 쓰면서 첫 화면에서 시작했다. `resolveCompanion`으로 걸러냄 | 완료 |
| 201 | 다시 시작이 만난 기록(`metDokebi`)을 남겨, 다시 조건을 채우면 자리에 가지 않아도 곧바로 부를 수 있었다 — 도깨비 자리가 한 번도 서지 않는다. 함께 지움 | 완료 |
| 202 | 지도 문장이 **도깨비 자리는 말하지 않았다** — 지도에는 몸 색 동그라미로 찍히는데 글에는 한마디도 없어, 눈으로 못 보는 사람에게는 갈 곳이 없는 도시였다. 가장 가까운 자리를 이름·방향·거리로 말한다(넷을 다 읊으면 고르는 데 더 걸린다) | 완료 |
| 203 | 터치 대응 선언(`CONTROLS[].touch`) 전 항목이 실재하는지 대조 — 줌은 포토 패널, 소리는 HUD에 있었다. **결함 없음**(정적 확인) | 확인 |
| 204 | **완주 화면의 수집 숫자가 절대 가득 차지 않았다.** 「만난 도깨비 n / 전체(4)」로 셌는데 초롱은 `home`이 없어 만남 목록에 영영 안 들어간다 — 다 모아도 3/4에 멈춰, 「남은 하나를 알려 주려고」 넣은 숫자가 반대로 거짓말을 했다. 분모를 `FINDABLE_DOKEBI`(자리 있는 것)로 바꿈. 기존 검사가 결함 쪽을 고정하고 있어 함께 고침 | 완료 |
| 205 | **도감이 잠긴 도깨비에게 「처음부터 함께 있다」고 안내했다.** `unlockHint`가 `requiresBoss`를 몰라 「자정」이 마지막 줄로 떨어졌다 — 조건을 숨기면 도감이 목표가 아니라 벽이 된다고 그 함수 위에 적어 두고서. 조건을 모아 만들도록 바꿈(둘 이상도 함께 말한다) | 완료 |
| 206 | 조건 필드가 늘 때 판정(`revealedDokebi`)만 고치고 안내를 빠뜨리는 것을 막는 검사 추가 — 인터페이스의 `requires*`/`required*`를 훑어 두 함수가 모두 보는지 대조 | 완료 |
| 207 | **직전 반복에서 내가 넣은 결함**: 동료 해금 판정에 `resumeFrom.questCompleted`(현재 여정의 완료)를 써서, 두 번째 여정 중에 이어하면 「물비늘」을 데리고 있던 사람이 조용히 초롱으로 되돌아갔다. 「첫 여정을 마친 적이 있는가」를 이어받기 계산이 갖도록 옮기고, 같은 규칙을 두 번 쓰던 `questView` 초기값도 그 값을 읽게 함 | 완료 |
| 208 | **첫 여정의 마지막 단계가 사실상 벽이었다.** 활강 2초가 목표인데 이동 규칙을 돌려 재 보니 평지 점프 0.62초, 2단 점프 0.83초, 가로등(5.4m)에서 뛰어내려 1.92초, 공중 점프까지 써야 2.12초 — **여유 0.12초.** 게다가 힌트는 「점프한 뒤 떨어질 때」라고만 적어 그대로 하면 영원히 못 채웠다(제목만 「높은 곳에서」였다). 목표를 1.5초로 낮추고 힌트에 올라가는 수단(G·가로등)을 적음 | 완료 |
| 209 | 활강 목표 달성 가능성을 **실제 도시에서 시뮬레이션**하는 검사 추가 — 앵커 높이는 `buildCityLayout()`에서 유도하고, 공중 점프 없이 20% 여유로 넘는지·평지로는 안 되는지(그래서 힌트가 올라가는 길을 말해야 하는지)를 함께 고정 | 완료 |
| 210 | 나머지 목표 종류의 달성 가능성도 고정 — 도착 지점은 반경 안에 **설 수 있는 땅**이 있는지 실제 충돌 상자로 확인(첫 여정 목표는 건물 안이지만 5m 거리에 빈 땅이 있어 반경 14m로 채워진다), 처치 목표는 가장 가벼운 품질 등급의 로봇 수와 대조. **결함 없음**이지만 도시가 바뀔 때 조용히 막히는 것을 막는다 | 완료 |
| 211 | 여정 힌트가 말하는 키가 실제로 묶여 있는지, 조작 표의 표기가 같은 코드를 가리키는지 대조 — 없는 키를 적으면 아무 일도 안 일어나고 사람은 자기가 잘못 눌렀다고 생각한다. **결함 없음**(정적 확인), 키를 옮길 때 안내가 따라오지 않는 것을 막는다 | 완료 |
| 212 | 조작 표가 **세계에 있다고 전제하는 물건**(자판기·가로등)이 실제로 생성되는지 확인 — 도시 생성이 바뀌면 표만 남아 거짓이 된다. 지금은 자판기 80대, 광장에서 16.6m 거리에 첫 대 | 완료 |
| 213 | 보스 예고를 **보고 실제로 피할 수 있는지** 계산해 고정 — 「예고가 길다」만 재고 있었는데, 피할 수 있느냐는 충격 반경이 사거리보다 얼마나 넓은지(1.6m)와 예고 중 접근 여부가 함께 정한다. 사거리에서 걷기(3.2m/s)면 초당 1.45m로 충분, 거리 0에서도 달리기(7.4)면 벗어난다. 예고 중 정지가 깨지면 걸어서는 못 피하게 되므로 그것도 함께 잠금. **결함 없음**(계산·시뮬레이션 확인) | 완료 |
| 214 | 클립 녹화를 멈출 때 **정상 경로에서만 스트림 트랙을 놓고 있었다** — 이미 멈춘 녹화기로 들어오거나 `stop()`이 던지면 캔버스 캡처가 켜진 채 남아 세션 내내 프레임을 퍼 간다(화면은 멀쩡해서 아무도 모른다). 끝나는 세 갈래 모두에서 놓도록 고치고, `resolve` 횟수만큼 놓는지 검사로 고정 | 완료 |
| 215 | **GPU 자원 누수 네 건.** 그래플 밧줄 지오메트리(바로 옆에서 표식은 해제하면서 빠뜨림), 창문 발광·가로등 빛웅덩이·포토 필터 텍스처(캐시가 Map이 아니거나 루프 밖이라 해제 함수를 그대로 지나침). 화면은 멀쩡하고 /play를 드나들 때마다 쌓이기만 해서 눈으로는 영영 모른다 | 완료 |
| 216 | 자원 해제 검사 파일 추가 — 지오메트리를 둘 이상 만드는 파일은 **목록으로 훑어** 놓는지(하나씩 적으면 하나를 잊는다), 텍스처 해제 함수가 **모든 캐시 변수**를 건드리는지. 뒤 검사가 `lampGlowCache`·`filterCache`를 그 자리에서 잡아냈다 | 완료 |
| 217 | 캐시 해제 검사를 한 파일에서 **해제 함수를 가진 모든 모듈**로 넓힘 — 한 파일만 지키면 다음에 생길 모듈에서 같은 누수가 반복된다. 아틀라스 텍스처는 정상. 렌더 타깃·손으로 만든 머티리얼은 없음(정적 확인) | 완료 |
| 218 | 탭 복귀 직후의 거대한 delta 상한이 **일곱 파일에 각자** 정의돼 있었다(값은 같아 증상은 없었다). 한 곳만 고치면 그 컴포넌트만 다른 시간을 살고, 화면에서는 「어떤 것만 느리다」로 나타나 원인을 찾기 어렵다. `tuning.ts` 한 곳으로 모으고, 정본이 하나인지·시뮬레이션을 부르는 곳이 모두 쓰는지 검사로 고정. `PlayerRig`에 두 줄 겹쳐 있던 주석도 정리 | 완료 |
| 219 | 인도 상판 높이를 소품 배치 두 파일이 **여전히 숫자로 들고 있었다.** 정본(`CITY.sidewalkHeight`)에는 「예전에 양쪽에 따로 적었다가 어긋난 적이 있어 여기로 올렸다」는 기록까지 있는데 그 정리에서 빠져 있었다 — 정본이 바뀌면 소품만 공중에 뜨거나 보도에 파묻힌다. 정본을 쓰도록 고치고, 숫자를 다시 적지 않는지·쓰는 곳이 정본에서 가져오는지 검사로 고정 | 완료 |
| 220 | 동료 능력이 **JSX 형제 순서에 기대고 있었다** — 되돌리기(PlayerRig) → 합치기(동료) → 읽기(적) 순서인데, R3F는 우선순위가 없으면 등록 순서대로 돌므로 한 줄만 옮기면 되돌리기가 매 프레임 능력을 지운다(예외도 오류도 없이 조용히). 지금 순서는 옳고, 그 전제를 검사로 고정: 순서·우선순위 미사용·되돌리는 곳과 합치는 곳의 존재 | 완료 |
| 221 | 상시 규칙 점검 — `Math.random()` 0건, `console` 0건, 매 프레임 setState 0건(`City.tsx`의 구역 전환은 기록된 예외), 적 배치 시드는 고정 상수라 결정적. 능력 효과 필드 6개 모두 소비처 존재 확인. **결함 없음**(정적 확인) | 확인 |
| 222 | **사람에게 부탁하는 문서가 안 되는 방법을 알려 주고 있었다.** README 2분 목록이 활강을 「Space 두 번」으로 안내했는데 그러면 0.83초라 목표에 닿지 않는다(가로등에서 뛰어내려야 한다). 완주 화면 설명도 분모를 바꾼 뒤에도 「n/4」로 남아 있었다 — 게다가 기존 검사가 그 표기를 **요구**하며 결함을 붙들고 있었다. 둘 다 고치고 정본과 묶음 | 완료 |
| 223 | 되돌림→합침 공유 필드를 전수 대조 — 프레임 순서에 민감한 것은 동료 능력 배율 둘뿐이고 나머지는 입력 누적이라 프레임과 무관. **결함 없음**(정적 확인) | 확인 |
| 224 | MVP 요구 목록이 **전부 미완**으로 남아 있었다 — 도감·저장·포토 모드가 다 있는데 문서만 보면 아무것도 없는 프로젝트라, 처음 오는 사람이 이미 있는 것을 다시 만들거나 없는 것을 있다고 믿는다. 항목마다 코드에서 확인해 표시하고(없는 것은 왜 없는지 한 줄로), **체크는 「코드에 있고 테스트가 지킨다」이지 「사람이 플레이해 확인했다」가 아님**을 명시 | 완료 |
| 225 | 시뮬레이션의 「거리로 나누기」를 전수 확인 — 겹쳐 선 순간(거리 0)이 NaN을 만들 수 있는 자리다. 넉백은 `Math.max(1e-4, …)`, 탄은 명시적 반환, 사수는 `distance < 1e-4` 분기, 근접·보스는 `standoffRadius`·`slamRange` 안쪽에서 먼저 멈춘다. **결함 없음** — 다만 뒤의 둘은 **상수가 0이 되면 조용히 열리므로** 값 자체를 검사로 잠금 | 완료 |
| 226 | 극단 입력 검사 파일 추가 — 겹쳐 선 로봇·보스, `dt = 0` 프레임, 대각선 최대 입력, 속도 배율 0에서 위치·속도·방향이 모두 유한한지. 한 번 NaN이 되면 화면에서 사라지고 거리 비교가 전부 false가 되어 **예외도 로그도 없이 굳는다** | 완료 |
| 227 | 설정의 만남 기록이 **중복을 거르지 않았다** — 모르는 id는 이미 버리고 있었지만 같은 도깨비가 두 번 들어 있으면 완주 화면이 분모를 넘는 수(4 / 3)를 보여 준다. 손으로 고친 저장값은 넣을 때의 확인을 거치지 않는다. 읽을 때 중복 제거(버전은 올리지 않는다 — 읽기 정규화라 설정을 지울 이유가 없다) | 완료 |
| 228 | **NPC 대화 구현** — MVP 「반드시 포함」에 남아 있던 넷 중 첫째. 주민 가까이에서 `T`(터치는 「말 걸기」)로 한 줄 대사. 분기·선택지 없음(여정과 경쟁하지 않는다). 군중이 이미 매 프레임 좌표를 계산하므로 가장 가까운 사람 찾기는 추가 비용이 거의 없고, 플레이어 위치는 도시 스트리밍이 쓰는 객체를 그대로 받는다 | 완료 |
| 229 | 말 걸기를 만들며 죽은 export 검사가 **쓰지도 않을 것 셋**(`TALK_RADIUS`·`RESIDENT_LINES`·`nearestResident`)을 잡아냄 — 군중이 자체 추적을 하므로 지우고, 상수는 내부로 되돌림 | 완료 |
| 230 | **이름 설정 구현** — MVP 「캐릭터 프리셋 선택 및 닉네임 설정」에서 이름만 빠져 있었다. 시작 화면에서 선택으로 넣고(요구하면 「시작」까지 한 걸음이 늘고 그 걸음에서 사람이 떠난다), 완주 화면과 **저장 파일 이름**에 쓴다 | 완료 |
| 231 | 이름은 사용자가 넣는 **유일한 자유 문자열**이자 파일 이름에 들어가는 값이라 경계를 셋으로 둠 — 읽을 때·저장할 때·파일 이름을 만들 때. 한글·영문·숫자·공백만 남기고 경로 구분자·상위 경로·제어 문자를 버린다. 입력칸의 `maxLength`는 붙여넣기와 IME가 지나칠 수 있어 실제 제한이 아니다 | 완료 |
| 232 | **조사 가능한 오브젝트 구현** — 간판을 살펴보면 무슨 가게인지 알려 준다(간판 아틀라스는 무늬만 그리므로 글자는 살펴보기가 만든다). 주민 말 걸기와 **같은 키**를 쓴다 — 대상마다 키가 다르면 무엇 앞에 서 있는지 먼저 판단하고 눌러야 하고, 그러면 아무것도 누르지 않게 된다 | 완료 |
| 233 | 그 과정에서 직전 반복의 구조를 고침 — 군중이 키 큐를 직접 소비하고 있었는데, 간판이 같은 키를 쓰게 되면 두 컴포넌트가 같은 프레임에 서로의 줄을 덮어써 화면이 깜빡인다. 군중은 **후보만 올리고**, 고르는 규칙(`chooseInteraction`)은 순수 함수로, 소비는 둘을 아는 `PlayerRig` 한 곳으로. 「소비하는 파일이 하나」를 검사로 고정 | 완료 |
| 234 | **단서 수집 구현** — 마지막 MVP 항목. 도시에 남은 흔적 세 자리를 살펴본다(지도에 마름모로 표시, 찾으면 사라진다). 자리는 도로 격자에서 유도해 건물 안에 놓일 수 없고, 여정 단계는 **끝에 덧붙여** 저장된 단계 번호의 의미가 밀리지 않게 했다. 골목 추격 미니게임은 만들지 않았고 고물 대장 전투가 그 자리를 맡는다 — 문서에 그대로 적음 | 완료 |
| 235 | MVP 목록 검사가 **사실을 거짓으로 만들라고 요구**하게 됨 — 「체크한 것과 아닌 것이 둘 다 있어야 한다」고 못 박았는데 항목이 실제로 다 구현됐다. 진짜 위험은 「전부 체크」가 「다 됐다」로 읽히는 것이므로, 빈 체크박스가 아니라 **못 한 부분을 그 줄에 적는 것**으로 막도록 고침 | 완료 |
| 236 | **직전 반복에서 내가 넣은 결함**: 흔적 안내를 「노란 물음표」로 적고 지도에는 **마름모**를 그렸다 — 지도를 펴 놓고 없는 표식을 찾게 된다. 문구를 맞추고, 「붉은 삼각형」 하나만 개별로 보던 검사를 **모든 모양**으로 일반화(지도에 없는 모양 낱말을 쓰면 걸린다) | 완료 |
| 237 | **조사한 흔적이 저장되지 않았다** — 처치 수는 남는데 흔적만 사라져, 새로고침 한 번에 도시를 다시 뒤져야 했다(몇 초짜리 활강과 달리 몇 분이 날아간다). 저장에 선택 필드로 추가(버전은 올리지 않는다), 이어받으면 지도의 남은 자리와 목표 계수기가 함께 따라온다 | 완료 |
| 238 | 흔적 표식을 지도에 더하면서 **지도를 말로 옮긴 문장은 갱신하지 않았다** — 도깨비 자리에서 이미 같은 누락을 겪고도 반복했다. 흔적을 말하도록 고치고(여정이 시킨 일이라 자리보다 먼저), 개별 항목을 세는 대신 **표식 목록 자체와 대조**하는 검사로 바꿔 다음에 표식이 늘어도 걸리게 함 | 완료 |
| 239 | 흔적 단계에 **계수기가 없었다** — 셋을 흩어 놓고 몇 개 남았는지 알려 주지 않으면 도시를 통째로 다시 돌게 된다. 처치 단계에는 있었는데 새 종류가 그 자리를 지나쳤다. 같은 방식(`accumulated`)으로 붙이고, 「수를 가진 목표」를 정본에서 훑어 계수기 유무를 확인하는 검사 추가 | 완료 |
| 240 | 새로 만든 것들(말 걸기·살펴보기·흔적·이름)을 **사람이 볼 방법이 없었다** — 확인 지점에도 2분 목록에도 없었다. `?see=clues` 지점 추가(시나리오가 여정 **중간 단계**를 지정할 수 있게 함 — 그 전에는 마지막을 넘어선 상태만 만들 수 있었다), README 목록 갱신. 지점이 실제로 흔적 단계를 열고 흔적 근처에서 시작하는지 검사로 고정 | 완료 |
| 241 | 흔적 조사에 분석 이벤트가 없었다 — **세 번째를 못 찾고 그만두는** 지점은 단계 완료 이벤트만으로는 보이지 않는다. `clue_found` 추가(이어받은 것은 세지 않는다 — 새로고침마다 수가 부풀어 오른다) | 완료 |
| 242 | `PlayClient`가 또 800줄을 넘어, 주석을 깎는 대신 **촬영·녹화 배선을 훅으로 분리**(732줄). 녹화기 정리도 함께 옮김 — 화면 쪽에 두었더니 참조를 의존성으로 요구받았고, 그건 「누가 수명을 책임지는가」가 흐려졌다는 신호다 | 완료 |
| 243 | **내가 절대 좌표 HUD 요소를 다시 넣었다** — 주민 대사 상자를 좌상단에 `+210px`로 박았는데, 그 코너의 겹침 검사는 「동료 대사가 **혼자 있으므로** 면제」라는 전제였다. 오늘은 6px 차이로 안 겹치지만 동료 대사가 좁은 화면에서 한 줄 늘면 곧바로 덮는다(좌표를 손으로 맞추다 여섯 번 겹쳤던 방식). 두 말풍선을 한 컨테이너로 쌓고, 검사도 「스스로 자리를 잡지 않는다」로 바꿈 | 완료 |
| 244 | **주민 대사 상자가 낭독기를 쉼 없이 떠들게 했다** — 「살펴보기」 안내와 대사를 한 `aria-live` 영역에 넣었는데, 주민이 걸어 다니므로 안내가 켜졌다 꺼졌다 하며 끝없이 읽혔다. 앞선 두 검사(이름이 바뀐다·본문 숫자가 흐른다)로는 안 걸린다 — 여기서 흔들리는 것은 값이 아니라 **영역의 존재 자체**다. 알릴 것이 있을 때만 live 영역이 되도록 분리 | 완료 |
| 245 | 그 유형을 검사로 고정 — 「늘 켜 있는 live 영역이 **알릴 것이 없을 때 다른 것을 그리는가**」. 처음엔 `nearby`라는 낱말을 찾게 짰다가 그 낱말이 영역보다 앞줄에 있어 **헛돌 뻔했다.** 고친 뒤 결함 형태로 되돌려 실제로 실패하는지 확인함 | 완료 |
| 246 | 최근 검사 다섯 개를 **결함 형태로 되돌려** 실제로 잡는지 확인 — 셋은 잡았고 **둘이 헛돌았다.** ① 캐시 해제 검사가 「이름이 나오는가」만 봐서 dispose 호출을 지우고 `= null`만 남겨도 통과했다(비우기만 하면 GPU 자원은 남는다). ② 말풍선 위치 검사가 함수 끝 앵커로 쓴 `\n}`가 **props 타입의 닫는 괄호**에 먼저 걸려, JSX를 아예 읽지 않은 채 통과했다 | 완료 |
| 247 | 둘 다 고치고 다시 되돌려 확인. 고치는 과정에서 정규식이 두 단계 접근(`atlas.texture.dispose()`)을 놓치는 것도 드러나 넓힘 — 실제 누수는 아니었다 | 완료 |
| 248 | **직전 기록이 틀렸다.** 말풍선 검사가 헛돈 원인을 「`readCode`가 문자열 리터럴을 지운다」로 적었는데, 확인해 보니 `readCode`는 **주석만** 걷어낸다. 진짜 원인은 함수 끝 앵커 하나뿐이었다. 주석과 백로그를 바로잡음 — 틀린 근거를 남기면 다음 사람이 없는 함정을 피하려 애쓴다 | 완료 |
| 249 | 같은 앵커(`\n}`)를 쓰는 검사 셋을 모두 되돌려 확인 — 전부 정상. 확인 절차를 `scripts/mutate.sh`로 남기고 README에 사용법 추가(자동 실행에는 걸지 않는다 — 원본을 잠깐 고치는 도구다) | 완료 |
| 250 | **터치 대응 검사가 헛돌았다** — 살펴보기 버튼을 통째로 지워도 통과했다. HUD 파일 전부를 이어 붙여 낱말을 찾는데, 같은 낱말이 안내 문구(`{talkKey} 살펴보기`)에도 있었기 때문이다. 이제 **버튼의 이름표**(`label=`·`aria-label=`)만 모아서 대조한다 | 완료 |
| 251 | 변이 검사를 하며 **약한 변이가 거짓 「헛돎」을 만든다**는 것을 배웠다 — 「X살펴보기X」는 원래 낱말을 포함하고, `<CompanionX`는 `<Companion`을 포함하며, 중복 상수는 같은 파일에 넣으면 위반이 아니다. 세 번 헛짚은 뒤 제대로 위반해 보니 프레임 순서·상한 정본·활강 도달 가능성·완주 분모는 모두 정상이었다 | 완료 |
| 252 | **반경 검사가 경계 밖만 보고 있었다** — 흔적 조사 반경과 도깨비 만남 반경을 각각 0.01m로 줄여도 통과했다. 「정확히 그 자리」와 「아주 멀리」만 확인했기 때문이다. 그러면 픽셀 단위로 서야 하는 기능이 된다. 달리기 속도(초당 7.4m, 한 프레임 12cm)를 근거로 **실제 쓰이는 거리**에서 걸리는지 확인하도록 보강 | 완료 |
| 253 | 「여러 파일을 이어 붙여 낱말만 찾는」 형태를 전수로 훑음 — 분석 이벤트 검사는 호출 형태(`track("...")`)로 찾아 튼튼했고, 죽은 export·지도 표식도 정상. 전투의 정지 반경은 0.01m로 줄여도 **NaN 안전이라는 불변식 자체는 지켜지므로** 통과가 맞다 | 확인 |
| 254 | **활강 힌트가 말한 가로등에 닿는지 아무도 확인하지 않았다** — 그래플 사거리를 1m로 줄여도 전부 통과했다. 활강 목표는 높은 데서 뛰어내려야 채워지고 올라갈 길은 그래플뿐이므로, 닿지 않으면 힌트대로 해도 안 된다. 도시의 실제 기둥 좌표로 도로 위 여러 지점에서 사거리 안에 기둥이 있는지 확인 | 완료 |
| 255 | 튜닝 값 관계 두 개 추가 — ① 당기는 속도 × 최대 유지 시간이 사거리를 넘어야 한다(느리면 사거리 끝 대상에는 걸어 놓고도 못 닿는다), ② 보스가 다시 서기까지 일반 로봇보다 오래 걸려야 한다(0.1초로 줄여도 통과했다 — 눕히자마자 서면 이기는 순간이 사라진다) | 완료 |
| 256 | 미니맵 반경을 1m로 줄여도 통과했다 — 그 크기면 자기 발밑만 보인다. 「한 블록 너머까지 보인다」와 「로봇이 나를 알아보기 전에 화면에 들어온다」로 관계를 검(표식이 뜨는 순간 이미 쫓기고 있으면 미리 보라고 만든 것이 사후 통보가 된다) | 완료 |
| 257 | **변이 도구를 고쳤다** — 이번 세션에서 「헛돎」이 세 번 잘못 나왔고 전부 **엉뚱한 검사 파일을 겨눈** 탓이었다(중력은 questContent가, 보조광은 timeOfDay가, 전투 정지 반경은 tuningRelations가 지킨다). `all`로 전체를 돌릴 수 있게 하고 README에 「되도록 all을 쓴다」고 적음 | 완료 |
| 258 | **조작표가 약속한 것이 실제로 일어나는지** 값으로 확인 — 공중 점프 속도를 0.1로 줄여도 전부 통과했다(표는 「Space, 공중에서 한 번 더」라고 적어 두는데 눌러도 아무 일이 없다). 첫 점프의 절반은 넘고 첫 점프보다 세지는 않아야 한다는 관계를 검 | 완료 |
| 259 | 음료도 같은 유형 — 배율이 1이면 마셔도 아무 일이 없고, 자판기 80대와 조작표의 「음료 뽑기」가 전부 헛말이 된다. 효과 배율과 「효과 < 재사용 대기」(길면 한 번 마시고 영영 빨라진다)를 검 | 완료 |
| 260 | **카메라가 플레이어를 놓쳐도 아무도 몰랐다** — 따라오는 계수를 0.01로 줄여도 전부 통과했다. 그 값이면 카메라가 사실상 제자리라 달리면 화면 밖으로 나간다. 지수 감쇠라 「1초에 몇 %를 따라잡는가」로 재도록 함(90% 이상). 저감 모션 쪽은 **더 빨리** 붙어야 목적(화면 변화량 줄이기)에 맞는다는 관계도 함께 검 | 완료 |
| 261 | 군중 컬링 거리를 1m로 줄여도 통과했다 — 사람이 아무도 안 보이는 도시가 되는데 검사는 조용했다. 「같은 블록 사람은 보인다」와 「절반 속도로 그리는 거리가 컬링보다 가깝다」로 검 | 완료 |
| 262 | 포토 모드 거리 관계 셋 — ① 최소 거리를 0.01m로 줄여도 통과했다(카메라가 몸 **안**에 들어가 모델 뒷면만 보인다), ② 기본 거리가 조절 범위 밖이면 들어가자마자 한쪽 끝으로 튄다, ③ 휠 한 칸이 범위 전체를 덮으면 조절이 아니라 토글이 된다 | 완료 |
| 263 | 교통 관계 넷 — 차간 거리를 0.1m로, 신호 감속 시작을 1m로 줄여도 전부 통과했다. **주석에는 근거까지 적혀 있었다**(「감쇠 감속이라 멈추는 데 3m 이상 필요하다」). 그 숫자를 검사로 옮기고, 차체 길이보다 차간이 넓은지·차가 걷는 속도보다 빠른지도 함께 검 | 완료 |
| 264 | 품질·조명 관계 넷 — ① 해상도 배율을 0.05로 줄여도 통과했다(자동 강등은 「끊기는 것보다 덜 예쁜 편이 낫다」는 판단인데, 알아볼 수 없으면 그 판단이 성립하지 않는다), ② 그림자를 **켠 등급만** 해상도를 확인(가벼움은 그림자를 꺼서 그 값이 쓰이지 않는다), ③ 모든 시간대에 해가 살아 있어야 한다(0이면 반구광만 남아 형태가 사라진다), ④ 밤이 가장 어두워야 보조광 규칙의 근거가 선다 | 완료 |
| 265 | **능력이 아무 일도 안 해도 통과했다** — 도감에 이름이 뜨고 버튼도 눌리는데 값이 전부 1이면 그만이다. 「하나라도 바꾼다」·「잠깐이라도 지속된다」·「다시 쓰기까지가 지속보다 길다」를 도깨비 전체에 검. 다만 그 규칙으로는 「자정」의 범위 배율만 1로 되돌린 경우를 못 잡아(빛 세기가 살아 있어 통과), **이름과 값을 잇는 검사**를 따로 달았다 — 「먼 불빛」이면 범위가 살아 있어야 한다 | 완료 |
| 266 | 클립 프레임률을 1로 줄여도 통과했다 — 슬라이드쇼가 저장된다. 이 게임이 파는 것은 움직임이고, 그래서 사진만이 아니라 클립을 넣었다. **기본값이 아니라 호출부**를 봐야 한다(기본값을 고쳐도 아무 일이 없었다 — 호출부가 값을 직접 준다) | 완료 |
| 267 | **조용한 기본값** — 해금 판정의 `met`에 기본값 `[]`가 있어, 빠뜨리면 자리 있는 도깨비가 이미 함께 다녀도 「못 만난 것」이 된다. 실제로 도감이 진행도를 구할 때 빠뜨리고 있었다(지금 화면 조건상 막대를 안 그려 드러나지 않았을 뿐이다). 기본값을 없애 빠뜨리면 **컴파일이 막게** 하고, 기본값이 되살아나는 것도 검사로 막음 | 완료 |
| 268 | 같은 「조용한 기본값」 유형을 시뮬레이션 함수들에도 적용 — 다만 단위 검사가 90곳 넘게 부르고 있어 기본값을 없애는 대신 **제품 호출이 값을 다 넘기는지** 고정했다. 빠뜨리면 동료의 인지 낮추기·회복 능력, 그래플이 걸 곳 찾기가 조용히 죽는다 | 완료 |
| 269 | 백로그 앞 요약이 「반복 100~194」에서 멈춰 있었는데 그 뒤로 60행이 늘었다 — 다음 사람이 읽는 것은 요약이므로 뒤쪽 절반이 없으면 없는 것과 같다. 새로 만든 것·스스로 낸 결함·검사가 헛돌던 것을 절로 나눠 덧붙이고, **마지막 표 행이 요약 범위 안에 드는지** 검사로 고정 | 완료 |
| 270 | 상태 기계를 **실제로 돌려** 도달 가능성 확인 — 보스 일곱 단계 전부에 닿고 어느 단계에서도 갇히지 않는지, 감정 표현이 눌러 나가면 전부 나오는지. 닿지 못하는 상태는 죽은 코드이거나 전이가 끊긴 것이고, 둘 다 화면에서는 「가만히 있다」로만 보인다. **결함 없음** — 다만 탐색 중 내 코드가 아니라 **내 탐색이** 틀려 한 단계를 못 찾았다(거리를 두 값만 오갔다) | 완료 |
| 271 | 버튼으로 돌리는 것들(시간대·포즈·색보정·동료)이 **모든 항목에 닿는지** 실제로 눌러 확인 — 화면에는 버튼 하나뿐이라 순환이 한 칸이라도 건너뛰면 그 항목은 영영 못 본다. 제자리에 머물지 않는지도 함께 봄. 동료는 예외라 「해금된 것은 전부 나오고 잠긴 것은 안 나온다」로 따로 검 | 완료 |
| 272 | 외형이 넷, 도깨비가 넷이 됐는데 **조합을 확인하는 것이 없었다** — 예전에 플레이어와 동료가 둘 다 보라색(색거리 41)이라 고친 전례가 있는데도. 열여섯 조합과 외형끼리를 전부 재도록 함(기준 50 — 그때 41을 안 된다고 판단했으므로 그 위) | 완료 |
| 273 | 가장 가까운 짝은 **민트 후드 + 물비늘로 55**다. 기준은 넘지만 둘 다 청록 계열이라 화면에서 뭉쳐 보일 수 있다 — 색거리로는 여기까지가 한계이므로 2분 목록에 「이 조합으로 다녀 보라」로 남김 | 사람 확인 필요 |
| 274 | 외형 × 하늘도 **노을·밤만** 보고 있었다 — 외형이 넷, 시간대가 넷인데 절반만 확인했다. 열여섯 조합 전부로 넓히고, 조합 수가 실제 목록 길이와 맞는지도 확인(시간대나 외형이 사라지면 빈 조합을 훑으며 통과한다). 새 이상치는 없었고 가장 낮은 값은 이미 기록된 노을/노을 52다 | 완료 |
| 275 | 색보정 × 시간대 × 외형(64조합)을 **합성해서** 재 봄 — 필터는 화면 전체를 물들이므로 배경과 인물이 같은 방향으로 끌려가 색거리가 함께 줄어든다. 가운데는 43으로 기준(40)을 넘지만, **가장자리 비네트에서는 52 → 23**으로 반토막 난다 | 완료 |
| 276 | 가운데는 40 기준으로, 가장자리는 「지금보다 나빠지지 않는다」(20)로 고정. 23이 실제로 읽히는지는 색거리로 판단할 수 없어 2분 목록에 「몽환으로 가장자리에서 찍어 보라」로 남김 | 사람 확인 필요 |
| 277 | 색보정이 **위험 신호**를 지우는지 확인 — 필터는 3D 화면 전체에 얹히므로 보스 예고 색에도 걸린다. 못 보면 못 피하니 취향이 아니라 공정성 문제다. 필터 없이 146, 가장 나쁜 조합(몽환 가장자리)이 66으로 기준(40)을 넉넉히 넘는다. **결함 없음** — 여유가 사라지는 것만 바닥으로 막음 | 완료 |
| 278 | 그 과정에서 **내 탐색이 두 번 깨졌다** — 예고 색 키를 `telegraph`로 짐작했는데 실제로는 `windup`이라 전부 0이 나왔다. 0을 「완전히 겹친다」로 읽었으면 없는 결함을 고칠 뻔했다. 측정값이 극단으로 나오면 대상부터 확인한다 | 기록 |
| 279 | 전투 소리 검사가 **세는 쪽**만 보고 있었다 — 몇 번 울릴지는 지키는데 **무슨 소리가 나는지**는 아무도 안 봤다. 세기가 0이면 아예 안 들리고, 높이가 겹치면 「내가 맞았다」와 「적을 때렸다」가 같은 소리가 된다. 주석에 적혀 있던 의도(「내가 맞은 소리는 높고 짧다」·「내려침은 가장 낮고 길다」)를 검사로 옮김. 음높이는 차이가 아니라 **비**로 잰다(1.3배) | 완료 |
| 280 | 소리는 이 저장소에서 **아무도 들어 본 적이 없는** 감각이다 — 값으로 지킬 수 있는 것만 지키고, 네 신호가 실제로 갈려 들리는지는 2분 목록에 주파수와 함께 남김 | 사람 확인 필요 |
| 281 | 주변 소리 모듈(`voices.ts`)에 **검사가 하나도 없었다** — 소리는 아무도 들어 본 적이 없어 값이 뒤집혀도 알아챌 방법이 없다. 짝지어진 값들(바람·보드 구름의 최소/최대, 차 간격, 가까이/멀리 대역)이 뒤집히지 않았는지, 세기가 0이 아닌지, 낮은 웅웅거림이 들을 수 있는 높이인지, 두 웅웅거림이 서로 다른지를 검 | 완료 |
| 282 | 오디오 노드를 만들지 않고 **값만 읽는다** — `AudioContext` 없는 환경에서도 돌아야 하고, 확인하려는 것은 배선이 아니라 숫자의 관계다 | 기록 |
| 283 | 음악은 진행(어떤 화음이 언제 오는가)은 잘 보고 있었는데 **소리가 나는지**는 아무도 안 봤다. 음량 0(배선은 다 있는데 조용하다), 빠르기가 사람이 걷는 속도 언저리인지, 근음이 들을 수 있는 높이인지, 음계가 겹치지 않고 올라가는지, 한 마디가 실제로 흐르는지를 검 | 완료 |
| 284 | 노이즈 버퍼에 검사가 없었다 — 가짜 컨텍스트로 **실제 소리 데이터**를 만들어 확인. 같은 시드는 같은 소리(문제 재현), 버퍼가 비어 있지 않음, 그리고 주석의 주장 「핑크 필터 출력을 0.11로 되돌려 ±1 안에 둔다」가 **참인지 돌려서 확인** | 완료 |
| 285 | **내가 쓴 검사가 지키는 척만 했다** — 「루프 경계가 이어져 있다」를 표본 차이로 재려 했는데 교차 페이드를 지워도 통과했다(핑크는 오히려 없을 때 경계가 더 작다: 0.05 vs 0.09). 이어 붙었는지는 귀로 판단할 문제라, 확인할 수 있는 것(여분 구간의 존재와 길이)만 남기고 나머지는 2분 목록으로 넘김. **확인할 수 없는 것을 확인한다고 적으면 그 자체가 거짓말이 된다** | 완료 |
| 286 | **「2분」이 거짓이 되어 있었다** — 이 세션에 항목을 여섯 개 더 넣어 열일곱 개가 됐고 다 보려면 15분쯤 걸린다. 거짓 시간표는 아무도 시작하지 않게 만든다(2분인 줄 알고 열었다가 끝이 안 보이면 덮는다). 「2분: 이것만은」(조작감 다섯 개 — 자동화가 원리적으로 못 보고 나머지 판단이 여기 얹힌다)과 「여유가 있다면」으로 나눔 | 완료 |
| 287 | 시간표가 다시 어긋나지 않도록 **적어 둔 개수와 실제 항목 수**를 묶고, 2분 묶음이 다섯 개를 넘지 않는지도 검(항목당 20초로 잡으면 여섯 개는 이미 2분이 아니다) | 완료 |
| 288 | 변이 검사를 세계 생성 쪽으로 넓혀 하나를 찾았다 — **보행자 다리 폭을 0.16m에서 5m로 바꿔도 전부 통과했다.** 화면에는 사람 대신 덩어리가 서 있는데 아무도 모른다. 절대 크기가 아니라 **비례**로 검(굵기 < 길이, 다리 < 몸통, 머리 < 몸통, 두 다리가 겹치지 않음, 팔이 몸통 밖에), 거기에 「사람 크기를 벗어나지 않는가」를 더함 — 비례만 보면 열 배 거인도 통과한다 | 완료 |
| 289 | 같이 훑은 것들은 정상 — 블록 크기, 구역 판정(고리 문턱), 자판기 생성 확률, 처치 목표 수. 구역 이름을 바꾸는 변이가 「헛돎」으로 나왔는데 **첫 등장이 주석이라 코드가 안 바뀐 것**이었다(약한 변이) | 확인 |
| 290 | 플레이어 몸 치수가 `Character.tsx` 안에 **숫자로 박혀 있어 검사할 대상 자체가 없었다** — 값을 아홉 배로 늘려도 전부 통과했다. 보행자(`PED_BODY`)처럼 `PLAYER_BODY`로 이름 붙여 옮기고(값은 하나도 바꾸지 않았다) 비례를 검: 팔다리가 몸통보다 가는가, 머리카락이 머리를 감싸는가(값이 0.2와 0.215로 아주 가까워 한쪽만 만지면 두피가 뚫고 나온다), 보드가 앞뒤로 긴가, 발이 상판 위에 올라가는가 | 완료 |
| 291 | **그린 키와 충돌 크기가 갈라지지 않는지**도 검 — 이동은 `PLAYER_HEIGHT`를 쓰고 그림은 이 치수를 쓴다. 갈라지면 보이는 것과 부딪히는 것이 달라져 벽에 몸이 반쯤 들어가 보인다. 그리는 쪽이 숫자를 다시 적지 않는지도 함께 고정(인도 높이가 그렇게 어긋난 적이 있다) | 완료 |
| 292 | 동료 치수도 `COMPANION_BODY`로 이름 붙여 옮기고 비례를 검 — **고리가 몸 밖을 도는가**(0.34와 0.52로 가까워 한쪽만 만지면 고리가 몸에 파묻힌다), 눈이 훨씬 작은가, 불꽃이 위로 서 있는가, 그리고 「플레이어 옆에 설 크기인가」(사람만 해지면 데리고 다니는 것이 아니라 둘이 걷는 것이 된다) | 완료 |
| 293 | **어디까지 했는지 남김** — 적·보스·도깨비 자리는 아직 치수가 숫자로 박혀 있다. 눈에 덜 띄는 자리라 여기서 멈췄고, 그 목록이 낡으면(이미 옮겼는데 남아 있으면) 검사가 실패해 갱신하게 만든다. 낡은 목록에 여러 번 속아 본 뒤의 방식이다 | 부분 완료 |
| 294 | 적 치수도 `ENEMY_BODY`로 옮기고 비례를 검 — 머리가 몸통보다 작은가, 팔이 가늘고 길어 흔들리는 것이 보이는가, 탄이 「보고 피할」 만큼 크되 로봇만 하지는 않은가, **플레이어와 비슷한 덩치인가**(훨씬 작으면 위협으로 안 읽히고 훨씬 크면 보스와 구분되지 않는다) | 완료 |
| 295 | 그 과정에서 **문자열 치환이 실패했는데 조용히 넘어갔다** — 새 파일만 생기고 연결이 안 됐다. 죽은 코드 검사가 즉시 잡았고(「새로 죽은 코드가 조용히 늘지 않는다」), 이어서 남은 목록 검사가 「Enemies는 이미 옮겼으니 목록에서 빼라」고 실패했다. 두 검사 다 설계대로 작동했다 | 기록 |
| 296 | 남은 둘(보스·도깨비 자리)도 이름 붙여 옮기고 비례를 검 — 대장은 「큰 놈」이 정체이므로 **일반 로봇의 1.8배·사람보다 큼**을, 자리는 **위로 갈수록 좁아지는 탑**인지를(층끼리의 관계가 곧 모양이라 한 층만 만지면 기둥이 된다). 예고 링은 몸 치수가 아니라 `BOSS.slamRadius`에서 와야 한다는 것도 고정 — 그림과 판정이 갈라지면 「링 밖인데 맞았다」가 된다 | 완료 |
| 297 | **목록을 규칙으로 바꿨다.** 「어디까지 했는가」를 적는 대신 「씬 컴포넌트 어디에도 치수를 숫자로 적지 않는다」로 세우자마자, **목록에 없던 파일이 하나 더 드러났다**(그래플 표식). 목록은 아는 것만 담고 규칙은 모르는 것까지 잡는다 | 완료 |
| 298 | 「목록은 아는 것만 담는다」를 검사 전체에 적용 — 접근성 검사가 대화창 **둘을 손으로 적어** 두고 있었다. 지금은 맞지만 세 번째 대화창이 생기면 조용히 빠진다(초점 이동·탭 순서가 확인되지 않은 채로). `role="dialog"`를 쓰는 파일을 찾아 훑도록 바꿈 — 주석은 걷어낸다(「예전에 role=dialog를 붙여 놓고」 같은 기록이 대화창으로 잡히면 안 된다) | 완료 |
| 299 | 규칙이 정말 새 것을 잡는지, **초점 처리 없는 대화창을 하나 더 만들어** 확인했다 | 완료 |
| 300 | `?see=party`에서 동료 넷이 **한 점에 겹쳐** 보였다. 자동화 탭에서 프레임이 안 도는 것을 알고 있었기에 「결함이 아니라 관측 한계」로 적었다 — **틀렸다.** 정지 화면이 못 보는 것은 움직임이지 시작 위치가 아니다. 다음 세 줄이 실제로 무엇이었는지 | 정정 |
| 301 | **동료 넷이 한 점에 쌓인 채 시작했다.** `?see=party`에서 하나만 보이길래 렌더 문제인 줄 알았는데, 씬은 넷을 모두 그린다 — `createCompanionState`가 **자리와 무관하게 늘 0번 각도**를 써서 시작 위치가 같았고, 흩어지는 것은 첫 1초의 `useFrame`이었다(자동화 탭에서는 그 프레임이 안 돌아 영영 겹쳐 보인다). 자리를 받게 고치고, 모든 쌍이 몸 지름보다 벌어져 시작하는지로 고정 — 변이(자리 무시)로 검사가 잡히는 것까지 확인 | 완료 |
| 302 | 고친 뒤 화면을 다시 보니 **쌍끼리 고리가 겹쳤다.** 간격 검사에 몸통 지름 `0.68`을 손으로 적어 둔 탓 — 실제 실루엣은 고리까지 1.13m다. 통과하면서 겹쳤다. 숫자를 `COMPANION_BODY`에서 유도하고 자리 각도 간격을 0.35→0.5로 벌림 | 완료 |
| 303 | 그래도 화면에서는 뭉쳐 보였다. 원인은 **시작 위치와 정지 궤도의 기준이 정반대**라는 것 — 하나는 플레이어에서 빼고 하나는 더한다. 그래서 시작하자마자 넷이 반 바퀴를 돌았고 그 도중에 서로 스쳤다. 끝값만 보던 검사는 이 순간을 통째로 놓쳤다 → **매 프레임의 최소 간격**을 보도록 바꿈. 이제 첫 프레임부터 1.32m로 일정하고, `?see=party` 화면에서 넷이 또렷이 갈라진 것을 눈으로 확인했다 | 완료 |
| 304 | 대신 **자리 계산 자체**를 검사로 확인 — 각도 검사가 자리 **셋만** 보고 있었다(도깨비가 넷이 된 뒤에도 그대로). 정본에서 자리 수를 가져오게 하고, 각도가 다른 것에 더해 **몸이 겹치지 않을 만큼 벌어지는지**(따라오는 거리에서 호의 길이가 동료 지름보다 큰지)를 검. 뒤 자리 간격을 지워 잡히는 것을 확인 | 완료 |
| 305 | **완주 화면과 도감이 정면으로 겹쳤다** — 화면에서 봤다. 「최고 속도」와 「만난 도깨비 3 / 3」이 도감에 가려진다. 완주 뒤 도감을 여는 것은 가장 자연스러운 동작인데 거기서 부딪힌다. 좌표를 서로 피하게 맞추는 것은 여섯 번 실패한 방식이라 **연 쪽이 이긴다**로 풀었다(대화창은 방금 누른 것, 완주 화면은 가만히 떠 있던 것 — 닫으면 다시 나타나고 기록은 굳어 있어 값이 변하지 않는다) | 완료 |
| 306 | 고친 뒤 화면이 검게 나와(자동화 탭에서 프레임이 안 도는 알려진 한계) **눈으로는 확인하지 못했다** — 논리와 변이로만 확인했고 2분 목록에 적어 둠 | 사람 확인 필요 |
| 307 | 같이 확인된 것(정상): 완주 화면의 「만난 도깨비 **3 / 3**」 — 분모를 「찾아갈 수 있는 수」로 바꾼 것이 실제로 가득 찬다. 도감은 「4 / 4」로 뜨고 넷 다 해금 상태가 맞다 | 확인 |
| 308 | **화면을 다시 열어 결함 하나를 찾았다** — 지도에서 변두리 구역이 바탕과 거의 구분되지 않았다. 재 보니 바탕과 34, 번화가와 35로 둘 다 기준(40) 아래. 지도의 목적이 「어느 구역인가를 한눈에」인데 셋 중 하나가 안 보였다. 알파를 올려 모든 쌍이 42 이상 되게 하고, 고친 뒤 화면에서 세 띠가 갈리는 것을 확인 | 완료 |
| 309 | 구역 색은 **반투명**이라 원색끼리 비교하면 실제로 보이는 것과 다른 답이 나온다 — 바탕 위에 얹은 결과로 재는 검사를 넣음. 검사가 아예 없어서 이 결함이 남아 있었다 | 완료 |
| 310 | 같이 확인된 것들(정상): 시작 화면의 이름 입력과 안내 문구, `?see=clues`가 흔적 단계를 열고 계수기가 「0 / 3」으로 뜨는 것, 지도 문장(「조사할 흔적 3곳, 가장 가까운 곳은 남쪽 16m」)이 흔적을 먼저 말하는 것, 범례에 흔적이 들어간 것, 동료 고리가 몸 밖을 도는 것 | 확인 |
| 311 | 순환 검사도 셋을 손으로 적어 두고 있었다 — 새 `next*`가 생기면 조용히 빠지고, 그러면 그 목록의 항목 하나가 영영 안 나와도 모른다. **정본에서 찾아 대조**하도록 바꾸고, 여기서 다루지 않는 둘(`nextDokebi`는 잠긴 것을 건너뛰므로 따로, `nextQuest`는 끝이 있어 순환이 아니다)은 **이유와 함께** 면제. 새 순환을 만들어 잡히는 것을 확인 | 완료 |
| 312 | 목록 훑기 마무리 — 규칙으로 바꾼 것 다섯(순수 모듈·대화창·화면·시뮬레이션 호출·순환), 판단이라 규칙화하지 않고 **신선도만** 지킨 것 둘(조용한 기본값, 한 방향 상태). 규칙으로 바꿀 때마다 **목록에 없던 것이 하나씩** 나왔다 | 완료 |
| 313 | 「조용한 기본값」 목록은 **판단이라 자동으로 만들 수 없다**(무엇이 죽는지는 사람이 정한다). 대신 낡는 것은 막았다 — 함수가 사라지거나 이름이 바뀌면 검사가 「부르는 파일이 없다」로 통과해 지키는 척만 하게 된다. 정의가 실재하는지부터 확인하도록 함 | 완료 |
| 314 | 프레임 상한 검사가 **시뮬레이션 함수 이름 여섯 개를 나열**하고 있었다 — 실제 `step*` 함수는 그 두 배가 넘고, 새로 만든 것은 조용히 빠졌다(그중 하나가 이번 세션에 내가 만든 `stepInteraction`이다). 이름을 모으는 대신 **모양**(`step` + 대문자)으로 잡도록 바꿈. 상한 없이 부르는 새 호출부를 만들어 잡히는 것을 확인 | 완료 |
| 315 | **오류 화면 셋에 본문 랜드마크가 없었다** — 랜드마크 검사가 화면 둘(시작·월드)을 손으로 적어 두고 있어서 아무도 몰랐다. `role="alert"`는 내용을 읽어 주지만 **구조를 주지 않아**, 낭독기 사용자가 「본문으로 건너뛰기」를 할 수 없다. 셋 다 `<main>`으로 감쌈 | 완료 |
| 316 | 목록을 규칙으로 — 「**자기 제목(`<h1>`)을 그리는 것은 한 화면**이고, 화면에는 본문이 있어야 한다」. 조각(패널·알림)은 제목을 안 그리므로 걸리지 않는다. 규칙으로 바꾸자마자 목록에 없던 **네 번째 화면**(씬 오류)이 드러났다 — 지난 반복의 그래플 표식과 같은 일이 또 일어났다 | 완료 |
| 317 | **내가 직전 보고에서 「의도적인 화이트리스트」라고 단정한 것이 틀렸다.** 확인해 보니 손으로 적은 목록이었고, 삭제는 잡지만(`existsSync`) **추가는 못 잡아** 이번 세션에 만든 순수 모듈 열한 개가 조용히 빠져 있었다 | 완료 |
| 318 | 규칙으로 바꿈 — 「`src/game` 아래 `.ts`는 화면 없이 도는 계산이다」. 예외는 텍스처를 굽는 둘뿐이고(캔버스를 GPU로 올리는 일이라 three가 필요하다), 그 예외 목록이 낡으면 실패한다. 새 모듈에 three를 넣어 실제로 잡히는지 확인 | 완료 |
| 319 | 그 표에서 **행 번호가 300부터 여덟 번 겹쳐** 있었다 — 요약이 「(반복 300)」이라고 적으면 어느 행인지 알 수 없다. 이미 있던 검사는 인용한 번호가 표에 **있는지만** 보아, 여덟 개 중 아무거나 하나만 맞아도 통과했다. 순서대로 다시 매기고 「번호가 겹치지 않는다」를 추가 | 완료 |
| 320 | 사람에게 넘기려던 질문(「달리거나 꺾을 때도 넷이 갈라지는가」)을 다시 봤더니 **목표 지점이 순수 계산이라 여기서 답할 수 있었다** — 급회전에서도 1.39m까지만 좁아진다. 검사로 옮기고 README의 부탁을 지웠다. 눈이 필요한 것과 게을러서 안 잰 것을 구분해야 부탁 목록이 신뢰를 얻는다 | 완료 |
| 321 | 문서의 테스트 수 검사가 **기획서 한 곳만** 보고 있었다. README는 831개(실제의 절반), 백로그는 923개라고 적힌 채였다 — 처음 오는 사람이 읽는 것은 README다. 「어느 문서가 숫자를 말하는가」를 손으로 적는 대신 루트의 모든 `.md`를 훑도록 바꾸자 **바로 둘 다 걸렸다** | 완료 |
| 322 | 「확인 지점 안내」 검사가 **파일 전체에서 이름만** 찾고 있었다 — `air`와 `result`는 다른 문장에 이름이 나온 덕에, 정작 안내표에서 빠진 채로 통과했다. 「어딘가에 적혀 있다」와 「안내표에 있다」는 다르다. 표 안만 보도록 좁히자 즉시 둘이 걸렸고 채워 넣었다 | 완료 |
| 323 | 「해금 조건 세 갈래」도 손으로 적은 낱말 목록이었다 — 네 번째 조건이 생기면 기획서에 안 적혀도 아무도 모른다. 조건 키를 `DokebiSpirit` 타입에서 뽑아(`requires*`) `Record`로 요구하도록 바꿈. **실제로 `requiresNight`를 넣어 보니 타입 검사가 문서 쪽까지 막았다**(로스터 넷을 다 채운 뒤에도 `docs.test.ts`가 걸린다) | 완료 |
| 324 | 자원 해제 규칙이 **지오메트리만** 보고 있었다. 재질·텍스처·렌더타깃도 똑같이 `dispose()`가 필요한데 규칙이 없다 — 지금 안 새는 것은 만드는 곳이 텍스처 두 파일뿐이라서지 규칙 덕이 아니다. 「`dispose()`가 있는 THREE 생성자」로 넓힘 | 완료 |
| 325 | 넓히면서 **`g` 붙은 정규식 하나를 `test()`로 돌려썼다** — `lastIndex`가 남아 호출마다 답이 달라진다(파일이 절반만 걸린다). 쓸 때마다 새로 만들도록 고침. 검사를 넓히는 김에 검사 자체에 결함을 넣을 뻔했다 | 완료 |
| 326 | 넓힌 규칙이 **멀쩡한 파일을 걸었다** — `textures.ts`는 캐시 넷 중 둘이 Map이 아니라 변수라 루프로 놓을 수가 없다(실제로는 다 놓는다). 「목록으로 훑어라」를 「목록으로 훑거나 만든 수 이상 놓아라」로 고침. 규칙을 넓힐 때는 **기존 통과 파일이 왜 통과했는지**를 먼저 봐야 한다 | 완료 |
| 327 | 넓힌 규칙이 정말 잡는지 확인 — `GrappleVisuals`에 재질 셋을 더하고 해제를 지우자 두 검사가 함께 실패했다(「해제가 없다」·「5개를 만드는데 0번만 놓는다」) | 완료 |
| 328 | 도깨비 이름 넷을 손으로 적어 둔 검사(알림이 이름을 미리 밝히지 않는가)를 `DOKEBI_ORDER`에서 가져오도록 바꿈 — 다섯 번째 도깨비의 이름은 검사되지 않았다 | 완료 |
| 329 | **컨테이너 안에서 스스로 뜨는 HUD 조각을 찾았다** — `CaptureNotice`가 `absolute left-1/2`를 들고 알림 넷을 쌓는 컨테이너 안에 있었다. 흐름에서 빠지므로 자판기 앞에서 사진을 찍으면 자판기 안내와 겹치고, 이미 가운데 정렬된 컨테이너 안에서 반쪽을 또 밀어 좌우로도 어긋난다. 두 호출부 모두 자리를 잡아 주고 있어 위치 클래스만 걷어냈다 | 완료 |
| 330 | 이 결함을 막으려던 검사는 **파일 넷을 손으로 적어 두었고, 그 넷은 원래 문제가 없던 파일들이었다.** 「어떤 파일을 볼 것인가」를 `WorldHud`의 쌓는 컨테이너에서 직접 읽도록 바꿈 — 새 알림을 넣으면 그 순간부터 같은 규칙을 받는다. 되돌려 보니 실제로 잡는다 | 완료 |
| 331 | 화면에서 큰 초록 상자들이 눈에 띄어 가로수를 봤더니 **개수만 세고 자리는 아무도 안 보고 있었다**(`perfBudget`이 인스턴스 수를 셀 뿐). 재 보니 배치는 멀쩡했다 — 건물 침범 0, 소품 겹침 0. 같은 부류에서 이미 「적 42% 건물 안」이 났으므로 고정해 둠(수관·기둥·소품·간격 네 가지) | 완료 |
| 332 | 처음엔 「나무끼리 수관이 안 겹친다」로 썼다가 두 쌍이 걸렸는데, **보러 가 보니 나뭇잎이 맞닿는 정상적인 가로수였다.** 검사를 고쳐 기둥 간격으로 봄 — 규칙이 걸렸다고 코드가 틀린 것은 아니다 | 완료 |
| 333 | 변이가 「헛돎」으로 나와 규칙부터 의심할 뻔했는데, **변이가 약했다**(`SIDEWALK_EDGE`가 18이라 6m를 밀어도 건물 사이 틈에 떨어진다). 수관을 1m만 옮기면 14개가 걸린다는 것을 따로 재서 규칙이 팽팽함을 확인하고, 그 형태로 다시 변이해 잡히는 것을 봤다 | 완료 |
| 334 | 「모든 레이어에 무언가 들어 있다」가 **레이어 스무 개를 손으로 적어** 두고 있었다 — 새 생성기가 죽은 채 들어와도 통과한다. `layout`·`details`에서 배열인 것을 그대로 훑도록 바꿈. 지금 목록과는 정확히 일치했지만(빠진 것 없음), 빈 레이어 `flowerBeds`를 넣어 보니 즉시 걸린다 | 완료 |
| 335 | 성능 예산의 스트리밍 목록 열넷도 같은 모양이었다 — `City.tsx`가 레이어를 하나 더 스트리밍하면 **화면 인스턴스가 늘었는데 상한 검사는 조용하다.** `useStreamed(...)` 호출을 정본에서 읽도록 바꿈. `roadMarks`를 하나 더 스트리밍해 보니 6443개로 상한(6000)을 넘겨 바로 실패한다 | 완료 |
| 336 | 「보기만 하는 컨테이너는 클릭을 가로채지 않는다」가 **마커 셋을 손으로** 적어 두고 있었다 — 구역은 넷이고, 새 구역은 아무 규칙도 못 받는다. 규칙을 내용에서 끌어냄: **누를 것이 있으면 통과시키고, 없으면 뒤를 막지 않는다.** 두 방향 다 화면에 단서가 없는 결함이라 양쪽으로 검사한다 | 완료 |
| 337 | 바꾸자마자 두 건이 걸렸는데 **둘 다 내 훑기 탓이었다.** 본문을 「다음 컨테이너가 나올 때까지」로 잘라 뒤 JSX가 딸려 들어왔고(알림 컨테이너에 버튼이 있다고 오인), 자식 컴포넌트가 품은 버튼을 못 봤다(도감 안 버튼 넷). `</div>` 깊이로 자르고 자식 파일까지 한 단계 들어가도록 고침 | 완료 |
| 338 | 같은 거친 훑기를 쓰던 `stackedComponents()`(반복 330의 `CaptureNotice` 규칙)도 새 자르기로 통일. 세 방향으로 되돌려 확인 — 좌하단이 클릭을 먹을 때, 도감 컨테이너가 막힐 때, `CaptureNotice`가 다시 뜰 때 모두 잡힌다 | 완료 |
| 339 | **한국어가 어절 중간에서 끊기고 있었다** — 랜딩에서 「숨어 있던 도깨비/와」, 「광장에서 시작합니/다.」로 나온다. 브라우저 기본값은 한글을 글자 아무 데서나 자른다. `body`에 `word-break: keep-all`을 넣고, 짝인 `overflow-wrap: break-word`도 함께(없으면 띄어쓰기 없는 긴 문자열이 상자를 뚫는다). 화면에서 고쳐진 것을 확인 | 완료 |
| 340 | **도감을 열었더니 뒤 간판이 글자를 뚫고 올라왔다.** 재 보니 패널 바탕이 0.72라 흰 간판(#f6f3e9) 앞에서 보조 글자 대비가 **3.93**(AA 4.5 아래)이다. 도감 설명·목표 라벨이 전부 이 색이다. 0.80으로 올려 5.31 확보(0.76은 4.55라 아슬아슬해 쓰지 않음) | 완료 |
| 341 | 대비 검사는 **배경색 위에서만** 재고 있었다 — 패널은 3D 화면 위에 뜨는데. 하늘로만 재면 최저 4.89라 통과한다(실제로 뒤에 오는 것은 간판이다). 지도 구역에서 쓴 알파 합성을 그대로 가져와 「무엇이 뒤에 와도 읽힌다」를 넣음 | 완료 |
| 342 | **포토 모드에서 사진을 고르는 동안 맞아 죽는다.** 지도를 열어 두고 화면을 보다가 체력이 5/5 → 1/5가 된 것을 보고 알았다. 포토 모드는 **플레이어 이동만** 멈추고 로봇·대장은 계속 다가와 때린다 — 움직일 수도 피할 수도 없는데. 둘 다 `frozen`으로 시간을 멈추게 함. 군중·차량은 그대로 둔다(배경까지 멈추면 사진이 정물이 된다) | 완료 |
| 343 | 타입 주석이 「포토 모드. 켜지면 **시뮬레이션을 멈추고** 카메라만 자유롭게 돈다」고 **단언하고 있었다.** 계약과 구현이 갈라진 채였고 아무도 대조하지 않았다 — 경계면 교차 검증이 잡았어야 할 종류다. 주석을 실제와 맞춤 | 완료 |
| 344 | 검사는 목록 대신 조건으로 찾는다 — 「`link.playerHp`나 `bossSlamHit`를 건드리는 `useFrame` 컴포넌트」가 곧 때릴 수 있는 것이다. 씬이 그 수만큼 `frozen`을 넘기는지도 함께 본다 | 완료 |
| 345 | 그 검사가 **이름만 보고 있었다** — 파일에 `frozen`이라는 낱말이 있는지만 봐서, `const dt = frozen ? 0 : ...`의 조건만 지워도 props에 이름이 남아 통과했다. 되돌려 보고 알았다. 시간을 재는 그 줄이 실제로 `frozen`을 보는지로 좁힘 | 완료 |
| 346 | **키보드로 버튼을 Space로 누를 수 없었다.** 월드 입력이 `Space`에 `preventDefault`를 걸어 브라우저가 초점 잡힌 버튼을 활성화하지 못한다 — 도감을 열고 Tab으로 「닫기」에 가서 Space를 눌러도 닫히지 않고 캐릭터만 뛴다. **같은 자리에서 Enter는 닫혔다**(브라우저에서 그렇게 확인). 버튼은 Enter와 Space 둘 다로 눌려야 한다 | 완료 |
| 347 | 예외가 「폼 입력」뿐이었다 — 버튼은 폼 입력이 아니다. 초점이 `button, a[href], [role=button], summary`에 있으면 활성화 키만 양보하도록 함. **이동 키는 그대로 받는다**: 전부 막으면 HUD 버튼을 한 번 누른 뒤로 걸어 다닐 수 없고 캔버스를 다시 클릭해야 한다 | 완료 |
| 348 | 고친 뒤 브라우저에서 다시 눌러 Space로 도감이 닫히는 것을 확인. 검사는 세 가지를 본다 — 양보 대상에 버튼이 있는가, 가드가 `Space` 처리보다 **앞**에 있는가(뒤면 이미 `preventDefault` 뒤라 무의미), 이동 키까지 양보하지는 않는가 | 완료 |
| 349 | 고치자 **기존 규칙이 내 코드를 잡았다** — 「키 코드를 문자열로 박아 두지 않았다」(정본 `CONTROL_CODES`에서 가져오라). 규칙의 뜻이 맞다. 다만 `Enter`는 게임 바인딩이 아니라 **브라우저 활성화 키**라 조작표에 뜨면 안 되므로, `ACTIVATION_CODES`로 이름을 붙여 구분했다. 검사도 그 상수를 보도록 옮김 | 완료 |
| 350 | 키보드 점검을 이어감 — 도감·지도의 `Escape` 닫기, 닫은 뒤 초점 복원(`useDialogFocus`), 랜딩 설정의 `aria-pressed`와 체크박스 이름은 **모두 제대로 되어 있었다.** 도구가 체크박스를 `checkbox "on"`으로 보여 줘 결함인가 했는데 실제 접근성 이름은 「모션 줄이기」였다 — 확인 없이 고쳤으면 멀쩡한 것을 건드릴 뻔했다 | 확인 |
| 351 | 반응형은 **자동화로 확인할 수 없다** — 창을 420px로 줄여도 화면이 그대로 나온다(스크린샷이 뷰포트를 따르지 않는다). 「모른다」로 기록하고 넘어감. 브라우저 자동화 한계 목록과 같은 종류다 | 기록 |
| 352 | 오디오 자원 검사가 **파일 셋을 손으로** 적어 두고 있었다. 지금 목록과는 맞지만 새 모듈은 검사를 받지 않는다 — 멈추지 않은 오실레이터는 예외도 소리도 없이 CPU만 먹으므로 검사가 보지 않으면 아무도 모른다. 만드는 파일을 직접 찾도록 바꿈. 안 멈추는 모듈을 하나 넣어 보니 즉시 걸린다 | 완료 |
| 353 | 「쌓이는 요소가 스스로 자리를 잡지 않는다」도 파일 둘을 손으로 적고 있었다 — `CaptureNotice`를 놓친 목록과 같은 종류다. `WorldHud`에서 읽은 쌓이는 컴포넌트 전부를 보도록 바꿈. 자기 파일에 있는 것과 `WorldHud` 안에 있는 것 양쪽으로 되돌려 확인 | 완료 |
| 354 | 그 과정에서 **내 훑기가 멀쩡한 것을 결함으로 봤다** — `SpeedReadout`을 보다가 넉넉히 자른 창이 옆 `KeyboardHints`(독립 배치라 정상)의 좌표를 끌어왔다. 함수 본문을 다음 최상위 선언까지로 정확히 자르게 고침. 이번 세션에서 훑기 경계 때문에 헛짚은 것이 세 번째다 | 완료 |
| 355 | 컨테이너 소속 검사 둘은 **의도한 배치**를 고정하는 것이라 이름 목록이 맞다. 다만 본문을 「첫 `</div>`까지」로 자르고 있어, 자식 하나를 `<div>`로 감싸면 뒤의 둘이 「밖에 있다」고 거짓 실패한다(확인해 보니 `HealthPanel`부터 안 보인다). 조용히 통과하는 쪽은 아니지만 멀쩡한 변경을 막는 검사는 결국 지워지므로 깊이 자르기로 통일 | 완료 |
| 356 | **늘 보이는 미니맵이 낭독기에 내용이 없었다** — `aria-label="주변 지도"` 한 줄뿐. 큰 지도는 같은 이유로 이미 `describeMap`으로 고쳤는데 정작 작은 쪽이 남아 있었다(로봇 수도 목표 방향도 없다). 같은 함수로 말하게 함. 흔적은 넘기지 않는다 — 미니맵은 흔적을 그리지 않으므로 말하면 화면에 없는 것을 안내하게 된다 | 완료 |
| 357 | 기존 검사는 「대체 텍스트가 **있는가**」만 봐서 고정 문자열로도 통과했다. 「지도 캔버스는 무엇이 있는지 말한다」를 추가 — 지도는 손으로 적지 않고 찾는다(대장 자리를 찍으려면 `BOSS_HOME`을 가져와야 한다) | 완료 |
| 358 | 그 검사가 처음에 `aria-label={...}`만 인정해 **큰 지도를 결함으로 잡았다** — 큰 지도는 눈에 보이는 글로 요약을 내고 그쪽이 더 낫다. 두 경로 다 인정하도록 고침 | 완료 |
| 359 | 「한쪽만 고쳐진 쌍」을 더 찾아봄 — 포토 필터는 씬 안 사각형이라 저장된 사진에 그대로 남고(주석이 그렇게 설계했다고 적혀 있고 실제로 그렇다), 완주한 판으로 다시 들어와도 결과 화면이 다시 뜨지 않는다. **둘 다 정상** | 확인 |
| 360 | `?see=` 확인 지점이 **저장된 판을 영구히 바꾼다**(보스 처치·흔적이 저장 감시에 걸려 기록된다). 내 관찰도 이것 때문에 흐려졌다 — 평범한 `/play`가 완주 상태로 열려 「목표가 안 뜬다」고 오해할 뻔했다. 다만 프로덕션은 `parseScenario`가 `null`을 돌려주므로(검사로 고정돼 있다) **사용자에게는 영향이 없다.** 고치지 않고 다음 사람이 같은 데서 헤매지 않도록 기록만 남긴다 | 기록 |
| 361 | 실패 경로를 봄 — 사파리 프라이빗 모드에서는 `localStorage`에 **접근하는 것만으로도** 예외가 난다. 실제 접근 다섯 곳은 모두 `try` 안에 있었다. 다만 「막혀도 살아남는다」 검사가 `saveGame`·`settings` **두 파일만** 보고 있어, 내일 다른 파일이 만지면 아무도 모른다. 만지는 곳을 전수로 찾아 모두 `try` 안인지 보도록 올림 | 완료 |
| 362 | 그 규칙을 두 방향으로 확인 — 보호 없이 만지는 새 파일을 넣으면 잡고(`_probe.ts:3`), 기존 읽기를 `try` 밖으로 빼도 잡는다. 주석은 세지 않는다(「프라이빗 모드에서 예외가 난다」 같은 설명이 걸린다) | 완료 |
| 363 | 같은 부류의 다른 API도 훑음 — `sessionStorage`·`indexedDB`·클립보드·전체화면은 **쓰지 않는다.** `matchMedia`는 네 곳 중 둘만 `typeof` 검사가 있지만, 나머지 둘은 `PlayShell`이 클라이언트에서만 마운트하는 자리라 `window`가 확실하고 `matchMedia`는 WebGL 되는 브라우저에 늘 있다 — **결함 아님**으로 판정 | 확인 |
| 364 | 분석 이벤트에 개인정보가 실리는지 봄 — 지금 실리는 것은 게임이 만든 값뿐(도깨비 id·처치 수·품질·mime)이고 sink는 아무 데도 보내지 않는다. 문제는 **지금은 안 새는 이유가 sink가 비어 있어서**라는 것이다. 수집 서버가 붙는 날 함께 새기 시작하고, 그때 이 코드를 다시 읽을 사람은 없다 — 「사용자가 적은 것을 싣지 않는다」를 규칙으로 못 박음. 완주 이벤트에 별명을 실어 보니 잡는다 | 완료 |
| 365 | 사용자 입력이 밖으로 나가는 다른 자리(사진·클립 파일 이름)는 **이미 막혀 있었다** — `buildCaptureFilename`이 호출부를 믿지 않고 다시 `sanitizeNickname`을 거치고, `../../evil`을 넣는 검사도 있다. 고칠 것 없음 | 확인 |
| 366 | **CSP를 넣었다.** 「배포 전에 브라우저에서 확인하며 넣는다」고 미뤄 둔 것인데, 이제 확인할 수 있다 — 프로덕션 빌드를 3113에 띄워 헤더가 붙는 것, 월드가 정상 렌더되는 것, 콘솔에 위반이 없는 것을 보고 넣었다. 개발 서버(3112)는 건드리지 않았다 | 완료 |
| 367 | nonce는 배선하지 않았다 — Next가 RSC 페이로드를 인라인 스크립트로 밀어 넣어 `'unsafe-inline'` 없이는 페이지가 죽는다. **인라인은 열고 나머지를 닫는다**: 외부 스크립트·플러그인·프레임 삽입·`<base>` 가로채기·바깥 연결. 인라인 주입은 이 앱에 통로가 없다(사용자 HTML도 `dangerouslySetInnerHTML`도 없다) — 판단과 이유를 코드에 남김 | 완료 |
| 368 | `blob:`을 빼면 사진·클립 저장이 **조용히** 실패한다(화면에는 「저장에 실패했습니다」만 뜬다). `img-src`·`media-src`에 넣고 그 자리를 검사로 못 박음 — 되돌려 보니 잡는다. 「이유를 남긴다」였던 검사를 「실제로 있다」로 올림 | 완료 |
| 369 | CSP를 넣으면서 프로덕션 빌드를 볼 수 있게 됐으니, **같은 이유로 미뤄 둔 것**을 다시 봄. 컨텍스트 손실 알림은 「검증할 방법이 없다」고 적혀 있었는데 `WEBGL_lose_context`로 강제로 끊어 확인할 수 있었다 — 이벤트도 오고 `role="alert"`에 안내와 새로고침 버튼도 뜬다 | 완료 |
| 370 | **처음엔 「안내가 아예 안 뜬다」고 결론 낼 뻔했다.** 캔버스가 300×150(기본값)인 것을 보고 알았다 — 숨은 탭이라 R3F가 아직 렌더러를 안 만들었고, 그래서 앱의 리스너도 안 붙은 상태였다. 스크린샷으로 초기화시킨 뒤 다시 하니 정상이었다. **관측 도구의 상태부터 확인한다** | 정정 |
| 371 | 그 과정에서 **진짜 결함을 찾았다** — 알림이 완주 화면 **아래에 깔려** 「그래픽 연결이 끊겼습니다」와 새로고침 버튼이 가려졌다. 이 프로젝트는 z-index를 안 쓰고 DOM 순서로 겹침을 정하는데 알림이 중간에 있었다. 맨 끝으로 옮김 — 게임이 죽었다는 소식은 무엇에도 가리면 안 된다(다른 겹침은 「연 쪽이 이긴다」로 풀지만 이것은 사람이 연 것이 아니라 사고다) | 완료 |
| 372 | 검사는 「사고 알림 뒤에 그리는 것이 없다」로 못 박음. 처음엔 파일 끝까지 훑어 뒤에 정의된 도우미 함수(`HudButton` 등)를 형제로 오인했고, 변이도 한 번 헛돌았다(알림을 **하나 더** 만드는 꼴이라 `lastIndexOf`가 여전히 맞았다). 같은 반환문 안만 보도록 좁히고 진짜 「옮기기」로 확인 | 완료 |
| 373 | 저장값을 브라우저에서 **직접 망가뜨려** 부팅을 확인 — 진행·설정 둘 다 깨진 JSON으로 바꿔도 기본값으로 정상 부팅한다(오류 화면도, 검은 화면도 없다). 망가진 값은 그대로 두고 덮어쓰지 않는다 — 읽기만으로 사용자 데이터를 고쳐 쓰는 편이 더 나쁘다 | 확인 |
| 374 | 그러다 **검사 구멍을 찾았다.** `bossDefeated`·`foundClues`는 이번 세션에 더한 필드인데 **틀린 타입으로 넣어 본 적이 한 번도 없었다.** 소스는 막고 있지만(`=== true`, `Array.isArray`) 검사가 없으면 다음 사람이 `Boolean(...)`으로 「단순화」한다 — 문자열이 `foundClues`로 새면 흔적 계수기가 글자 수를 센다. 틀린 타입·모르는 id·중복 세 가지를 넣고 되돌려 확인 | 완료 |
| 375 | WebGL 미지원 화면도 확인 — `<main>` 랜드마크, `h1`, 설명, **새로고침과 되돌아가기 둘 다** 이미 갖춰져 있었다. 고칠 것 없음 | 확인 |
| 376 | 저장 쪽에서 찾은 구멍(「새 필드에 틀린 타입을 넣어 본 적이 없다」)이 **설정 쪽에도 있었다** — 필드별 검증 검사가 `sound` **하나만** 보고 있었다. 정본(`DEFAULT_SETTINGS`)의 키를 훑어 전부 쓰레기(`{}`)로 채우고 결과가 기본값과 같은지 보도록 바꿈. 새 필드는 만들어지는 순간 이 검사를 받는다 | 완료 |
| 377 | `version`만 성한 값으로 남긴다 — 그것까지 망가뜨리면 통째로 되돌아가서 **필드별 검증이 하나도 없어도 통과**한다. 외형·별명 검증을 각각 빼 보고 잡히는 것을 확인 | 완료 |
| 378 | 저장 쪽에도 같은 훑기를 넣음. 여기는 「이상하면 버린다」와 「기본값으로 대체한다」가 섞여 있으므로 **어느 쪽이든 쓰레기가 그대로 나오지 않는 것**만 본다. 필드 목록은 정상 저장을 한 번 왕복시켜 얻는다 — 손으로 적지 않는다 | 완료 |
| 379 | **`?see=constructor`가 시나리오로 통했다.** `SCENARIOS[value] ?? null`이 프로토타입의 것을 못 걸러 `Object` 함수를 시나리오랍시고 돌려준다 — 뒤쪽 코드가 `scenario.timeOfDay`를 읽으므로 전부 undefined가 되어 조용히 이상한 상태로 시작한다. `Object.hasOwn`으로 자기 것만 보게 고침 | 완료 |
| 380 | 같은 모양을 훑었더니 **네 곳이 더** 있었다 — 시간대·도깨비·색보정·외형·포즈의 「안전하게 꺼낸다」 함수들. `timeOfDayPreset("constructor")`가 `Object`를 돌려주고 호출부가 읽는 `sky`는 undefined다. **「모르는 id면 기본값」이라고 주석에 적어 둔 바로 그 약속이 깨지는 자리**였다 | 완료 |
| 381 | 목록 대신 규칙으로 — 「캐스팅한 키로 객체를 찾는 곳」을 전부 찾아 자기 것인지 확인하고 쓰는지 본다. 정적 훑기만으로는 「확인하는 코드가 있다」까지라, 다섯 함수를 프로토타입 이름 넷으로 **실제로 불러 보는** 검사를 함께 넣음 | 완료 |
| 382 | 자바스크립트 함정을 한 바퀴 훑음 — 비교 함수 없는 `sort()` **없음**, 나머지 연산은 각도 정규화 두 곳 다 음수를 제대로 다룬다, 배열 인덱스로 쓰이는 값들은 하드코딩 0..N이거나 시드 난수라 음수가 될 수 없다, mulberry32도 정상. **고칠 것 없음** | 확인 |
| 383 | 성능 패널을 처음 열어 봄 — `role="status"` + `aria-live="off"`(숫자가 계속 바뀌므로 읽지 않는 것이 맞다) + `<th scope="row">`까지 갖춰져 있다. 대비도 이제 기준을 넘는다(간판 색은 흰색보다 어둡다). **고칠 것 없음** | 확인 |
| 384 | **처음으로 실제 측정값을 얻었다** — 광장 시작 지점에서 드로우콜 **95**, 삼각형 **144,690**, 힙 **46MB**, 자동 품질 판정 **high**. fps는 여전히 못 잰다(숨은 탭이라 rAF가 안 돈다 — 2813ms로 나온다). 문서에 적어 둔 절대 수치가 없어 낡은 것은 없었고, 이 값이 첫 기준선이다 | 기록 |
| 385 | 여정 안내문 검사 둘이 **문구가 바뀌면 조용히 사라지는** 모양이었다 — `hints.includes("두 번 때리면")`이 거짓이면 `return`이라, 누가 「세 번 때리면」으로 고쳐 적는 순간 검사가 없어진다. **틀린 안내를 막으려던 검사가 틀린 안내에만 반응하지 않는** 꼴이다 | 완료 |
| 386 | 문장을 찾는 대신 **수와 모양을 읽어** 정본과 대조하도록 바꿈 — 「n번 때리면」은 `COMBAT_TUNING.maxHp`와, 「붉은 삼각형」은 `MARKS.boss`의 색·모양과 맞춘다. 어떤 낱말로 적든 값이 맞아야 한다 | 완료 |
| 387 | 양쪽으로 되돌려 확인 — 안내문만 바꿔도(「세 번」·「마름모」), 지도 정본만 바꿔도 잡힌다. 그리는 자리가 실제로 `lineTo`와 `MARKS.boss.color`를 쓰는지 보던 기존 검사는 그대로 살렸다(정본에 적힌 것과 실제로 그리는 것은 다르다) | 완료 |
| 388 | 「스스로 무력화되는 검사」 모양을 **검사 전체에서** 훑음 — `if (!x) return;`이 스물여덟 군데인데 대부분은 앞에 `expect`가 있는 타입 좁히기였고, **단언 없이 빠져나가는 것이 둘**이었다(팔레트 주석 대조, 명부 진행도). 조건을 못 찾으면 그냥 통과하던 자리다 | 완료 |
| 389 | 규칙으로 못 박음 — 「빠져나가기 전에 그 값이 있다고 단언한다」. 훑기가 **문자열 안에 적힌 코드**를 자기 자신으로 오인한 것을 고치고(소스에서 그 문장을 찾는 검사가 있다), 훑기가 망가지면 빈 목록으로 통과하는 것도 함께 막음 | 완료 |
| 390 | 되돌려 확인하다 **「규칙이 헐겁다」고 잘못 결론 낼 뻔했다** — 팔레트 쪽 단언 하나를 지워도 안 잡혔는데, 같은 파일의 형제 검사가 같은 정규식을 단언하고 있어 **어차피 시끄럽게 실패**하기 때문이었다. 파일 안의 단언을 전부 지우니 잡힌다. 규칙이 파일 단위인 것은 느슨한 게 아니라 맞는 것이고, 그 이유를 주석에 남겼다 | 정정 |
| 391 | 「스스로 무력화되는 검사」의 사촌을 훑음 — **빈 목록을 돌며 통과하는** 검사. 정본 상수(`DOKEBI_ORDER` 등)를 도는 것은 안전하니 빼고, **훑어서 만든 배열**만 좁히니 여섯 곳이 나왔다(디렉터리 목록·README 지점·캐시 이름·갱신 함수·가로등 필터) | 완료 |
| 392 | 다섯 곳에 「실제로 찾았다」를 넣고, 파일마다 도는 둘(캐시·갱신 함수)은 **합계**를 세도록 함 — 파일 하나가 비는 것은 정상이지만 전부 비면 훑기가 망가진 것이다. 정규식·필터 조건을 낡게 만들어 세 곳에서 잡히는 것을 확인 | 완료 |
| 393 | 여섯 번째(`public` 폴더)는 **넣지 않았다.** 외부 에셋을 두지 않는 프로젝트라 비어 있는 것이 정상이고, 빈 목록을 도는 것이 곧 「무거운 파일이 없다」이다. 「전부 채워라」가 아니라 **비어도 되는 이유가 있으면 적어 둔다** — 그렇게 주석을 남겼다 | 완료 |
| 394 | 합계 기준을 처음에 5로 잡았다가 **파일마다 도는 검사라는 것을 놓쳐** 정상인 파일(갱신 함수 1개)을 실패시켰다. 0보다 크면 되도록 고침 | 정정 |
| 395 | **「이동은 관측 불가」가 틀렸다.** 확장 프로그램이 만든 합성 키가 안 닿는 것은 맞지만, **페이지 안에서** `window.dispatchEvent(new KeyboardEvent('keydown', {code:'KeyW'}))`를 보내면 닿는다. 실제로 달려서 확인 — 달리는 자세, 월드 흐름, 카메라 추적, 동료 동행, 여정 진행 막대가 차는 것까지 | 완료 |
| 396 | 그 한계 기록을 백로그·README 양쪽에서 고침. 남은 것은 **연속적인 감각**이다(가속·관성·타격감) — 시간이 스크린샷을 찍는 동안에만 흐르므로. 속도계가 0으로 보이는 것도 함께 적었다(rAF로 갱신하는 값이라, 움직였는지는 지도 요약이나 성능 패널의 보스 거리로 봐야 한다) | 완료 |
| 397 | 달리는 김에 지도 요약의 거리가 안 변하길래 결함인가 했는데, **보스가 추격 중이라 간격이 유지된 것**이었다. 성능 패널(13.8m)과 지도 요약(14m)이 같은 값이라 오히려 요약이 신선하다는 증거였다 — 「안 변한다」를 「안 갱신된다」로 읽을 뻔했다 | 확인 |
| 398 | 새 능력으로 **드래그 시점 회전**을 확인 — 「자동화 드래그가 중간 `pointermove`를 안 만들어 증명된 수정이 아니다」고 적어 둔 것이다. 캔버스에 `PointerEvent`를 직접 보내니 돈다(김밥천국 간판이 중앙에서 왼쪽으로 옮겨 갔다). **증명됨** | 완료 |
| 399 | 보드(`B`)도 확인 — 여정이 「스케이트보드 타기」에서 「동네 반대편까지 가기」로 넘어갔다. 이로써 2분 목록 다섯 중 셋(달리기·시점·보드)이 자동화로 확인됐다 | 완료 |
| 400 | **보스 전투는 못 했다.** `?see=boss`에서 `J`를 눌러 봤지만 대장이 13.9m에서 같은 속도로 따라와 사거리(약 2m) 안으로 들어가지 못했다. 스크린샷을 찍는 동안에만 시간이 흐르니 거리를 좁힐 수가 없다 — **가장 큰 미검증 구간**으로 README에 명시 | 기록 |
| 401 | 가만히 서 있으면 보스가 다가올 줄 알았는데 **13.8m에서 `chase`인 채 멈춰 있었다.** 결함인가 했지만 원인은 `MAX_DELTA_SECONDS`(1/30초) — 숨은 탭에서는 실제로 7초가 흘러도 시뮬레이션은 33ms만 진행하므로 프레임당 7.3cm씩만 좁혀진다. **상한은 맞는 코드다**(큰 dt에서 벽을 뚫는 것을 막는다) | 확인 |
| 402 | 그래서 2분 목록 4번의 문구를 다시 씀 — 「이길 수 있는가」가 아니라 **「규칙은 검사가 증명하고, 못 본 것은 화면과 손맛」**으로. `bossSim.test.ts`가 다가옴·예고·충격·빈틈·비틀거림·쓰러짐을 모두 고정하고 있다. 사람에게 물어볼 것은 **링이 제때 보이는지, 때린 느낌이 나는지, 피할 시간이 실제로 있는지**다 | 완료 |
| 403 | **프레임 예산을 쟀다** — `requestAnimationFrame` 카운터를 걸고 재니 스크린샷 한 번에 **5~7프레임**이 돈다. `dt` 상한이 1/30초이므로 스크린샷 한 번 = 시뮬레이션 약 0.2초다. 이 숫자가 있으면 「자동화로 볼 수 있는가」를 계산으로 답할 수 있다 | 완료 |
| 404 | 그 예산으로 보스에 붙어 보기를 시도 — 달리면 프레임당 0.25m씩 좁힐 수 있어 14m면 이론상 10스크린샷이다. 그런데 **이동이 카메라 기준**이라 방향을 맞추려면 드래그로 시점부터 돌려야 하고, 그만큼 더 든다. 여기서 멈췄다 — 얻는 것(링·타격감은 어차피 못 본다)보다 드는 것이 크다 | 기록 |
| 405 | 검사가 **한 번도 언급하지 않은** 소스를 찾으니 `streetProps`(386줄)·`streetExtras`(315줄)가 나왔다 — 가장 큰 미검사 생성기다. 도시를 한 판 만들어 소품 1035개의 tone을 세어 봤다 | 완료 |
| 406 | **`tentOrange`(팔레트 5번)가 죽어 있었다** — 색까지 정해 두었는데 아무도 안 쓴다(1035개 중 5만 없다). 포장마차 천막은 이 팔레트가 아니라 차양 팔레트를 쓴다. 지우고 뒤 항목을 당김 — 숫자를 직접 쓰는 곳이 없어(전부 `FIXTURE_TONE.이름`) 안전하고, 화면으로도 색이 안 밀린 것을 확인 | 완료 |
| 407 | 규칙으로 못 박음 — 「정한 색이 모두 도시에 나온다」와 그 반대 방향 「정하지 않은 색을 쓰지 않는다」. 색을 정해 두고 안 쓰면 다음 사람은 그 색이 어딘가 나온다고 믿는다. 양쪽 다 되돌려 확인 | 완료 |
| 408 | 같은 파일에서 **주석과 산술이 어긋난 것**도 찾았다 — 「나머지는 비워 둔다」고 적혀 있는데 가중치 합이 정확히 1이라 **비는 자리가 없다.** 주석을 사실대로 고치고, 합이 1을 넘지 않는지(넘으면 뒤쪽 소품이 영영 안 나온다)와 **설명이 산술과 같은 편인지**를 검사로 묶음. 빈 자리를 둘지는 화면을 보고 정할 일이라 값은 건드리지 않았다 | 완료 |
| 409 | 소품이 건물을 파고드는지 재 보고 **「포장마차 48개 중 9개가 묻혔다」고 결론 낼 뻔했다.** 회전을 빠뜨린 계산이었다 — 소품은 `rotationY`로 도로를 향해 돌아 있어서, 2.6×1.1 상판이 90° 돌면 월드 축에서는 1.1×2.6이다. 반영하니 1m 넘게 파고든 것이 **0개**(최대 0.54m, 벽에 붙은 정상 범위) | 정정 |
| 410 | 그래도 검사는 넣었다 — 미검사 생성기이고 「적 42% 건물 안」 전례가 있는 부류다. 「붙었다」와 「묻혔다」를 가르는 선은 1m(지금 가장 깊은 것이 0.54m). **회전을 실제로 쓰고 있는지도 함께 본다** — 전부 0이면 축 정렬로 재는 셈이라 거짓 경보가 난다. 자리를 3m 밀거나 회전을 잃게 해 두 방향으로 확인 | 완료 |
| 411 | 이번 세션에서 **측정이 먼저 틀린 것이 다섯 번째**다(보스 예고 정규식, 「헛돎」 변이 셋, 이번 회전). 규칙이 굳어졌다 — **수치가 이상하면 코드보다 자를 먼저 의심한다** | 기록 |
| 412 | `useFoundClues`(63줄)는 검사가 **한 번도 언급하지 않은** 파일인데, 안에 진행 저장과 직결된 규칙이 있었다 — **이어받은 흔적은 다시 세지 않는다**(어기면 새로고침마다 「찾았다」가 다시 찍혀 수가 부풀어 오른다). `setInterval` 안에 묻혀 검사할 수가 없어 순수 함수 `reportNewClues`로 떼어 냄. 로직은 원래 맞았다 | 완료 |
| 413 | 그 규칙을 네 가지로 고정 — 새로 찾은 것만 알림, 이어받은 것은 안 알림, 여러 번 불러도 두 번 안 알림, 알린 수가 **누적**인지(「2 / 3」에 쓰는 값이다). 「매번 처음부터 센다」와 「이번 회차 수를 말한다」로 되돌려 확인 | 완료 |
| 414 | `resolveResume`도 호출이 둘뿐이라 분기가 거의 안 덮여 있었다 — **이어하는 사람이 동료와 보스 기록을 유지하는지**를 정하는 자리이고, 파일 주석이 「실제로 그렇게 두 번 틀렸다」고 적어 둔 곳이다. 여덟 갈래를 표로 채움(저장 없음·첫 여정 진행 중·완료·다음 여정으로 넘어감·여정 id 없는 예전 저장 / 보스 표시·보스 여정 완료·진행 중) | 완료 |
| 415 | 검사 커버리지를 **내보낸 이름 기준**으로 다시 셌다(파일명 기준은 부정확했다 — `discoveryEffect`는 전용 검사가 있는데 「1건」으로 잡혔다). 그러니 `voices.ts`가 **6개 중 0개**로 가장 큰 미검사 모듈이었다 | 완료 |
| 416 | `voices.ts`는 전부 `AudioContext`가 필요한 팩토리라 단위 검사가 어렵다. 대신 **주석이 단언하는 관계**를 고정했다 — 「달리기는 중심 대역이 낮고, 저역 충격이 붙으며, 여운이 길다. 이 세 가지를 같이 바꿔야 *같은 소리를 크게 튼 것*으로 들리지 않는다」. 달리기를 걷기의 볼륨만 키운 값으로 되돌리니 잡힌다 | 완료 |
| 417 | 착지도 같은 모양 — 「크기뿐 아니라 **저역의 길이도** 함께 늘려야 *높은 데서 떨어졌다*가 전달된다」가 `AudioContext` 안에 묻혀 있었다. `landingThumpSeconds`로 떼어 내 단조 증가를 고정 | 완료 |
| 418 | 그 검사에 `NaN`까지 요구했다가 **내가 계약 밖을 요구한 것**임을 확인하고 뺐다 — `impact`는 체공 시간에서 오고 부르는 쪽이 이미 0~1로 자른다. 닿는다면 이 함수가 아니라 위쪽이 고장 난 것이고, 조용히 0으로 바꾸면 그 고장을 덮는다 | 정정 |
| 419 | `cityContent.ts`(내보낸 이름 7개 중 2개만 검사에 나옴)를 훑음 — 여기 값은 **화면에 글자로 그대로 나가는데** 잘못돼도 예외가 안 난다. 주석이 단언하는 「세로 간판은 4자를 넘으면 글자가 급격히 작아진다」와 배색 번호 범위를 고정(쓰는 쪽이 `% 길이`라 넘치면 예외 대신 **엉뚱한 배색**이 나온다) | 완료 |
| 420 | **간판·현수막 아틀라스가 셋 다 정확히 꽉 차 있었다** — 가로 16/16, 세로 16/16, 현수막 8/8. 칸 고르는 식이 `index % columns`라 **하나만 더해도 캔버스 밖에 그려진다**(예외도 경고도 없이 그 문구만 빈칸). 「내용 수 ≤ 칸 수」를 검사로 넣고 양쪽(문구를 늘리기·격자를 줄이기)으로 확인 | 완료 |
| 421 | 같이 확인된 것(정상): `PROP_CELL_INDEX`의 개수·범위·4×2 일치는 이미 `constantPairs`가 보고 있었고, `AWNING_STRIPE_METERS`는 `textures.ts`가 **다시 export**해 쓰므로 값이 갈라질 수 없다 | 확인 |
| 422 | 「정확히 꽉 참」을 다른 고정 크기 버퍼에서도 찾음 — **`MAX_BLIPS`(24)와 최고 품질 적 수(24)가 같다.** `collectBlips`가 `if (count >= MAX_BLIPS) break;`로 끊으므로, 적을 늘리는 순간 넘치는 만큼이 지도에서 조용히 사라진다. 성능을 만지는 사람이 적 수를 올리는 것은 아주 자연스러운 일이라 더 위험하다 | 완료 |
| 423 | 「가장 많은 적 ≤ 표식 자리」와 「버퍼 = 자리 × 2」를 검사로 넣고 양쪽으로 확인(적을 32로 올리기·버퍼를 절반으로 줄이기) | 완료 |
| 424 | 같이 본 것(정상): 파티클 풀은 96개에 타격당 14개라 동시 6.9회분이지만 **링 버퍼라 넘쳐도 오래된 것을 재활용**한다 — 주석이 「몇 번은 견딘다」고 정확히 적어 두었다. 적 스폰도 실제 벽 판정으로 8·16·24를 정확히 채우고 벽 안에 하나도 없다 | 확인 |
| 425 | 화면 문구의 말투를 훑음 — 해요체 5 대 합니다체 2인데, 합니다체 둘은 지시가 아니라 서술이라 자연스럽다. **고칠 것 없음** | 확인 |
| 426 | 그 과정에서 「진행 상황은 저장되지 않습니다」를 발견하고 **살아남은 거짓말인가 했는데 주석이었다** — 과거 수정을 설명하는 기록이다. 내가 주석을 안 걷어낸 탓이다(이번 세션 여섯 번째 오측정) | 정정 |
| 427 | 그 함정이 **검사에도 있는지** 훑었다. 문서를 읽는 셋은 무관하고, `metadata.test.ts`만 소스에서 화면 문구를 찾으면서 주석을 안 걷어냈다 — 문구를 지우고 「예전에 이런 안내가 있었다」는 주석만 남겨도 통과한다. `readCode`로 통일하고 되돌려 확인 | 완료 |
| 428 | 위치가 필요 없는 단발 키 셋을 화면에서 확인 — **반딧불(E)**: 동료가 밝아지며 바닥에 빛웅덩이가 생긴다. **춤(R)**: 자세가 바뀌고 초롱이 「누가 보면 어때.」로 반응한다. **동료 해제(C)**: 동료가 작아지며 사라진다(즉시가 아니라 줄어드는 연출까지). 셋 다 **처음으로 동작이 확인**됐다 | 완료 |
| 429 | 남는 질문은 여전히 **느낌**이다 — 빛이 충분히 밝은지, 춤이 BPM 96과 맞아 보이는지, 해제 0.35초가 적당한지. 「도는가」와 「좋은가」를 계속 갈라 적는다 | 기록 |
| 430 | README에 항목 하나를 더하자 **기존 규칙이 나를 잡았다** — 「적어 둔 전체 개수가 실제와 맞는다」(열여덟 개라고 써 두고 열아홉이 됐다). 사람에게 「15분 걸린다」고 말하는 문서라 개수가 틀리면 시간표도 거짓이 된다. 이런 검사가 실제로 값을 하는 순간이다 | 확인 |
| 431 | **그래플(G)이 동작한다** — 눌렀더니 플레이어가 공중으로 끌려 올라갔다(카메라가 위에서 내려다본다). 활강은 여전히 확인 못 함: 1.5초가 필요한데 스크린샷 한 번이 0.2초다 | 완료 |
| 432 | 그 과정에서 **내가 넣은 코드의 실제 버그를 발견했다.** 개발 오버레이의 「1 Issue」를 보고 콘솔을 읽으니 `target?.closest is not a function` — `?.`는 null만 막는다. `window`에 직접 보낸 키 이벤트의 target은 `Window`이고 거기엔 `closest`가 없다. **전역 키 처리기에서 예외가 나면 그 키 입력이 통째로 사라진다** | 완료 |
| 433 | 「있는지 물어보고 쓴다」로 고침(`typeof closest !== \"function\"`). `instanceof Element`를 안 쓴 이유도 남겼다 — DOM 없이 검사할 수 있어야 하고, 실제로 필요한 것은 「Element인가」가 아니라 「`closest`를 부를 수 있는가」다. 브라우저에서 다시 눌러 예외가 사라진 것을 확인 | 완료 |
| 434 | 그 검사를 쓰면서 **바로 앞 반복에 고친 함정에 그대로 다시 빠졌다** — 주석 안의 `target?.closest(...)`를 코드로 잡았다. `readCode`로 걷어냄. 같은 실수를 이틀 연속 한 셈이라, 소스를 문자열로 뒤질 때는 `readCode`를 기본으로 쓴다 | 정정 |
| 435 | 「버튼을 양보 대상에서 뺀다」 변이가 **헛돌았다** — 이번엔 변이가 아니라 검사가 문제였다. 부분 문자열로 보니 `[role='button']` 안의 「button」에 걸려 통과한 것이다(이번 세션에서 세 번째로 만난 부분 문자열 함정). 쉼표로 나눠 정확히 비교하도록 고치니 잡힌다 | 완료 |
| 436 | 남은 키를 전부 눌러 보며 콘솔을 지켜봤다(T·F·J·B·R·E·C·G·Space·F3·Z·X·P) — **예외도 경고도 없다.** 앞 반복의 `closest` 사고를 찾은 방법이라 한 번 더 훑었다 | 확인 |
| 437 | **동료 바꾸기를 처음으로 확인했다** — 초롱 → 그을음 → 물비늘로 돌고, 바뀔 때마다 **셋이 함께 따라온다**: 버튼 이름, 말풍선 화자(물비늘이 「저쪽에서 쿵쿵 소리가 나」), 그리고 조작 안내의 능력 이름이 「E 반딧불」에서 「E 물무늬」로. 「동료 이름을 박아 두지 않았다」가 실제로 지켜지고 있다 | 완료 |
| 438 | `?see=party`가 이제 **자기 질문에 완전히 답한다** — 넷이 고리까지 안 겹치고 갈라져 서고, 색이 서로 구분되고, 선두를 바꿀 수 있다. 반복 300에서 「자동화로는 답할 수 없는 지점」이라고 적었던 곳이다 | 완료 |
| 439 | **백로그의 유일한 미완 항목을 끝냈다** — 섹션 1-1의 「`ability` 발동 — 주변 흔적 강조」. 도감이 초롱을 「주변에 숨은 흔적을 잠깐 빛나게 한다」고 소개하는데 **흔적은 월드에 그려지지도 않았다**(지도 마름모와 `T` 판정만). 도감이 없는 기능을 팔고 있었다 | 완료 |
| 440 | 도깨비마다 목록을 두지 않고 **드러나는 범위 = 그 도깨비의 빛이 닿는 거리**로 했다. 그러면 넷의 소개가 저절로 참이 된다 — 초롱(범위 ×2.2)이 가장 잘 드러내고, 자정(×2.6)은 「골목 구석까지 한꺼번에」가 문자 그대로 맞고, 그을음(×0.7)은 몸을 감추는 능력이라 오히려 덜 드러난다 | 완료 |
| 441 | 처음엔 **아무것도 안 나왔다.** 원인은 프레임 순서 — 표식을 동료보다 **앞에** 그려서, `PlayerRig`가 막 0으로 되돌린 빛 반경을 읽고 있었다. 이 프로젝트가 이미 적어 둔 「되돌리기 → 합치기 → 읽기」와 같은 함정이다. 동료 뒤로 옮기니 바닥에 떠오른다(화면 확인) | 완료 |
| 442 | 린트가 `useMemo`가 돌려준 재질을 못 바꾸게 막았다. 재질을 JSX로 선언하도록 바꿈 — R3F가 해제까지 맡으므로 오히려 더 낫다. 지오메트리만 직접 만들고 해제한다 | 완료 |
| 443 | 새 컴포넌트가 기존 규칙에 실제로 걸리는지 확인 — 자원 해제 훑기와 프레임 상한 훑기 **둘 다 `ClueGlow`를 포함**한다. 규칙을 조건으로 세워 둔 값이 여기서 나온다(목록이었다면 새 파일은 빠졌다) | 확인 |
| 444 | 이 기능만의 약속을 고정 — **지도 범례와 같은 색**(다른 색이면 지도 보고 온 사람이 같은 것인 줄 모른다), 저감 모션에서 안 숨쉬기, 매 프레임 지오메트리 안 만들기, 바닥에서 띄우기(z-파이팅). 색과 저감 모션은 되돌려 확인 | 완료 |
| 445 | README에 스무 번째 항목을 더하자 개수 검사가 「개수를 못 읽었다」로 실패했다 — **문서가 맞는 말을 썼는데 검사가 못 읽은 것**이다. 스물은 단위 앞에서 「스무 개」가 되는데 낱말 목록에 그 꼴이 없었다. `스무`와 스물몇까지 더함 | 완료 |
| 446 | 백로그 섹션이 전부 완료돼 **트레일러 분석**(프로젝트의 성적표)을 훑었다. 6.1·6.2는 자전거 하나 빼고 전부 구현이고 그것도 「보드가 같은 역할」이라 의도적 보류, 6.3은 명시적 제외 — **기능 목록은 실제로 끝났다** | 확인 |
| 447 | 대신 **낡은 숫자를 찾았다** — 「키 **7종** 전부에 터치 버튼」인데 정본 `CONTROL_CODES`는 **13개**다. 이 문서는 다음 사람이 「어디까지 했나」를 읽는 곳이라, 절반짜리 숫자가 적혀 있으면 판단이 틀어진다. 13으로 고침 | 완료 |
| 448 | 같은 문서의 다른 숫자도 전수로 확인 — 포즈 4·시간대 4·mood 4·감정 표현 3은 **전부 정본과 맞았다.** 넷 다 검사로 묶음(「문서가 말하는 것 → 정본」 짝을 표로 두어, 새 숫자를 적으면 한 줄만 더하면 되게) | 완료 |
| 449 | 디자인 가이드의 숫자도 훑음 — 대부분은 낡을 수 없는 목표치지만, 「터치 영역 최소 44×44」는 검증 가능하다. 검사가 `HudButton` **하나만** 보고 있었다 | 완료 |
| 450 | **도감의 「데리고 다니기」가 `text-xs underline`뿐이었다** — 높이가 글자 높이(약 16px)라 기준의 3분의 1이다. 손가락으로는 옆 카드가 눌린다. 글자는 작게 두고 누르는 자리만 44px로 잡음(화면에서 44px 확인) | 완료 |
| 451 | 규칙으로 올림 — **버튼을 그리는 모든 파일**이 최소 크기를 잡는지. 토큰이든 리터럴이든 인정한다: `global-error`는 앱 스타일시트 없이 뜰 수 있어 일부러 `44px`을 직접 적는다(그래서 처음엔 위반으로 잘못 잡았다). 가이드의 값과 토큰이 같은지도 함께 본다 | 완료 |
| 452 | **디자인 가이드의 입력 표가 낡아 있었다** — 「상호작용 **E**」인데 정본에서 `E`는 동료 능력이고 살펴보기는 `T`다. 「메뉴 Esc」도 메뉴가 아니라 패널 닫기다. 다음 사람이 입력 구조를 읽는 문서라 여기가 틀리면 엉뚱한 키로 설계한다. 표를 실제와 맞추고 **「이 표는 정본이 아니다」**를 명시 | 완료 |
| 453 | 표의 한 글자 키가 `CONTROL_CODES`에 있는지 검사로 묶음. 「어떤 행동에 붙었는지」까지는 안 맞춘다 — 그러려면 표를 정본으로 만들어야 하는데 그건 이 문서의 역할이 아니다. 없는 키(`Q`)를 넣어 잡히는 것을 확인 | 완료 |
| 454 | 「HUD는 모서리가 아니라 safe area 토큰 기준」도 훑음 — `WorldHud`의 좌표 12개가 **전부** 토큰을 쓴다. 모서리에 붙는 넷은 삼분할 안내선과 랜딩 스카이라인으로 **의도된 full-bleed**다. 위반이 없고, 구분하려면 예외 목록이 필요해 검사는 넣지 않았다 | 확인 |
| 455 | 그 훑기에서 **또 오측정했다**(여덟 번째) — `left-1/2`(가운데 정렬)가 `left-1`로 잘려 17건이 걸렸다. 정규식을 고치니 6건이고 그마저 전부 정상이었다. **숫자가 많이 나오면 자를 먼저 본다** | 정정 |
| 456 | 세 번째 문서(`PROJECT_PLAN` 18절)도 훑음 — 스스로 「이 절만이 코드의 실제 상태를 기술한다」고 밝히는 절이다. **「첫 산책 5단계」인데 실제는 6단계**였다(흔적 조사가 늘어난 뒤로 문서만 그대로). 6으로 고치고 여정 개수·단계 수를 검사로 묶음 | 완료 |
| 457 | 같이 대조한 것(정상): 격자 6×6, 구역 성격 3종, 속도 3.2/7.4/15.5, 외형 4종, 자판기 8초 1.25배 — **전부 맞았다.** 속도 셋은 조작감을 좌우하는 값이라 문서에 남아 있는지 함께 검사로 묶음 | 완료 |
| 458 | 절 번호가 **18 → 17 순으로 뒤집혀** 있었다(18절을 나중에 끼워 넣으면서). 번호를 바꾸면 다른 문서와 검사의 「18절」 참조가 깨지므로 **위치만 맞바꿨다** | 완료 |
| 459 | 문서 셋을 다 훑은 결과: 트레일러 분석 「키 7종 → 13종」, 디자인 가이드 「상호작용 E → T」, 기획서 「5단계 → 6단계」. **셋 다 기능이 늘어난 뒤 문서만 남은 것**이고, 셋 다 다음 사람이 판단 근거로 읽는 자리였다 | 기록 |
| 460 | 새로 넣은 흔적 표식을 18절에 반영 — 그 절이 「이 절만이 코드의 실제 상태를 기술한다」고 선언하므로, **내가 기능을 넣고 문서를 안 고치면 그 선언부터 거짓이 된다.** 도깨비 행에 능력의 실제 효과를 적음 | 완료 |
| 461 | 새 컴포넌트 자리가 관례에 맞는지 확인 — 이 코드베이스는 씬 컴포넌트를 **도메인 폴더**에 둔다(`Shrine`은 dokebi, `Enemies`는 combat, `Crowd`는 world). `ClueGlow`가 quest에 있는 것이 일관되고, `scene/`은 조립·카메라만 갖는다. README 구조 설명만 맞춤 | 완료 |
| 462 | 「문서가 새 기능을 빠뜨리지 않는가」는 **기계로 강제할 수 없다**고 판단했다. 숫자는 정본과 묶을 수 있지만 「빠진 서술」은 잡을 방법이 없다 — 약한 검사를 만들어 안심하는 것보다 못 한다고 적어 두는 편이 낫다 | 기록 |
| 463 | 흔적 검사가 「막히지 않았다」까지만 보고 **걸어서 닿는지는 아무도 안 봤다.** 사방이 건물인 안뜰이면 막히지 않아도 못 들어가고, 그러면 첫 여정이 영영 끝나지 않는다 — 퀘스트 목적지에서 이미 같은 검사를 하는데 흔적 단계가 나중에 붙어 그 검사를 못 받았다 | 완료 |
| 464 | **완주 시뮬레이션도 도움이 안 됐다** — `cluesFound: 99`로 이 단계를 통째로 건너뛴다. 계산이 아니라 **위치** 문제라 시뮬레이션으로는 못 본다. 「검사가 있다」와 「그 검사가 이것을 본다」는 다르다 | 기록 |
| 465 | 재 보니 흔적 셋 다 닿았다(결함 없음). 그래도 목적지와 같은 방식으로 고정 — 걸어서 닿는지 + 조사 반경 안에 설 자리가 있는지. 흔적을 구역 안쪽(건물 자리)으로 옮겨 잡히는 것을 확인 | 완료 |
| 466 | 같은 구멍을 다른 「나중에 붙은」 자리에서도 찾아봄 — 도깨비 자리 셋과 자판기 80대 **전부 닿았다.** 도깨비 자리는 검사로 묶고, 자판기는 80번의 격자 탐색이 느려 한 번 확인한 것으로 두었다(그 판단도 적어 둔다) | 완료 |
| 467 | 검사가 **앞 N개만 보는** 자리를 훑음 — `.slice(0, N)` 대부분은 실패 메시지 자르기라 무해했지만, 둘은 진짜 커버리지 제한이었다: 가로등 **20 / 384**, 기둥 **40 / 384**. 열에 하나만 보고 있었다 | 완료 |
| 468 | 상한을 풀자 **결함이 나왔다** — (51.9, 52.0)과 (52.0, 51.9)에 기둥 둘이 0.14m 거리로 겹쳐 서 있다. 가로 도로용과 세로 도로용을 따로 도는데 **교차로 모서리에서 두 줄이 같은 지점을 가리킨다.** 화면에서는 한 개로 보이지만 그 자리만 유난히 두껍고 충돌체도 둘이다 | 완료 |
| 469 | 자리를 기억해 두 번 세우지 않도록 고침(384 → 383). 나무에서 「교차로에서 같은 자리에 두 번 심길 수 있다」를 이미 겪어 검사를 넣어 뒀는데, 가로등에는 같은 검사가 없었다 — **같은 함정을 두 번 다른 곳에서 만난 셈** | 완료 |
| 470 | 처음에 검사 로직을 **재구현해서 재다가 384개 전부 실패**로 나왔다. 자가 다른 것이었다(충돌체 비교 방식). 재구현 대신 **실제 검사의 상한만 풀어** 돌리니 정확히 2개가 나왔다 — 아홉 번째 오측정이고, 교훈은 같다: 자를 새로 만들지 말고 있는 자를 쓴다 | 정정 |
| 471 | 같은 부류를 더 훑음 — 「스스로 사라지는 알림은 live region이다」가 `Notices.tsx` **한 파일만** 보고 있었다. 그 뒤로 알림이 쪼개져 `CaptureNotice`·`ShrineNotice`·`DowngradeNotice`가 따로 나갔고 셋 다 이 검사를 못 받았다(지금은 우연히 짝이 맞다) | 완료 |
| 472 | `role="status"`를 쓰는 곳을 **전부**(여덟 파일) 찾아 `aria-live`와 짝이 맞는지 보도록 바꿈. 예전 검사가 못 보던 둘에서 `aria-live`를 빼 보니 이제 잡힌다 | 완료 |
| 473 | 도중에 **열 번째 오측정** — `Notices.tsx`가 status 4 / aria-live 5로 보여 「검사가 왜 통과하지?」 했는데, 내 grep이 `aria-live=`가 아니라 `aria-live`로 찾아 주석 안의 `` `aria-live` ``를 센 것이었다. 검사의 정규식이 맞았다. **자를 쓸 때는 검사가 쓰는 것과 같은 패턴으로** | 정정 |
| 474 | 번들 크기 검사가 **상한만** 보고 있었다(150KB·400KB). 그래서 실측이 127→128, 274→283으로 커지는 동안 문서의 숫자는 그대로였다 — 상한 안이라 아무도 몰랐다. 이 숫자는 「외부 에셋을 안 쓴다」는 이 프로젝트의 핵심 주장을 뒷받침하는 근거라, 틀리면 그 주장까지 의심받는다 | 완료 |
| 475 | 실측으로 갱신하고 **문서와 실측이 10% 넘게 벌어지면 잡히도록** 묶음. 정확히 맞추라고 하지 않은 이유도 적었다 — 청크 이름이 바뀔 때마다 문서를 고치게 되면 검사가 미움받는다 | 완료 |
| 476 | 가장 큰 청크를 재는 코드가 검사 안에 묻혀 있어 함수로 뺐다. **상한 검사와 문서 대조가 같은 자를 쓰게** 하려는 것이다 — 이번 세션에서 자를 새로 만들었다가 세 번 헛짚었다 | 완료 |
| 477 | 되돌려 보니 **한 번 헛돌았다** — 두 문서를 이어 붙여 한 번만 찾았더니 앞 문서(README)에서 맞아 버려 뒤 문서(기획서)가 틀려도 통과했다. 각각 보도록 고치니 잡힌다 | 정정 |
| 478 | **번들 검사가 조용히 안 돌고 있었다.** `pnpm verify`가 `test`를 `build`보다 먼저 돌리는데, 그 검사들은 `.next`를 읽는다(`skipIf`) — 새로 받은 저장소에는 통째로 건너뛰고, 있더라도 **직전 빌드**를 잰다. 무거운 의존성을 더해도 그 반복에서는 안 걸린다 | 완료 |
| 479 | 재 보니 빌드 2초·테스트 3초·타입 1초라, 빌드를 테스트 앞으로 옮겨도 「빠른 것부터」가 거의 지켜진다. 순서를 바꾸고 README의 근거 문장도 다시 씀(왜 바꿨는지까지) | 완료 |
| 480 | 그러자 **기존 검사가 막았다** — 「README가 적은 verify 순서가 실제와 같다」가 순서를 `[\"typecheck\", \"lint\", \"test\", \"build\"]`로 **손으로 적어** 두고 있었다. 문서를 같이 고쳤는데도 막힌다는 것은 검사가 정본이 되어 버렸다는 뜻이다. 스크립트에서 순서를 읽어 문서와 대조하도록 바꿈 — 어느 쪽으로 바꾸든 둘이 같기만 하면 된다 | 완료 |
| 481 | 「안 도는 검사」를 더 찾다가 **검사를 끄는 주석**을 훑음 — 테스트 파일은 83개가 모두 실행되고 있었다(이름이 어긋나 빠진 것 없음). 억제는 아홉 곳인데 여덟은 `react-hooks/immutability` 파일째 끄기로, 절대 규칙 4(공유 가변 객체를 `useFrame`에서 직접 쓴다)와 정면으로 부딪히는 구조적 선택이다 | 확인 |
| 482 | 나머지 하나는 **이번에 내가 넣은** `exhaustive-deps`였다. `useMemo`에 제자리 배열을 넣으려다 길이로 우회하면서 생긴 것 — 그냥 매 렌더 계산하도록 단순하게 고치니 필요가 없어졌다. **억제가 늘어난다는 것은 대개 설계가 어긋났다는 신호다** | 완료 |
| 483 | 끌 수 있는 규칙 이름을 허용 목록으로 좁힘. 새 이름을 더하는 것이 **의도적인 행동**이 되게 한다 — 지금은 `react-hooks/immutability` 하나뿐이다 | 완료 |
| 484 | 그 검사를 만들면서 `readCode`로 읽었다가 **0곳이 나왔다** — 주석을 걷어내는 함수인데 억제 주석이 바로 그 주석이다. 함께 넣은 「실제로 훑었다」 가드가 즉시 잡았다. 이번 세션에 여러 번 만든 그 가드가 **내 실수를 잡은 첫 사례**다 | 정정 |
| 485 | **저감 모션을 처음으로 켜 봤다** — 여러 컴포넌트가 참조하는 설정인데 한 번도 켠 적이 없었다. 월드가 정상 렌더되고(깨지거나 빈 화면이 되지 않는다), 실제로 요소를 끈다(장식 2개 → 1개, 속도선이 빠진다) | 완료 |
| 486 | 그 설정이 **저장되고, 시작 화면과 같은 값을 본다**는 것도 확인 — 게임 안 버튼으로 켜면 `reducedMotion: true`가 저장되고 랜딩의 체크박스에도 반영된다. 두 화면이 서로 다른 값을 보는 종류의 결함이 없다 | 확인 |
| 487 | 확인 뒤 설정을 원래대로 되돌렸다(다음 사람이 저감 모션이 켜진 채로 화면을 보지 않도록) | 기록 |
| 488 | **품질 설정을 처음으로 바꿔 봤다** — 시작 화면에서 「가벼움」을 고르면 저장되고(`quality: \"low\"`), 월드에서 성능 패널이 `품질 low`로 뜬다. 캐릭터 그림자가 사라진 것도 화면에서 확인(가벼움 = 그림자 없음) | 완료 |
| 489 | **실제로 부하가 준다** — 드로우콜 99 → 68, 삼각형 132,066 → 85,800(-35%). 「설정은 있는데 아무것도 안 바뀐다」가 아니었다. 확인 뒤 `auto`로 되돌림 | 완료 |
| 490 | 이로써 사용자 설정 넷을 다 확인했다 — 외형(색거리 검사가 이미 지킴), 소리, 모션 줄이기, 품질. 모두 **저장되고 화면에 반영되고 시작 화면과 게임 안이 같은 값을 본다** | 기록 |
| 491 | 마지막 설정인 **소리**는 자동화로 확인할 수 없다고 확정했다 — 탭이 `document.visibilityState === \"hidden\"`이라 오디오가 아예 시작하지 않는다. `AudioContext` 생성자를 가로채 봤지만 **0개**였다. 이건 결함이 아니라 **올바른 동작**이다(안 보이는 탭에서 소리를 내지 않는다) | 확인 |
| 492 | 코드로 확인한 것: 제스처 배선이 `pointerdown`·`keydown`·`touchstart` 셋을 본다(iOS는 `touchstart`까지 봐야 한다는 주석과 함께), 생성자는 **제스처 뒤에** 해석한다(미리 만들면 브라우저가 suspended로 잠그고 iOS는 그 뒤 resume해도 안 난다) | 확인 |
| 493 | 그래서 소리는 fps와 같은 칸에 남는다 — **원리적으로 사람만 답할 수 있는 것.** README의 「소리로 구분되는가」(320/520/180/90Hz)와 「루프마다 틱 소리가 나는가」가 그 질문이고, 이번 확인으로 **그 둘이 남은 이유가 분명해졌다** | 기록 |
| 494 | **이번 세션에 만든 흔적 원반(ClueGlow)을 처음으로 눈으로 봤다.** `?see=clues`에서 **E(반딧불)** 를 누르니 어두운 아스팔트 위에 따뜻한 노란 타원이 떴다 — 횡단보도 흰 줄·중앙선과 구분된다. 능력이 켜져 있을 때만 뜨므로 도감의 「잠깐 빛나게 한다」가 문자 그대로 맞다. 프레임 순서(동료 뒤에 등록) 수정도 실제 앱에서 함께 확인됐다 | 확인 |
| 495 | **결함:** `?see=clues`로 들어갔는데 **완주 화면이 먼저 떴다.** 저장이 둘째 여정(`boss-hunt`)을 마친 상태였고, 시나리오는 단계 번호만 주므로 `questId`·`questCompleted`를 저장에서 물려받았다 — 첫 여정의 번호를 다른 여정에 꽂아 「단계 없음 = 완주」로 읽혔다 | 수정 |
| 496 | `resolveResume`에 `pinsStep`을 두어 **단계를 집은 시나리오는 여정 진행을 물려받지 않게** 했다(`questCompleted` 무시, `questId`는 첫 여정). 처치 수·대장 기록처럼 여정 밖의 값은 그대로 둔다 — 섞여도 목표가 흐려지지 않는다. 재현 테스트 4개를 먼저 쓰고 고쳤고, 브라우저에서 패널이 사라진 것까지 확인했다 | 수정 |
| 497 | 테스트는 `clues` 하나가 아니라 **단계를 집는 모든 시나리오**를 훑는다. 「목록은 아는 것만 담고 규칙은 모르는 것까지 잡는다」 — 필터가 전부 걸러 내 검사가 조용히 사라지는 것을 막는 검사도 함께 넣었다 | 확인 |
| 498 | **도감 4칸과 동료 넷을 처음으로 눈으로 봤다.** `?see=party`에서 도감이 **4 / 4**로 열리고, 넷(초롱 노랑·그을음 회주황·물비늘 파랑·자정 보라)이 색으로 뚜렷이 구분된다. 뒤로는 동료 넷이 **겹치지 않고 나란히** 따라온다 — 이번 세션의 자리 분리 수정이 실제로 동작한다 | 확인 |
| 499 | **README의 「완주 뒤 도감 열기」를 닫았다.** 완주 화면이 뜬 채 도감을 누르니 완주 화면이 비켜나고 도감이 깨끗하게 열린다. 예전에 「화면이 검게 나와 확인 못 했다」고 적은 것은 결함이 아니라 **장면이 붙기 전에 찍은 것**이었다 | 확인 |
| 500 | **고칠 뻔한 것(일곱째):** 완주 화면은 「만난 도깨비 **3 / 3**」인데 도감은 **4 / 4**라 분모가 틀린 줄 알았다. `roster.ts`의 `FINDABLE_DOKEBI`는 **찾아갈 자리가 있는** 도깨비만 센다 — 초롱은 처음부터 동행이라 `home`이 없어 빠진다. 주석에 「n / 전체로 세는 바람에 다 모아도 3/4에 멈췄다」는 과거 수정 경위까지 적혀 있었다. **고쳤다면 고쳐 둔 것을 되돌릴 뻔했다** | 확인 |
| 501 | **README의 「흔적 세 곳」을 닫았다.** 눈이 필요한 부분(지도에 노란 마름모가 셋)은 `?see=clues`에서 확인했고, 나머지 둘(`T`로 수가 오르는가·새로고침해도 남는가)은 `tests/clues.test.ts`가 이미 덮는다 — 「자리에 서면 찾는다」·「남은 목록이 줄어든다」·「이어받으면 그대로 돌아온다」·「저장이 흔적을 함께 쓴다」. 스크린샷 열두 장으로 걸어가는 대신 **자를 확인하고 끝냈다** | 확인 |
| 502 | 같은 화면에서 **494~496의 수정이 실제로 동작하는 것도 함께 봤다** — 목표가 「골목에 남은 흔적 조사하기 0 / 3」으로 열린다. 고치기 전이라면 완주 화면이 떠 있었다 | 확인 |
| 503 | 지도 요약이 「가장 가까운 흔적은 **남쪽** 16m」라고 알려 준다. 앞서 흔적을 찾는다며 북쪽으로 걸었던 것은 **요약을 안 읽었기 때문**이다 — 화면이 이미 답을 들고 있었다 | 기록 |
| 504 | **README의 「키보드만으로」를 닫았다 — 셋 다 답이 나왔다.** ① Tab이 시작 화면의 **「동네로 들어가기」에 첫 번째로** 닿는다(187×59, 최소 44 훨씬 위). ② 도감을 `Space`로 열고 `Escape`로 닫는다. ③ 포토 모드의 `WASD`·`Z`/`X`는 `tests/cameraRig.test.ts`가 이미 덮는다(「이동 키가 카메라를 돌린다」·「포토 모드에서만」·「거리도 키보드로」·「확대와 축소가 반대」) | 확인 |
| 505 | 덤으로 **포커스 복귀가 양방향으로 맞다**: 지도를 `Escape`로 닫으면 「지도」 버튼으로, 도감을 닫으면 「도감」 버튼으로 돌아온다. 열 때는 포커스가 패널 안으로 들어간다 — 대화상자가 지켜야 할 순서 그대로다 | 확인 |
| 506 | **이번 세션에 고친 `Space` 활성화가 브라우저에서 확인됐다.** 「도감」에 포커스를 두고 `Space`를 누르니 점프가 아니라 도감이 열린다 | 확인 |
| 507 | **자에 속은 여덟째:** 시작 화면에서 `Tab`을 두 번 눌러도 `activeElement`가 `BODY`였다 — 접근성 결함인 줄 알았다. **탐색 직후에는 문서에 포커스가 없어서**였고, 빈 곳을 한 번 클릭하니 바로 잡혔다. 자동화로 `Tab`을 볼 때는 **먼저 페이지에 포커스를 줘야 한다** | 기록 |
| 508 | **README의 「공중에서」를 닫았다.** `?see=air`의 40m 상공에서 주황 후드·남색 바지가 **옅은 하늘색 하늘에도 회색 옥상에도 묻히지 않는다** — 주황과 하늘색은 보색이라 색상 거리가 가장 멀다. 동료(노랑+청록 고리)도 마찬가지다 | 확인 |
| 509 | **495의 수정을 넓혔다.** `?see=air`에서도 완주 화면이 떴다 — 단계를 집지 않는 확인 지점은 여전히 저장을 물려받았기 때문이다. 이제 **확인 지점이 있으면 여정 진행(`questStepIndex`·`questCompleted`·`questId`)은 전부 시나리오가 정한다.** 완주 화면을 보고 싶은 `result`는 스스로 켜므로 물려받을 이유가 없었다 | 수정 |
| 510 | 넓히면서 조건식이 오히려 **줄었다** — `pinsStep` 분기가 사라지고 세 필드가 각각 한 줄이 됐다. 「예외를 늘려 막기」가 아니라 **규칙을 옳게 다시 쓰기**였다는 신호다 | 기록 |
| 511 | 테스트도 「단계를 집는 시나리오」에서 **모든 확인 지점**으로 넓혔고, **지우는 것과 남기는 것의 경계**를 검사로 굳혔다 — 처치 수·대장 기록·첫 여정 기록(`firstQuestDone`)은 물려받아야 한다. 여기까지 지우면 확인 지점에 들어갈 때마다 이미 만난 도깨비가 잠긴다 | 확인 |
| 512 | **「완주 화면」은 닫지 않고 좁혔다.** 세 질문 중 「만난 도깨비 수」는 화면으로 확인했지만(3 / 3), 나머지 둘을 덮는 검사는 `captured.current = true`·`setDismissed(false)` 같은 **소스 문자열 래칫**이다 — 장치가 코드에 있다는 증거이지 실행 결과가 아니다. 「검사가 있다 ≠ 그 검사가 이것을 본다」를 나한테도 적용했다 | 확인 |
| 513 | 그래서 항목을 지우는 대신 **무엇이 어디까지 확인됐는지**로 다시 썼다. 개수는 16 그대로다 — 줄이려고 참인 것보다 세게 말하면 목록이 거짓말을 시작한다 | 기록 |
| 514 | 「자정」 항목도 확인해 보니 **취향(그 대열이 예쁜가)만 남아** 있었다. 사람 몫이 맞다 — 더 줄일 것이 없다 | 확인 |
| 515 | **백로그의 마지막 후보였던 흑백·대비 톤 변환을 만들었다.** 「후처리 패스가 필요하다」는 판단을 뒤집었다 — 화면은 CSS `filter`, 사진은 2D 캔버스의 `ctx.filter`로 **같은 문자열**을 쓰면 된다. 새 의존성도, 렌더 타깃도 없다 | 완료 |
| 516 | 새 UI를 만들지 않았다. 톤을 **기존 색보정 순환에 항목 둘**(「흑백」·「또렷하게」)로 넣으니 버튼·설정 저장·검증이 전부 그대로 쓰인다. 색을 얹는 필터와 톤을 바꾸는 필터가 한 목록에 있어도 서로 간섭하지 않는다 — 톤 항목은 색을 얹지 않고, 색 항목은 톤을 쓰지 않는다(검사로 굳혔다) | 완료 |
| 517 | **화면과 사진이 갈라지는 사고를 구조적으로 막았다.** 고른 id를 사진 쪽에 따로 넘기지 않는다 — `useCapture`가 `activeTone(canvas)`로 **화면에 실제로 걸린 값**을 읽어 넘긴다. 두 곳에 값이 없으니 갈라질 수가 없다 | 완료 |
| 518 | `ctx.filter`를 **넣고 다시 읽어** 지원을 확인한다(속성 유무만 보면 조용히 무시하는 구현에 속는다). 못 쓰면 톤 항목을 목록·순환에서 빼고, 저장에 남은 톤 항목에서도 톤만 뗀다 — **되는 척하고 다르게 저장하느니 없다고 말한다.** 브라우저가 없는 곳(테스트·서버)에서는 거짓이다 | 완료 |
| 519 | 브라우저에서 확인: 「흑백」을 고르면 R3F 캔버스 컨테이너(캔버스의 **조부모**)에 `grayscale(1) contrast(1.12)`가 걸린다. `activeTone`이 3단계까지 올라가므로 정확히 닿는다 — 캔버스만 봤으면 `none`을 읽고 못 걸린다고 오판했을 자리다 | 확인 |
| 520 | **자에 속은 아홉째:** 필터 버튼을 여러 번 누르려고 `await requestAnimationFrame`을 넣었다가 **렌더러가 45초 타임아웃**을 냈다. 숨은 탭에서는 프레임이 안 돈다 — 내가 이미 백로그에 적어 둔 사실이다. 한 번에 한 번씩 누르고 다음 호출에서 읽는 방식으로 바꿨다 | 기록 |
| 521 | 기존 검사 넷이 새 설계와 부딪혔고 **전부 기준을 옳게 옮겨서** 통과시켰다 — 「필터 없음만 투명」 → 「**색을 얹지 않는 것**이 투명」(id로 세면 톤이 늘 때마다 빈 사각형을 그린다), 순환 검사는 **톤 있음/없음 두 갈래**로 나눴다. 검사를 느슨하게 한 것이 아니라 규칙을 다시 쓴 것이다 | 완료 |
| 522 | **직접 만든 기능에서 구멍을 찾았다 — 클립 녹화.** `canvas.captureStream()`은 **캔버스 픽셀만** 가져가므로 조상에 걸린 CSS `filter`가 담기지 않는다. 사진은 막아 두고 클립은 열어 둔 셈이라, 「어떤 저장은 맞고 어떤 저장은 틀린」 더 나쁜 상태였다 | 수정 |
| 523 | 톤이 있을 때만 **거울 캔버스**를 두고 그쪽을 녹화한다. 톤이 없으면 원본에서 바로 뜬다 — 쓰지도 않을 그리기를 매 프레임 할 이유가 없다. 녹화를 멈추면 거울도 멈춘다(`release`에서 함께 놓는다) | 수정 |
| 524 | 거울에서 **매 프레임 `ctx.filter`를 다시 건다.** 캔버스 크기를 바꾸면 2D 상태가 초기화되어 필터가 날아간다 — 한 번만 걸어 두면 **창 크기가 바뀌는 순간 조용히 컬러로 돌아간다.** 검사로 굳혔다 | 수정 |
| 525 | 사진·클립 둘 다 `activeTone(canvas)` 하나에서 값을 받는다. 화면·사진·클립 셋이 **같은 한 곳**에서 오므로 갈라질 자리가 없다 | 완료 |
| 526 | 기존 검사 하나가 `startClipRecording(canvas, 30` 한 줄을 전제한 정규식이라, 인자를 줄바꿈한 것만으로 죽었다. 공백을 견디게 고쳤다 — **서식 하나에 검사가 죽으면 그 검사는 서식을 지키는 것이지 동작을 지키는 것이 아니다** | 수정 |
| 527 | `activeTone`이 `depth < 3`으로 **올라갈 단계를 세고 있었다.** 지금은 맞지만 래퍼가 하나 늘어나는 날 조용히 못 찾고 **사진만 컬러**가 된다 — 숫자로 박아 둔 구조 가정은 언젠가 틀린다 | 수정 |
| 528 | 숫자를 지우고 **CSS가 실제로 하는 일**을 그대로 했다: 조상에 걸린 필터는 모두 겹쳐 캔버스에 보이므로, `body`까지 올라가며 걸린 것을 **전부 모아 안쪽부터 이어 붙인다.** 멈추는 자리가 임의의 수가 아니라 문서 구조다. HUD는 캔버스의 조상이 아니라 **형제**라 걸리지 않는다 | 수정 |
| 529 | 검사 둘로 굳혔다 — 「올라갈 단계를 숫자로 박지 않는다」와 「조상 필터를 모두 겹친다」. 브라우저에서 같은 논리로 읽어 `saturate(1.35) contrast(1.1)`가 그대로 나오는 것까지 확인했다 | 확인 |
| 530 | **또 하나의 「다른 말」을 찾아 막았다.** 톤을 못 쓰는 브라우저에서 톤만 떼고 이름을 남겼더니, 버튼에 **「흑백」이라 적힌 채 화면은 컬러**였다. 사용자는 흑백인 줄 알고 찍고 컬러 사진을 받는다 | 수정 |
| 531 | 못 쓰면 **통째로** 기본값을 돌려준다(이름 포함). 저장된 값 자체는 건드리지 않아 지원하는 브라우저로 돌아오면 살아난다 — 화면에 거짓말하지 않으면서 고른 것도 잃지 않는다 | 수정 |
| 532 | 화면이 이름을 `PHOTO_FILTERS[id].name`으로 **정본에서 바로 꺼내 해석을 건너뛰고 있었다.** 해석을 거치게 고치고, 정본에서 바로 꺼내는 것을 막는 검사를 넣었다 — 해석 함수를 아무리 옳게 만들어도 부르지 않으면 소용이 없다 | 수정 |
| 533 | 「해석을 건너뛴다」가 이 파일만의 문제인지 훑었다. 해석 함수 다섯 중 **넷은 모르는 id에 대한 폴백일 뿐**이라(`timeOfDayPreset`·`dokebiPreset`·`appearancePreset`·`photoPosePreset`) 타입이 보장하는 id로 기록을 바로 꺼내도 결과가 같다 — **결함이 아니다.** 고칠 뻔한 것 여덟째 | 확인 |
| 534 | 그런데 **그 비대칭이 곧 위험이다.** `photoFilterPreset`만 값을 바꾸는데, 나머지 넷을 보고 「그냥 폴백이겠지」 하고 건너뛰기 딱 좋다 — 실제로 버튼 이름이 그렇게 새어 나갔다. 그래서 한 호출부가 아니라 **규칙**으로 막았다: 소유 모듈 밖에서 `PHOTO_FILTERS[id]`를 직접 꺼내면 실패한다 | 완료 |
| 535 | 변이 검사로 무는지 확인했다 — 고친 줄을 원래대로 되돌리니 **잡았다**. 「목록은 아는 것만 담고 규칙은 모르는 것까지 잡는다」를 다음에 생길 호출부에도 적용한 셈이다 | 확인 |
| 536 | **아직 안 밟은 경로를 밟았다 — 저장된 톤이 새로고침 뒤 살아나는가.** `mono`를 저장하고 새로 열어 포토 모드에 들어가니 이름은 「흑백」, 화면에는 `grayscale(1) contrast(1.12)`가 걸린다. 설정 → 해석 → 표시 → 화면이 한 줄로 이어진다 | 확인 |
| 537 | **흑백을 처음으로 눈으로 봤다.** 밤 도시가 느와르 사진처럼 읽히고 대비 보정 덕에 골목이 뭉개지지 않는다. **HUD는 컬러로 남는다** — 필터가 캔버스 컨테이너에만 걸린다는 설계가 화면으로 확인됐다(버튼이 흑백이 되면 읽기 나빠진다) | 확인 |
| 538 | **자에 속은 아홉째 — 없는 결함을 찾을 뻔했다.** 「이름은 또렷하게인데 필터가 안 걸렸다」고 읽고 결함으로 보고할 뻔했다. 실제로는 캔버스를 못 찾아 빈 배열을 「필터 없음」으로 읽은 것이었다. `chain`을 함께 찍어 보니 **배열 자체가 비어 있었다** | 기록 |
| 539 | 그 뒤 캔버스를 세어 보니 300×150 하나뿐 — **장면이 안 붙은 상태**였다. 숨은 탭에서는 스크린샷을 찍어야 프레임이 돈다는, 이 세션에서 이미 세 번 밟은 함정이다. **측정값이 「없음」일 때는 「없다」인지 「못 쟀다」인지부터 가른다** | 기록 |
| 540 | **「저장된 사진이 화면과 같은가」를 파일을 내려받지 않고 확인했다.** 저장 경로와 **같은 합성**을 페이지 안에서 돌려 픽셀을 읽었다 — 원본은 `[160,119,121]`처럼 컬러인데 합성 뒤에는 `[128,128,128]`로 **완전한 회색(R=G=B)** 이다. 설계 논증이 아니라 관측이다 | 확인 |
| 541 | 그래서 README 항목을 **취향 질문만 남기고** 줄였다. 「아무도 열어 보지 않았다」는 이제 사실이 아니다 — 픽셀은 봤다. 남은 것은 밤 골목이 흑백으로 좋은가, 대비를 더 올릴까 같은 사람만 답할 수 있는 것뿐이다 | 확인 |
| 542 | 거울은 **매 프레임 2560×1626을 그리는 루프**다. 녹화 중 화면을 떠나면 언마운트 정리가 유일한 안전줄인데 **그 고리를 잡는 검사가 없었다.** 고리는 전부터 있었지만 그때 걸린 것은 스트림 하나였고, 이제는 잊었을 때의 값이 훨씬 비싸다 | 완료 |
| 543 | 검사 둘로 굳혔다 — 「화면을 떠날 때 녹화기를 멈춘다」와 「멈추는 길이 하나뿐이다」(스트림을 놓는 곳과 거울을 놓는 곳이 같은 함수여야 한다). 변이로 확인했다: `mirror?.stop()`을 지우니 **잡았다** | 확인 |
| 544 | **백로그에 남은 작업이 없다.** 「다음 후보」는 비었고, 사람 몫 17개는 전부 취향·감각·소리처럼 **원리적으로 사람만 답할 수 있는 것**이다. 새 결함을 찾으면 여기에 잇는다 | 기록 |
| 545 | 픽셀 측정을 「민트 후드 + 물비늘 색거리」에도 써 보려다 **접었다.** 좌표를 스크린샷 기준으로 잡았는데 그 사이 동료가 자리로 이동해, 후드도 도깨비도 아닌 배경을 쟀다(`[87,80,95]`·`[191,192,123]` — 청록도 파랑도 아니다). 대상을 안정적으로 찾는 데 드는 품이 답의 값어치를 넘었다 | 기록 |
| 546 | **숫자를 고쳐 쓰지 않았다.** 이 항목이 묻는 것은 「한눈에 갈리는가」라는 **지각**이고, 팔레트 거리 55는 이미 문서에 있다. 화면 색은 조명·카메라에 따라 매번 달라지므로 한 번 잰 값을 답으로 내놓으면 그럴싸한 거짓이 된다 — 이 저장소가 「빈 예보 → 한파 0°C」로 배운 것과 같은 종류다 | 기록 |
| 547 | 눈으로 본 것만 남긴다: `?see=party` 화면에서 민트 후드는 **청록**, 물비늘은 **중간 파랑**으로 보였고 두 덩어리로 뭉치지 않았다. 이건 관측이지 측정이 아니며, 사람의 판단을 대신하지 못한다 | 기록 |
| 548 | 내가 바꾼 이어받기가 `?see=party`를 어떤 상태로 두는지 확인했다. 목표가 「고물 대장을 찾아가기」인데 이건 **보스 여정의 첫 단계**다 — 「첫 산책을 마치고 도깨비를 다 만났으니 이제 보스 차례」라는 앞뒤 맞는 상태다(시나리오가 `questCompleted`만 켜고 여정 id를 말하지 않으면 씬이 체인을 한 칸 넘긴다) | 확인 |
| 549 | **부수 효과로 이 확인 지점이 나아졌다.** 전에는 완주 화면이 떠서 **정작 보여 주려던 동료 넷을 가리고** 있었다. 이제 바로 대열이 보인다 — 「자리 배치와 색 구분」이라는 이 지점의 목적과 화면이 처음으로 일치한다 | 확인 |
| 550 | 이어받기 변경이 **여정 필드를 안 건드리는 확인 지점**(`boss`·`night`·`noon`)도 첫 여정 0단계로 열게 만든다. `?see=boss`로 확인: 「고물 대장」 이름표와 가득 찬 체력 바가 뜨고 보스가 그 자리에 서 있다 — **목표 단계와 무관하게 보스는 월드에 있으므로 이 지점의 목적(예고 링·크기감)은 그대로다** | 확인 |
| 551 | 저장에 `bossDefeated`가 있어도 보스는 체력이 가득 찬 채 서 있다. 앞뒤가 맞다 — 완주 문구가 「고물 대장은 잠시 뒤 다시 일어나니 언제든 다시 겨뤄 보세요」라고 말한다. 눕힌 기록(`bossDefeated`)은 **자정 해금**에 쓰이지 보스를 지우는 데 쓰이지 않는다 | 확인 |
| 552 | **`?see=shrine`에 빛기둥이 없었다.** 이 지점의 이름이 「빛기둥이 골목에서 보이는지」인데 볼 것이 없었다 — 앞서 `?see=party`로 들어가 저장에 도깨비 넷이 「만났다」로 남았고, **이미 만난 도깨비는 자리를 치운다** | 수정 |
| 553 | 여정 진행에서 고친 것과 **같은 유형**이다: 확인 지점이 저장에 덮인다. 원인도 같은 모양이었다 — `scenario?.metDokebi ?? loadSettings().metDokebi`. 「말하지 않음」을 「저장을 쓰라」로 읽고 있었다 | 수정 |
| 554 | 화면 안 조건식을 순수 함수 `resolveMetDokebi`로 빼서 고쳤다 — `resumeProgress.ts` 머리말이 이미 「화면 안에 조건식으로만 두었을 때는 확인 지점으로 들어가 봐야 알 수 있었다」고 적어 둔 그대로다. 시나리오가 말하지 않으면 **아무도 안 만난 상태**로 연다 | 수정 |
| 555 | 게임 자신의 지도 요약으로 확인했다 — 「찾아갈 도깨비 자리 **3곳**, 가장 가까운 그을음은 남쪽 22m」. 고치기 전이라면 **0곳**이었다 | 확인 |
| 556 | ~~자리가 뒤쪽이라 안 보인다~~ → **틀렸다. 정면이고, 보인다.** 반복 603에서 확대해 보니 골목 끝에 옅은 노란 기둥이 건물 사이로 솟아 있다 | 확인 |
| 557 | 같은 모양(`scenario?.X ?? 저장의 X`)을 **패턴으로** 훑어 넷째를 찾았다 — `timeOfDay`. **그런데 이건 고치면 안 된다.** 앞의 셋(`questCompleted`·`questId`·`metDokebi`)은 **진행 상태**라 시나리오가 만들어 줘야 하지만, 시간대는 **취향**이다. 밤을 켜 둔 사람이 확인 지점마다 노을로 돌아가면 도와주는 게 아니다 | 확인 |
| 558 | 물려받기가 괜찮은 이유는 **「중요하면 말한다」가 지켜지기 때문**이다 — 시간대가 중요한 지점(`night`·`noon`)은 스스로 시간대를 정한다. 그 전제까지 검사로 묶었다 | 완료 |
| 559 | **구분을 짝으로 적었다** — 「시간대는 물려받는다」와 「진행 상태는 물려받지 않는다」를 같은 자리에 둔다. 하나만 두면 다음 사람이 한쪽으로 읽는다. 앞선 수정 셋을 흉내 내다 시간대까지 바꾸는 것이 지금 가장 그럴듯한 다음 실수다 | 완료 |
| 560 | **다섯째 인스턴스 — 찾은 흔적.** 세 곳을 다 조사한 사람이 `?see=clues`로 들어가면 남은 흔적이 **0곳**이라 표식도 초롱 빛도 지도 마름모도 없다. 빛기둥이 없던 것과 같은 사고다 | 수정 |
| 561 | **내가 이 세션에 직접 「물려받는 쪽」으로 적어 둔 것이 틀렸다.** 주석에 「여정 밖의 값 — 처치 수·대장 기록·**찾은 흔적**」이라고 썼는데, 흔적은 여정 단계가 세는 **진행 상태**다. 경계를 그어 놓고 한 항목을 잘못된 칸에 넣었다 | 수정 |
| 562 | 경계가 다시 분명해졌다 — **물려받는 것**: 처치 수·대장 기록·첫 여정 기록·시간대(취향). **만들어 주는 것**: 여정 단계·완주 여부·여정 id·만난 도깨비·찾은 흔적. 「목적과 상태가 어긋나면 확인 지점이 아니다」가 판별 기준이다 | 완료 |
| 563 | 경계를 **말이 아니라 목록으로** 세우고 동작으로 다시 확인하게 했다. `ResumeState`의 모든 필드가 「물려받음/만들어 줌」 두 칸 중 하나에 들어 있어야 하고, 각 칸은 저장값이 살아남는지/안 남는지로 검증한다 — 값을 하나하나 적지 않아 기본값이 바뀌어도 규칙이 성립한다 | 완료 |
| 564 | 변이로 확인했다 — ① 흔적을 물려받는 옛 코드로 되돌리니 **잡았다**, ② 분류 안 된 런타임 필드를 반환에 끼워 넣으니 **잡았다**. 새 필드가 조용히 물려받는 쪽으로 흘러가는 것이 지금까지 다섯 번의 실패 방식이었고, 이제 그 길이 막혔다 | 확인 |
| 565 | 처음 변이 하나는 **타입 전용 필드**라 문법이 깨져 잡힌 것일 수 있었다. 「잡았다」를 그대로 믿지 않고 런타임 필드로 다시 재서 확인했다 — 「검사가 있다 ≠ 그 검사가 이것을 본다」를 변이 결과에도 적용한다 | 기록 |
| 566 | **두 수정을 화면에서 확인했다.** 저장에 흔적 셋을 「다 찾음」, 도깨비 넷을 「다 만남」으로 넣고 `?see=clues`에 들어가니 목표가 **「골목에 남은 흔적 조사하기 · 0 / 3」** 으로 뜨고 동료는 초롱 하나뿐이다. 고치기 전이라면 3/3에 표식도 마름모도 없고 도깨비 자리도 전부 치워져 있었다 | 확인 |
| 567 | 이 세션에서 **확인 지점이 저장에 덮이는 사고를 다섯 개 찾아 고쳤다.** 하나같이 「이 링크로 들어가면 볼 것이 있다」는 약속을 조용히 깨고 있었고, 사람이 플레이테스트를 시작하는 입구가 바로 그 링크들이다 | 기록 |
| 568 | README에 **한 방향 규칙**을 적었다 — 「확인 지점 → 저장」은 흐르고 「저장 → 확인 지점」은 흐르지 않는다. 플레이테스트하는 사람이 「내 저장 때문에 안 보이는 건가」를 의심하지 않아도 된다는 뜻이고, 지금까지는 그 의심이 옳았다 | 완료 |
| 569 | 문서 세 곳의 테스트 수가 낡아 있었다(1604·1623·1604 → 실제 1647). 갱신하면서 **숫자가 필요 없는 자리는 숫자를 뺐다** — README의 「1604개의 테스트 중 상당수는 순수 로직이 아니다」는 요지가 개수가 아니라 성격이므로 「그중 상당수는」으로 충분하다. 유지할 지점이 셋에서 둘로 줄었다 | 완료 |
| 570 | 백로그에서도 「지금 N개」를 빼서 현재 개수를 한 곳으로 모으려 했는데 **기존 검사가 막았고, 검사가 옳았다.** 「다른 문서도 같이 늙지 않는다」가 남은 923을 **현재 주장**으로 읽었기 때문이다 — 사람도 똑같이 읽는다. 현재 수치 없이 옛 숫자만 남기면 그 옛 숫자가 현재가 된다 | 확인 |
| 571 | **내 규칙(같은 수를 한 곳에만)과 기존 규칙(문서들이 같이 늙지 않는다)이 부딪혔고 기존 쪽이 이겼다.** 앞 문장의 「그중 상당수는」처럼 **숫자가 필요 없는 자리**를 없애는 것은 옳았지만, 대조가 요지인 자리에서는 숫자 둘이 다 있어야 한다 | 기록 |
| 572 | **내가 방금 만든 데이터 손실 경로를 찾아 막았다.** 확인 지점이 「아무도 안 만난」 상태로 열리게 한 뒤, 거기서 도깨비를 하나 만나면 **한 마리짜리 목록이 저장을 덮어써 모아 둔 넷이 하나가 됐다** | 수정 |
| 573 | 무게가 다른 결함이다 — 앞의 다섯은 「볼 것이 안 보인다」였지만 이건 **사람이 모은 것을 지운다.** 게다가 확인 지점은 README가 플레이테스트 입구로 안내하는 링크이고, 저장은 같은 브라우저에 있다 | 수정 |
| 574 | 저장할 값을 **세션 목록이 아니라 저장에서** 만들게 했다(`withMetDokebi`). 「확인 지점에서 한 일은 기록된다」는 **더하는** 것이지 **줄이는** 것이 아니다 — 문서에 적은 한 방향 규칙의 정확한 뜻을 코드에 맞췄다 | 수정 |
| 575 | **교훈: 상태를 「깨끗하게」 만들어 주는 변경은 그 상태가 어디로 흘러나가는지까지 봐야 한다.** 나는 들어오는 쪽(저장 → 확인 지점)만 막고 나가는 쪽(확인 지점 → 저장)을 그대로 두었다. 다섯 번을 고치는 동안 한 번도 나가는 쪽을 보지 않았다 | 기록 |
| 576 | **교훈을 나머지에도 적용했더니 더 큰 것이 나왔다.** 저장 훅이 `saved.current`를 **만들어진 상태**로 씨앗 삼는다 — 확인 지점에서 한 단계만 진행돼도 `saveProgress`가 통째로 덮어 **사람이 진행한 여정 단계·완주 여부·여정 id·찾은 흔적이 전부 사라진다** | 수정 |
| 577 | 만난 도깨비보다 잃는 양이 크다. 흔적 하나를 조사하거나 로봇 하나를 눕히는 것만으로 트리거된다 — 확인하러 들어간 사람이 **자기 판을 잃는다** | 수정 |
| 578 | `mergeSandboxProgress`로 **확인 지점에서는 늘어나는 것만** 받게 했다: 처치 수는 큰 쪽, 대장 기록은 켜진 채, 흔적은 합집합, 여정 단계·완주·id는 **저장된 것 그대로**. 「기록된다」는 약속은 지키되 지우지 않는다 | 수정 |
| 579 | 평소 플레이는 건드리지 않는다(`sandboxed`일 때만 합친다). 합쳐 버리면 여정이 영영 뒤로 못 가 **다시 하기가 막힌다** — 그것도 검사로 묶었다. 변이로 확인: 합치기를 빼니 **잡았다** | 확인 |
| 580 | **같은 뿌리에서 일곱 개가 나왔다.** 들어오는 쪽 다섯(볼 것이 안 보임), 나가는 쪽 둘(사람 것이 지워짐). 앞의 다섯을 고치는 동안 뒤의 둘을 못 본 이유는 분명하다 — **경계를 한 방향으로만 그렸다** | 기록 |
| 581 | **나가는 쪽을 끝까지 훑었고 세 번째는 없다.** 설정에 쓰는 곳 일곱을 모두 봤다 — 시간대·색보정·포즈·동료는 **사람이 버튼을 눌러야** 저장되고, `metDokebi: []`는 「다시 하기」다. 시나리오가 정한 값이 **사람의 동작 없이** 저장으로 새는 곳은 없다 | 확인 |
| 582 | 앞의 둘과 다른 점이 판별 기준이다 — 고친 둘은 **사람이 고르지 않은 값**(빈 만난 목록·되돌려진 여정)이 저장으로 갔고, 나머지 일곱은 **누른 것**이 간다. 확인 지점에서도 누른 것은 기록되는 게 맞다 | 기록 |
| 583 | 이 세션의 「확인 지점」 계열은 여기서 닫힌다 — 들어오는 쪽 다섯, 나가는 쪽 둘, 그리고 **없음을 확인한 것 하나**. 없음을 확인한 것도 적는다: 다음 사람이 같은 의심으로 같은 일곱 곳을 다시 읽지 않게 | 기록 |
| 584 | **두 방향을 브라우저에서 한 번에 확인했다.** 풍부한 저장(단계 4·완주·여정 `boss-hunt`·흔적 3·처치 99·도깨비 4)을 넣고 `?see=clues`에 들어갔더니 — 화면은 **「흔적 조사하기 0/3」에 동료 초롱 하나**(저장이 화면으로 안 샌다), 저장은 **여섯 값이 전부 그대로**(화면이 저장을 안 지운다) | 확인 |
| 585 | 이게 이 계열 전체의 결론이다. 들어가는 것만으로 잃는 것이 없고, 볼 것은 늘 있다 — 사람이 플레이테스트 링크를 **자기 판 걱정 없이** 눌러도 된다 | 확인 |
| 586 | 확인 지점이 **더 강력해졌으므로**(여정·만난 도깨비·흔적을 만들어 주고 저장과 주고받는다) 프로덕션 차단을 다시 봤다. 함수 쪽은 옳고(`if (!isDevelopment) return null`) 양쪽 갈래 테스트도 있다 | 확인 |
| 587 | 빈칸은 **계약면**이었다 — 부르는 쪽이 참을 넘기면 그 차단은 없는 것과 같은데, 호출부를 보는 검사가 없었다. 「선언 ↔ 실제 호출」 대조를 검사로 넣었다: 호출부가 `process.env.NODE_ENV !== "production"`을 넘기는지, 그리고 **참을 박아 두지 않았는지** | 완료 |
| 588 | 변이로 확인했다 — 호출부를 `true`로 바꾸니 **잡았다.** 디버깅하다 참으로 두고 잊는 것이 가장 그럴듯한 사고이고, 그러면 프로덕션에서 아무나 `?see=party`로 전부 열 수 있다 | 확인 |
| 589 | **이번 세션에 넣은 CSP가 실제로 나가는지 처음 확인했다.** 프로덕션 빌드를 3119에 띄워 헤더를 읽었다 — `Content-Security-Policy`·`X-Frame-Options: DENY`·`X-Content-Type-Options: nosniff`가 모두 나온다. **안 나가는 CSP는 없느니만 못하다**(있다고 믿게 만든다) | 확인 |
| 590 | 더 중요한 것도 확인했다 — **CSP가 게임 자신을 막지 않는다.** 프로덕션 빌드에서 월드·HUD·미니맵이 온전히 그려지고 콘솔에 위반도 오류도 없다. `blob:`·`data:`를 열어 둔 것이 실제로 필요했고 충분했다는 뜻이다 | 확인 |
| 591 | 덤: 프로덕션에서는 속도계가 **「0.0 m/s」로 깨끗하다.** 앞서 「N」으로 보여 결함으로 읽을 뻔했던 것이 Next.js 개발 배지였다는 진단이 이렇게 확정됐다 | 확인 |
| 592 | 띄운 서버는 정리했고 개발 서버(3112)는 그대로다 | 기록 |
| 593 | **`?see=`가 프로덕션에서 실제로 막히는 것을 눈으로 확인했다.** 프로덕션 빌드에서 `/play?see=party`를 열었더니 동료는 **초롱 하나뿐**이고 넷이 아니다 — 시나리오가 완전히 무력화됐다 | 확인 |
| 594 | 이건 테스트가 증명하지 못하는 것이다. 단위 테스트는 `parseScenario(.., false)`가 null임을 보고 호출부 검사는 `NODE_ENV`를 넘기는지 보지만, **Next.js가 그 값을 인라인해 죽은 코드로 지우는지**는 실제 빌드를 열어 봐야 안다 | 기록 |
| 595 | 확인 지점이 이번 세션에 훨씬 강해졌으므로(상태를 만들어 주고 저장과 주고받는다) 이 차단이 새는지는 **넘겨짚을 일이 아니었다.** 앞선 CSP 확인과 같은 이유다 — 넣어 둔 방어가 실제로 서 있는지는 산출물에서 본다 | 기록 |
| 596 | 「산출물에서 확인한다」를 번들 예산에도 적용해 봤는데 **빈칸이 없었다.** 이 검사는 이미 `.next/build-manifest.json`을 읽어 실제 청크를 gzip으로 잰다 — 대용물이 아니다 | 확인 |
| 597 | 주석에 적힌 측정값(127KB)이 밀렸는지 재 봤더니 **127.6KB로 그대로**다. 이번 세션에 더한 것(톤 두 항목·순수 함수 몇 개)은 예산을 움직이지 않았고, 테스트는 애초에 실려 나가지 않는다 | 확인 |
| 598 | 산출물 확인 훑기는 여기서 끝난다 — **CSP(새는 것 없음)·`?see=` 차단(막힘)·번들 예산(빈칸 없음)** 셋을 봤고, 앞의 둘은 테스트가 증명 못 하던 것이라 값이 있었다 | 기록 |
| 599 | `tests/sessionContinuity.test.ts`가 **921줄**이 되어 나눴다 — **확인 지점과 저장이 주고받는 것**이 한 덩어리라 `tests/devScenarioState.test.ts`로 뺐다(570 + 375줄). 새 파일 머리말에 왜 따로 있는지를 적었다: 같은 뿌리에서 일곱 개가 나왔고 규칙은 한 줄이다 | 완료 |
| 600 | **그런데 내가 든 근거는 틀렸다.** 「800줄 상한을 넘겼다」고 적었는데, `tests/fileSize.test.ts`를 읽어 보니 800은 **`src` 전용**이고 테스트는 「사례 목록이라 소스보다 길어도 된다」는 이유로 **1200줄**이 의도된 한도다. 921줄은 위반이 아니었다 | 기록 |
| 601 | 나눈 것 자체는 응집도로 정당하지만 **규칙을 어겼다고 말한 것은 거짓**이었다. 그대로 두면 다음 사람이 「테스트도 800줄」이라는 없는 규칙을 배운다 — 고친 내용보다 **왜 고쳤다고 적었는가**가 오래 남는다 | 기록 |
| 602 | 검사가 나를 안 잡은 것도 결함이 아니었다 — **잡을 이유가 없었다.** 「검사가 있다 ≠ 그 검사가 이것을 본다」의 반대편 실수다: 이번엔 검사가 보지 않는다고 결함이라 단정했다 | 기록 |
| 603 | **빛기둥을 찾았다 — 처음부터 화면 한가운데 있었다.** 골목 끝에 옅은 노란 기둥이 건물 사이로 솟아 있고 하늘·간판과 구분된다. 22m 떨어진 노을 화면에서 보인다 | 확인 |
| 604 | 앞서 두 번 틀리게 적었다 — 「특정하지 못했다」와 「지도상 뒤쪽이라 안 보인다」. **뒤쪽이 아니라 정면이었고**, 기둥은 프레임 중앙에 있었는데 하늘의 일부로 읽었다. 지도의 「남쪽 22m」를 카메라 방향으로 넘겨짚은 것이 첫 단추였다 | 기록 |
| 605 | 그래서 사람에게 남는 질문이 바뀐다 — 「**보이는가**」는 답이 났다(보인다). 남은 것은 「**눈길을 끄는가**」다. 있는 것과 눈에 띄는 것은 다른 질문이고, 뒤의 것만 사람이 답할 수 있다 | 기록 |
| 606 | 같은 잣대를 다른 라벨에도 댔다 — `air`(「실루엣이 보이는지」)와 `result`(「한 화면에 들어가는지」)도 **이미 답이 난 것**을 묻고 있었다. 사람이 15분을 어디에 쓸지 이 라벨을 보고 정하는데, 끝난 질문을 가리키면 그 시간이 낭비된다 | 완료 |
| 607 | 남은 질문으로 바꿨다 — 공중은 **활강 감각**, 완주 화면은 **두 번째 완주에도 다시 뜨는가**(장치는 코드에 있지만 소스를 읽는 검사만 붙들고 있고 두 번째까지 가 본 사람이 없다) | 완료 |
| 608 | 고치는 중 검사가 하나 막았다 — 「활강」을 말하면서 **높이를 안 말하면** 평지에서 눌러 보게 된다. 「높은 곳에서」를 넣었다. 라벨을 짧게 다듬다 필요한 말을 떨어뜨릴 뻔한 것을 자가 잡았다 | 확인 |
| 609 | `party`·`clues` 라벨도 마저 고쳤다 — 둘 다 이번 세션에 확인한 것을 묻고 있었다(넷이 겹치지 않고 색이 갈린다 / 지도에 마름모 셋). 남은 질문으로 바꿨다: **대열이 보기 좋은가**, **흔적 원반의 크기가 알맞은가** | 완료 |
| 610 | 여덟 개 확인 지점 중 **다섯의 라벨이 이미 답이 난 것을 묻고 있었다**(`air`·`result`·`party`·`clues`·`shrine`). 확인이 쌓이는 동안 라벨은 그대로였기 때문이다 — **확인할 때마다 그 자리의 질문도 함께 좁혀야 한다**는 것이 이 정리의 교훈이다 | 기록 |
| 611 | 이제 여덟 라벨이 전부 **아직 답이 없는 것**을 가리킨다. 사람이 링크를 눌러 도착한 자리에서 「이건 이미 아는데」라고 느낄 일이 없다 | 완료 |
| 612 | **자동화로 텍스트를 입력할 수 없다** — 새로 확인한 한계다. 입력칸이 최상단에 있고 JS로 포커스까지 줘도 확장 프로그램의 타이핑이 닿지 않는다. 소리·fps와 같은 칸에 들어간다 | 기록 |
| 613 | 그래서 앱 배선만 페이지 안에서 확인했다 — 네이티브 setter + `input` 이벤트로 넣으니 **필드와 저장에 「Doggabi Test」로 들어간다.** 14자를 넣었는데 12자다: 길이 자르기가 실제로 동작한다(`nickname` 테스트의 「길이를 자른다」가 화면에서도 참이다) | 확인 |
| 614 | **자에 속은 열째:** 처음 클릭이 필드 **아래**(y 670, 필드는 603~647)를 눌렀다. 창 크기가 도중에 1372→1280으로 바뀌었는데 예전 배율 1.072를 계속 적용했다. **스크린샷 좌표와 CSS 좌표의 배율은 창이 바뀌면 함께 바뀐다** | 기록 |
| 615 | **README의 「이름」을 닫았다.** 세 고리를 전부 확인했다 — 입력 → 저장(`Doggabi Test`, 14자가 12자로 잘림), 저장 → 완주 화면(**「Doggabi Test의 완주」**가 뜬다), 저장 → 사진 파일명(`buildCaptureFilename` 순수 함수 검사) | 확인 |
| 616 | 완주 화면 부분은 그동안 **소스를 읽는 검사**만 붙들고 있었다(「완주 화면이 이름을 부른다」). 이제 화면에서 봤다 — 래칫이 지키던 것이 실제로 참이었음이 확인된 셈이다 | 확인 |
| 617 | **README의 「소리·모션 버튼」을 닫았다.** 이름은 실제로 같다 — 보이는 글자는 「모션」으로 짧지만 **접근 가능한 이름이 「모션 줄이기」**로 시작 화면과 일치한다. 화면에 적힌 글자만 보고 「다르다」고 할 뻔했다 | 확인 |
| 618 | 끄고 켜는 것도 확인했다 — 누르니 `aria-label`이 **「켜기」→「끄기」**(다음에 할 일을 말하는 올바른 토글 문구), `aria-pressed`가 `false`→`true`, 저장에 `reducedMotion: true`. 눌러서 되돌려 놓았다 | 확인 |
| 619 | 이번 세션에 사람 몫이 **20 → 15**로 줄었다. 줄어든 다섯은 전부 「사람만 답할 수 있는 줄 알았는데 아니었던 것」이다 — 확인 지점을 고치고 화면·픽셀·DOM으로 재면서 하나씩 갈렸다 | 기록 |
| 620 | 「말 걸기·살펴보기」를 확인하러 가게 앞을 스치듯 지나 봤지만 안내가 안 떴다. **결함이 아니었다** — 상호작용 반경이 `CLUE_RADIUS = 3.4`m인데 나는 길 한가운데(5~8m)를 지나고 있었다 | 확인 |
| 621 | 그 사실이 항목에 빠져 있어서 README를 고쳤다 — **「길 한가운데서는 안 뜬다, 손 닿을 거리까지 붙어야 한다」.** 이걸 모르면 사람도 나처럼 지나가며 「안 뜬다」고 오해한다. 대신 물어볼 것을 **그 거리가 답답하지 않은지**로 좁혔다 | 완료 |
| 622 | 걸어 다니며 확인하려던 것을 **소스의 상수 하나로 끝냈다.** 화면에서 답이 안 나오면 자를 의심하기 전에 **판정 규칙부터 읽는 것**이 더 빨랐다 | 기록 |
| 623 | **그래플과 활강을 처음으로 눈으로 봤다.** `G`를 누르니 속도계가 **8m/s**로 뛰며 가로등 높이까지 끌려 올라가고, 그 상태에서 `Space`를 누르고 있으니 간판보다 높은 곳에서 **팔을 벌린 자세로 떠서** 내려온다(4m/s) | 확인 |
| 624 | README의 「보드까지는 확인했고 **그래플·활강은 아직 아무도 못 봤다**」가 더 이상 사실이 아니다. 이 저장소에서 가장 오래 「못 본 것」으로 남아 있던 항목이다 — 남은 것은 **감각**뿐이다 | 완료 |
| 625 | 걸어서 못 하던 것을 **키 두 번으로** 했다. 앞서 「말 걸기」를 확인하려 5~8m를 헤맨 것과 대조적이다 — 그래플은 사거리가 넉넉해 서 있던 자리에서 바로 걸렸다. **무엇이 확인 가능한지는 조작의 사거리가 정한다** | 기록 |
| 626 | 보스에게 **달려가 봤다.** 확인된 것: 교전 거리에 들면 **「고물 대장」 체력 바가 뜬다**, 달리는 동안 **여정 단계가 실시간으로 넘어간다**(「달려서 몸을 풀기」 → 「스케이트보드 타기」, 초롱이 「보드! 그거 타면 진짜 빨라져.」로 받는다) | 확인 |
| 627 | **예고 링과 타격은 못 봤다.** 스크린샷 8장(≈1.6초)으로도 사거리에 못 닿았다 — 화면당 0.2초뿐이라 14m를 좁히는 데 사람의 3초가 자동화로는 수십 장이다. 「자동화로는 붙기 어렵다」던 기존 기록이 **맞았다** | 기록 |
| 628 | 뜻밖의 확인: **자동 품질 조정이 실제로 동작한다** — 「프레임이 낮아 그래픽 품질을 보통으로 낮췄습니다」가 떴다. 숨은 탭은 프레임이 낮으니 당연한 반응이고, 동시에 **자동화 확인의 주의사항**이다: 오래 열어 두면 품질이 내려가 화면이 달라진다 | 확인 |
| 629 | 「눈이 필요한 것」 1번(후드 대 노을 하늘, 거리 52)을 재 보려다 **확인 지점이 없다는 것**을 알았다. `?see=air`는 하늘을 배경에 두지만 **일부러 한낮**이다 — 「묻힌다면 밝은 하늘에서 묻힌다」는 **밝기** 기준이다 | 확인 |
| 630 | 그런데 이 항목이 걱정하는 것은 **색상**이다: 주황(#ff8a3d)이 주황(#f0a06a) 앞에 선다. **두 어려움이 서로 다르고**, 밝기 쪽만 확인 지점이 있다. 한낮에서는 묻히지 않는 것을 확인했지만 그것이 노을을 대신하지 못한다 | 기록 |
| 631 | 목록에 **어떻게 봐야 하는지**를 적었다 — 노을에서 뛰어오르거나 `G`로 가로등에 올라가 `Space`로 활강해 하늘을 등질 것. 자동화로는 카메라를 캐릭터 아래에 두기 어려웠다 | 완료 |
| 632 | 확인 지점을 새로 만들지 **않기로** 했다. 노을 역광을 보여 준다고 약속해도 3인칭 카메라가 캐릭터 아래로 가지 않으면 하늘을 등지지 않는다 — **보여 준다고 적어 놓고 안 보이는 자리**를 또 만드는 것이 더 나쁘다(`noon`·`shrine` 라벨에서 두 번 겪은 실수다) | 기록 |
| 633 | 대신 수치 쪽 검사가 그 경우를 실제로 보는지 확인했다 — **본다.** `tests/appearance.test.ts`가 **네 시간대 × 네 외형 16조합**을 대조하고, 노을만 기준을 50으로 **일부러 낮춰** 두었다(기본 후드가 52밖에 안 되기 때문). 이 저장소는 여유가 빠듯하다는 것을 알고 받아들인 상태다 | 확인 |
| 634 | 그래서 이 항목은 **더 준비할 것이 없다.** 수치는 지켜지고 있고, 사람이 「묻힌다」고 하면 기본 색과 그 기준(50)을 함께 올리면 된다 — 무엇을 고칠지까지 이미 적혀 있다 | 기록 |
| 635 | 「눈이 필요한 것」의 수치가 목록에 잘못 적혔나 의심했다 — 테스트에서 `7.4`가 **달리기 속도**로 쓰이는데 목록은 「이단 점프 7.4」라고 한다. 정본을 보니 **둘 다 7.4다**: `run.maxSpeed = 7.4`, `AIR_JUMP_VELOCITY = 7.4`. 목록이 맞았다 | 확인 |
| 636 | **자에 속은 열한째** — 같은 숫자가 다른 튜너블에 우연히 겹친 것을 「목록이 틀렸다」로 읽었다. 값으로 정본을 찾을 때는 **이름으로 찾아야** 한다. 숫자는 겹친다 | 기록 |
| 637 | 확인 결과 이 목록은 준비가 끝나 있다 — 수치는 정본에 있고 이름이 붙어 있으며, 사람이 답하면 어느 상수를 고칠지가 바로 나온다. **더 좁힐 것이 없다** | 기록 |
| 638 | **적어 두고 강제하지 않던 규칙을 하나 찾았다.** 「억제가 늘어난다는 것은 설계가 어긋났다는 신호」라고 써 놓았는데, 검사는 **새로운 종류**의 억제만 잡고 같은 종류가 몇 개로 불어나든 통과했다 | 수정 |
| 639 | 파일 수 래칫(한도 8)을 달았다. 지금 여덟은 전부 **프레임마다 공유 객체를 고쳐 쓰는 렌더러**다(적·보스·군중·차량·동료·플레이어·포토 조작·터치 조작) — 매 프레임 `setState`를 안 하기로 한 결과라 의도된 것이다 | 완료 |
| 640 | 아홉째가 생기면 멈추고 **둘 중 하나를 하게** 했다 — 그것도 프레임 렌더러가 맞으면 한도를 올리고 **왜인지 적는다**, 아니면 억제하지 말고 설계를 고친다. 변이로 확인: 아홉째를 넣으니 **잡았다** | 확인 |
| 641 | **같은 유형을 셋 더 찾았다 — 적어 두었지만 막는 검사가 없던 절대 규칙들.** `Math.random()` 금지, 프로덕션 `console` 금지, 외부 에셋 금지. 지금은 셋 다 지켜지고 있었지만 **지키고 있다는 것과 지켜진다는 것은 다르다** | 수정 |
| 642 | 무작위가 특히 중요하다 — 도시 배치·소품·군중이 전부 씨앗 난수로 만들어지므로 `Math.random()`이 하나만 끼어도 **같은 씨앗에서 다른 도시가 나오고** 배치 정합성 검사가 무엇을 보는지 알 수 없게 된다. 사진도 매번 달라진다 | 수정 |
| 643 | 외부 에셋 금지는 **CSP(`connect-src 'self'`)와 같은 약속**이다. `fetch`나 `TextureLoader`가 하나 생기면 약속이 조용히 깨지고 프로덕션에서 차단되어 화면이 빈다 — 개발 중에는 안 보이는 종류의 사고다 | 수정 |
| 644 | 변이로 확인: `cityDetails.ts`에 `Math.random()`을 끼워 넣으니 **잡았다** | 확인 |
| 645 | **핵심 성능 규칙도 강제되지 않았다** — 「매 프레임 `setState` 금지」. `useFrame` 본문만 도려내 검사하게 만들었다(파일 단위로 보면 `Shrine`의 정당한 `useEffect` 상태나 `City`의 일반 헬퍼 `setUvAttributes`를 오탐한다) | 수정 |
| 646 | 검사가 **진짜 예외 하나**를 찾아냈다 — `City.useVisibleBlocks`가 `useFrame`에서 `setKey`를 부른다. **위반이 아니다**: 구역 키가 바뀔 때만(몇 초에 한 번) 부르고, 그 대가로 매 프레임 수천 인스턴스 재필터를 피한다. 규칙의 목적에는 오히려 맞는다 | 확인 |
| 647 | 그래서 **이유를 적은 허용 목록**으로 바꿨다. 「새 예외를 넣기 전에 정말 몇 초에 한 번인지 확인하고 이유를 적어라 — 이유 없는 줄이 하나 늘면 이 목록은 규칙이 아니라 통과권이 된다」고 함께 적었다 | 완료 |
| 648 | **허용 목록이 낡는 것**도 막았다: 예외가 코드에서 사라졌는데 목록에 남아 있으면 다음에 같은 이름이 생겼을 때 묻지도 않고 통과한다. 변이로 확인: 동료 프레임에 `setFoo(1)`을 넣으니 **잡았다** | 확인 |
| 649 | **주석으로만 이어져 있던 계약을 검사로 이었다** — `capture.ts`는 「`preserveDrawingBuffer`가 켜져 있어야 픽셀이 남는다」고 적고 `GameScene`은 켜 두었는데, **아무도 그 둘을 대조하지 않았다** | 수정 |
| 650 | 이 플래그는 성능 비용이 있어 **「최적화」로 꺼 보기 딱 좋다.** 끄면 화면은 멀쩡하고 **사진과 클립만 빈 이미지가 된다** — 눌러 보기 전에는 모르는 고장이다. 변이로 확인: `false`로 바꾸니 **잡았다** | 확인 |
| 651 | 검사와 함께 **이유가 두 파일에 다 남아 있는지**도 본다. 검사만 있으면 다음 사람은 왜 켜 두는지 모른 채 끄려다 실패하고 **검사를 고칠 생각부터 한다** | 완료 |
| 652 | 「짝을 이룬다」·「같아야 한다」고 **주석이 주장하는 결합**을 패턴으로 훑었다. 대부분은 검사가 있었는데 하나가 **한쪽만** 보고 있었다 — 「흔적 조사 거리는 살펴보기와 같아야 한다」 | 수정 |
| 653 | 그 검사는 둘 다 **3.3m에서 걸리는지**만 봤다. `CLUE_RADIUS`를 10으로 바꿔도 통과한다 — 「같은 거리」를 검사한다면서 **같음을 안 보고 있었다** | 수정 |
| 654 | 양쪽 사거리를 **직접 재서 비교**하게 바꿨다(0.05m 간격으로 훑어 경계를 찾고 차이가 0.06m 이내인지). 값을 박지 않아 사거리를 함께 바꾸면 그대로 통과한다 — 지키는 것은 **숫자가 아니라 두 값이 같다는 사실**이다 | 완료 |
| 655 | 변이로 확인: `CLUE_RADIUS`만 5.0으로 바꾸니 **잡았다** | 확인 |
| 656 | 같은 잣대를 **예고 링 ↔ 충격 판정**에도 댔다. 링은 `BOSS.slamRadius * 2`를 쓰고 판정은 `<= BOSS.slamRadius`를 쓰므로 결합은 성립한다 — **결함은 없었다** | 확인 |
| 657 | 그런데 검사가 **한 방향만** 잡았다: `slamRadius + 1`에서 안 맞는 것만 보므로 판정이 **넓어지면** 걸리지만 **좁아지면** 통과한다. 링을 믿고 도망친 사람이 손해를 보는 쪽이 안 잡히고 있었다 | 수정 |
| 658 | 실제 경계를 0.02m 간격으로 재서 상수와 맞추게 했다. 공정성 문제는 **방향을 가리지 않아야** 한다 — 「링 밖인데 맞았다」와 「링 안인데 안 맞았다」 둘 다 약속을 깨는 것이다. 변이로 확인: 판정을 0.7배로 줄이니 **잡았다** | 확인 |
| 659 | **검사가 하나도 없던 관계를 찾았다** — `strikeRange`(적이 때리는 사거리)와 `standoffRadius`(적이 서는 거리). 전자가 후자보다 넓어야 **붙은 적이 실제로 위협**이 된다 | 수정 |
| 660 | 뒤집히면 적은 1.6m에 서서 **영영 때리지 않는다** — 화면에는 적이 몰려드는데 아무 일도 없는 게임이 된다. 테스트도 화면도 아무 말을 하지 않는 종류의 고장이다 | 수정 |
| 661 | 지금은 `standoffRadius + 0.8`로 **파생**되어 저절로 성립한다. 검사는 **그 파생을 리터럴로 바꾸는 날**을 위한 것이다. 여유가 0.5m 이상인지도 함께 본다 — 간신히 넓기만 하면 밀림·충돌 보정에 따라 「가끔 안 맞는 적」이 된다 | 완료 |
| 662 | 변이로 확인: `strikeRange: 1.2`로 바꾸니 **잡았다** | 확인 |
| 663 | **주석이 없는 장치를 주장하고 있었다.** `textures.ts`가 「낮 텍스처와 밤 발광 텍스처가 **같은 상수**를 쓴다」고 적어 두었는데, 실제로는 두 함수가 같은 식을 각각 적은 **복제**였다 | 수정 |
| 664 | 한쪽만 만지면 밤에 **창틀 밖으로 빛이 새어 나온 것처럼** 보인다 — 화면을 봐야만 아는 어긋남이라 테스트도 타입도 아무 말을 하지 않는다 | 수정 |
| 665 | `WINDOW_BOX` 한 곳에서 가져오게 고쳐 **주석이 참이 되게** 했다. 검사는 갈라진 것을 잡는 것이 아니라 **다시 복제하는 것**을 막는다 — 좌표를 만드는 곳이 하나뿐인지, 두 텍스처가 모두 쓰는지 본다 | 완료 |
| 666 | 이번 것은 성격이 다르다: 검사를 더한 게 아니라 **검사가 필요 없는 구조로 바꾼 것**이 본체이고, 검사는 그 구조를 붙잡는 역할이다 | 기록 |
| 667 | **여정 달성 가능성**을 의심해 봤다 — 활강 1.5초는 평지에서 0.83초뿐이라 닿지 않고, 못 채우면 **여정이 막혀 게임이 끝나지 않는다.** 확인 결과 **빈틈이 없다** | 확인 |
| 668 | `questContent.test.ts`가 도시에서 **실제로 가장 높은 앵커를 찾아** 거기서 공중 점프 없이 목표의 **1.2배**를 넘는지 보고, **그래플이 그 가로등에 닿는지**까지 본다. 「사거리를 1m로 줄여도 모든 검사가 통과했다」는 과거 기록과 함께 적혀 있다 | 확인 |
| 669 | 첫 단계(7m/s 달리기)도 5초 시뮬레이션으로 실제 통과를 확인한다 — 최고 속도가 7.4로 아슬아슬하지만 **가속 중 판정까지 돌려 본다.** 이 갈래는 더 팔 것이 없다 | 확인 |
| 670 | **사용자에게 보이는 진짜 결함을 찾았다 — 간판이 딴 가게 이름을 읽어 줬다.** 간판에 그려지는 가게는 `cell`(무작위 브랜드)로 정해지는데 살펴보기 대사는 **배열 순서**로 골랐다. 둘이 무관해서 **치킨집 앞에서 「약국」**이라고 말했다 | 수정 |
| 671 | 대사 목록에는 **월드에 없는 가게**도 있었다 — 「이용원」·「철물」. 반대로 노래방·치킨·PC방·곱창·국밥·빨래방·안경·태권도는 대사가 아예 없었다(8종 대 16종) | 수정 |
| 672 | 대사를 `SHOP_BRANDS`와 **같은 순서·같은 길이**로 다시 쓰고, `cell`을 살펴보기까지 실어 보냈다. 이제 보이는 간판이 곧 읽히는 말이다 | 완료 |
| 673 | 검사 셋으로 굳혔다 — 개수가 같은가, **각 대사가 그 자리 가게 이름을 실제로 부르는가**(순서만 맞추면 가게를 하나 끼울 때 조용히 밀린다), `cell`로 고르고 순서로 고르지 않는가. 변이로 확인: `cell`을 무시하니 **잡았다** | 확인 |
| 674 | 이건 **내용을 읽어야만 보이는 결함**이었다. 구조 검사를 아무리 늘려도 「이 대사가 저 간판의 것인가」는 묻지 않는다 — 이번 세션에서 코드가 아니라 **글을 읽어서** 나온 유일한 결함이다 | 기록 |
| 675 | 같은 렌즈를 **도감 설명 ↔ 실제 능력**에도 댔다. 값은 맞았다(그을음 `aggroScale 0.4`, 물비늘 `regenScale 4`) — **결함은 없었다** | 확인 |
| 676 | 그런데 검사가 **id마다 숫자를 박아** 두어 목록이지 규칙이 아니었다. **설명을 바꿔도 통과한다** — 그을음 소개를 「아물게 한다」로 고쳐도 아무도 막지 않는다. 도감은 사람이 능력을 배우는 유일한 곳이라 거기서 거짓말하면 「눌렀는데 아무 일도 안 난다」가 된다 | 수정 |
| 677 | **설명에서 기대를 끌어내** 양방향으로 보게 했다 — 숨는다고 적었으면 인지 반경이 줄어야 하고, **줄였으면 설명에 적혀 있어야 한다**(조용히 강해지면 왜 쉬워졌는지 모른다). 새 도깨비가 늘어도 규칙이 따라간다 | 완료 |
| 678 | **자에 속은 열두째:** 「숨」이 초롱의 **「숨은 흔적」**을 맞혔다 — 숨는 것은 흔적이지 도깨비가 아니다. 「몸을 감춰」로 좁혔다. 이번 세션에서 부분 문자열에 당한 것이 네 번째다 | 기록 |
| 679 | 변이로 확인: 그을음 설명을 회복 문구로 바꿔치기하니 **잡았다** | 확인 |
| 680 | 같은 렌즈를 **여정 힌트가 말하는 키**에도 댔다(`Shift`·`B`·`J`·`G`·`Space`·`T`) — 틀린 키를 안내하면 사람이 그 단계에 갇힌다. **이미 덮여 있다**: 「모든 키 표기가 묶인 키다」·「조작 표의 키 표기도 같은 코드를 가리킨다」, 그리고 훑기 자체를 지키는 「힌트를 실제로 읽었다」 | 확인 |
| 681 | 글↔동작 렌즈로 나온 것은 결국 둘이다 — **간판 대사(진짜 결함)**와 **도감 설명(규칙 없음)**. 나머지 글 표면(힌트·표식·완주 문구·조작표)은 전부 정본과 대조되고 있었다. 이 렌즈도 여기서 바닥이 보인다 | 기록 |
| 682 | 실제로 더 플레이해 전투 고리를 밟아 보려 했는데, 지도 요약이 **로봇은 개수만** 말해서(대장·흔적·도깨비 자리는 방향을 준다) 찾아갈 수가 없었다. **결함이 아니다** — 로봇은 인지 반경 16m 안에서 **쫓아온다.** 찾아다니는 대상이 아니라 다가오는 대상이고, 0기일 때는 「없다」로 알려 준다 | 확인 |
| 683 | 자동화로 로봇을 쫓아가는 것은 보스와 같은 이유로 비싸다(화면당 0.2초). 전투 규칙은 `combatSim`·`bossSim`이 증명하고 **손맛만 사람 몫**이라, 여기서 더 밟는 것은 값이 없다 | 기록 |
| 684 | 이번 구간(490~) 요약을 썼다 — 200행을 다 읽지 않아도 **무엇이 왜 바뀌었는지**와 **반복하지 말아야 할 실수**를 알 수 있게. 앞선 두 구간이 요약을 두는 것이 이 저장소의 방식이다 | 완료 |
| 685 | 처음에 **닫힌 범위(490~683)**로 적었다가 고쳤다 — 다음 행을 더하는 순간 거짓이 된다. 그리고 새 절을 열린 절(195~) **뒤에** 붙이면서 범위가 겹쳤길래 가운데를 195~489로 닫았다. **요약을 더하는 일 자체가 앞 요약을 낡게 만든다** | 수정 |
| 686 | 방금 낸 실수를 **검사로** 막았다 — 요약 구간이 끊기지도 겹치지도 않는가, 마지막만 열려 있는가, 열린 구간이 실제 행 번호 안에서 시작하는가. 사람이 셀 일이 아니다 | 완료 |
| 687 | 변이로 확인: 가운데 구간을 다시 열린 범위로 되돌리니 **잡았다**. 이 검사는 **다음에 요약을 하나 더 쓰는 사람**을 위한 것이다 — 그때 같은 자리에서 같은 실수가 난다 | 확인 |
| 688 | 이번 세션에 200행을 프로그램으로 삽입했으니 **번호가 겹쳤을 수** 있어 세어 봤다 — **겹치지 않았다**(679행, 최대 687). 빠진 번호 몇 개는 옛 정리에서 지워진 자리다 | 확인 |
| 689 | 그래도 겹침 검사를 달았다. 요약과 본문이 서로를 **번호로** 가리키므로(「반복 495에서 고쳤다」) 같은 번호가 둘이면 그 참조가 어디를 말하는지 알 수 없다. 빠진 번호는 막지 않는다 — 그건 문제가 아니다 | 완료 |
| 690 | **README가 사실이 아닌 것을 말하고 있었다** — 「이미지 파일이 `public/`에 없고」인데 Next.js 템플릿 SVG 다섯 개가 있다. `src`·`tests`·문서 어디에서도 참조하지 않는 잔재다. 문서를 사실에 맞추고 삭제는 「다음 후보」에 올렸다(git 추적이 아니라 되돌릴 수 없고, 삭제는 요청 범위 밖이다) | 수정 |
| 691 | 같은 렌즈로 다른 절대 주장도 봤다 — **웹폰트 없음**(`next/font`·`@font-face`·google fonts·`.woff` 전부 0), **의존성 다섯 개뿐**(`react-dom`은 직접 임포트가 0이지만 Next 런타임 필수라 정상). **둘 다 참이다** | 확인 |
| 692 | `dependencies.test.ts`가 이미 허용 목록에 **이유를 적어** 두고(「react-dom은 Next가 쓴다」) 선언한 패키지를 모두 쓰는지·상태 관리 라이브러리를 두지 않는지·다섯을 넘지 않는지까지 본다. **이 갈래도 빈틈이 없다** | 확인 |
| 693 | 문구만 고치고 끝낼 뻔했다 — **`public/`에 에셋이 들어오는 것을 막는 것이 없었다.** 소스에서 `fetch`·`TextureLoader`를 금지해 뒀지만, 파일을 `public/`에 넣고 `<img src="/x.png">`로 부르면 그 검사를 **비켜 간다** | 수정 |
| 694 | 모델·텍스처·오디오·폰트·영상 확장자를 막고, 파일 수 상한(5)도 뒀다 — 확장자만 막으면 `.json`이나 새 SVG가 슬금슬금 는다. **줄이는 것은 언제든 통과**하므로 「다음 후보」의 SVG 삭제와도 어긋나지 않는다 | 완료 |
| 695 | `public/texture.png`를 심어 확인: **둘 다 잡았다**(확장자 검사와 개수 상한). 심은 파일은 지웠다 | 확인 |
| 696 | 약속을 비켜 가는 **마지막 길**을 막았다 — CSS의 `url(https://...)`이나 마크업이 밖을 가리키는 것. 소스의 `fetch`를 막고 `public/`을 막아도 이 길은 열려 있었다 | 완료 |
| 697 | 이 길이 특히 나쁜 이유: 프로덕션에서는 CSP가 막지만 **개발 중에는 멀쩡히 보이다가 배포 후에만 빈다.** 가장 늦게 발견되는 종류다. `localhost`와 표준 네임스페이스(schema.org·w3.org)는 예외로 뒀다 | 완료 |
| 698 | 변이로 확인: `globals.css`에 외부 `url()`을 심으니 **잡았다**. 이제 「외부 에셋을 하나도 쓰지 않는다」는 **소스·public·스타일 세 방향에서** 지켜진다 | 확인 |
| 699 | 같은 「세 방향」 사고를 **「도시는 씨앗만의 함수」**에도 댔다. `Math.random`은 이미 막았고, 남은 길은 **시간**이다 — `Date.now()`를 씨앗에 섞으면 같은 씨앗에서 다른 도시가 나온다 | 수정 |
| 700 | **기존 왕복 검사(「두 번 호출해도 같은 도시」)로는 못 잡는다.** 연달아 두 번 부르므로 밀리초가 같아 통과한다. 시간·암호 난수는 소스에서 막는 편이 확실하다 — 월드 생성 모듈 넷에 한정했다(알림 타이머·카메라 흔들림 같은 정당한 쓰임을 막지 않으려고) | 완료 |
| 701 | **변이로 가설을 증명했다.** 같은 결함(`Date.now()` 침입)에 대해 새 검사는 **잡음**, 기존 왕복 검사는 **헛돎 — 결함을 넣었는데 통과한다**. 「검사가 있다 ≠ 그 검사가 이것을 본다」를 눈앞에서 확인한 셈이다 | 확인 |
| 702 | **변이로 기존 검사를 뚫어 보다가 진짜 구멍을 찾았다.** 군중의 위아래 흔들림과 도깨비 자리의 흔들림에서 `reducedMotion` 게이트를 지웠는데 **검사 1692개가 전부 통과했다** | 수정 |
| 703 | 저감 모션은 **접근성 약속**이다 — 흔들림에 예민한 사람이 켜 놓은 설정이 조용히 무력해지면 그 사람은 이 게임을 못 한다. 그런데 그 약속을 지키는 것이 두 곳에서 없었다 | 수정 |
| 704 | 파일 단위로 「`reducedMotion`을 언급하는가」를 보면 **못 잡는다** — 게이트 하나를 지워도 같은 파일 다른 곳에 이름이 남는다. **게이트 자체를 세는 래칫**(현재 20개)을 달았다. 줄이려면 왜 필요 없어졌는지 적고 수를 낮춰야 하고, 늘리는 것은 언제든 통과한다 | 완료 |
| 705 | 변이로 확인: 뚫었던 둘을 다시 심으니 **둘 다 잡았다.** 「변이로 기존 검사를 뚫어 본다」가 이 세션에서 가장 값진 기법이 됐다 — 없는 결함을 찾는 대신 **없는 검사**를 찾는다 | 확인 |
| 706 | 같은 기법으로 **소리 끄기**를 뚫었더니 또 통과했다 — 재생 판단에서 `enabled`를 빼도(**음소거가 아예 안 먹어도**) 1694개가 전부 통과 | 수정 |
| 707 | 소리 버튼은 사용자와의 약속이다 — 도서관에서, 옆에 자는 사람이 있을 때, 소리에 예민할 때 누른다. **안 먹으면 게임을 끄는 수밖에 없다** | 수정 |
| 708 | 오디오는 노드에서 못 돌리므로 소스에서 지킨다 — 재생 판단이 **한 곳(`shouldPlay`)에 모여 있고**(흩어지면 새 소리를 더할 때 하나를 빠뜨린다), 그 판단이 **소리 설정과 탭 가시성을 모두 보고**, **끄면 이미 나던 소리도 멈추는지**까지 본다 | 완료 |
| 709 | 변이로 확인: `enabled`를 빼니 **잡았다** | 확인 |
| 710 | **포토 모드 정지는 뚫리지 않았다** — 적·보스 둘 다 잡힌다(이번 세션에 검사를 강화한 덕이다) | 확인 |
| 711 | **손상된 저장은 뚫렸다.** `questId`의 타입 검사를 없애도 통과한다 — 저장에 숫자나 객체가 들어 있으면 그대로 흘러든다. localStorage는 **외부 입력**이다(콘솔·옛 버전·확장 프로그램) | 수정 |
| 712 | 이상한 여정 id는 **조용히 아프다** — 없는 여정으로 열려 목표가 안 뜨거나 첫 여정으로 되돌아가 **진행을 잃은 것처럼** 보인다. 「나중에 더한 필드가 이상해도」는 이미 있었지만 **여정 id 자체**는 아무도 보지 않았다 | 수정 |
| 713 | 동작으로 막았다 — 문자열이 아닌 id는 떨어뜨리되 **나머지 진행은 살린다**(한 필드가 깨졌다고 판을 지우는 쪽이 더 나쁘다), 멀쩡한 id는 그대로 통과시킨다(막기만 하면 이어하기가 늘 첫 여정에서 시작한다). 변이로 확인: **잡았다** | 완료 |
| 714 | `questId`가 뚫렸으니 **나머지 저장 필드도** 뚫어 봤다 — 소수 단계 허용·대장 기록 느슨하게·흔적 중복 허용·모르는 흔적 허용, **넷 다 잡힌다.** 구멍은 `questId` 하나뿐이었고 이제 막혔다 | 확인 |
| 715 | 이 훑기로 알게 된 것: **검증 코드가 있다고 검증이 지켜지는 것은 아니고, 반대로 대부분은 잘 지켜지고 있었다.** 다섯 중 하나가 구멍이었다 — 뚫어 보지 않았으면 어느 하나인지 몰랐을 것이다 | 기록 |
| 716 | 설정 검증(동료 id·이름 정제)과 지도 방향(남북 뒤집기·90도 회전)을 뚫었다 — **넷 다 잡힌다.** 미니맵은 진행 방향 위, 도시 지도는 북쪽 고정으로 **서로 다른 선택이고 각각 이유가 적혀 있다** | 확인 |
| 717 | **변이 훑기 결산: 열다섯 번 뚫어 다섯 구멍.** 저감 모션 둘(접근성 약속이 안 지켜짐)·음소거·손상된 저장의 여정 id·도시 결정성의 시간 의존. 나머지 열은 이미 막혀 있었다 | 기록 |
| 718 | 다섯 다 **코드를 읽어서는 안 보이던 것**이다. 검증 코드는 다 있었고 검사도 다 있었다 — 다만 그 검사가 그것을 보지 않았을 뿐이다. **뚫어 보는 것 말고는 알 방법이 없었다** | 기록 |
| 719 | 훑기를 넓혔다 — **그래플 사거리**(과거 「1m로 줄여도 통과했다」던 자리), 달리기 속도·이단 점프·활강 속도, 적 인지 반경, 동료 자리 간격. **여섯 다 잡힌다** | 확인 |
| 720 | 특히 그래플은 **과거 사고 뒤에 넣은 검사가 살아 있음**을 확인한 셈이고, 동료 자리 간격은 **이번 세션에 내가 고친 것**이 지켜지는지 확인한 것이다 | 확인 |
| 721 | 나쁜 변이도 하나 배웠다 — `GRAPPLE`에 안 쓰는 필드를 **더하는** 것은 결함이 아니라 통과하는 게 맞다. **변이는 「동작이 달라지는 것」이어야** 의미가 있다 | 기록 |
| 722 | 가장 약해 보이는 종류(**소스 문자열 래칫**)를 뚫어 봤다 — 완주 기록 굳히기. 값 뒤집기와 논리 반전 **둘 다 잡혔다** | 확인 |
| 723 | **다만 둘 다 「글자가 사라져서」 잡혔다.** `captured.current = true`나 `!`가 없어지니 래칫이 걸린 것이지, 동작을 본 것이 아니다. **글자를 남긴 채 동작만 바꾸는 변이**는 문자열 치환으로 만들기 어렵다 | 기록 |
| 724 | 그래서 이 기법에는 **한계가 있다**: 래칫이 실제보다 튼튼해 보인다. 「잡혔다」가 「동작을 지킨다」를 뜻하지 않는 경우가 남아 있고, 그건 이 기법으로는 못 가른다 — **기법 자체에도 「검사가 있다 ≠ 그 검사가 이것을 본다」가 적용된다** | 기록 |
| 725 | **내가 이번 세션에 넣은 검사들도 같은 의심을 받아야 한다.** 변이로 확인 안 하고 넘어간 셋을 마저 확인했다 — 창문 좌표 복제 복귀·`console` 침입·`TextureLoader` 침입. **셋 다 잡는다** | 확인 |
| 726 | 이제 이 세션에 넣은 검사 **전부**가 변이로 확인됐다. 「검사를 넣었다」로 끝내면 그 검사가 헛도는지 알 수 없고, 그건 이번 세션에 기존 검사에서 다섯 번이나 본 상태다 — **자기 검사에 예외를 두지 않는다** | 기록 |
| 727 | 이번 세션에서 가장 값진 도구(`scripts/mutate.sh`)의 README 설명이 **낡아 있었다** — 「최근 다섯 개 중 둘」이라는 옛 통계뿐이고 **무엇을 뚫어야 하는지**가 없었다. 도구가 있어도 어디에 겨눌지 모르면 안 쓴다 | 수정 |
| 728 | 이번 근거로 바꿔 썼다 — **스물한 번 뚫어 다섯 구멍**, 그리고 **「사용자에게 한 약속」에서 고른다**는 기준. 뚫린 넷을 표로 적고, 잘 지켜지던 곳도 함께 적었다(**뚫어 보지 않으면 어느 쪽인지 알 수 없다**) | 완료 |
| 729 | 한계도 적었다 — 문자열 치환이라 **글자를 남긴 채 동작만 바꾸는 변이**를 만들기 어렵고, 그래서 소스를 읽는 검사는 실제보다 튼튼해 보인다 | 완료 |
| 730 | 쓰는 중에 자가 또 잡았다: 새 글머리표(`- **…**`)를 **사람 목록 항목으로 세어** 개수가 15→19로 틀어졌다. 표로 바꿨다 — **문서에도 형식이 곧 의미인 자리가 있다** | 수정 |
| 731 | 방금 밟은 함정의 **원인**을 고쳤다 — 변이 도구 설명이 「사람이 봐 줘야 하는 것」 절 **안의 `###`** 라서, 거기 글머리표를 더하면 사람 몫 개수가 틀어졌다. 도구 설명은 사람 몫이 아니므로 **제 절로 분리**했다 | 완료 |
| 732 | 검사에도 왜 그런지와 **다시 났을 때 무엇을 할지**를 적었다 — 「개수를 고치지 말고 그 목록을 절 밖으로 옮겨라」. 증상(숫자)을 고치면 다음에 또 난다 | 완료 |
| 733 | **검사 하나가 엉뚱한 범위를 세고 있었다.** 「2분 묶음이 다섯을 넘지 않는가」가 `### 여유`까지 잘랐는데 **그 제목이 없어졌다** — `indexOf`가 -1을 주니 절 전체를 셌다 | 수정 |
| 734 | 그런데도 **우연히 통과**하고 있었다: 뒤쪽 항목이 글머리표라 숫자 항목이 마침 다섯뿐이었다. 이번 세션의 「검사가 있다 ≠ 그 검사가 이것을 본다」가 **범위** 형태로 나타난 것이다 | 수정 |
| 735 | 경계를 **사라질 수 있는 이름이 아니라 구조**(다음 `###`)로 바꾸고, 경계를 못 찾으면 실패하게 했다. 실제로 2분 묶음에 여섯 번째를 넣어 확인: **잡았다**(넣기 전에는 못 잡았을 변이다) | 확인 |
| 736 | 「사라진 이정표」를 **유형으로** 훑었다. 검사가 문서 제목을 짚는 곳은 여섯이고 **지금은 다섯 다 살아 있다** — 방금 고친 하나만 죽어 있었다 | 확인 |
| 737 | 그래도 **이름이 바뀌면 조용히 어긋나는 것**은 그대로라, 「검사가 짚는 제목이 문서에 실제로 있는가」를 규칙으로 만들었다. 제목을 바꾸는 사람이 여기서 멈추고 검사도 함께 고치게 된다 | 완료 |
| 738 | 변이로 확인: `### MVP에서 제외`를 다른 이름으로 바꾸니 **잡았다** | 확인 |
| 739 | 제목만 보던 검사를 **모든 이정표**로 넓혔다 — 검사가 `indexOf`로 짚는 문자열 83개가 소스·CSS·문서 어딘가에 실제로 있는지 본다. **지금은 전부 살아 있다** | 완료 |
| 740 | **자를 두 번 고쳤다.** 처음엔 `src`의 `.ts`만 봐서 CSS 선택자와 문서 제목을 「죽었다」고 잘못 골라냈고(이스케이프 `\n`도 안 풀었다), 그다음엔 문서 목록을 손으로 적어 `DESIGN_GUIDE`를 빠뜨렸다. **목록을 박지 말고 최상위 `.md` 전부**를 보게 고쳤다 | 수정 |
| 741 | 변이로 확인: `shouldPlay`를 다른 이름으로 바꾸니 **잡았다.** 이제 함수·상수·제목 어느 것을 바꿔도 그것을 짚던 검사가 함께 멈춘다 | 확인 |
| 742 | 이번 세션에 검사를 크게 늘렸으니(1604 → 1703) **순서 의존**이 생겼는지 확인했다 — 순서를 섞어 세 번 돌려 **전부 통과**. 특히 전역을 스텁하는 저장 검사를 건드렸는데 새지 않는다 | 확인 |
| 743 | 이 확인법을 README에 적었다. **`pnpm verify`에는 넣지 않는다** — 검증은 되풀이 가능해야 하는데 섞는 것은 그 반대다. **검사를 여러 개 더한 날 한 번 돌려 보는 용도**로 자리를 잡아 줬다 | 완료 |
| 744 | 검사를 100개 가까이 더했으니 **돌아가는 시간**도 쟀다 — 전체 **2.35초**. 소스를 통째로 훑는 검사를 여럿 더했는데도 느려지지 않았다(파일 읽기는 밀리초 단위다) | 확인 |
| 745 | 이걸 확인한 이유: 검사가 느려지면 **매 반복이 비싸지고**, 비싸지면 사람이 안 돌린다. 안 돌리는 검사는 없는 검사다 | 기록 |
| 746 | 백로그가 **1252행**인데 처음 여는 사람이 「지금 뭘 해야 하나」를 알려면 전부 훑어야 했다. 맨 위에 **「지금 상태 — 여기부터 읽으라」**를 두었다 | 완료 |
| 747 | 표 하나로 갈라 준다 — **남은 일이 있는지**(다음 후보) · **사람이 해 줄 것**(README 목록) · **이 구간에 무슨 일이 있었나**(요약) · **새 결함을 찾고 싶다**(변이 뚫기) · **헛다리를 안 짚으려면**(한계와 「자」) | 완료 |
| 748 | 핵심 한 줄을 맨 앞에 뒀다 — **「이 파일은 할 일 목록이 아니라 기록이다.」** 이름이 「백로그」라 매번 할 일이 있는 줄 알고 700행을 훑게 된다 | 완료 |
| 749 | 방금 쓴 헤더에 **「15개」**를 박았다가 뺐다 — 그 수는 README가 소유하고 **검사가 지킨다.** 두 곳에 적으면 한 곳이 낡고, 이 세션에서 같은 함정을 세 번 밟았다(요약 범위·테스트 수·사람 몫 개수) | 수정 |
| 750 | 규칙으로 굳히면 이렇다 — **수를 적기 전에 「이 수를 누가 소유하는가」를 먼저 정한다.** 소유자가 아니면 가리키기만 한다 | 기록 |
| 751 | 규칙을 세우고 **같은 반복 안에서 또 어겼다** — 표에서 「15개」를 빼면서 바로 아래 문단의 **「남은 열다섯 질문」**은 그대로 뒀다. 훑어보고서야 찾았다 | 수정 |
| 752 | 이게 이 함정의 성질이다: **수는 눈에 안 띈다.** 한 곳을 고치고 고쳤다고 생각한다 — 그래서 「소유자를 정한다」는 규칙보다 **검사가 지키는 편**이 낫고, 이미 지키는 수들(테스트 수·사람 몫·수집 수)은 이런 일이 안 난다 | 기록 |
| 753 | 결론대로 **검사를 달았다** — 길잡이 절(「지금 상태」)이 **살아 있는 개수**를 들고 있으면 실패한다. 이 절은 가리키는 자리이지 기록하는 자리가 아니다 | 완료 |
| 754 | 과거 기록(「스물한 번 뚫어 다섯 구멍」)은 막지 않는다 — **이미 일어난 일은 낡지 않는다.** 막는 것은 다른 문서가 소유한 **현재 개수**다 | 완료 |
| 755 | 변이로 확인: 지운 「열다섯 질문」을 되돌리니 **잡았다.** 규칙을 세우고 두 번 어긴 뒤에야 검사를 달았는데, 그게 정확히 「규칙보다 검사」의 근거다 | 확인 |
| 756 | 되살아나기를 뚫다가 구멍을 찾았다 — 「지금 피해를 받을 수 있는 상태인지」를 정의하는 `isPlayerVulnerable`에서 **쓰러짐 조건을 지워도 통과했다** | 수정 |
| 757 | **제품 동작은 멀쩡하다** — 실제 진행은 쓰러진 프레임에서 곧바로 반환한다. 문제는 그 함수가 **「무적 규칙의 정의」**로 쓰인다는 것이다: 정의가 실제와 어긋나도 모르면 다음 사람이 **틀린 규칙을 기준 삼아** 다른 코드를 고친다 | 수정 |
| 758 | 정의와 실제를 **둘 다** 굳혔다 — 쓰러지면 맞을 수 없다, 무적이 끝나도 쓰러진 동안엔 안 맞는다(두 조건이 각각 필요하다), 그리고 실제로 적이 붙어 있어도 안 맞는다(쓰러진 자리에 적이 서 있는 게 보통이라 여기서 맞으면 **일어나기도 전에 다시 눕는다**) | 완료 |
| 759 | 나쁜 변이도 하나 더 배웠다 — 초기 상태의 `respawnRemaining`을 바꾸는 것은 **쓰러지지 않은 값이라 동작이 안 바뀐다.** 「헛돎」이 나오면 **변이가 진짜 결함인지부터** 본다 | 기록 |
| 760 | **「검사만 쓰는 정의 함수」를 유형으로 훑었다** — `deadCode` 허용 목록에 넷이 더 있다(`distanceToTarget`·`isEmoting`·`currentEmote`·`normalizeAngle`). 같은 이유로 실제와 어긋날 수 있는 자리다 | 확인 |
| 761 | 정의를 **뒤집어** 봤더니(이름 바꾸기는 임포트가 깨져 잡히므로 규칙 검사가 아니다) **전부 잡힌다.** `isPlayerVulnerable` 하나만 구멍이었고 이제 막혔다 | 확인 |
| 762 | 이 유형이 위험한 이유를 적어 둔다: 정의 함수는 **제품이 안 쓰므로** 틀려도 화면에 아무 일이 없다. 대신 **검사들이 그것을 기준 삼아** 다른 코드를 지키므로, 정의가 틀리면 **검사 전체가 틀린 것을 지킨다** | 기록 |
| 763 | 「드물게만 실행되는 상태」 부류를 마저 뚫었다 — **WebGL 컨텍스트 손실**(복구 기회 차단·안내 제거)과 **저장소가 막힌 경우**(시크릿 모드·용량 초과). **셋 다 잡힌다** | 확인 |
| 764 | 이 부류에서 구멍이 나온 것은 **쓰러진 사이 무적**(정의 함수) 하나뿐이었다. 드문 상태를 다루는 코드는 오히려 검사가 두텁다 — **드물다는 것을 아는 사람이 짰기 때문**으로 보인다 | 기록 |
| 765 | 다듬은 가설(**늘 돌지만 실패가 안 보이는 것**)로 겨눴더니 바로 나왔다 — `shadows={quality.shadows}`를 `shadows={true}`로 **박아도 통과한다** | 수정 |
| 766 | 이 실패는 눈에 안 보인다: 저사양에서 **자동 강등 안내는 그대로 뜨는데** 그림자가 계속 켜져 있어 프레임이 안 돌아온다. 사용자는 「낮췄다는데 왜 여전히 버벅이지」만 겪는다 | 수정 |
| 767 | 캔버스가 품질에서 값을 가져오는지(그림자·픽셀비·안티앨리어싱), **박아 두지 않았는지**, 안개와 카메라 거리가 **각각** 품질을 타는지 본다 | 완료 |
| 768 | **자가 또 약했다** — 파일 전체에서 `quality.fogFar`만 찾으니 안개가 쓰고 있어 **카메라 쪽을 박아도 통과**했다. 구간을 나눠 각각 보게 고쳤고, 그제야 잡혔다. 「검사가 있다 ≠ 그 검사가 이것을 본다」가 **내가 방금 쓴 검사**에도 적용됐다 | 수정 |
| 769 | 같은 렌즈로 더 겨눴다 — **구역 컬링**(반경을 전체로·구역 키 고정) 둘 다 잡힌다. **텍스처 캐시**는 뚫렸다: 적중 경로를 우회해도 통과한다 | 수정 |
| 770 | 이 실패도 눈에 안 보인다 — 화면은 똑같고 오류도 없다. 대신 부를 때마다 캔버스와 GPU 텍스처가 새로 생기고 **해제는 마지막 것만** 한다. 메모리가 자라다 탭이 무거워질 뿐이라 **원인을 찾기 가장 어려운 종류**다 | 수정 |
| 771 | 캔버스가 필요해 노드에서 못 돌리므로 소스로 지킨다 — 캐시가 있고, **적중하면 곧바로 돌려주고**(안 그러면 쌓기만 하고 아무도 안 읽는 자료구조가 된다), 만든 것을 넣는지 | 완료 |
| 771b | **처음 쓴 검사가 안 잡았다.** 이 파일에는 캐시가 셋(외벽·필터·타일)인데 「어딘가 한 곳이 맞으면 통과」로 써서 **한 곳을 뚫어도 통과**했다. 「읽는 곳 수 = 곧바로 돌려주는 곳 수」로 바꾸니 잡혔다 | 수정 |
| 772 | 나쁜 변이를 둘 더 배웠다 — **이름 전체 치환**(정의와 호출을 함께 바꾸면 동작이 같다)과 **안 쓰는 상수 추가**. 둘 다 「헛돎」이 나왔지만 검사 잘못이 아니었다 | 기록 |
| 773 | 두 반복 연속으로 **내가 쓴 검사가 약했다**(안개 거리 구간·캐시 개수). 공통점은 「어딘가 한 군데만 맞으면 통과」다 — 파일 전체를 한 덩어리로 보면 **같은 이름이 여러 번 나오는 곳에서 반드시 샌다** | 기록 |
| 774 | 두 반복의 교훈을 **도구 설명에 넣었다** — 「검사를 새로 쓸 때 가장 흔한 약점은 **어딘가 한 군데만 맞으면 통과**」와 그 처방(구간을 나누거나 개수를 맞춰라). 실제 사례 둘을 함께 적었다 | 완료 |
| 775 | **「헛돎」이 나오면 검사를 의심하기 전에 변이부터 보라**도 적었다. 이름 전체 치환·안 쓰는 상수 추가는 통과하는 것이 맞다 — 이번에 셋을 그렇게 헛짚었다 | 완료 |
| 776 | 같은 렌즈로 둘 더 겨눴다 — **씨앗 난수**(상수 반환·씨앗 무시)와 **분석 이벤트**(시각 고정·이벤트 조용히 버림). **넷 다 잡힌다** | 확인 |
| 777 | 이 렌즈(**늘 돌지만 실패가 안 보이는 것**)로 총 열 번쯤 겨눠 **둘**을 찾았다 — 품질 설정이 화면에 안 닿는 것, 텍스처 캐시 우회. 나머지 여덟은 이미 막혀 있었다 | 기록 |
| 778 | 구멍 둘의 공통점: **다른 장치가 「제대로 도는 것처럼」 보이게 만든다.** 강등 안내는 뜨는데 안 낮아지고, 캐시는 쌓이는데 안 읽힌다. **거짓 신호가 있는 실패**가 가장 늦게 발견된다 | 기록 |
| 779 | 새 기준(**실패해도 「정상」이라 말해 주는가**)으로 겨눠 **진짜 하나**를 찾았다 — 시작 화면은 「이 브라우저에 저장된다」고 **약속**하는데, 프라이빗 모드·용량 초과에서는 조용히 실패한다. 한참 놀다 새로고침하고서야 안다 | 수정 |
| 780 | `saveProgress`가 **성공 여부를 돌려주게** 하고, 실패하면 「이 브라우저에서는 진행이 저장되지 않습니다」를 **한 판에 한 번** 알린다. 삼키는 것 자체는 그대로다 — 저장 실패로 게임이 멈추면 안 된다 | 완료 |
| 781 | **기존 검사가 나를 잡았다** — 「근거 없는 한 방향 상태가 늘지 않는다」가 새 `warned` 플래그를 막았다. 완주 화면의 「닫음」이 안 꺼져 보스 완주 화면이 영영 안 뜬 사고에서 나온 규칙이라, 왜 한 방향인지 적고서야 통과했다 | 기록 |
| 782 | 동작으로 굳혔다 — 막히면 거짓, **막혀도 안 던진다**(그게 삼키는 이유다), 되면 참(거짓만 돌려주면 알림이 항상 뜬다). 변이로 확인: 실패를 성공이라 하니 **잡았다** | 확인 |
| 783 | 브라우저에서 `localStorage.setItem`을 던지게 막고 확인했다 — **월드가 정상으로 돌고 여정 진행 막대도 차오른다.** 설계의 「삼킨다」 절반(저장 실패로 게임이 멈추지 않는다)이 화면으로 확인됐다 | 확인 |
| 784 | **알림 자체는 못 봤다** — 저장은 여정 단계가 넘어갈 때 일어나는데 40m 낙하 중에는 화면당 0.2초라 단계 완료까지 프레임이 많이 든다. 나머지 절반은 **동작 검사로만** 확인된 상태다(거짓 반환·안 던짐·참 반환) | 기록 |
| 785 | 이건 이번 세션에 여러 번 본 자리다 — **가까이 붙거나 오래 기다려야 하는 것은 자동화로 확인이 비싸다.** 그래도 「무엇을 봤고 무엇을 못 봤는지」를 갈라 적어야 다음 사람이 헛짚지 않는다 | 기록 |
| 786 | 같은 기준으로 **사진 저장**을 뚫었더니 또 나왔다 — `downloadBlob`이 실패를 **성공으로** 돌려줘도 통과한다 | 수정 |
| 787 | 그러면 화면에 「…저장됨」이 뜨고 분석에 `photo_saved`까지 기록되는데 **파일은 없다.** 사용자는 나중에 폴더를 열고서야 알고, 그때는 그 장면이 지나갔다. 게다가 「사진을 몇 명이나 저장하나」라는 **신호까지 오염된다** | 수정 |
| 788 | 부르는 쪽은 이미 참/거짓으로 갈라 쓰고 있었다 — 문제는 **그 값이 진실인지**였다. 주소를 못 만들 때·내려받기가 막힐 때 각각 거짓을 돌려주는지, 되면 참을 돌려주고 파일 이름을 제대로 넘기는지 동작으로 굳혔다. 변이로 확인: **잡았다** | 완료 |
| 789 | 형제 모듈인 **설정 저장**도 봤다 — 실패를 삼키는데 **의도적이고 이유가 적혀 있다**(「사용자에게 알릴 만큼 중요한 실패는 아니다」). 판단이 옳다: 설정은 다시 고르면 되고 진행은 몇 시간이 사라진다 | 확인 |
| 790 | **그 비대칭을 짝으로 적었다.** 한쪽만 보면 「왜 여긴 안 알리지」나 「왜 여긴 알리지」로 읽혀 둘 중 하나를 맞춘다고 고치게 되고, 그러면 **잃는 것의 무게 차이가 사라진다** | 완료 |
| 791 | 이번 세션에 같은 처방을 세 번째 썼다 — 시간대 물려받기 ↔ 진행 물려받지 않기, 진행 저장 알림 ↔ 설정 저장 삼키기. **다르게 구는 두 코드는 반드시 함께 적어야** 다음 사람이 「통일」하지 않는다 | 기록 |
| 792 | 클립도 뚫었더니 나왔다 — 조각이 없을 때 `null` 대신 **빈 Blob**을 돌려줘도 통과한다. 빈 파일도 내려받기는 성공하므로 「저장됨」이 뜨고 `clip_saved`가 기록되는데 **0바이트짜리 열리지 않는 파일**이 남는다 | 수정 |
| 793 | 반환 지점이 둘(멈춘 녹화기 경로·`onstop` 경로)이라 **개수를 맞춰** 막았다 — 「어딘가 한 곳이 맞으면 통과」로 썼으면 하나를 뚫어도 지나갔을 것이다. 이번 세션에 배운 처방을 처음부터 적용했다 | 완료 |
| 794 | **거짓 성공 계열 결산: 다섯.** 품질 강등이 안 낮추는 것·텍스처 캐시 우회·저장 실패 침묵·사진 저장 거짓 성공·빈 클립 저장. 전부 **옆에서 무언가가 「됐다」고 말해 주는** 자리였다 | 기록 |
| 795 | 적중률이 가장 높았던 기준을 도구 설명에 넣었다 — **「실패해도 옆에서 무언가가 『됐다』고 말해 주는가」**. 「약속에서 고른다」보다 좁고 잘 맞는다 | 완료 |
| 796 | 다섯을 표로 적었다: 거짓 신호와 실제로 일어난 일을 나란히. 다음 사람이 **어디를 겨눌지**를 예시로 배우게 된다 — 규칙만 적으면 「내 코드엔 해당 없다」로 읽힌다 | 완료 |
| 797 | **내가 두 반복 전에 적은 짝 규칙이 너무 넓었다.** 「설정은 삼켜도 된다」고 했는데, 같은 저장 키에 **만난 도깨비와 이름**이 들어 있다 — 소리·모션과 달리 그건 **모은 것**이다 | 수정 |
| 798 | 게임이 「새 도깨비를 만났다」고 축하해 놓고 다음에 오면 사라져 있으면 그건 잃은 것이다. 그런데 그 저장은 **조용히 실패**했고, 진행 저장 알림은 다른 키라 뜨지 않는다 | 수정 |
| 799 | 저장 쪽은 **성공 여부만 돌려주고 알릴지는 부르는 쪽이 정하게** 했다 — 수집이 안 남으면 알리고, **취향은 여전히 조용하다**(다 알리면 알림이 화면을 덮는다). 그 구분도 검사로 굳혔다 | 완료 |
| 800 | **판단 기준을 「무엇이 저장되는가」가 아니라 「무엇을 잃는가」로 바꿨다.** 한 저장 키에 무게가 다른 것이 섞여 있으면 키 단위 규칙은 반드시 한쪽을 틀린다 | 기록 |
| 801 | 설정에 든 나머지를 무게로 갈랐다 — 소리·모션·품질·시간대·색보정·포즈·동료·외형은 **취향**, 이름은 다시 치면 되는 것, **만난 도깨비만 모은 것**이었다. 흔적은 애초에 진행 저장에 있어 이미 알림 대상이다 | 확인 |
| 802 | 이 세션에 검사를 120개 넘게 더했으니 다시 **섞어 돌렸다** — 1726개 전부 통과, **2.71초**. 순서 의존도 없고 느려지지도 않았다 | 확인 |
| 803 | 새로 넣은 화면 문구 둘을 점검했다 — 기존과 **말투가 같고**(「이 브라우저에서는 …지 않습니다」는 클립 미지원 안내와 같은 꼴), `role="status"`·`aria-live="polite"`로 **낭독기에도 전달된다** | 확인 |
| 804 | 그 낭독 검사는 이미 **`role="status"`를 쓰는 파일 전부**를 찾아 짝을 맞춘다 — 예전에 알림이 쪼개져 한 파일만 보다 놓친 적이 있어 일반화해 둔 것이다. 새 문구를 더해도 자동으로 덮인다 | 확인 |
| 805 | 저장 실패를 **눈으로만** 알리면 안 되는 이유: 그 순간 사용자는 화면 구석 토스트를 안 보고 있을 수도 있고, 낭독기 사용자는 아예 못 본다. **잃는 것을 알리는 문구일수록 전달 경로를 확인해야** 한다 | 기록 |
| 806 | 새 축(**화면에 수를 보여 주는 것**)으로 겨눠 하나 더 찾았다 — 하트 수를 `5`로 **박아도 통과한다.** 체력을 조정하는 날 화면은 그대로 다섯을 그리고, 플레이어는 「한 대 더 맞아도 되나」를 **틀린 수로 판단한다** | 수정 |
| 807 | 값을 박지 않고 **정본에서 오는지·그 수로 실제로 그리는지·숫자로 박은 곳이 없는지** 셋을 본다. 간판이 딴 가게를 읽어 주던 것과 같은 종류다 — 화면이 진실에서 떨어진다 | 완료 |
| 808 | **체력 최댓값 변경 자체는 결함이 아니다**(표시가 같은 상수를 쓰므로 튜닝이다). 처음에 그걸 「헛돎」으로 읽었다가 다시 겨눴다 — **변이가 진짜 결함인지 먼저 본다**를 또 확인했다 | 기록 |
| 809 | **자에 속은 열네째:** 창을 400자로 잡아 렌더 줄이 안 들어와 「읽어 놓고 안 그린다」로 헛짚었다. 900자로 넓히니 맞았다 | 기록 |
| 810 | 「화면이 수를 보여 주는 곳」을 마저 훑었다 — 도감은 `DOKEBI_ORDER.length`, 여정은 목표에서, 만난 도깨비는 `FINDABLE_DOKEBI`에서 온다. **HUD·시작 화면에 숫자로 박은 반복이 없다** — 하트가 유일했고 이제 막혔다 | 확인 |
| 811 | 새 축(**프레임 순서**)으로 겨눠 찾았다 — 씬에서 `<ClueGlow>`를 **통째로 지워도** 통과한다. 계산과 컴포넌트는 검사가 지키는데 **씬에 붙어 있는지**는 아무도 안 봤다 | 수정 |
| 812 | 그러면 도감이 「주변에 숨은 흔적을 잠깐 빛나게 한다」고 약속해 놓고 눈앞에서는 아무 일도 안 난다 — **이 기능이 애초에 그 결함을 고치려고 생긴 것**이다. 고친 결함이 조용히 되돌아갈 수 있었다 | 수정 |
| 813 | 순서도 함께 지킨다: `PlayerRig`가 되돌리고 → `Companion`이 빛 범위를 실어 주고 → `ClueGlow`가 읽는다. 동료보다 먼저 등록되면 **되돌려진 0을 읽어** 원반이 영영 안 뜬다 — **이번 세션에 내가 실제로 그렇게 만들었다** | 완료 |
| 814 | 변이로 둘 다 확인: 제거하니 잡히고, 동료 앞으로 옮기니 **순서 검사가 잡았다**(「동료는 5806번째, 표식은 5049번째」) | 확인 |
| 815 | 같은 유형을 씬 전체로 훑었더니 **셋 더** 나왔다 — `<Shrines>`·`<Crowd>`·`<Traffic>`을 통째로 지워도 통과한다. 특히 **도깨비 자리가 사라지면 수집이 아예 불가능**해지는데 아무도 안 본다(`<Enemies>`만 잡혔다) | 수정 |
| 816 | 컴포넌트마다 손으로 목록을 만들면 새 것이 늘 때 조용히 빠진다. **「들여온 것은 그린다」**는 규칙으로 바꿨다 — 안 그릴 거면 들여오지 말아야 하고 그건 죽은 import 검사가 따로 잡는다. 목록을 유지할 필요가 없다 | 완료 |
| 817 | 변이로 셋 다 확인했다. **이 축(「씬에 붙어 있는가」)이 이번 세션에서 가장 늦게 떠올랐는데 수확이 컸다** — 네 개가 나왔고 그중 하나는 게임을 못 하게 만든다 | 확인 |
| 818 | 이 축이 잘 통해서 **규칙을 씬 하나가 아니라 모든 `.tsx`로** 넓혔다. 전체를 재 보니 위반이 **하나뿐**이었다 — `ErrorBoundary`가 React `Component`를 상속용으로 들여오는 것(정당하므로 이유와 함께 예외로 뒀다) | 완료 |
| 819 | 이제 HUD도 덮인다 — `WorldHud`에서 `<Minimap>`을 지우니 **잡았다.** 화면 조각이 어떻게 나뉘든 「들여왔으면 그린다」 하나로 지킨다 | 확인 |
| 820 | 목록을 만들지 않은 것이 핵심이다. 컴포넌트가 늘 때마다 손으로 추가해야 하는 검사는 **반드시 낡는다** — 이번 세션에 그렇게 낡은 검사를 여럿 봤다(알림 파일이 쪼개져 놓친 낭독 검사, 캐시가 셋인데 하나만 보던 검사) | 기록 |
| 821 | 「화면에 연결되어 있는가」 축을 **조작과 분석**으로도 옮겼다 — 키 처리 제거(춤·음료)와 분석 이벤트 제거(`clue_found`), **셋 다 잡힌다.** props는 타입이 지키므로 이 축이 안 통한다 | 확인 |
| 822 | 분석 이벤트는 **선언 15개가 전부 발생**한다(세어 확인). 하나가 조용히 죽으면 「사람이 무엇을 하는지」를 보는 유일한 통로에 구멍이 나는데, 지금은 막혀 있다 | 확인 |
| 823 | 이 축의 수확은 **화면 쪽에만** 있었다 — 씬·HUD 컴포넌트 넷. 조작·분석·props는 이미 다른 방식(검사·타입)으로 묶여 있었고, **화면에 붙이는 일만 아무도 안 보고 있었다** | 기록 |
| 824 | 새 축(**디자인 토큰**)으로 겨눴다 — 컴포넌트에 색을 박아도 통과한다. 명암비 검사는 **토큰만** 보므로 박아 둔 색은 **아예 검사 밖으로 나간다** | 수정 |
| 825 | 실제로 하나 있었고 **하필 가장 중요한 글자**였다 — 보스의 「내려친다 — 피해!」 경고가 `text-[#ff8a3d]`였다. 그 값은 이미 토큰(`--color-brand-sunset`)인데 복사해 둔 것이라, **토큰을 바꾸면 이 경고만 안 따라온다.** 토큰 별칭(`text-sunset`)으로 고쳤다 | 수정 |
| 826 | 규칙은 좁게 잡았다 — 시작 화면의 노을 그라디언트 같은 **일러스트는 예술 자산이라 막지 않고**, 막는 것은 **글자와 면에 색을 박는 유틸리티**다. 그쪽이 명암비가 걸리는 자리다. 변이로 확인: **잡았다** | 완료 |
| 827 | 같은 축의 접근성 쪽에서 하나 더 — **최소 터치 크기**를 시작 화면에서 `16px`로 줄여도 통과한다(오류 화면 쪽은 잡혔다). **목록이지 규칙이 아니었다** | 수정 |
| 828 | 손가락 크기에서 온 값이라 화면마다 다르게 정할 이유가 없다. **px로 박는 것**을 막았다 — `dvh`·`rem`은 레이아웃이라 놔두고, px로 적은 최소 높이가 곧 「손가락 크기를 손으로 정했다」는 뜻이다 | 완료 |
| 829 | `global-error.tsx`만 예외다 — **앱 스타일시트가 실리지 않는 화면**이라 토큰을 못 쓴다. 이번 세션 앞부분에서 그 `44px`를 결함으로 고칠 뻔했다가 이유를 확인한 자리이기도 하다 | 기록 |
| 830 | 토큰 축의 나머지를 훑었다 — 여백·모서리에 박아 둔 것은 **둘뿐이고 둘 다 정당**하다(하트 2px·지도 표식 3px는 가장 작은 토큰 8px보다 작은 장식이다) | 확인 |
| 831 | **모션 토큰 셋은 정의만 있고 안 쓰인다.** 죽은 토큰으로 보고 지울 뻔했는데, 파일 머리말이 「**Figma 변수명과 1:1로 유지**해야 하므로 이름을 임의로 바꾸지 않는다」고 못 박아 두었다 — 코드가 아니라 **디자인 도구와의 계약**이다 | 확인 |
| 832 | 「안 쓰이면 지운다」가 늘 옳지는 않다는 사례다. **무엇과의 약속인지**를 보지 않고 사용처만 세면 계약을 끊는다 — 고칠 뻔한 것 아홉째 | 기록 |
| 833 | 공유 카드(메타데이터) 축은 **막혀 있다** — `openGraph` 블록을 통째로 지우니 잡힌다. 처음엔 키 이름만 바꿔 「헛돎」이 나왔는데 **그건 타입이 잡는 종류**라 나쁜 변이였다(이번 세션 네 번째) | 확인 |
| 834 | **탐색 축 결산.** 화면 연결(넷)·거짓 성공(다섯)·눈에 안 보이는 실패(셋)·디자인 토큰(둘)·글과 동작(둘)에서 나왔고, 조작·분석·props·메타데이터·드문 상태·난수·컬링은 이미 막혀 있었다 | 기록 |
| 835 | 새 축 — **확인 지점의 스폰 좌표**. 기존 검사는 **월드 범위 안**인지만 봤다. 범위 안이어도 **건물 안**이면 확인하러 온 사람이 첫 화면에서 벽에 갇힌다 | 완료 |
| 836 | 재 보니 **지금은 전부 정상**이다. 그래도 검사를 넣었다 — 좌표는 `BOSS_HOME.z + 14`처럼 손으로 고르는데, **도시 배치가 바뀌면 그 자리가 건물이 될 수 있고** 아무도 다시 재지 않는다. 새 확인 지점을 더하는 날을 위한 것이다 | 완료 |
| 837 | **배선 축에서 하나 더** — 이동 입력의 `speedScale`을 `1`로 박아도 통과한다. 계산은 지켜지는데 **그 값을 이동에 넘기는 일**을 아무도 안 봤다 | 수정 |
| 838 | 그러면 조작 안내는 「F 음료」라 말하고 자판기는 반응하는데 **몸은 안 빨라진다.** 흔적 표식이 씬에 안 붙어 있던 것과 같은 종류다 — **모듈은 맞고 배선이 없다** | 수정 |
| 839 | 도깨비·보스 자리, 흔적 좌표는 이미 두텁다(걸어서 닿는지·설 자리가 있는지·소품과 안 겹치는지). 손으로 고른 좌표 중 **확인 지점 스폰만** 빈칸이었고 앞 반복에서 막았다 | 확인 |
| 840 | 배선 축에서 **둘 더** — `reducedMotion={false}`로 군중·차량의 배선을 끊어도 통과한다. 게이트 개수 래칫은 **컴포넌트 안의 장치**를 지키는데, **값이 전달되지 않으면** 설정을 켜도 그 조각만 계속 흔들린다 | 수정 |
| 841 | 장치는 멀쩡한데 **아무도 스위치를 안 눌러 준 꼴**이다. 값을 박아 넘기는 것을 규칙으로 막았다 — 지금 리터럴은 0개이고, 새 컴포넌트가 늘어도 자동으로 덮인다 | 완료 |
| 842 | **배선 축 결산: 일곱.** 씬 컴포넌트 넷(표식·도깨비 자리·군중·차량) · 음료 속도 · 저감 모션 배선 둘. 전부 **「만든 것」은 두텁게 검사되는데 「연결한 것」은 한 줄이라 아무도 안 적은** 자리였다 | 기록 |
| 843 | 배선 축에서 **둘 더** — `nightGlow={0}`(밤 발광이 통째로 꺼진다)과 `worldHalfExtent={0}`(조명 범위가 무너진다)으로 끊어도 통과했다 | 수정 |
| 844 | 개별로 막지 않고 규칙으로 잡았다 — **씬은 배선판이라 여기서 숫자를 직접 적는다는 것은 곧 어딘가에서 계산한 값을 버렸다는 뜻**이다. 지금 그런 곳은 0개이고, 새 배선이 늘어도 자동으로 덮인다 | 완료 |
| 845 | **배선 축 결산: 아홉.** 이 축이 이번 세션 최대 수확이 됐다. 규칙 넷으로 덮었다 — 들여온 컴포넌트는 그린다 · 저감 모션을 박아 넘기지 않는다 · 음료 속도를 박지 않는다 · 씬에 숫자를 박지 않는다 | 기록 |
| 846 | **처음 쓴 규칙이 절반만 잡았다.** `^\\s+prop={` 로 줄머리에 묶어서, 한 줄에 몰아 쓴 `<WorldLighting quality={q} worldHalfExtent={0} preset={s} />` 를 통째로 놓쳤다 — 그 변이가 그대로 빠져나갔다 | 수정 |
| 847 | 줄머리 묶음을 풀고 다시 재니 둘 다 잡힌다. **서식에 기대는 검사는 서식이 바뀌면 조용히 눈이 먼다** — 이번 세션에 같은 실수를 세 번째 했다(한 줄 호출 정규식·400자 창·줄머리 묶음) | 기록 |
| 848 | 세 번 당한 함정(**서식에 기대는 검사**)을 도구 설명에 넣었다 — 한 줄 호출 전제·400자 창·줄머리 묶음 셋을 사례로 적고 처방을 붙였다(공백을 견디게, 창은 넉넉히, 줄머리에 묶지 마라) | 완료 |
| 849 | 이제 검사 쓰는 사람을 위한 함정이 셋 적혀 있다 — **어딘가 한 군데만 맞으면 통과** · **서식에 기대면 눈이 먼다** · **헛돎이면 변이부터 의심**. 셋 다 내가 이 세션에 직접 당한 것이다 | 기록 |
| 850 | 교훈을 **내 검사들에도** 적용했다 — 좁은 창을 쓴 곳을 찾아 가장 좁은 것(`{0,40}`)을 200자로 넓혔다. 주석 한 줄만 끼어도 눈이 멀 자리였다 | 수정 |
| 851 | 서식 변화(주석 두 줄 추가)를 실제로 넣어 **여전히 통과하는지** 확인했다. 결함이 아닌 변화에는 견디고 결함에는 무는 것이 좋은 검사다 | 확인 |
| 852 | 새 기법을 **넓게** 적용했다 — 씬 배선·설정·HUD 셋에 주석을 끼워 서식만 바꿨더니 **1746개가 그대로 통과**한다. 결함이 아닌 변화에는 견딘다 | 확인 |
| 853 | 이 기법을 도구 설명에 **변이의 짝**으로 적었다. 지금까지는 「결함을 넣으면 무는가」만 봤는데, **서식만 바꿔도 깨지는 검사는 다음 사람이 검사를 고치는 데 시간을 쓰게** 만든다 — 한쪽만 재면 그런 검사를 좋은 검사로 착각한다 | 완료 |
| 854 | 견딤 검사를 **저장소 전체로** 넓혔다 — 소스 14개에 주석을 끼우고(마지막엔 함수마다 여러 줄 주석까지) 돌렸더니 **깨진 검사 0개**. 이 저장소의 검사들은 서식에 안 흔들린다 | 확인 |
| 855 | 이건 뜻밖이었다. 소스를 읽는 검사가 많아 약할 줄 알았는데 **정작 서식에 약했던 것은 이 세션에 내가 쓴 셋뿐**이었다 — 기존 검사들은 이미 공백을 견디게 쓰여 있었다 | 기록 |
| 856 | 교훈이 뒤집힌다: 「소스를 읽는 검사 = 약한 검사」가 아니다. **약한 것은 지금 코드 모양을 보고 급히 쓴 검사**이고, 그건 소스를 읽든 동작을 보든 마찬가지다 | 기록 |
| 857 | 근거가 나왔으니 **내가 쓴 안내를 고쳤다** — 「소스를 읽는 검사가 약하다」가 아니라 「**급히 쓴 검사**가 약하다」. 근거(소스 14개에 주석을 끼워도 기존 검사는 하나도 안 깨진다)도 함께 적었다 | 수정 |
| 858 | 안내를 쓴 지 두 반복 만에 고친 셈인데, 그 사이에 **재 보았기 때문**이다. 처음 쓸 때는 내 실수 세 건만 근거였고 그것으로 저장소 전체를 판단했다 | 기록 |
| 859 | **검사에 이름조차 안 나오는 소스 3개**를 찾았다(103개 중) — `DowngradeNotice`·`GrappleVisuals`·`FilterOverlay`. 이름이 없다는 것 자체가 「아무도 안 본다」의 신호다 | 확인 |
| 860 | 셋을 화면에서 떼 보니 **전부 잡혔다** — 씬 마운트 규칙이 파일 이름이 아니라 **규칙 모양**으로 쓰여 있어서, 이름이 검사에 없어도 배선은 지켜지고 있었다 | 확인 |
| 861 | 그런데 `FilterOverlay` **안**에서 구멍 넷이 나왔다: `depthTest`·`depthWrite`·`frustumCulled`·`renderOrder`를 하나씩 뒤집어도 아무 검사가 몰랐다 | **결함** |
| 862 | 넷 다 **그 파일 주석이 이미 못 박은 제약**이었다. 「깊이 판정을 끄고 렌더 순서를 마지막으로 민다」라고 적혀 있는데 지키는 것은 아무도 없었다 — 글로만 있던 규칙이다 | 기록 |
| 863 | 증상이 넷 다 다르다: 깊이 판정이 켜지면 앞을 지나는 동료에게 가려지고, **시야 판정이 켜지면 통째로 사라지며**(위치를 매 프레임 바꿔 경계 구가 낡는다), 순서가 앞이면 투명한 것들 밑에 깔리고, 톤매핑이 켜지면 고른 색이 안 나온다 | 기록 |
| 864 | 넷을 한 규칙으로 막았다. `renderOrder`는 값을 고정하지 않고 **100 이상**만 본다 — 999를 박으면 조정할 때마다 검사가 깨진다 | 검사 |
| 865 | 넣은 뒤 **양쪽을 다 쟀다**: 결함 넷은 전부 물고, 속성을 여러 줄로 풀어 쓴 서식 변화에는 견딘다. 이제 이게 검사를 쓰고 나서의 기본 절차다 | 확인 |
| 866 | 「이름조차 안 나오는 파일」은 값싸고 좋은 표적이었다 — 한 번 훑어 파일 세 개를 좁혔고 그 중 하나에서 구멍 넷이 나왔다. 표적 고르기 목록에 넣는다 | 기록 |
| 867 | 앞 발견을 **표적 고르는 법으로 일반화**했다: 「주석이 못 박은 제약」을 전부 긁어 봤다 — 58개가 나왔다(`반드시`·`금지`·`~하면 안 된다`) | 확인 |
| 868 | 값·구조로 잴 수 있는 것들을 뒤집어 봤다. `quality.fogFar`는 지키는 검사 파일 이름까지 주석에 적혀 있었고 실제로 지켜졌다. 팔레트 순서도 잡혔다 | 확인 |
| 869 | `settings.getServerSettingsSnapshot`이 호출마다 새 객체를 돌려주게 바꿔도 통과했다. 주석은 「무한 렌더로 이어진다」고 적혀 있다 — **화면이 멈추는 급인데 글로만 있었다** | **결함** |
| 870 | `atlasTextures`의 감싸기를 Repeat으로 바꿔도 통과했다. 주석은 「반드시 ClampToEdge」라고 적혀 있다 — 간판 가장자리에 반대쪽 칸 색이 새는 증상이다 | **결함** |
| 871 | 스냅샷은 **값이 아니라 같은 것인지**(`toBe`)로 본다. `toEqual`로 쓰면 새 객체를 돌려줘도 통과해 아무것도 안 막는다 — 이 세션에 반복해서 걸린 「어딘가 한 군데만 맞으면 통과」의 사촌이다 | 검사 |
| 872 | 감싸기는 **두 파일을 같이** 본다. 타일은 반복해야 이어지고 아틀라스는 반복하면 샌다 — 정반대다. 한쪽만 보면 규칙을 헷갈려 반대로 고쳐도 통과한다 | 검사 |
| 873 | 넷 다 다시 넣어 무는 것을 확인했다(아틀라스 새게·타일 안 이어지게·스냅샷 새 객체·스냅샷 안 얼림) | 확인 |
| 874 | 새 표적 고르는 법이 자리 잡았다: **「주석이 못 박았는데 아무도 안 지키는 것」.** 코드베이스가 스스로 어디가 중요한지 적어 둔 곳이라 명중률이 높다 — 58개 중 값으로 잴 수 있는 것만 봐도 둘이 나왔다 | 기록 |
| 875 | 주석 제약을 계속 뒤집었다. `PerfPanel`의 「안 잰 상태를 0으로 보여 주지 않는다」와 팔레트 순서는 잡혔다 | 확인 |
| 876 | `MAX_BLIPS`를 24→240으로 올려도 통과했는데 **이건 결함이 아니다.** 관계 검사(버퍼 길이·최대 적 수)는 이미 있고, 24는 조정값이다. 「헛돎이면 변이부터 의심」이 또 맞았다 | 기록 |
| 877 | `Companion`이 사라지는 동료를 **지도에 계속 찍게** 바꿔도 통과했다. 화면에 안 보이는데 지도에는 점이 남으면 그 자리에 쫓아간 사람은 아무도 못 만난다 | **결함** |
| 878 | 문턱을 `showsOnMap`으로 뺐다 — 바로 옆 `canUseAbility`가 이미 같은 모양이라 구조를 새로 만들지 않았다. 숫자가 이름을 얻었고 값으로 잴 수 있게 됐다 | 수정 |
| 879 | 검사는 문턱 숫자를 안 박고 **관계**만 본다: 다 나타나면 찍고, 다 사라지면 안 찍고, 점 문턱이 능력 문턱보다 낮다(점은 「저기 있다」지만 버튼은 눌러서 돼야 한다) | 검사 |
| 880 | 그런데 순수 함수를 물게 해 놓고도 **화면 쪽에서 `= true`로 되돌리면 통과했다.** 규칙은 지켜지고 배선만 끊긴 상태 — 이 저장소에서 가장 자주 나온 결함 모양 그대로다 | **결함** |
| 881 | 그래서 **규칙 모양**으로 막았다: 공유 객체 필드가 **어디서도 계산된 값을 안 받으면** 실패. 앞 검사(「쓰기만 하고 안 읽는 값」)의 짝이다 — 저쪽은 안 읽히는 값, 여기는 늘 같은 값 | 검사 |
| 882 | 조건부 초기화(`if (...) x = false;`)에는 **일부러 안 걸리게** 했다. 「그 필드가 어디서도 계산 값을 안 받을 때만」으로 좁혔고, 실제로 초기화를 끼워 넣어 통과하는 것까지 확인했다 | 확인 |
| 883 | 지금 소스에는 걸리는 곳이 0개다 — 규칙을 넣기 전에 헛걸림부터 세어 봤다. 0이 아니었으면 규칙이 틀렸거나 결함이 더 있다는 뜻이었다 | 확인 |
| 884 | `mathx`의 「따라붙는 동작에는 **반드시** damp를 쓴다」를 어겨 봤다 — 카메라 fov를 `lerp(현재, 목표, 0.1)`로 바꿔도 전부 통과했다 | **결함** |
| 885 | 지금 소스에 어기는 곳은 **없었다.** 규칙은 지켜지고 있는데 지키게 하는 것이 아무것도 없던 상태 — 「글로만 있는 규칙」의 또 다른 얼굴이다 | 기록 |
| 886 | 이 부류가 특히 고약하다: 검사도 통과하고 화면도 멀쩡한데 **모니터마다 조작감이 달라진다**(120Hz면 60Hz의 두 배로 붙는다). 사람이 「감도가 이상하다」고 느껴도 원인을 짚기 어렵다 | 기록 |
| 887 | 규칙 모양으로 막았다: **매 프레임 도는 파일에서만** 상수 계수 보간을 금한다(`useFrame`이 있거나 `dt`를 쓰는 파일). 정적인 섞기(중간색·중간점)까지 막으면 시끄럽다 | 검사 |
| 888 | 양쪽을 다 쟀다 — fov를 프레임률에 끌리게 하면 물고, 매 프레임과 무관한 파일의 `lerp(0, 1, 0.5)`에는 견딘다 | 확인 |
| 889 | 사람 몫 목록의 「속도감·감도」와 이어지는 발견이다. 사람이 그걸 확인해 주기 **전에** 프레임률 의존이 새로 들어오면 그 판단이 통째로 흐려진다 | 기록 |
| 890 | 여정 기준값을 뒤집었다. `createQuestProgress`의 `baseline`을 0으로 바꿔도 통과했다 — **여정을 처음 여는 순간**은 아무도 안 보고 있었다 | **결함** |
| 891 | 같은 규칙의 검사가 이미 있었는데 **단계를 넘길 때만** 봤다. 같은 규칙이 두 자리에 있으면 한 자리만 지켜지기 쉽다 — 리뷰 규칙의 「경계면 교차 검증」이 여기서도 맞는다 | 기록 |
| 892 | 사람이 겪는 모습: 도시를 돌며 스무 기를 잡아 둔 사람이 새 여정을 열면 **첫 단계가 시작하자마자 완료된다.** 할 일을 안내받기도 전에 끝나 있다. 확인 지점으로 여정을 되돌릴 때도 같은 경로다 | 기록 |
| 893 | 검사는 기준값 필드를 직접 안 본다 — **진행률**로 본다. 필드 이름이 바뀌어도 규칙은 그대로여야 하고, 사람이 겪는 것은 필드가 아니라 진행률이다 | 검사 |
| 894 | 두 자리를 다 넣어 봤다: 새 여정 기준값·단계 넘김 기준값 둘 다 문다. 기존 검사가 자기 자리를 지키는 것도 함께 확인했다 | 확인 |
| 895 | 첫 스폰(`광장 한가운데`)은 이미 잡혔다 — 14m 옮기니 바로 물었다 | 확인 |
| 896 | 앞 발견을 표적 고르는 법으로 굳혔다: **소스에서 똑같이 반복되는 줄**을 세어 각각 뒤집는다. 278개가 나왔고 그 중 `Math.min(rawDelta, MAX_DELTA_SECONDS)`(5곳)가 눈에 띄었다 | 확인 |
| 897 | 프레임 루프 전체를 훑으니 `Shrine`만 공유 상수 대신 **`1 / 30`을 직접 박고** 있었다. 값은 같아 지금 안 아프지만 상수를 조정하면 그 하나만 안 따라온다 | **결함** |
| 898 | 그 상수의 주석이 **바로 이 사고를 적어 두었다** — 「파일마다 따로 두었더니 **일곱 벌**이 됐고, 한 곳만 고치면 그 컴포넌트만 다른 시간을 살게 된다」. `Shrine`이 여덟 벌째였다 | 기록 |
| 899 | 검사는 **값이 아니라 출처**를 본다. 「지금 같은가」로 보면 여덟 벌이어도 통과한다 — 이 저장소가 이미 한 번 겪은 실패다 | 검사 |
| 900 | 처음엔 「delta를 받는 루프는 전부 잘라야 한다」로 썼다가 **되돌렸다.** `GrappleVisuals`·`ClueGlow`가 걸렸는데 둘은 delta를 사인 위상에만 더한다 — 숨쉬기 빛의 위상이 튀는 건 결함이 아니다(탭을 두고 오면 위상은 어차피 임의다) | 기록 |
| 901 | **규칙이 결함 아닌 것을 물면 코드를 규칙에 맞추게 되고, 그건 거꾸로다.** 코드를 안 고치고 규칙을 좁혔다 — 「테스트가 아니라 구현을 고친다, 테스트가 틀린 경우 제외」의 그 예외다 | 기록 |
| 902 | 이 과정에서 **내 자가 두 번 갈렸다**: 처음 훑을 때 `rawDelta`로만 세어 `delta`로 받는 두 파일을 못 봤다. 나중 자가 그것을 찾아냈고, 그 덕에 규칙이 과하다는 것도 알았다 | 기록 |
| 903 | 반복 줄 표적을 이어갔다. `(gridSize - 1) / 2`가 **소스 13곳**에 복제돼 있어 하나씩 `gridSize / 2`로 바꿔 봤다 | 확인 |
| 904 | **열한 곳은 잡히고 두 곳이 통과했다** — 군중(`crowdLayout`)과 지도(`minimap`). 「같은 규칙이 여러 자리에 있으면 일부만 지켜진다」가 13벌 규모에서도 그대로였다 | **결함** |
| 905 | 사람이 겪는 모습: 보행자가 인도가 아니라 **건물 속을 걷고**, 지도를 보고 찾아간 자리에 아무것도 없다 | 기록 |
| 906 | 바로 위 도로 대조 검사의 주석이 「실제로 **roster와 minimap이 반 칸 어긋난** 채 오래 굴러갔다」고 적어 뒀다. 그 사고를 겪고도 정작 minimap의 구역 중심 공식은 아무도 안 보고 있었다 | 기록 |
| 907 | 공식을 베끼지 않고 **도로 정본과의 관계**로 봤다 — 구역 중심은 이웃한 두 도로 중심의 한가운데다. 같은 식을 다시 쓰면 둘 다 틀려도 서로 맞는다고 나온다 | 검사 |
| 908 | 스폰이 지도가 아는 구역 중심 하나와 같은지도 함께 본다. 여기가 어긋나면 시작하자마자 지도와 발밑이 다르다 | 검사 |
| 909 | 정본이 비면 어떤 좌표든 통과하므로 **도로 목록이 비어 있지 않은지부터** 잰다. 이 세션에 자를 여러 번 놓쳐서 이제 기본으로 넣는다 | 검사 |
| 910 | `blockPitch`(구역 간격)도 열 곳에 복제돼 있어 하나씩 뒤집었다. 아홉은 잡히고 **`City.tsx`의 보도블록 하나가 통과**했다 | **결함** |
| 911 | 어긋나면 **보도블록이 도로 위에 깔리고 구역 사이가 빈다.** 차도와 보도를 높이로 구분하는 게 이 판의 목적인데 그 구분이 어긋난다 | 기록 |
| 912 | `cityLayout`에 이미 정본 `blockCenter`가 있었는데 **export를 안 해서** 다들 베끼고 있었다. 내보내고 부르게 했다 — 새 상수를 만들지 않고 있던 것을 열었다 | 수정 |
| 913 | `cityDetails`의 지역 사본도 지웠다. 그 주석이 **베낀 이유를 적어 뒀다** — 「cityLayout이 export하지 않는다」. 이유가 사라졌으므로 사본도 사라진다 | 수정 |
| 914 | 그래도 **배선은 여전히 안 물렸다** — 정본을 검사로 묶어도 화면 쪽에서 딴 값을 쓰면 통과했다. `showsOnMap` 때와 똑같다 | **결함** |
| 915 | 보도블록 배치를 `buildSidewalkSlabs`로 빼서 값으로 재게 했다. 화면 안(useMemo)에 있으면 좌표가 틀려도 잴 데가 없다 — 이 저장소에서 가장 자주 나온 결함 모양이다 | 수정 |
| 916 | 뺀 뒤 배선을 끊어 보니 문다. 「순수 함수로 빼고 → 값으로 재고 → 배선을 끊어 확인」이 이제 한 벌의 절차다 | 확인 |
| 917 | 리팩터를 **기존 검사 둘이 잡았다**: 파일 크기 상한(cityDetails 809줄)과 「렌더가 상수를 직접 읽는가」. 둘 다 정당한 신호였다 | 확인 |
| 918 | 크기 상한이 좋은 질문을 대신 해 줬다 — 「이 판은 도시 세부와 다른 책임인가」. 답이 그렇다여서 `sidewalks.ts`로 뺐다. 상한이 설계를 물은 셈이다 | 기록 |
| 919 | 상수 검사는 **자리가 바뀌어서** 깨진 것이지 결함이 아니었다. 파일 하나를 지목하는 검사는 코드가 옮겨 다닐 때마다 깨지고, 옮겨 간 자리에 숫자를 박아도 모른다 | 기록 |
| 920 | 「숫자 0.16을 어디서도 박지 마라」로 넓혔다가 **되돌렸다** — 팔 두께·숨쉬기 진폭·음량에도 0.16이 있다. 결함 아닌 것을 무는 규칙이었다. 두 반복 연속 같은 함정이다 | 기록 |
| 921 | 글자 대신 **깔린 결과**를 재게 고쳤다: 판의 두께가 정본과 같은가, 윗면이 인도 높이에 오게 반만큼 띄웠는가. 파일이 어디로 가든 숫자를 어떻게 쓰든 걸린다 | 검사 |
| 922 | 교훈: 소스를 읽는 검사가 깨지면 **결함인지 이사인지부터** 가른다. 이사라면 검사를 옮기지 말고 **값으로 재는 쪽으로 바꿀 기회**다 | 기록 |
| 923 | 보스 위상 타이머도 같은 줄이 **네 벌**이라 하나씩 「시간을 0으로 굳힌다」로 바꿔 봤다 — **셋이 통과**했다(기절 하나만 잡혔다) | **결함** |
| 924 | 가장 아픈 것은 예고(`windup`)다. 한 프레임만 보이면 **피할 시간이 없는데 화면에는 여전히 링이 뜬다** — 보스전에서 읽고 반응하는 재미가 통째로 사라진다 | 기록 |
| 925 | 내려찍기는 순간에 끝나 맞았는지도 모르고, 눕힌 대장은 곧바로 다시 서서 이긴 실감이 없다 | 기록 |
| 926 | **「단계가 바뀌는가」가 아니라 「얼마나 머무르는가」**를 잰다. 전이만 보면 시간이 0이어도 통과한다 — 실제로 그렇게 통과하고 있었다 | 검사 |
| 927 | 사람 몫의 「보스 링 타이밍과 손맛」과 바로 이어진다. 사람이 그걸 봐 주기 전에 지속 시간이 새로 무너지면 그 판단이 통째로 흐려진다 | 기록 |
| 928 | 네 자리 다 다시 굳혀 보고 전부 무는 것을 확인했다 | 확인 |
| 929 | 같은 결함 모양을 일반 적에게도 걸어 봤다 — **쓰러진 시간이 뚫려 있었다.** 「쓰러진 적은 추격을 재개하지 않는다」·「시간이 지나면 일어난다」 둘 다 **한 프레임씩만** 본다 | **결함** |
| 930 | 사람이 겪는 모습: 때려 눕혀도 곧바로 다시 서니 **한 구역을 정리했다는 감각이 아예 없다.** 게다가 일어날 때 스폰 지점으로 돌아가므로 눈앞에서 사라졌다 멀리서 나타나는 것처럼 보인다 | 기록 |
| 931 | 경직(`hit`)도 함께 막았다 — 없으면 때린 손맛이 사라진다. 플레이어 휘두르기 단계는 이미 잡히고 있었다 | 검사 |
| 932 | **한 모듈에서 찾은 결함 모양은 이웃 모듈에도 있다.** 보스 → 일반 적으로 옮겨 걸어 바로 하나 더 나왔다. 표적 고르는 법에 넣는다 | 기록 |
| 933 | 자를 동료에게도 옮겨 댔다. 능력이 **닳는 속도**는 잡히는데 둘이 뚫려 있었다 — 재사용 대기를 0으로 만들어도, 등장·퇴장 시간을 없애도 통과했다 | **결함** |
| 934 | 대기가 없으면 능력 버튼이 **꺼지지 않는다.** 계속 누르면 되니 「언제 쓸까」를 고를 이유가 없어지고, 흔적 찾기가 그냥 켜 두는 일이 된다 | 기록 |
| 935 | 등장이 즉시면 동료가 **팝 하고 나타났다 사라진다.** 부르고 보내는 것이 연출이 아니라 스위치가 된다 | 기록 |
| 936 | 세 모듈(보스·적·동료)에서 **같은 자 하나로 여섯 구멍**이 나왔다. 「전이만 보고 지속을 안 본다」가 이 저장소에서 두 번째로 흔한 결함 모양이다 (첫째는 배선) | 기록 |
| 937 | 자를 화면 안내에도 댔다. 대사에는 「0이면 뜨자마자 사라진다」 검사가 있었는데 **HUD 안내 둘에는 없었다** — 0.05초로 줄여도 통과했다 | **결함** |
| 938 | 잃는 것이 크다: 해금 알림은 **수집의 순간 그 자체**다(그 파일 주석이 「이게 없으면 도감을 열기 전까지 아무도 알려 주지 않는다」고 적어 뒀다). 구역 배너는 넓은 도시에서 「어디쯤 왔다」의 유일한 단서다 | 기록 |
| 939 | 값의 범위와 **둘 사이의 관계**를 함께 잰다. 관계도 이미 글로 있었다 — 「해금 알림은 구역 배너보다 길다, 처음 보는 이름이라」 | 검사 |
| 940 | 소멸 연출(1.4초)과 대사(4초)는 이미 잡히고 있었다. **먼저 쓴 검사가 있는 곳은 대체로 지켜지고, 나중에 붙은 화면 조각이 비어 있다** | 기록 |
| 941 | 「나중에 붙은 것이 비어 있다」를 파일 수정 시각으로 재려다 **못 썼다** — 복원이 mtime을 바꿔서 자가 내 변이 흔적만 재고 있었다. 자를 먼저 의심하는 습관이 또 값을 했다 | 기록 |
| 942 | 대신 **내보낸 이름 중 검사가 부른 적 없는 것**을 셌다: 523개 중 138개. 타입·팔레트를 빼면 진짜 후보는 몇 안 됐고 그 중 하나가 **입력**이었다 | 확인 |
| 943 | `consumeJump`·`consumeGrapple`이 큐를 안 비우게 바꿔도 통과했다. 사람에게 **가장 가까운 표면**인데 검사가 이름조차 부른 적이 없었다 | **결함** |
| 944 | `InputState` 주석이 또 미리 적어 뒀다 — 「누른 상태를 그대로 넘기면 **의도치 않은 연속 점프**가 된다」. 이 세션에 같은 모양을 몇 번째 보는지 모르겠다 | 기록 |
| 945 | 사람이 겪는 모습: 점프를 안 비우면 한 번 눌러 계속 떠오르고, 그래플은 누르지도 않았는데 매 프레임 날아간다. **게임이 아니라 조작이 고장 난 것처럼** 느껴진다 | 기록 |
| 946 | 「돌려주는 값이 맞는가」가 아니라 **「두 번째에도 나가는가」**를 잰다. 한 번만 부르면 비우든 안 비우든 참이라 못 잡고 있었다 | 검사 |
| 947 | 반대쪽도 넣었다 — 소비한 뒤 다시 누르면 또 나가는가. 「안 나가게」 막는 검사만 있으면 아예 죽은 조작도 통과한다 | 검사 |
| 948 | 안 부르는 export를 이어 훑어 `atlasCellUv`를 봤다 — **셋 다 뚫려** 있었다. 도시의 간판·소품 그림이 **전부** 이 함수를 지난다 | **결함** |
| 949 | 증상이 셋 다 다르다: 감싸기가 없으면 셀 번호가 넘칠 때 아틀라스 밖을 가리켜 간판이 깨지고, 세로 뒤집기가 빠지면 **모든 간판이 엉뚱한 줄의 그림**을 달고, 여백이 없으면 밉맵이 이웃 셀을 물어 가장자리에 남의 색이 낀다 | 기록 |
| 950 | 세 번째는 앞서 막은 감싸기 방식(ClampToEdge)과 **같은 가족**이다. 「한 곳에서 찾은 모양은 이웃에도」가 또 맞았다 — 아틀라스가 새는 길이 둘이었다 | 기록 |
| 951 | 실제 아틀라스는 캔버스가 있어야 만들어지므로 **치수만 있는 대역**을 썼다. 이 함수가 읽는 것은 `image.width/height`와 격자 수뿐이라 그것으로 충분하다 | 검사 |
| 952 | 「첫 셀이 위쪽 끝에 딱 붙는다」로 적었다가 걸렸다 — **틀린 것은 코드가 아니라 내 기대**였다. 여백만큼 안쪽에 있는 게 맞고, 그건 바로 옆 검사가 요구하는 바다 | 기록 |
| 953 | 여백이 **px 기준**이라는 것도 잰다: 큰 텍스처일수록 깎이는 비율이 작아야 한다. 비율로 바꿔 놓으면 큰 아틀라스에서 그림이 필요 이상으로 잘린다 | 검사 |
| 954 | 안 부르는 export를 이어 훑어 **발소리·점프·착지 전체가 비어 있는 것**을 찾았다. 리듬 이월을 버려도, 보드 타며 발소리가 나게 해도, 점프 소리를 아예 꺼도 통과했다 | **결함** |
| 955 | 소리 내는 일과 **언제 낼지**를 갈라 `audio/footsteps.ts`로 뺐다. 오디오 루프 안에 있으면 값으로 잴 데가 없다 — 이번 세션에 여러 번 쓴 그 절차다 | 수정 |
| 956 | 리듬은 **걸음 수**로 잰다. 「울리기는 하는가」만 보면 이월을 버려도 통과한다 — 10초 달리기에 기대 걸음 수와 맞는지 본다 | 검사 |
| 957 | 배선을 끊어 보니 **기존 「죽은 export」 검사가 잡았다.** 린트가 아니라 검사였다 — 확인해 보길 잘했다 | 확인 |
| 958 | 다만 그 보호는 **부분적**이다. 그 함수를 다른 데서 쓰면서 이 호출부만 손으로 다시 쓰면 안 걸린다. 「죽은 export」는 「아무도 안 쓴다」만 보지 「여기서 안 쓴다」는 못 본다 | 기록 |
| 959 | 소리는 사람 몫 확인 목록에 있다(전투 네 신호, 노이즈 「틱」). 그 판단은 **판정이 맞다는 전제 위에서만** 뜻이 있다 — 여기가 흔들리면 사람이 들은 것이 무엇 때문인지 알 수 없다 | 기록 |
| 960 | 텍스처 캐시 셋 중 **둘이 비어** 있었다 — `facadeCache.set`·`tileCache.set`을 지워도 통과했다. 필터 캐시 하나만 지켜지고 있었다 | **결함** |
| 961 | 안 채우면 매번 새 캔버스를 그리고 새 텍스처를 만든다. 화면은 똑같은데 **텍스처가 계속 쌓이고**, 해제 함수는 캐시만 훑으므로 그렇게 만든 것은 영영 안 지워진다 | 기록 |
| 962 | 규칙으로 막았다: **Map으로 든 캐시는 `get`과 `set`이 둘 다 있어야 한다.** 반대 방향(채우기만 하고 안 읽음)도 같은 무게다 — 메모리만 쌓이고 이득이 0이다 | 검사 |
| 963 | **변수 하나로 든 캐시는 일부러 뺐다.** 문법이 달라 헛걸린다 — 같은 파일 위쪽에 「캐시가 변수인 파일을 잘못 걸었다」는 기록이 이미 있었다. 남이 적어 둔 함정을 읽고 피한 셈이다 | 검사 |
| 964 | 세 캐시(파사드·타일·아틀라스)를 다 비워 보고 전부 무는 것을 확인했다 | 확인 |
| 965 | 해제 함수 둘(파사드·아틀라스)은 이미 잡히고 있었다. 「캐시」 가족에서 남은 구멍은 채우기 쪽이었다 | 확인 |
| 966 | 상호작용 길목(`stepInteraction`)에서 **셋**이 나왔다. 흔적 조사·말 걸기·간판이 키 하나로 지나는 자리인데 검사가 없었다 | **결함** |
| 967 | 가장 무겁다: `playerLink.cluesFound`를 안 실어도 통과했다 — **여정이 흔적을 찾은 줄 모른다.** 조사는 되는데 단계가 영영 안 끝난다. 진행이 막히는 급이다 | **결함** |
| 968 | `talkView.nearby`가 흔적을 안 세면 **조사할 수 있다는 안내가 안 뜬다.** 앞에 서 있어도 누를 이유를 모르니 실질적으로도 막힌다 | **결함** |
| 969 | 시간이 다한 대사를 안 지우면 다음에 누를 때 이전 줄이 잠깐 보인다 — 그 파일 주석이 이미 적어 둔 그대로다 | **결함** |
| 970 | 「제자리에서 밀어 넣는다」도 검사로 고정했다. 새 배열을 만들면 HUD가 보던 것과 갈라지는데, 이건 **공유 가변 객체 계약**이라 글로만 두면 다음 사람이 무심코 깬다 | 검사 |
| 971 | 셋 다 「값이 맞는가」가 아니라 **「밖으로 나갔는가」**의 문제였다. 이 세션에서 배선이 압도적 1위라는 것이 다시 확인됐다 | 기록 |
| 972 | 배선을 손으로 찾지 않고 **쓸어냈다**: 공유 객체에 쓰는 줄 60개를 하나씩 지우고 매번 전체 검사를 돌렸다 | 확인 |
| 973 | **60개 중 51개는 지워도 아무도 몰랐다.** 배선이 1위 정도가 아니라 거의 전부 비어 있다는 것이 숫자로 나왔다 | **결함** |
| 974 | 51개는 전부 컴포넌트 프레임 루프 안이라 값으로 재려면 하나씩 밖으로 빼야 한다. 대신 **「아무도 안 채우는 칸이 없다」** 규칙을 뒀다 — 지운 줄이 그 칸의 유일한 쓰기면 걸린다 | 검사 |
| 975 | 자를 **세 번** 고쳤다: (1) 최상위 인터페이스만 읽어 중첩 화면 객체를 통째로 놓쳤고, (2) 「원시 타입만」으로 좁혔다가 `district: DistrictId` 같은 이름 붙은 값 타입이 빠졌고, (3) `SceneProps` 최상위는 props인데 매 프레임 칸으로 셌다 | 기록 |
| 976 | **네 번째가 제일 아팠다**: `elapsedSeconds`가 안 채워진다고 결함이라 말했는데, `PlayerRig`가 `+= dt`로 올리고 있었다. 내 정규식이 `+=`를 못 읽은 것이다 — 말하기 전에 한 번 더 봤어야 했다 | 기록 |
| 977 | 표본 7개로 다시 재니 **5개를 문다**(고치기 전엔 2/5). 못 잡는 둘은 같은 이름의 칸이 두 곳에 있어 다른 쪽이 가려 주는 경우다 — 한계를 검사 주석에 적었다 | 확인 |
| 978 | 이름 붙은 그릇(`combat: CombatCues`)만 목록으로 뺐다. 목록은 「아는 것만 담는」 약점이 있지만 이유가 분명하고 수가 하나다 — 규칙이 문법으로 못 가리는 자리다 | 검사 |
| 979 | 51개 중 가장 값진 덩어리부터 실제로 뺐다 — 대장 화면 칸 여섯을 `projectBossView`로. 사람 몫의 「보스 링 타이밍과 손맛」과 바로 닿는 자리다 | 수정 |
| 980 | `telegraph`가 특히 아프다. 지속 시간은 앞서 막았지만 **그 시간 동안 화면에 나가는 길은 이 한 줄뿐**이었다 — 예고가 안 뜨면 피할 수 있다는 것 자체를 모른다 | 기록 |
| 981 | 빼고 나니 다섯 칸은 다 문다. 그런데 **배선은 여전히 비었다** — 컴포넌트에서 호출을 지워도 몰랐다 | **결함** |
| 982 | 「죽은 export」 검사가 여기선 못 잡는다. **검사 파일이 부르고 있으니 아무도 안 쓰는 건 아니기** 때문이다. 「쓰이는가」와 「어디서 쓰이는가」는 다른 질문이다 | 기록 |
| 983 | 이름 규칙으로 묶었다: `project…`로 시작하는 함수는 **화면(.tsx)에서 불려야 한다.** 앞으로 같은 식으로 뺄 때도 이 이름을 쓰면 자동으로 지켜진다 | 검사 |
| 984 | 이게 51개를 하나씩 빼 나가는 **틀**이다. 빼고 → 칸을 값으로 재고 → 이름 규칙이 배선을 지킨다. 다음 반복부터 같은 틀로 이어 간다 | 기록 |
| 985 | 틀을 두 번째 덩어리에 적용했다 — 플레이어 활력(체력·쓰러짐)을 `projectPlayerVitals`로 뺐다. 두 칸뿐이지만 **화면에서 가장 눈에 띄는 둘**이다 | 수정 |
| 986 | 하트가 안 줄면 맞은 줄 모르고, 쓰러짐이 안 나가면 **쓰러졌는데 화면은 멀쩡하다** — 부활을 기다리는 동안 왜 조작이 안 먹는지 알 수 없다 | 기록 |
| 987 | 검사에서 `COMBAT_TUNING.maxHp`로 비교했다가 걸렸다 — **그건 적의 체력**이다. 플레이어 시작값은 `createPlayerCombat()`이 안다. 상수 이름이 비슷하면 엉뚱한 자를 집는다 | 기록 |
| 988 | 칸 둘과 배선 셋 다 문다. 이름 규칙(`project…`)이 배선을 자동으로 지키는 것도 두 번째로 확인됐다 | 확인 |
| 989 | `Enemies`의 나머지 넷(`attackQueued`·`attackElapsed`·`bossSlamHit`·`enemyBlipCount`)은 **루프 중간에 얽혀 있어** 한 덩어리로 못 뺀다. 순서를 바꾸면 동작이 달라진다 — 무리해서 빼지 않고 남긴다 | 기록 |
| 990 | 세 번째 덩어리 — 동료가 내보내는 다섯 칸을 `projectCompanionEffects`로 뺐다. 지도 점·흔적을 밝히는 범위·능력 버튼이 여기서 나간다 | 수정 |
| 991 | 「덮어쓰지 않고 합친다」를 값으로 고정했다. 덮어쓰면 마지막 동료 하나만 남아 **셋을 데리고 다니는 것이 뜻을 잃는다** — 주석에만 있던 규칙이다 | 검사 |
| 992 | 뜻밖의 부수 효과: 컴포넌트가 더 이상 공유 객체를 직접 안 만져 **`eslint-disable react-hooks/immutability`가 필요 없어졌다.** 예외를 지웠다 — 배선을 뺀 것이 규칙 위반도 함께 없앤 셈이다 | 수정 |
| 993 | 기존 검사 둘이 깨졌는데 **이사**였다(`Companion.tsx`가 특정 글자를 담는지 봤다). 내가 적어 둔 대로 따라 옮기지 않고 **나가는 값**으로 바꿨다 | 수정 |
| 994 | 그 검사의 주석이 이미 경고하고 있었다 — 「죽은 export 검사는 못 잡는다: 테스트가 import하면 「쓰이는 중」으로 센다」. 오늘 `projectBossView`에서 같은 벽을 다시 만났다 | 기록 |
| 995 | 칸 다섯과 배선 여섯 다 문다. 「빼고 → 값으로 재고 → 이름 규칙이 배선을 지킨다」가 세 덩어리 연속으로 통했다 | 확인 |
| 996 | 네 번째 덩어리 — 여정 화면 다섯 칸을 `projectQuestView`로 뺐다. 진행이 사람에게 보이는 유일한 창이다 | 수정 |
| 997 | 실행기에 여정 **내용**을 끌어들이지 않으려고 첫 여정 id를 인자로 받게 했다. 「실행기는 규칙만 알고 어떤 여정이 있는지는 모른다」를 지킨다 | 수정 |
| 998 | `firstQuestDone`이 특히 아프다. 덮어쓰면 첫 여정을 마치는 순간 다시 거짓이 되고(다음 여정에서 `completed`가 초기화된다) **만난 도깨비가 도로 잠긴다** | 검사 |
| 999 | 처음 쓴 검사가 `counter`·`completed`를 **안 채워도 통과**했다 — 빈 화면의 초기값(빈 문자열·false)이 마침 기대값과 같았다. 「어딘가 한 군데만 맞으면 통과」의 사촌이다 | 기록 |
| 1000 | **초기값을 일부러 기대값과 다르게** 뒀다(`"아직-안-채움"`, `-1`, 반대 불리언). 안 채운 칸이 그대로 남아 눈에 띈다 — 화면 칸을 재는 검사에는 이게 기본이어야 한다 | 검사 |
| 1001 | 진행 막대 검사도 한 번 헛돌았다: 「보드 타기」를 20프레임 돌렸더니 **그 사이 단계가 끝나** 다음 단계의 0을 재고 있었다. 부분 진행이 남는 목표로 바꿨다 | 기록 |
| 1002 | 다섯 번째 덩어리 — 이동 상태 여덟 칸을 `projectMotionView`로 뺐다. 캐릭터 자세·발소리·지도·카메라 흔들림이 전부 여기서 온다 | 수정 |
| 1003 | 증상이 곧바로 눈에 띄는 것들인데도 비어 있었다: `mode`가 안 나가면 **달려도 걷는 자세**, `x`·`z`가 안 나가면 **지도의 내 점이 안 움직인다**, `landingImpact`가 안 나가면 아무리 높이 떨어져도 카메라가 안 흔들린다 | 기록 |
| 1004 | 자리·방향 쓰기가 프레임 뒤쪽에 따로 있어 앞으로 합쳤다. **`corrected`가 const이고 그 사이 안 바뀌는지, 읽는 쪽이 프레임 안에 없는지** 먼저 확인했다 — 순서를 옮기는 것은 조용히 동작을 바꾼다 | 확인 |
| 1005 | 그 과정에서 `RuntimeStats.mode`가 `string`인 것이 드러나 **실제 값 집합**(`LocomotionMode`)으로 좁혔다. 오타로 넣은 값이 조용히 통과하면 소리와 자세가 각자 다른 것을 본다 | 수정 |
| 1006 | 칸 여덟과 배선 다 문다. 다섯 덩어리 연속으로 같은 틀이 통했다 — 남은 것은 대부분 성능 패널용 계측이다 | 확인 |
| 1007 | 다섯 덩어리를 뺀 뒤 **배선 훑기를 다시 돌렸다: 51/60 → 18/61.** 고쳤다고 말하기 전에 같은 자로 다시 재는 것이 이 세션의 규칙이다 | 확인 |
| 1008 | 남은 18개 중 넷은 **이미 순수 함수 안**이었다 — 내 검사가 흔적 경로만 보고 주민·간판 경로를 안 봤다. 「한 경로를 막았으니 됐다」가 아니었다 | **결함** |
| 1009 | 사람이 겪는 모습: 주민에게 말을 걸면 **말풍선은 뜨는데 누가 말하는지가 비거나** 아예 아무 줄도 안 뜬다. 도시를 읽는 재미가 사라지는데 화면은 멀쩡하다 | 기록 |
| 1010 | 「말하는 이가 셋 다 다른가」를 셋으로 잰다. 하나씩 보면 늘 같은 값을 넣어도 통과한다 — 이 세션에 반복된 「한 군데만 맞으면 통과」다 | 검사 |
| 1011 | 「흔적이 주민보다 먼저」도 값으로 고정했다. 그 파일 주석이 「옆에 주민이 서 있다고 진행이 막히면 안 된다」고 적어 둔 규칙이다 | 검사 |
| 1012 | 여섯 번째 덩어리 — 동료가 읽어 갈 플레이어 상태를 `projectCompanionTarget`으로 뺐다. 동료가 따라오는 근거가 전부 이 여섯 칸이다 | 수정 |
| 1013 | 끊기면 동료가 이상하게 군다: 자리가 안 나가면 **첫 자리에 멈춰 서서 안 따라오고**, `speed`가 안 나가면 달릴 때 뒤처지고, `facing`이 안 나가면 대열이 등 뒤가 아니라 엉뚱한 쪽에 서고, `grounded`가 안 나가면 점프해도 안 따라 뜬다 | 기록 |
| 1014 | 「칸이 채워졌는가」에서 멈추지 않고 **그 값을 받은 동료가 실제로 따라오는지**까지 봤다. 칸만 채우고 끝나면 뜻이 없다 | 검사 |
| 1015 | `position` 객체를 갈아 끼우지 않는 것도 고정했다 — 동료가 들고 있던 참조와 갈라진다. 공유 가변 객체 계약이라 글로만 두면 다음 사람이 무심코 깬다 | 검사 |
| 1016 | **내가 보고한 「51/60」과 「18/61」은 분모가 틀렸다.** 훑기 정규식에 `combatView`·`summaryView`·`grappleView` 같은 이름이 빠져 있었다 — 실제 공유 객체 쓰기는 **92개**다 | 기록 |
| 1017 | 안 본 33줄을 마저 재니 **20개가 지워도 아무도 모르는 채**였다. 자에 빠진 것이 있으면 「좋아졌다」는 숫자가 그만큼 거짓이 된다 | **결함** |
| 1018 | 그 중 열넷을 `hudProjection.ts`로 뺐다 — 전투 여덟, 자판기 셋, 구역 셋. 계산 없는 대입뿐이라 「검사할 게 있나」 싶은데 **바로 그래서 비어 있었다** | 수정 |
| 1019 | 끊기면 화면이 통째로 거짓말을 한다: 하트가 안 줄고, 쓰러져도 표시가 없고, 지도에 적이 안 뜨고, **배너가 옛 구역 이름을 계속 달고 있는다** | 기록 |
| 1020 | 구역 이름은 검사에 베끼지 않고 `DISTRICTS`에서 가져온다. 베껴 두면 이름을 바꿀 때 **검사만 옛 이름을 들고 통과한다** | 검사 |
| 1021 | 열네 줄 전부 무는 것을 확인했다(14/14). 표식 버퍼가 같은 객체인지도 `toBe`로 본다 — 복사하면 초당 60번 쓰레기를 만든다 | 확인 |
| 1022 | 갈고리 화면 상태를 `projectGrappleView`로 뺐다. `hasTarget`이 안 나가면 **걸 수 있는 자리에 서 있어도 표시가 안 떠서 걸 수 있다는 것을 모른다** — 이 도시를 도는 방식 자체가 사라지는데 화면은 멀쩡하다 | 수정 |
| 1023 | 배선을 확인하려다 **내 탐침이 틀렸다**(`void 0 &&` 단락은 이름을 남긴다). 실제로 호출을 통째로 지우는 형태로 다시 쟀다 | 기록 |
| 1024 | 그랬더니 **안 걸렸다 — 내가 만든 규칙에 구멍이 있었다.** `GrappleVisuals.tsx`가 `.tsx`라서 **자기 정의를 자기가 부른 것으로** 셌다 | **결함** |
| 1025 | 부르는 쪽 목록에서 **정의한 파일을 뺐다.** 이제 두 함수 다 호출을 지우면 문다. 규칙을 만든 그 반복에 검증했다고 생각했는데, 그때 쓴 탐침이 마침 정의가 없는 파일이라 안 드러났다 | 수정 |
| 1026 | 이번 반복에서만 자를 두 번 고쳤다(훑기 정규식, 배선 규칙). **검사를 지키는 검사가 없다**는 것이 이 세션의 남은 약점이다 | 기록 |
| 1027 | 「검사를 지키는 검사」를 실제로 했다: **채우기 함수 열 개의 호출을 하나씩 지워** 규칙이 다 잡는지 봤다 — 10/10 잡는다 | 확인 |
| 1028 | 새 규칙이 나왔다: **규칙을 쓰면 그 규칙이 지키기로 한 모든 사례에 변이를 걸라.** 하나만 걸면 오늘처럼 구멍이 남는다 — 처음 검증한 사례가 마침 정의가 `.ts`에 있어 자기 정의를 세는 버그가 안 드러났다 | 기록 |
| 1029 | 완주 집계를 `projectSummaryView`로 뺐다. 옮기기만 하는 다른 것들과 달리 **쌓는** 함수라 규칙이 둘 있는데, 둘 다 주석에만 있었다 | 수정 |
| 1030 | 「완주한 뒤에는 시간을 더 세지 않는다」 — 안 지키면 완주 화면을 열어 둔 채로 **「1분 12초에 끝냈다」가 볼 때마다 달라진다** | 검사 |
| 1031 | 「최고 속도는 줄지 않는다」 — 지금 속도를 그대로 쓰면 결과 화면에 **「최고 0.0 m/s」**가 뜬다. 멈춰 서서 화면을 여니까 | 검사 |
| 1032 | `bossDefeated`가 또 **초기값=기대값** 함정에 걸렸다. 「초기값을 기대값과 다르게」를 두 반복 전에 적어 두고도 같은 실수를 했다 — 불리언은 **두 방향을 다 재야** 한다 | 기록 |
| 1033 | 규칙을 적어 두는 것과 지키는 것은 다르다. 이 세션에서 내가 적은 규칙을 내가 어긴 것이 이번이 세 번째다(자 확인·규칙 좁히기·초기값) | 기록 |
| 1034 | 규칙을 기억에 맡기지 않고 **도구로** 옮겼다 — `tests/support/stale.ts`. 원본을 주면 **모든 원시 칸이 다른** 객체를 만든다(불리언 뒤집기, 숫자 부호+1, 문자열 표식) | 수정 |
| 1035 | 헬퍼 자체가 눈멀 수 있으므로 **「낡은 화면의 모든 칸이 실제로 다른가」**를 함께 잰다. 불리언을 안 뒤집게 바꿔 보니 바로 물었다 | 검사 |
| 1036 | 참조 칸(버퍼)은 헬퍼가 **일부러 안 건드린다** — 「같은 객체를 쓰는가」 검사를 망치지 않으려고. 대신 그 검사가 스스로 다른 버퍼로 시작한다 | 검사 |
| 1037 | 그 자리가 실제로 뚫려 있었다: 버퍼 옮기기를 지워도 통과했다(처음부터 같은 버퍼였으니까). 지금은 **안 옮겨도, 복사해도** 문다 | **결함** |
| 1038 | 「적어 둔 규칙을 세 번 어겼다」의 답은 **더 굳게 적는 것이 아니라 어길 수 없게 만드는 것**이었다. 다음에 화면 칸 검사를 쓰는 사람은 헬퍼를 쓰면 이 함정을 못 밟는다 | 기록 |
| 1039 | 새 도구를 만들었으니 **먼저 쓴 검사들도 다시 쟀다**: 채우기 함수 안의 대입 63줄 전부 문다(0/63 헛돎) | 확인 |
| 1040 | 남은 것 중 가장 무거운 것을 뺐다 — **도깨비를 만나는 계기**(`projectDiscovery`). 이 한 줄이 수집의 유일한 입구다 | 수정 |
| 1041 | 지워도 아무도 몰랐다: 도깨비 자리에 가 서도 아무 일이 없고 **누구도 모을 수 없는데 화면은 멀쩡하다.** 도감·완주 화면·해금이 전부 이 뒤에 매달려 있다 | **결함** |
| 1042 | 「이미 대기 중이면 건너뛴다」도 값으로 고정했다 — `PlayClient`가 가져가기 전에 덮어쓰면 **만남이 하나 사라진다.** 두 자리가 가까우면 실제로 일어난다 | 검사 |
| 1043 | 변이 하나가 헛돌았는데 **결함이 아니었다**: `if (!found) return null;`을 없애도 그 시점 `pending`은 이미 null이라 결과가 같다. 「헛돎이면 변이부터 의심」이 또 맞았다 | 기록 |
| 1044 | 되돌리기 블록을 `resetCompanionEffects`로 뺐다 — 합치기의 **짝**이다. 없으면 **능력이 한 번 걸린 뒤 영영 안 풀린다**(인지 반경이 줄어든 채 남고 흔적이 계속 밝다) | 수정 |
| 1045 | 화면에 아무 표시가 없어 「원래 이런 게임인가」 싶게 되는 종류다. 지워도 아무도 몰랐다 | **결함** |
| 1046 | 「되돌린 뒤 합치면 그 프레임 값만 남는다」로 **두 조각이 맞물리는 것**을 함께 잰다. 하나씩 보면 둘 다 통과하면서 실제로는 안 맞물릴 수 있다 | 검사 |
| 1047 | 프레임 순서 검사가 **두 번째로 이사 때문에 깨졌다**(먼저 합치기, 이번엔 되돌리기). 이제 둘 다 「그 일을 부르는가」로 대칭이다 | 수정 |
| 1048 | 「한쪽만 검사가 있으면 다른 쪽이 조용히 사라진다」가 이 자리의 교훈이다. 합치기는 값 검사가 있었는데 되돌리기는 없어서, 순서 검사가 통과하는 채로 능력이 망가질 수 있었다 | 기록 |
| 1049 | 「짝은 짝으로 재라」를 남은 자리에 걸었다 — **공격과 충격이 한 번만 나가는지.** 입력 큐에서 찾은 것과 같은 모양이 컴포넌트 사이에도 있었다 | 수정 |
| 1050 | 안 비우면: 공격은 **한 번 누른 것이 매 프레임 다시 나가** 가만히 서서 계속 휘두르고, 충격은 **매 프레임 다시 들어와** 무적이 끝나는 족족 또 맞는다 — 대장 앞에서 아무것도 못 하고 쓰러진다 | 기록 |
| 1051 | 「둘이 서로를 지우지 않는가」도 잰다. 한 통로로 합쳐지지만(무적 시간을 한 번만 걸려고) **출처가 다르다** — 하나를 꺼내며 다른 하나를 지우면 맞은 것이 사라진다 | 검사 |
| 1052 | 배선을 손으로 되돌리는 변이(`consumeAttack(link)` → `link.attackQueued`)도 잡힌다. 「비우는 일을 잊은 채 값만 읽는」 것이 원래 결함 모양이었다 | 확인 |
| 1053 | 리팩터가 **포토 모드 검사의 자**도 무너뜨렸다. 「때릴 수 있는 컴포넌트」를 `link.playerHp` 같은 **대입 모양**으로 찾고 있었는데, 그 대입을 함수로 빼자 목록이 비었다 | 기록 |
| 1054 | 조건을 **하는 일**로 바꿨다(체력·충격을 다루는가). 코드가 어떻게 생겼든 걸린다 — 두 컴포넌트 다 다시 찾고, 시간 멈춤을 지우면 둘 다 문다 | 수정 |
| 1055 | 이번 세션에 **소스 모양에 기댄 검사가 이사 때문에 깨진 것이 다섯 번째**다(동료 둘, 여정, 순서, 포토 모드). 매번 「결함인지 이사인지」를 먼저 갈랐고, 다섯 번 다 이사였다 | 기록 |
| 1056 | 패턴이 분명하다: **코드를 좋은 방향으로 옮길수록 소스 모양 검사가 깨진다.** 그때 검사를 따라 옮기면 다음 이사에 또 깨지므로, 값으로 재거나 「하는 일」로 찾게 고친다 | 기록 |
| 1057 | 자를 고치고 **넓은 범위로 제대로 쟀다: 17 / 107.** 시작할 때는 같은 범위에서 71개가 지워도 모르는 상태였다 | 확인 |
| 1058 | 남은 것 중 가장 무거운 **대장을 눕힌 기록**을 뺐다(`recordBossHit`). 네 줄이 각자 다른 곳으로 흘러가는데 하나씩 지워도 아무도 몰랐다 | 수정 |
| 1059 | `bossDefeated`가 그 중 제일 아프다 — **네 번째 도깨비의 해금 조건**이다. 이것만 빠지면 대장을 눕혀도 아무 일이 없고 **왜 안 열리는지 알 방법이 없다** | **결함** |
| 1060 | 내려치기 소리도 뺐다(`recordSlamStart`). 「**판정보다 조금 일러야 피할 수 있다**」가 규칙이라 단계가 바뀌는 순간만 센다 — 매 프레임 울리면 소리가 뭉갠다 | 수정 |
| 1061 | 그 규칙을 값으로 고정했다: 내려치는 동안 계속 돌려도 한 번만 울린다. 조건을 반만 남기는 변이도 문다 | 검사 |
| 1062 | 일반 적을 때린 기록도 뺐다(`recordEnemyHits`). 「**부활은 누적에서 빼지 않는다**」가 규칙이다 — 여정은 「몇 번 쓰러뜨렸나」를 묻지 「몇 기가 누워 있나」를 묻지 않는다 | 수정 |
| 1063 | 그런데 **배선이 안 잡혔다** — 내 규칙이 `project*`만 봤다. `record*`·`reset*`·`consume*`도 같은 이유로 불려야 하는데 빠져 있었다 | **결함** |
| 1064 | 규칙을 **이름의 동사**로 넓혔다: `project`(칸을 채운다)·`record`(일어난 일을 적는다)·`reset`(매 프레임 되돌린다)·`consume`(한 번짜리 신호를 꺼낸다) | 수정 |
| 1065 | 부르는 자리도 「화면(.tsx)」에서 **「프레임 루프가 있는 파일」**로 바로잡았다 — `consumeCues`는 화면이 아니라 오디오 루프에서 돈다. 안 그랬으면 멀쩡한 코드를 결함으로 몰 뻔했다 | 수정 |
| 1066 | **23개 전부에 변이를 걸어 23/23 잡는 것을 확인했다.** 「사례 하나만 확인하면 구멍이 남는다」를 지난 반복에 적어 두고 이번엔 지켰다 | 확인 |
| 1067 | 자기 정의를 세는 버그가 돌아오지 않게 **그 상황 자체를 검사로 고정했다**(정의가 `.tsx`에 있는 사례가 목록에 남아 있는지). 사례가 사라지면 그 검사가 헛도는 것도 함께 잡는다 | 검사 |
| 1068 | 만남의 **짝**을 뺐다(`consumeDiscovery`). 안 비우면 `projectDiscovery`가 계속 건너뛰어 **도깨비를 하나 만난 뒤로는 아무도 못 만난다** — 첫 만남은 되니까 「되긴 되는데 왜 더는 안 열리지」가 된다 | 수정 |
| 1069 | 처음엔 `takeDiscovery`로 지었는데 **배선 검사가 아예 못 봤다** — 그 검사는 `project`·`record`·`reset`·`consume` 같은 **하는 일을 말하는 동사**로 대상을 찾는다 | **결함** |
| 1070 | 이름을 가족에 맞추자 규칙이 저절로 따라왔다. **이름 규칙으로 만든 검사는 이름을 잘못 지으면 조용히 비켜 간다** — 규칙이 강제력을 가지려면 이름이 곧 계약이어야 한다 | 기록 |
| 1071 | 「도는 곳」에 `setInterval`도 넣었다. `PlayClient`는 프레임이 아니라 **일정 간격**으로 신호를 가져간다(useFrame 안에서 setState를 부르면 매 프레임 리렌더가 걸린다) — 안 넣었으면 멀쩡한 코드를 잡을 뻔했다 | 수정 |
| 1072 | 「안 가져가면 다음 만남이 막힌다」를 검사로 못 박았다. **짝이 필요한 이유 자체**를 재는 셈이라, 다음 사람이 둘 중 하나를 지우려 할 때 왜 안 되는지 바로 보인다 | 검사 |
| 1073 | 「이름을 잘못 지으면 규칙이 비켜 간다」를 **기계로** 막았다: 넘겨받은 객체를 고치는 함수는 넷 중 한 동사여야 한다. 지금 22개가 규칙 안이다 | 검사 |
| 1074 | 처음엔 React **컴포넌트**가 걸렸다 — 구조 분해한 props의 첫 이름을 인자로 읽었다. 훅과 같은 이유로 뺐다(이름 앞자리가 React 규칙이라 바꿀 수 없다) | 기록 |
| 1075 | 컴포넌트 안의 그 대입들이 바로 **밖으로 빼는 중인 대상**이다. 다 빼고 나면 여기 걸릴 컴포넌트도 없어진다 — 규칙이 진행 상황을 함께 보여 준다 | 기록 |
| 1076 | 이름을 `takeDiscovery`로 되돌려 보니 **문다.** 검사가 검사를 지키는 자리가 하나 생겼다 | 확인 |
| 1077 | 이번 구간의 구조가 정리됐다: **동사 이름 → 배선 검사 → 이름 검사.** 셋이 맞물려야 「값은 맞는데 화면엔 안 나오는」 결함이 다시 안 들어온다 | 기록 |
| 1078 | 프레임 루프를 열다섯 번 넘게 고쳤으니 **브라우저에서 실제로 도는지** 확인했다. 검사가 다 통과해도 그건 별개 질문이다 | 확인 |
| 1079 | 뺀 배선들이 **화면까지 값을 나른다**: 미니맵에 적 표식이 찍히고(`projectCombatView`), 걸으니 속도가 **2.6 m/s**로 오르고(`projectMotionView`), 내 점과 격자가 따라 돌고, 동료 대사가 뜬다(`dialogueView`) | 확인 |
| 1080 | 완주 화면의 **걸린 시간 0:00·최고 속도 0.0**은 결함이 아니다 — 이미 완주한 저장이라 `projectSummaryView`가 더 안 쌓는다. 방금 값으로 고정한 그 규칙이 화면에서 그대로 보였다 | 확인 |
| 1081 | 콘솔 오류 없음. 「검사 통과」와 「실제로 돈다」를 둘 다 본 채로 이 구간을 닫는다 | 확인 |
| 1082 | 백로그 입구(「지금 상태」)가 반복 490 기준이라 지금과 많이 달라서 고쳤다. **이 파일은 다음 사람이 처음 읽는 자리**다 — 낡은 입구는 낡은 길로 보낸다 | 수정 |
| 1083 | 새로 생긴 **맞물린 규칙 셋**(동사 이름 → 배선 검사 → 이름 검사)을 입구에 적었다. 이걸 모르면 새 코드가 조용히 규칙 밖으로 샌다 | 수정 |
| 1084 | 그런데 내가 전에 만든 검사가 **바로 잡았다** — 「길잡이에 살아 있는 개수를 적지 마라」. 「107개 중 71개」 같은 숫자를 넣었는데, 그건 다음 주에 거짓이 된다 | 기록 |
| 1085 | 숫자를 빼고 「표 끝쪽 행에 있다」로 바꿨다. **낡을 것을 낡지 않는 자리로 미루는 것**이 이 검사가 요구하는 바다 | 수정 |
| 1086 | 문서를 고치다 내 검사에 걸린 것이 이번 세션에 네 번째다(테스트 수, 사람 몫 개수, 수집 숫자, 길잡이 개수). 문서도 코드처럼 검사가 지킨다 | 기록 |
| 1087 | 입력을 전투·동료로 넘기는 세 줄을 `projectCommands`로 뺐다. 누르는 곳(키보드·터치)과 쓰는 곳(적·동료)이 멀어 공유 객체를 한 번 거치는 자리다 | 수정 |
| 1088 | 안 넘기면: **J를 눌러도 아무 일이 없고**, 동료를 불러도 안 오고, **능력이 영영 안 나간다**(버튼은 켜져 있다). 셋 다 지워도 아무도 몰랐다 | **결함** |
| 1089 | 「**공격은 켜기만 한다**」를 값으로 못 박았다. 끄는 일은 쓰는 쪽(`consumeAttack`)이 하고, 여기서 매 프레임 꺼 버리면 **그 사이에 못 읽은 입력이 사라진다** | 검사 |
| 1090 | 「누르지 않은 프레임에는 요청이 안 는다」도 잰다 — 안 비우면 능력 요청이 **초당 60번 쌓인다** | 검사 |
| 1091 | 옮기면서 `summoned`·`abilityRequests`가 프레임 앞쪽으로 왔다. 읽는 쪽(Companion)은 PlayerRig가 **끝난 뒤에** 도므로 안전하다 — 순서를 옮기기 전에 확인했다 | 확인 |
| 1092 | `Enemies`의 마지막 두 줄을 뺐다 — 공격 자세 타이밍(`projectAttackTiming`)과 지도 표식 수(`projectEnemyBlips`) | 수정 |
| 1093 | 자세 타이밍이 안 가면 **때리는 시늉조차 안 한다** — 소리는 나고 적은 날아가는데 캐릭터만 가만히 서 있다. 표식 수가 안 가면 좌표는 있는데 **지도에 적이 하나도 안 뜬다** | 기록 |
| 1094 | 「**없음(null)과 0은 다르다**」를 값으로 못 박았다. 0으로 바꾸면 평소에도 휘두르기 시작 자세로 서 있게 된다 — `?? 0` 변이가 물린다 | 검사 |
| 1095 | 그러자 `combatSim`이 800줄을 넘었다. **상한이 또 설계를 물었고** 답은 「시뮬레이션과 내보내기는 다른 책임」이었다 — `combatLink.ts`로 갈랐다 | 수정 |
| 1096 | 이번 세션에 크기 상한이 설계를 물은 것이 두 번째다(먼저 `sidewalks.ts`). 둘 다 **쪼갤 자리를 상한이 먼저 알려 줬다** | 기록 |
| 1097 | 부활 신호를 `consumeRespawn`으로 뺐다. 안 비우면 **매 프레임 스폰 지점으로 끌려가 아예 움직일 수 없다** — 쓰러진 것도 아닌데 조작이 안 먹는다 | 수정 |
| 1098 | 「이동 계산 앞에서 처리해야 한다」는 순서 규칙은 **씬이 지킬 몫이라 여기서 못 잰다**고 주석에 적었다. 못 재는 것을 재는 척하지 않는다 | 기록 |
| 1099 | 캐릭터 자세 신호 셋을 `projectCharacterCues`로 뺐다. 안 옮기면 춤을 춰도 가만히 서 있고, 휘둘러도 팔이 안 나가고, 동료를 불러도 캐릭터가 모른 척한다 | 수정 |
| 1100 | 「**감정 객체는 복사하지 않고 그대로 넘긴다**」를 값으로 못 박았다 — 안쪽 경과 시간이 매 프레임 바뀌므로 복사하면 **한 프레임 낡은 자세**를 그린다 | 검사 |
| 1101 | 동료 말을 `projectDialogue`로 뺐다. 안 나가면 동료가 **입을 다문다** — 부르고 보내고 능력을 써도 아무 말이 없다. 동료가 사람처럼 느껴지는 거의 유일한 통로다 | 수정 |
| 1102 | 「**사라지는 것도 내보낸다**」를 함께 잰다. 시간이 다한 대사(`null`)를 안 내보내면 마지막 말이 화면에 **영영 붙어 있는다** — 「안 나가는 것」과 「사라지는 것」이 짝이다 | 검사 |
| 1103 | `if (state.line) view.line = ...` 변이도 문다. 값이 있을 때만 내보내는 것은 **딱 그 절반**을 놓치는 형태라 특히 지나치기 쉽다 | 확인 |
| 1104 | `stats.district`는 이미 잡히고 있었다. 이 구간에서 손으로 확인한 마지막 후보였다 | 확인 |
| 1105 | 구간을 닫는 숫자를 쟀다: **2 / 109.** 시작할 때 같은 범위에서 **71개**가 지워도 아무도 모르는 상태였다 | 확인 |
| 1106 | 남은 둘은 **참조 대입**이다(`locomotion.current`) — 씬이 프레임 사이에 플레이어 상태를 들고 있는 자리. 지우면 **아예 못 움직이거나 부활이 안 된다** | 기록 |
| 1107 | 이 둘은 **단위 검사가 원리적으로 못 본다.** 컴포넌트 안에만 있는 참조라 값으로 잴 데가 없고, 소스 모양으로 막으면 이사할 때마다 깨진다(이번 세션에 다섯 번 겪었다) | 기록 |
| 1108 | 그래서 **브라우저 한 걸음이 이 둘의 유일한 검사**라고 README에 적었다. 이번 반복에 실제로 걸어 2.6 m/s를 봤다 — 못 재는 것을 재는 척하는 대신 **재는 방법을 적어 둔다** | 수정 |
| 1109 | 이 구간의 결론: 배선은 **거의 전부 값으로 잴 수 있고**, 못 재는 자리는 손에 꼽을 만큼 적으며 그마저 사람이 한 걸음 걸으면 드러난다 | 기록 |
| 1110 | 다음 줄 갈래로 넘어갔다 — **조건문**. 순수 모듈의 이른 반환 17개를 하나씩 지워 보니 **2개가 통과**했다 | 확인 |
| 1111 | `if (!target) return null` — 걸 곳이 없는데 `G`를 누르면 없는 대상에 걸린 것으로 처리돼 `grounded`가 꺼진다. **빈 하늘에 G를 누른 것만으로 땅에서 떨어진다** | **결함** |
| 1112 | `if (!step) return ...completed` — 이어하기·확인 지점이 단계 번호를 **일부러 끝 너머로** 둔다(완주 화면). 그 번호로 단계를 꺼내면 **없는 것을 꺼내 터진다** | **결함** |
| 1113 | 두 번째가 안 보인 이유가 있다: 완주로 들어오는 경로가 위쪽 `completed` 검사에 **대부분** 걸려서 남은 좁은 길이 가려졌다. 「대부분 막혀 있다」가 「막혀 있다」는 아니다 | 기록 |
| 1114 | 둘 다 **반대쪽도 넣었다**(걸 곳이 있으면 걸린다 / 있는 단계는 진행한다). 「안 되는 것」만 막으면 아예 죽은 기능도 통과한다 | 검사 |
| 1115 | 조건문 갈래가 배선보다 훨씬 깨끗했다(2/17 대 71/109). **이미 값으로 재고 있던 모듈이라 그렇다** — 조건은 대부분 결과를 바꾸므로 값 검사가 자연히 잡는다 | 기록 |
| 1116 | 조건문 훑기를 나머지 순수 모듈로 넓히다 **시간 초과로 죽었고, 한 줄이 되돌려지지 않았다**(`resumeProgress`의 대장 기록). 전체 검사가 바로 잡아 복원했다 | 기록 |
| 1117 | 훑기 스크립트에 **죽어도 되돌리는 장치**(`atexit`)를 넣고 덩어리로 쪼갰다. 오래 도는 작업은 중간에 죽는 것을 전제로 짜야 한다 | 수정 |
| 1118 | 첫 덩어리 70개에서 14개가 통과했다. 그 중 `roster`의 넷을 파 보니 **넷 다 결함이 아니었다** — 일어날 수 없는 상태를 막는 **방어선**이다 | 기록 |
| 1119 | 근거를 값으로 확인했다: 초롱은 **만남 후보에 안 들어가고**(집이 없다), 초롱이 늘 열려 있어 **목록이 빌 수 없고**, `index < 0`은 `(-1+1)%n = 0`이라 **결과가 같다** | 확인 |
| 1120 | **조건문 갈래는 대입 갈래와 성격이 다르다.** 대입은 「지워도 모른다 = 거의 결함」이었는데, 조건은 상당수가 방어선이다. 「헛돎」을 결함으로 세기 전에 **닿을 수 있는 상태인지** 먼저 봐야 한다 | 기록 |
| 1121 | 방어선을 지우는 대신 **왜 안 밟히는지**를 검사로 못 박았다. 초롱에게 집을 줘 보니 문다 — 전제가 깨지면 먼저 걸리고, **그때부터 방어선이 진짜 일한다** | 검사 |
| 1122 | 나머지 조건 중 **닿을 수 있는** 것들을 막았다: 벽 미끄러짐 두 줄과 겹쳐 선 적 판정 | 수정 |
| 1123 | 벽 미끄러짐이 없으면 **벽에 닿는 순간 완전히 멈춰 끼인다** — 좁은 골목에서 특히 답답하다. 겹침 예외가 없으면 **껴안은 채로 휘둘러도 안 맞는다**(`atan2(0,0)`이 0이라 등 뒤로 잡힌다) | 기록 |
| 1124 | 미끄러짐 함수는 내보내지 않았다. **검사 때문에 내보내는 대신** 실제 입구(`stepEnemy`의 넉백)로 밀어 잰다 | 검사 |
| 1125 | 그 과정에서 **벽을 안 준 채로 「안 뚫는다」를 재고 있었다** — `isBlocked`를 다섯 번째로 넘겼는데 그 자리는 `aggroScale`이었다. 인자가 많은 함수는 자리를 세어 보고 써야 한다 | 기록 |
| 1126 | 처음엔 한 축만 재서 **두 줄 중 하나가 안 물렸다.** 미끄러짐은 두 축을 각각 시도하므로 반대 축 갈래를 따로 넣어야 한다 — 「절반만 재는」 실수를 또 했다 | 기록 |
| 1127 | 훅 안의 **판단**만 순수 함수로 뺐다 — 「저장할 것이 있는가」(`progressChanged`)와 「목록이 바뀌었는가」(`stableClueList`) | 수정 |
| 1128 | 없으면: 저장은 **매 주기 localStorage에 쓰고**(값은 같은데 쓰기만 쌓인다), 목록은 매번 새 배열이라 **아무 일도 없는데 화면이 계속 다시 그려진다**(초당 세 번) | **결함** |
| 1129 | 「**저장을 되돌리지는 않는다**」를 값으로 못 박았다 — 저장에는 있는데 지금 꺼진 대장 기록으로 덮으면 기록이 사라진다. `!==`로 바꾸는 변이가 물린다 | 검사 |
| 1130 | 이름을 `nextClueList`로 지었다가 **검사에 걸렸다.** `next…`는 버튼으로 돌리는 것들의 가족(도깨비·시간대·필터·자세)이고 이건 그게 아니다 — `stableClueList`로 바꿨다 | 수정 |
| 1131 | **이름 가족을 검사가 지킨 것이 이번 세션에 두 번째**다(`takeDiscovery`, `nextClueList`). 이름 규칙으로 만든 검사는 이름을 검사해야 강제력이 생긴다는 것이 실제로 값을 한다 | 기록 |
| 1132 | 조건 훑기 둘째 덩어리에서 40/70이 나왔지만 **대부분 환경 방어선**이다(`typeof window`·`!ctx`·오디오 내부 플래그) — node에서는 원리적으로 안 밟힌다 | 기록 |
| 1133 | 그 중 진짜는 **처음 켤 때 품질을 정하는 판정**이었다. `qualityForDevice`로 떼어 냈다 — 브라우저를 읽는 부분과 섞여 있어 값으로 잴 데가 없었다 | 수정 |
| 1134 | 여기가 틀리면 **좋은 기기가 흐린 화면으로 시작하거나, 약한 기기가 첫 화면부터 끊긴다.** 모든 사람이 처음 밟는 길인데 네 갈래가 다 비어 있었다 | **결함** |
| 1135 | 갈래를 하나씩 재는 데 그치지 않고 **「모바일이 데스크톱보다 높게 잡히는 사양이 없다」**를 사양 20가지로 훑는다. 갈래별로만 보면 표가 뒤집혀도 통과한다 | 검사 |
| 1136 | `downgrade`의 `if (level === "medium") return "low"`도 「지워도 모른다」였는데 **결함이 아니다** — 지워도 아래로 떨어져 같은 값이 나온다. 조건 갈래에서 세 번째로 만난 무해한 변이다 | 기록 |
| 1137 | 지도의 「중심이면 방향을 정할 수 없다」 줄이 **영영 안 밟히는 줄**이었다 — 그 위의 「반경 안이면 그대로」가 이미 중심을 거른다. 지우고 **왜 안 밟히는지**를 검사로 옮겼다 | 수정 |
| 1138 | 죽은 줄은 그냥 낭비가 아니다. **거기 적힌 주석이 「이 경우를 다룬다」고 말하므로**, 다음 사람이 다뤄진 줄 알고 지나간다 | 기록 |
| 1139 | 대사의 「남은 시간이 있으면 그대로 둔다」가 뚫려 있었다. 지우면 **뜨자마자 다음 프레임에 사라져** 사람 눈에는 안 뜬 것과 같다 | **결함** |
| 1140 | 이미 「시간이 다하면 지워진다」는 검사가 있었는데 **반대쪽을 안 봤다.** 사라지는 쪽만 재면 늘 사라져도 통과한다 — 이 세션에 「절반만 재기」를 몇 번째 만나는지 모르겠다 | 기록 |
| 1141 | 「절반만 재기」도 **도구로** 옮겼다 — `tests/support/bothWays.ts`. 입력 묶음을 주면 결과가 실제로 갈리는지 알려 준다. `staleCopy`와 같은 방식이다 | 수정 |
| 1142 | 갈래를 하나씩 재는 것만으로는 **늘 참을 돌려주는 함수도 통과한다.** 실제로 발소리·착지·걸음 신호를 「늘 참」으로 바꿔 보니 이 검사들이 물었다 | 확인 |
| 1143 | 도구 자체가 눈멀면 위 검사가 통째로 먼다. **도구를 지키는 검사**를 붙였다 — 갈리는데 안 갈린다고 하는지, 한 갈래인데 갈린다고 하는지, 빈 묶음은 어떤지 | 검사 |
| 1144 | 그 확인 중에 **내 탐침이 없는 줄을 찾고 있었다**(`return sawTrue && sawFalse`는 그 파일에 없다). 「헛돎」을 보고 구멍이라 믿을 뻔했고, 실제 줄로 다시 재니 둘 다 문다 | 기록 |
| 1145 | 이 세션에 탐침이 틀린 것이 세 번째다. **「헛돎」이 나오면 (1) 변이가 진짜 결함인가 (2) 탐침이 실제로 적용됐는가**를 둘 다 본다 | 기록 |
| 1146 | `bothWays`를 대장·동료 술어에도 댔다 — 맞힐 수 있는 단계, 교전 거리, 내려찍기 자리, 지도 표시, 능력 버튼 | 검사 |
| 1147 | 다섯 다 이미 양쪽을 재고 있었다. **한쪽으로 쏠린 곳은 없었다** — 이 세션에 술어를 여러 번 고치면서 자연히 채워진 듯하다 | 확인 |
| 1148 | 그래도 「늘 참」 변이를 넣어 무는 것을 확인했다. **양쪽이 나온다는 것과 그 검사가 문다는 것은 다른 말**이라 둘 다 봐야 한다 | 확인 |
| 1149 | 「점만 찍히고 아직 못 쓰는 구간이 있는가」도 넣었다. 두 문턱이 같아지면 그 구간이 사라지는데, **각각을 따로 재면 안 보인다** | 검사 |
| 1150 | 조건 훑기 마지막 덩어리(41/76)도 대부분 환경 방어선이었다. 진짜는 **저장값 검증** 둘이었다 | 확인 |
| 1151 | 완주 여부가 불리언이 아니거나 처치 수가 수가 아니어도 그대로 들이고 있었다. 저장은 **사람이 손댈 수 있는 자리**다(개발자 도구·확장·다른 탭) | **결함** |
| 1152 | 처치 수가 문자열이면 비교가 다 거짓이 되어 **여정이 영영 안 끝난다.** 왜 안 끝나는지 알 방법이 없는 종류다 | 기록 |
| 1153 | `clampStepIndex`의 `stepCount <= 0`은 **결함이 아니다** — 지워도 `max(0, min(-1, n))`이 0이라 결과가 같다. 조건 갈래에서 네 번째로 만난 무해한 변이다 | 기록 |
| 1154 | 아틀라스 캐시 **읽기**를 지워도 안 잡힌다. 캐시 규칙은 `get`/`set`이 있는지만 보므로 읽고 안 쓰는 것은 못 본다 — 캔버스가 없어 값으로도 못 잰다 | 기록 |
| 1155 | 이건 `locomotion.current`와 같은 부류다: **단위 검사가 원리적으로 못 보는 자리.** 소스 모양으로 막으면 이사할 때마다 깨지므로, 막는 대신 **여기 적어 둔다** | 기록 |
| 1156 | 세 번째 갈래(**비교 방향**)를 훑다 사고가 났다. `while (delta > Math.PI)`를 뒤집으니 **무한 반복**이 되어 검사가 안 끝났고, 훑기가 시간 초과로 죽으며 그 줄을 남겼다 | 기록 |
| 1157 | 앞서 넣은 「죽어도 되돌리기」가 **안 통했다** — 되돌리기는 정상 종료·시그널에 걸리는데, **자식이 매달려 있으면 부모가 그 안에서 못 빠져나온다** | 기록 |
| 1158 | 증상이 고약했다: 검사가 실패하는 게 아니라 **끝나지 않는다.** 「빨간 줄」이 아니라 「아무 일도 안 일어남」이라 처음엔 환경 문제인 줄 알았다 | 기록 |
| 1159 | 찾은 방법은 **짝 비교**였다. 같은 파일 497~498줄에 각도 정규화 짝이 그대로 있어서, 436줄만 방향이 다른 것이 바로 보였다 — 대칭이 깨진 자리를 눈이 먼저 잡는다 | 기록 |
| 1160 | 훑기에 **한 번당 시간 제한**을 넣고, 안 끝나는 것도 「잡았다」로 센다. 무한 반복은 결함이므로 그 셈이 맞다 | 수정 |
| 1161 | 교훈: **변이 검사는 「틀린 답」뿐 아니라 「답이 없음」도 만든다.** 시간 제한 없는 훑기는 첫 무한 반복에서 통째로 멈춘다 | 기록 |
| 1162 | 훑기가 도는 동안 **src를 안 건드리는 일**을 했다 — 배경 작업이 소스를 바꾸는 중에 같은 파일을 고치면 서로를 덮는다 | 기록 |
| 1163 | README의 「검사가 정말 잡는지」 절이 이번 세션에 규칙 **스무 개**로 불었다. 시간 순으로 쌓여 있어 찾기 어려워서 **길잡이 표**를 앞에 뒀다 | 수정 |
| 1164 | 표는 「무엇을 하려는가」로 묶는다 — 표적 고르기 / 크게 쓸어내기 / 결과 읽기 / 검사 쓰기 / 쓰고 나서 확인. 규칙 자체는 그대로 두고 **입구만** 만들었다 | 수정 |
| 1165 | 도구 둘(`stale.ts`·`bothWays.ts`)도 함께 가리킨다. **규칙을 기억에 맡겼다가 반복한 실수**를 옮겨 둔 것들이라 규칙과 같이 보여야 뜻이 산다 | 수정 |
| 1166 | 이 구간 요약(**「배선을 판 구간」**)을 백로그에 썼다. 반복 490 요약은 있는데 그 뒤 200줄은 요약이 없어, 다음 사람이 표를 한 줄씩 읽어야 했다 | 수정 |
| 1167 | 요약에 **갈래별 성격 차이**를 표로 넣었다 — 대입은 「지워도 모른다 = 거의 결함」, 조건은 대부분 방어선. 이걸 모르면 조건 훑기 결과를 결함으로 잘못 센다 | 수정 |
| 1168 | 「내가 가장 많이 틀린 것은 여전히 **자**」도 적었다. 이번 구간에서만 개선을 과장해 보고했고, 없는 구멍을 세 번 만들어 냈고, 초기값 때문에 두 번 눈이 멀었다 | 기록 |
| 1169 | 요약을 넣자 **행 번호 검사가 잡았다** — 앞 요약이 열린 구간(490~)인데 960부터 시작하는 절을 그 앞에 끼웠다. 닫고(490~959) 순서를 바로잡았다 | 수정 |
| 1170 | 이 검사도 내가 전에 같은 실수를 하고 만든 것이다(겹치는 구간을 적었다). **요약은 눈에 안 띄게 낡으므로** 기계가 지켜 준다 | 기록 |
| 1171 | 비교 방향 훑기 첫 덩어리: **4 / 55.** 조건 갈래와 달리 **넷 다 진짜 결함**이었다 — 방향을 뒤집는 것은 방어선을 지우는 것과 달리 늘 결과를 바꾼다 | 확인 |
| 1172 | 가장 무거운 것: `downed: hp <= 0`을 뒤집으면 **맞는 순간 무조건 쓰러진다**(체력은 늘 0 이상이다). 하트가 다섯이든 첫 대에 쓰러져 부활을 기다린다 | **결함** |
| 1173 | 「맞으면 체력이 준다」는 이미 재고 있었는데 **쓰러짐 여부는 안 봤다** — 값 하나만 보고 **옆 칸을 안 본** 자리다 | 기록 |
| 1174 | 대장은 `hitsUntilStagger <= 0`을 뒤집으면 **한 대에 휘청한다.** 「몇 대를 넣어야 빈틈이 생긴다」는 리듬이 사라지고 샌드백이 된다 — 「휘청하는가」는 봤지만 **몇 대 만인지**는 안 봤다 | **결함** |
| 1175 | 이동은 `speed > 0.05`가 뒤집히면 **달릴 때 바라보던 방향을 쓰고 멈췄을 때 속도를 본다** — 정확히 거꾸로다. 미끄러지는 동안 몸이 안 따라가고 멈춘 순간 홱 돈다 | **결함** |
| 1176 | 검사를 쓰다 **적에게 `mood`를 안 줘서 한 대도 안 맞고 있었다.** 쫓는 중인 적만 때린다 — 「맞았는데 안 쓰러진다」를 재려다 「안 맞았다」를 재고 있었다 | 기록 |
| 1177 | 갈래별 성격 차이를 README에 표로 적었다. 같은 「지워도 모른다」라도 **무엇을 건드렸느냐에 따라 뜻이 다르다** — 이걸 모르면 조건 훑기 결과를 결함으로 잘못 센다 | 수정 |
| 1178 | 훑을 순서도 적었다: **대입 → 비교 → 조건.** 앞의 둘은 나온 것을 거의 그대로 믿을 수 있고, 조건은 하나하나 따져야 해서 품이 가장 많이 든다 | 수정 |
| 1179 | 비교 갈래에서 나온 셋이 전부 같은 모양이었다 — **값은 재고 있었는데 그 옆 칸을 안 봤다**(체력은 봤지만 쓰러짐은, 휘청은 봤지만 몇 대 만인지는) | 기록 |
| 1180 | 이건 「절반만 재기」의 사촌인데 `bothWays`로는 안 잡힌다. **한 동작이 바꾸는 모든 칸을 세는** 습관이 따로 필요하다 | 기록 |
| 1181 | 다음 도구를 **스크래치패드에서 시제품으로** 확인했다 — 프록시로 읽힌 칸 세기. 읽기는 세고 **쓰기는 안 세며**, 한 칸만 읽은 검사와 다 읽은 검사가 구분된다 | 확인 |
| 1182 | 소스를 읽어 세는 길은 **버렸다.** 이 세션에 소스 모양 검사가 이사 때문에 다섯 번 깨졌다 — 실행 중에 재는 편이 옳다 | 기록 |
| 1183 | 사람 몫 1번(한 걸음 걷기)이 **단위 검사가 못 보는 두 줄까지 확인해 준다**고 README에 이어 뒀다. 사람에게 일을 더 주는 게 아니라, 이미 하는 일이 무엇을 덮는지 알려 주는 것이다 | 수정 |
| 1184 | 훑기가 도는 중에 검사를 돌렸다가 **가짜 실패**를 봤다. 소스가 그 순간 변이 상태였다 — 훑는 동안에는 검사 결과를 믿을 수 없다 | 기록 |
| 1185 | 배경 훑기를 **멈췄다.** 도는 동안에는 매 반복 검증(tsc·eslint·검사·빌드)을 할 수 없는데, 그게 이 작업의 조건이다 — 오래 도는 훑기와 반복 검증은 같이 못 간다 | 수정 |
| 1186 | 멈추자 **또 변이가 남았다**(`minimap.ts`의 `row > CITY.gridSize`). 검사 둘이 바로 잡았고 복원했다 — 되돌리기 장치가 SIGTERM에도 자식이 매달려 있으면 안 통한다 | 기록 |
| 1187 | 이번엔 **검사가 먼저 알려 줬다.** 앞의 무한 반복 때는 「안 끝난다」였는데, 이번은 「구역 수가 도시와 다르다」로 **무엇이 틀렸는지까지** 말해 줬다 | 기록 |
| 1188 | 앞으로 훑기는 **앞에서 작은 덩어리로** 돌린다. 한 번에 열다섯쯤이면 시간 제한 안에 끝나고, 끝날 때마다 검증할 수 있다 | 기록 |
| 1189 | 훑기를 앞에서 작은 덩어리로 재개했다. 여정의 「가서 보라」가 뚫려 있었다 — 거리 판정을 뒤집으면 **출발하자마자 완료**되고 정작 도착하면 부분 진행으로 떨어진다 | **결함** |
| 1190 | 「도착하면 완료된다」는 재고 있었는데 **가는 동안의 진행률**은 안 봤다. 그 값이 있는 이유가 「방향이 맞는지 알려 주려고」인데, 거꾸로 알려 주면 없느니만 못하다 | 기록 |
| 1191 | 동료에서 다섯이 나왔고 하나는 **플레이어에게서 방금 고친 것과 같은 모양**이었다(움직임/정지 방향 판정). 「한 곳에서 찾은 모양은 이웃에도」가 또 맞았다 | **결함** |
| 1192 | 파고 보니 더 나빴다: 문턱이 0.35인데 **쉬는 동료는 공전만으로 0.94**가 나온다 — 「멈추면 플레이어를 본다」 갈래가 **영영 안 밟혔다.** 가만히 서 있어도 동료는 한 번도 이쪽을 안 봤다 | **결함** |
| 1193 | 문턱을 **공전 속도에서 유도**했다(`idleOrbitRate × followDistance`). 손으로 숫자를 고르면 자리 수나 공전 속도를 조정할 때 같은 일이 조용히 돌아온다 | 수정 |
| 1194 | 옛 문턱(0.35)으로 되돌리는 변이도 문다 — **갈래가 죽는 것 자체**를 검사가 잡는다. 죽은 갈래는 「지워도 모른다」로만 보여서 훑기 없이는 못 찾았다 | 확인 |
| 1195 | 남은 동료 셋을 다시 쟀다. 「지속 중에도 또 나감」은 **결함이 아니다** — 첫 발동에서 대기가 켜지므로 대기 검사가 이미 막는다(중복 조건) | 기록 |
| 1196 | 「속도 역산을 끈다」는 진짜였는데 **내 검사가 구분을 못 했다.** 플레이어가 앞에 있으면 「플레이어 쪽」과 「진행 방향」이 같아서 두 갈래가 겹쳐 보였다 | **결함** |
| 1197 | 속도를 **직접** 재는 검사를 넣었다 — 이동량 나누기 dt와 같은지. 방향으로 에둘러 재면 그 둘이 같아지는 배치에서 눈이 먼다 | 검사 |
| 1198 | 이번 것도 「옆 칸을 안 본」 자리다. 다만 이번엔 **에둘러 재서** 그랬다 — 같은 값을 두 경로로 얻을 수 있으면 검사는 어느 쪽이 죽었는지 모른다 | 기록 |
| 1199 | 비교 방향 훑기를 끝냈다: **121줄 중 9개**가 뚫려 있었고 **아홉 다 진짜 결함**이었다(중복 조건 하나 제외) | 확인 |
| 1200 | 뒤쪽 36줄은 **0개**였다 — 흔적·상호작용·이어하기처럼 이 세션에 이미 값으로 재 놓은 모듈들이다. **먼저 판 곳은 다시 안 나온다** | 기록 |
| 1201 | 세 갈래를 다 돌았다: 대입 71 → 2, 조건 5, 비교 9. **찾은 결함 여든 남짓**이고 전부 「검사가 통과하는데 화면은 틀린」 부류였다 | 기록 |
| 1202 | 「다음 후보」에 적어 둔 도구를 실제로 만들었다 — `tests/support/readsAll.ts`. **실행 중에 읽힌 칸을 센다**(프록시 `get` 기록) | 수정 |
| 1203 | 「값은 재고 있었는데 옆 칸을 안 봤다」가 이 세션에서 네 번 나왔는데, 앞선 도구 둘(`bothWays`·`staleCopy`)로는 안 잡히는 모양이었다 | 기록 |
| 1204 | **쓰기는 안 센다.** 채우는 함수가 대역에 값을 써도 「읽었다」로 안 잡혀서 `project…` 계열을 그대로 감쌀 수 있다 — 시제품에서 먼저 확인한 성질이다 | 검사 |
| 1205 | 도구를 지키는 검사도 같이 넣었다. 「읽기를 안 세게」·「늘 다 읽었다고 하게」 바꿔 보니 둘 다 문다 — 도구가 눈멀면 그것을 쓰는 검사가 통째로 먼다 | 확인 |
| 1206 | 이로써 검사 도구가 넷이 됐다: `mutate.sh`(결함을 넣어 본다)·`staleCopy`(초기값)·`bothWays`(양쪽)·`readsAll`(모든 칸). **넷 다 내가 같은 실수를 반복한 뒤에 생겼다** | 기록 |
| 1207 | 새 도구를 **결함이 났던 자리에** 댔다 — 쓰러짐 버그가 살던 `stepPlayerCombat` 결과와 방향 버그가 살던 이동 상태 | 검사 |
| 1208 | 검사에서 칸 하나(`facing`)를 안 보게 지워 보니 **문다.** 도구가 실제로 「안 보는 칸」을 잡는 것을 그 자리에서 확인했다 | 확인 |
| 1209 | 이 도구의 값어치는 **검사를 고칠 때** 드러난다. 앞으로 누가 단언 한 줄을 지우면, 그 칸이 무엇으로 바뀌든 통과하게 되는 것을 바로 알려 준다 | 기록 |
| 1210 | 동작을 바꾼 수정이 여럿이라(동료 시선 문턱·여정 진행률·대장 휘청) **화면에서 확인**했다. 세계·여정·구역 배너·동료 대사·미니맵 표식이 다 정상이다 | 확인 |
| 1211 | 동료 시선 수정이 눈으로 보인다: 두 장 사이에 **공전으로 자리는 옮겨졌는데 넷 다 얼굴이 이쪽을 향한다.** 접선을 보고 있었다면 옆모습으로 돌아갔을 것이다 | 확인 |
| 1212 | 첫 스크린샷은 **화면이 검었다** — 장면이 마운트되기 전이었다. 한 장만 보고 「깨졌다」고 할 뻔했다. 자동화에서는 늘 두 장 이상 본다 | 기록 |
| 1213 | 비교 훑기에서 **빠뜨린 모듈들**을 마저 훑었다. 스트리밍은 0/16(이미 관계로 묶여 있다), 도시 배치에서 다섯이 나왔다 | 확인 |
| 1214 | 대부분 **무언가가 통째로 사라지는** 자리인데 「0보다 많다」로만 재고 있어 통과했다 — 빈 필지 비율을 뒤집으면 **도시의 86%가 빈 땅**, 화단 반복이 뒤집히면 **첫 화면의 화단이 하나도 없다** | **결함** |
| 1215 | 줄무늬 경계는 처음 쓴 검사가 **못 잡았다.** 뒤집어도 바깥 구역에만 수백 개가 남아 「0보다 많다」를 통과했다 — **구역당 몇 줄인지**로 재니 물었다 | 기록 |
| 1216 | 「0보다 많다」는 이 세션에서 여러 번 헐렁했다. 개수를 잴 때는 **무엇으로 나눌지**(구역당·초당·자리당)를 먼저 정해야 뜻이 생긴다 | 기록 |
| 1217 | 필지 수 비율(2:3)은 **조정값이라 안 막았다.** 대신 「구역당 2채 초과 9채 미만」으로 범위를 묶었다 — 너무 비어도 너무 들어차도 걸린다 | 검사 |
| 1218 | `cityDetails`에서 **열셋**이 나왔다. 대부분 「간판을 어느 면에 붙일까」 판정인데, 뒤집히면 간판이 **건물 사이 틈이나 구역 안쪽**을 향한다 — 인스턴스 수는 그대로라 성능도 같고 화면만 조용히 빈다 | **결함** |
| 1219 | 그 파일 주석이 이미 적어 뒀다: 「무작위로 고르면 **절반은 영영 보이지 않는다**」. 또 글로만 있던 규칙이다 | 기록 |
| 1220 | 면 고르기 함수는 안 내보내므로 **결과물의 방향**으로 쟀다 — `rotationY`에서 법선을 뽑아 구역 중심 바깥을 향하는지 | 검사 |
| 1221 | 그런데 그것만으로는 **두 갈래를 못 갈랐다.** 두 면 다 바깥이라, 「길에 가까운 면」과 「좁은 틈을 향한 면」이 같아 보인다 — **더 많이 밀린 축**으로 좁히니 물었다 | 기록 |
| 1222 | 「모서리 필지만 두 면을 쓴다」는 아직 못 잰다. 결과물에 건물 신원이 없어 **어느 간판이 같은 건물 것인지** 알 수 없다 — 못 재는 것으로 적어 둔다 | 기록 |
| 1223 | 도로·옥상에서 셋이 더 나왔다: 옥상 실외기 반복, 차선 점선 반복, 정지선 차로 | **결함** |
| 1224 | 옥상 검사를 「구역당 하나 넘게」로 썼다가 **못 잡았다** — 실외기를 죽여도 물탱크·난간이 남아 8.7개였다(정상은 13개). **얼마나 있어야 하는지**를 재야 한다 | 기록 |
| 1225 | 개수 검사가 헐렁했던 것이 이 세션에 **세 번째**다(줄무늬·옥상·건물). 매번 「있다」와 「있어야 할 만큼 있다」의 차이였다 | 기록 |
| 1226 | 「도시 밖 표시가 없다」로 썼다가 181개가 걸렸는데 **내 가정이 틀렸다** — 도시 바깥에 둘레 도로가 있다. 여유를 도로 폭만큼 주되 「어디까지든」은 아니게 묶었다 | 기록 |
| 1227 | 못 재는 것 둘을 적어 뒀다: 갓길 주차가 도로 양쪽에 있는지, 정지선이 다가오는 차로에 그려지는지. **결과물에 축·방향이 안 실려** 세계 좌표만으로는 못 가른다 | 기록 |
| 1228 | 재려면 렌더가 안 쓰는 값을 결과물에 넣어야 한다. **검사를 위해 제품을 부풀리지 않는** 쪽을 택했다 — 대신 못 재는 것으로 남겼다 | 기록 |
| 1229 | 전깃줄이 특히 고약했다 — 축 판정을 뒤집으면 **대각선으로 이어져 도시가 그물에 덮이고**, 길이 판정을 뒤집으면 **멀리 떨어진 기둥끼리 이어져** 하늘을 가로지른다 | **결함** |
| 1230 | 전깃줄은 「가닥마다 축을 따라가는가·이웃 간격을 넘지 않는가」로 잡았다. 셋 다 문다 — **선분 하나하나를 재는** 것이 개수보다 강하다 | 검사 |
| 1231 | 현수막 검사가 **또 헐렁했다.** 「도로당 몇 개」로 썼는데 현수막이 **두 곳에서** 나온다(건물 벽 6.05m, 도로 위 4.15m) — 도로 쪽을 통째로 죽여도 건물 것 40개가 남았다 | 기록 |
| 1232 | 개수 검사가 헐렁했던 **네 번째**다. 매번 「같은 통에 다른 출처가 섞여 있다」였다 — **세는 것만으로는 어느 출처가 죽었는지 모른다** | 기록 |
| 1233 | 탐침도 또 빗나갔다: 같은 문자열의 반복문이 여럿이라 **엉뚱한 곳을 고쳤다.** 줄 번호로 바꿔 다시 재니 둘 다 물었다 | 기록 |

## 확인해 보니 이미 처리된 항목 (반복 33에서 코드로 확인)

- **반경 밖 구역의 지오메트리 해제** — `useStreamed`가 보이는 구역의 인스턴스만 돌려주고
  `instancedMesh`의 count가 그 길이다. 반경 밖 구역은 애초에 할당되지 않는다. 할 일 없음.
- **크라우드의 구역 인지** — `Crowd`가 120m 밖을 접고, 그보다 가까워도 먼 보행자는
  두 프레임에 한 번만 갱신한다. 거리 기준이 구역 기준보다 정확하다. 할 일 없음.
- **교통의 구역 인지** — 행렬 갱신은 이미 거리로 접는다. 시뮬레이션은 전부 돌지만
  차량 상한이 36대라 조기 최적화다. 프레임을 실제로 깎는 것이 측정되면 그때 한다.
- **퀘스트 보상** — 첫 여정을 마치면 도깨비 「물비늘」이 해금된다 (반복 30). 별도 보상 불필요.

## 이름과 캐릭터 (루프 종료 후)

자동 반복을 끄고 사람이 방향을 준 뒤의 작업이다.

### 이름을 `DokeV`로 바꿨다

**저장을 살리는 것이 일의 대부분이었다.** 키를 그냥 바꾸면 그때까지 플레이한
사람의 진행·설정·만난 도깨비가 통째로 사라진다 — 이 저장소가 가장 조심해 온
사고다. `migrateStorageKey`가 읽기 직전에 한 번 옮기고, 세 규칙을 검사로 못
박았다: 새 키가 있으면 안 덮어쓰고, 옛 키가 없으면 아무것도 안 하고, 옮긴 뒤
옛 키를 지운다.

브라우저에 옛 키만 심어 놓고 실제로 확인했다 — 처치 수·대장 기록·흔적·만난
도깨비·소리 설정이 전부 살아남았다.

절대 규칙 8(원작 이름 복제 금지)은 사용자 판단으로 삭제했다.

### 캐릭터 GLB를 들였다

**「외부 에셋 0개」를 「정해 둔 하나만」으로 바꿨다.** 사람 몸은 상자로 흉내
내기 가장 어려워 여기서 얻는 것이 가장 크다는 판단이다.

원본은 동작마다 파일이 따로여서 12개 68MB였다(같은 메시가 열두 벌). 파일의
92%가 텍스처라 거기서 승부가 났다 — `scripts/build-character.mjs`가 하나로
합치고 2048px PNG를 1024px JPEG로 줄여 **1.1MB**로 만든다.

화면에서 두 번 틀렸고 두 번 다 화면이 알려 줬다:

- 가만히 있을 때 「일어서기」를 처음부터 반복해 **계속 바닥에서 일어났다** →
  끝 자세로 보내고 거기서 멈춘다(`holdsLastFrame`)
- 모델 정면이 -Z인데 +Z로 가정해 **뒤로 달렸다** → 반 바퀴 돌린다

동작 고르기는 `characterClips.ts`로 빼서 값으로 잰다. 동작 이름은 **GLB 안의
이름과 글자 그대로 맞아야 하는 계약**이라, 검사가 파일의 JSON 청크를 읽어
양방향으로 대조한다(코드에만 있는 이름·파일에만 있는 이름 둘 다).

받는 동안과 실패했을 때는 **예전 절차적 캐릭터가 그대로 보인다** — 캐릭터가
없으면 게임이 성립하지 않는 핵심이라 대체물을 남겼다.

## 다음 후보

- **`public/`의 Next.js 템플릿 SVG 다섯 개를 지운다** (`file`·`vercel`·`next`·
  `globe`·`window`). `src`·`tests`·문서 어디에서도 참조하지 않는 시작 템플릿
  잔재이고, 지우면 「외부 에셋을 하나도 쓰지 않는다」가 단서 없이 참이 된다.

  **직접 지우지 않은 이유:** 이 저장소는 git 추적이 아니라 되돌릴 수 없고,
  지우는 일은 요청받은 범위 밖이다. 20KB짜리 문제라 급하지 않다.

- ~~**「한 동작이 바꾸는 칸을 다 세는가」를 도구로 만든다**~~ — **완료.**
  `tests/support/readsAll.ts`가 프록시로 읽힌 칸을 센다. 쓰기는 안 세므로
  `project…` 계열을 그대로 감쌀 수 있고, 도구를 지키는 검사도 함께 있다.
  쓰러짐 버그가 살던 `stepPlayerCombat` 결과와 이동 상태에 적용해 두었다.

## 반복 작업의 한계 — 반드시 읽을 것

**94번의 반복 동안 화면을 한 번도 확인하지 않았다.** 그때 923개, 지금 1746개의 테스트가 통과한다는
사실은 "코드가 서로 어긋나지 않는다"는 뜻이지 "재미있다"는 뜻이 아니다.

### 이제는 추측이 아닌 것 (관계 테스트가 고정함)

아래는 다른 값과의 관계로 묶여 있어, 한쪽을 바꾸면 테스트가 먼저 알린다.
`tests/tuningRelations.test.ts`가 근거다.

| 값 | 무엇에 묶였나 |
|---|---|
| 탄 판정 높이 0.8 | 점프 정점 — 회피 구간 0.34초(반응 시간 이상) |
| 보스 예고 1.1초 | 걷기 속도 — 걸어서도 충격 반경을 벗어난다 |
| 보스 빈틈 1.6초 | 휘두르기 주기 0.48초 — 최소 한 번은 때릴 수 있다 |
| 그래플 사거리 24m | 가로등 간격 17m — 다음 가로등에 이어 걸 수 있다 |
| 안개 100/145/190m | 스트리밍 반경 2/3/4구역이 덮는 거리 |
| 무적 1.1초 | 적 공격 주기 0.48초의 1~4배 |
| 음료 배율 1.25 | 달리기 7.4 → 9.25, 보드 15.5 아래 |
| 색 대비 전반 | 하늘 4종·서로 간 거리 (`worldContrast`, `appearance`) |

### 여전히 눈이 필요한 것

관계로는 옳지만 **느낌은 알 수 없는** 값들이다:

- **기본 캐릭터 후드(#ff8a3d)와 노을 하늘(#f0a06a)의 거리가 52뿐이다.** 역광에서
  실루엣이 묻히는지 가장 먼저 볼 것. 묻히면 기본 색과 `tests/appearance.test.ts`의
  기준(50)을 함께 올린다.

  **이걸 볼 수 있는 확인 지점이 없다.** `?see=air`는 하늘을 배경으로 두지만
  **일부러 한낮**이다(「어두운 하늘은 쉬운 쪽이다 — 묻힌다면 밝은 하늘에서
  묻힌다」). 그 판단은 **밝기** 기준이고, 여기서 걱정하는 것은 **색상**이다:
  주황이 주황 앞에 선다. 두 어려움이 서로 다르다.

  한낮 하늘에서는 확인했다 — 묻히지 않는다. **노을 역광은 사람이 직접
  노을에서 뛰어올라(또는 `G`로 가로등에 올라가 `Space` 활강) 하늘을 등지고
  봐야 한다.** 자동화로는 카메라를 캐릭터 아래로 두기 어려웠다.
- 이단 점프 7.4 / 활강 4.2m/s — 높이와 체공이 시원한지
- 넉백 9m/s / 체력 5 — 맞았을 때의 무게감
- BPM 96과 춤 박자 — 음악과 동작이 실제로 맞아 보이는지
- 카메라 FOV 곡선 — 속도감이 나는지, 멀미가 나지 않는지
- 밤 조명(창문·가로등·전조등)과 예고 링의 실제 밝기
- 도깨비·보스·간판의 **크기감** — 수치는 맞아도 커 보이거나 작아 보일 수 있다

### 정적 검증으로 잡은 결함 27건

도로 좌표 반 칸 · 적 스폰 42% 건물 안 · 적 벽 통과 · 조작표 누락 · 탄 벽 통과 ·
저장값 NaN · 지도 대체 텍스트 · 터치 버튼 이름 · 잠긴 도깨비 동행 · 화면 문구
5곳 · 문서 인용 27곳 · 물비늘 하늘 동화 · 점프 회피 불가 · 안개/스트리밍 어긋남 ·
클립 무제한 녹화 · 지오메트리 32개 누수 · 컨텍스트 손실 미처리 · 오류 경계 부재 ·
공유 카드 없음 · 보안 헤더 없음 · README 템플릿 · 미사용 의존성 · 분석 이벤트 7개 ·
텍스처 캐시 미해제 · 거짓 캐스트 · 파일 크기 위반 2건

**그래도 `http://localhost:3112`에서 한 번 플레이하는 것이 어떤 새 기능보다
가치가 크다.** 위 "눈이 필요한 것" 목록의 절반은 한 판이면 정해진다.

### 가장 많이 틀린 것은 코드가 아니라 **자**였다

이 구간에서 「결함을 찾았다」고 판단한 것 중 **열 건이 측정 실수**였고, 다섯 건은
**결함이 아닌 것을 고치려던 것**이었다. 코드가 틀린 것보다 **재는 도구가 틀린
경우가 더 많았다.**

자가 틀린 방식은 늘 같은 몇 가지였다:

| 어떻게 틀렸나 | 실제 사례 |
|---|---|
| 부분 문자열이 딴것을 맞혔다 | `[role='button']` 안의 "button", `left-1/2`를 자른 `left-1` |
| `g` 플래그 정규식을 `.test()`로 재사용 | `lastIndex`가 남아 결과가 번갈아 나왔다 |
| 찾으려는 것을 읽기 전에 지웠다 | 주석을 지우는 `readCode`로 주석을 찾았다 |
| 잘라낸 범위가 옆 것을 물고 왔다 | 문서 둘이 붙어 앞것이 조건을 대신 만족시켰다 |
| 검사를 새로 구현하다 원본과 어긋났다 | 충돌 판정을 다시 짜서 384곳 전부 실패 |
| 변이가 약해서 「헛돎」으로 보였다 | 값을 바꿔도 결과가 같은 자리를 골랐다 |

그래서 규칙 넷이 나왔다 — **「수치가 이상하면 코드보다 자를 먼저 의심한다」**,
**「자를 새로 만들지 말고 있는 자를 쓴다」**, **「검사가 있다 ≠ 그 검사가 이것을
본다」**, **「억제가 늘어난다는 것은 대개 설계가 어긋났다는 신호」**.

**다음 사람에게:** 「결함을 찾았다」 다음에 곧바로 고치지 말고, **그 판단을 만든
측정을 한 번 더 재라.** 이 구간에서는 그 한 걸음이 열다섯 번 필요했다.

### 확인 지점으로 바로 가기 (개발 빌드 전용)

확인해야 할 것들이 도시 곳곳에 흩어져 있다. 보스는 걸어서 2분, 도깨비 자리는
로봇을 열두 기 잡아야 드러난다. 주소로 건너뛴다:

| 주소 | 무엇을 보는가 |
|---|---|
| `/play?see=boss` | 미니 보스 앞 — 예고 링과 크기감 |
| `/play?see=summon` | 보스전 합동 공격 — 도깨비 넷이 도는지, 능력 자국이 보이는지 |
| `/play?see=night` | 밤 — 창문·가로등·전조등 밝기 |
| `/play?see=noon` | 한낮 — 색이 날아가는지, 그림자 길이 |
| `/play?see=air` | 공중 — 밝은 하늘을 등진 실루엣이 보이는지 |
| `/play?see=result` | 완주 화면 — 기록과 수집 현황이 한 화면에 들어가는지 |
| `/play?see=party` | 도깨비 4종 동행 — 자리 배치와 색 구분 |
| `/play?see=clues` | 흔적 조사 — 지도의 마름모 표식과 살펴보기 |
| `/play?see=shrine` | 도깨비 자리 앞 — 빛기둥이 골목에서 보이는지 |

프로덕션 빌드에서는 아무 효과가 없다. 진행을 지우지도 않는다 — 저장된 판 위에
얹기만 한다.

## 보고

각 반복이 끝나면 무엇을 추가했는지, 인스턴스/드로우콜 변화, 검증 명령 결과, 남은 한계를 기록한다.

## 아직 확인하지 못한 것

### 브라우저 자동화의 한계 (반복 112에서 확정)

**나중에 덧붙인 것들** — 이 세션에서 새로 확인한 한계 넷:

| 무엇이 | 왜 |
|---|---|
| **소리를 들을 수 없다** | 탭이 `hidden`이라 오디오 엔진이 아예 시작하지 않는다. 안 보이는 탭에서 소리를 내지 않는 것이 **옳은 동작**이다 |
| **텍스트를 입력할 수 없다** | 입력칸이 최상단이고 JS로 포커스를 줘도 확장 프로그램의 타이핑이 닿지 않는다. 앱 배선은 네이티브 setter + `input` 이벤트로 대신 확인한다 |
| **품질이 저절로 내려간다** | 프레임이 낮으니 자동 품질 조정이 「보통」으로 낮춘다. **화질을 판단하는 확인은 탭을 연 직후에** 해야 한다 |
| **가까이 붙어야 하는 것은 어렵다** | 화면당 0.2초뿐이라 14m를 좁히는 데 사람의 3초가 수십 장이다. 반대로 **사거리가 긴 조작(그래플)은 제자리에서 바로** 확인된다 |

**할 수 있는 것도 늘었다:** 페이지 안에서 `KeyboardEvent`를 보내면 조작이 되고,
`PointerEvent`를 **`<main>` 안의 요소로** 보내면 시점이 돈다. 픽셀은 2D 캔버스로
옮겨 담아 `getImageData`로 읽는다 — 저장 경로와 같은 합성을 돌리면 **파일을
내려받지 않고도** 「저장될 사진」을 검사할 수 있다.


**자동화로 연 탭은 `document.hidden === true`다.** Chrome은 숨은 탭에서
`requestAnimationFrame`을 돌리지 않으므로 **시뮬레이션이 아예 진행되지 않는다.**
스크린샷을 찍는 순간에만 몇 프레임이 그려진다.

이 때문에 「보스가 다가오지 않는다」를 실제 버그로 오해하고 네 번의 반복을 썼다.
보스 규칙은 처음부터 옳았다(반복 108의 테스트가 증명). 다시 헛짚지 않도록:

- **화면(정지 화면)은 신뢰할 수 있다.** 밤 명암비·빛기둥·카메라 각·HUD 폭은 전부 유효하다.
- **시간이 흐르는 것은 신뢰할 수 없다.** 이동·추격·전투·애니메이션 진행은 관측 불가다.
- 계기판에 「아직 재는 중」이 계속 뜨면 그건 게임이 아니라 **탭이 숨어 있다는 신호**다.

- **~~달리기·전투·활강은 관측 불가~~ → 움직일 수 있다.** 확장 프로그램이 만든 합성
  키 이벤트는 페이지에 안 닿는 것이 맞다. 그런데 **페이지 안에서** 보내면 닿는다:

  ```js
  window.dispatchEvent(new KeyboardEvent('keydown', { code: 'KeyW' }));
  // 스크린샷을 찍으면 그 사이 프레임이 돈다
  window.dispatchEvent(new KeyboardEvent('keyup',   { code: 'KeyW' }));
  ```

  이렇게 달려서 확인한 것: 달리는 자세로 바뀌고, 월드가 흐르고, 카메라가 따라오고,
  동료가 옆에 붙어 오고, 여정 진행 막대가 찬다. 시간은 스크린샷을 찍는 동안에만
  흐르므로 **연속적인 감각(가속감·관성)은 여전히 사람 몫**이지만, 「움직이는가」는
  이제 확인할 수 있다.
- **속도계는 여전히 0으로 보인다.** rAF로 갱신하는 값이라 그렇다 — 움직였는지는
  지도 요약이나 성능 패널의 보스 거리로 봐야 한다.
- **~~마우스 드래그 시점은 확인 불가~~ → 돌아간다. 눈으로 확인했다.**
  확장 프로그램의 드래그는 중간 `pointermove`를 만들지 않지만, **페이지 안에서**
  보내면 닿는다. 단 하나 조건이 있다 — **`window`가 아니라 `<main>` 안의 요소로
  보내야 한다.** 리스너는 `containerRef`(= `<main>`)에 붙어 있고 `window`에서는
  거기로 내려가지 않는다. 캔버스로 보내면 버블링으로 닿는다:

  ```js
  const cv = document.querySelector('canvas');           // <main> 안이면 무엇이든 된다
  const send = (t,x,y,b) => cv.dispatchEvent(new PointerEvent(t,
    {bubbles:true, pointerId:1, pointerType:'mouse', buttons:b, clientX:x, clientY:y}));
  send('pointerdown', 640, 406, 1);
  for (let i=1;i<=20;i++) send('pointermove', 640+i*20, 406, 1);
  send('pointerup', 1040, 406, 0);
  ```

  이렇게 400px 끌었더니 간판이 왼쪽 밖으로 밀리고 오른쪽에서 새 건물이 들어왔다.
  좌표 차이 계산은 이제 **일관성 개선이 아니라 확인된 동작**이다. (감도가 알맞은지는
  여전히 사람 몫이다 — 도는지와 얼마나 도는지는 다른 질문이다.)
- **개발 빌드에서는 Next.js 개발 배지가 왼쪽 아래 속도계와 겹친다.** 검은 원에
  흰 `N`이 찍혀 속도 숫자를 가린다. **게임 UI가 아니고 프로덕션에는 없다** —
  `elementsFromPoint`로 보면 맨 위가 `NEXTJS-PORTAL`이다. 스크린샷으로 왼쪽 아래를
  판단할 때 이걸 결함으로 읽지 마라(읽을 뻔했다).

## 다음에 할 만한 것 (백로그 밖, 검토 결과)

- ~~보스를 잡아도 수집물이 없다~~ → **반복 129에서 구현.** 「자정」이 보스 처치로 열린다.
  **반복 131에서 밤 화면으로 확인:** 보라색(#8a4fd8)이 밤 거리에서 또렷하고
  다른 셋과 계열이 겹치지 않는다. 남은 것은 도감이 4칸이 된 모습과 완주 화면의
  수집률 표시 — 둘 다 사람이 눌러야 열린다.
