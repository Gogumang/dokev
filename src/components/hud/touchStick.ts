/**
 * 가상 스틱의 크기.
 *
 * 손잡이를 그리는 쪽(`TouchControls`)과 손가락을 값으로 옮기는 쪽
 * (`useVirtualStick`)이 **같은 값**을 써야 한다. 둘로 나뉜 뒤에도 한 곳에서
 * 온다는 것을 지키려고 따로 둔다 — 어긋나면 손잡이가 판정 밖으로 나간다.
 */
export const STICK_RADIUS = 56;
