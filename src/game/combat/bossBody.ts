/**
 * 고물 대장 몸 치수.
 *
 * `Boss.tsx`에 숫자로 박혀 있던 것을 옮긴다 — 플레이어·보행자·동료·적에서
 * 같은 구멍을 네 번 만났다. 이름이 없으면 검사할 대상 자체가 없다.
 *
 * 예고 링은 여기 두지 않는다. 그 크기는 **충격이 닿는 반경**(`BOSS.slamRadius`)
 * 이어야 하고, 그림과 판정이 갈라지면 「링 밖인데 맞았다」가 된다.
 *
 * 값은 옮기기만 했다.
 */

export const BOSS_BODY = {
  /**
   * 발바닥에서 머리 꼭대기까지(m).
   *
   * 상자 몸이 만들던 실루엣과 같은 값이다(몸통 위 2.55 + 머리 3.45). GLB를
   * 그 키에 맞추는 데 쓴다 — **둘이 같은 크기여야** 모델을 못 받은 사람과 받은
   * 사람이 같은 게임을 한다. 예고 링과 충격 반경은 이 값과 무관하다.
   */
  height: 3.45,
  bodyWidth: 1.7,
  bodyHeight: 1.9,
  bodyDepth: 1.4,
  headWidth: 1.1,
  headHeight: 0.9,
  headDepth: 1.0,
  armWidth: 0.42,
  armHeight: 1.5,
  armDepth: 0.42,
  /**
   * 가슴에 박힌 점. 일반 로봇과 **같은 조형**이다.
   *
   * 대장만 없으면 「저 안에 갇혀 있다」는 규칙이 대장에게는 해당되지 않는 것으로
   * 읽힌다. 몸이 큰 만큼 점도 크다.
   */
  coreRadius: 0.26,
} as const;
